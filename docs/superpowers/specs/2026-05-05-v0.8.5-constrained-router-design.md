# v0.8.5 Constrained LLM Router with Self-Correcting Retry — Design Spec

> **⚠️ SUPERSEDED (2026-05-15)**
> § 4.2 PLAN_REGISTRY 4-plan 分叉 / § 4.4 retry on plan_correctness / § Task 6 PlanCorrectnessScorer
> 已被 [`2026-05-15-v1.x-plan-template-validator-design.md`](2026-05-15-v1.x-plan-template-validator-design.md) 替代。
>
> 本 spec 中仍然 valid 的部分:11 维 SOP / Python 覆写
> (classify_recommendation / compute_position_size_pct) / forward concerns
> (pe_historical_percentile_value / debt_ratio_assessment).

> **Status:** Design (brainstormed 2026-05-05). Implementation deferred to v0.8.5 (after v0.8.4 ship).
> **Ref to:** `docs/superpowers/specs/2026-05-04-v0.8.4-b1-single-deep-design.md` (v0.8.4 baseline)
> **Brainstorming context:** v0.8.4 dogfood revealed plan-driven multi-agent's instability — Planner LLM 输出 `required_tools` 自由组合，prompt 微调即让 plan 走样，Critic 评分受 plan 噪声污染，3 differential golden case 验证失败时无法分辨是 prompt 退化还是 plan drift。

---

## § 0 元信息

- **Branch:** `feat/v0.8.5-constrained-router`（独立于 v0.8.4 worktree，待 v0.8.4 merge 后 cut）
- **工期估计:** 2.4 周 wall time（含 8 新 tool + Anthropic Skills bundle 17 components + dogfood + 重录 cassette）
- **Pre-req:** v0.8.4 ship + dogfood 通过
- **业界对照:**
  - Reflexion (NeurIPS 2023) — verbal feedback self-correction
  - Anthropic Constitutional AI — critic-as-judge feedback iteration
  - LangGraph self-correcting agents tutorial — retry edge official pattern
  - ChatGPT Plugins (2023) failure mode — plan-driven plugin selection unstable

## § 1 问题陈述

v0.8.4 的 Planner agent 是「自由生成 ResearchPlan」模式：

```python
# 当前 v0.8.4
class ResearchPlan(BaseModel):
    subtasks: list[Subtask]  # ← LLM 自由生成，每 Subtask.required_tools 自由组合
```

**3 个具体痛点（dogfood 验证）：**

1. **Prompt 改动 → plan 走样**：Planner prompt 加一句"请重视 KB 检索"，同 input 的 plan subtask 数量、tool 组合、顺序都可能变。
2. **评测失真**：Critic 6 scorers 的 `coverage` / `factuality` 评分混入 plan 自由度噪声 — 同 6 字段输入两次跑可能拿不同 plan，分数浮动 ±1.5。
3. **简历叙事不稳**：D6 系统化评测时无法 ground truth，「3 differential golden case 通过」变成 "在某个 prompt 版本下通过"，不可移植。

## § 2 9 个核心决策汇总

| # | 决策点 | 选择 | 否决项 |
|---|---|---|---|
| 1 | 架构方向 | LLM-driven router but schema 严约束 | 全 hardcode（去 LLM）/ LangGraph hard edges / Anthropic Skill bundle 替换整个 agent |
| 2 | Planner 输出 | `plan_id` (Literal 4 值) + `rationale` (≤200 字符) | 极简 router 无 rationale / router + subtask_count override |
| 3 | 6 字段 → plan_id 映射 | 主映射 + override 规则在 prompt | 严格 1:1（LLM 是装饰）/ 完全模糊（=plan-driven 老问题）|
| 4 | Subtask 内容 | template + ts_code format，required_tools hardcode | 全 hardcode 无个性化 / LLM 生成 description |
| 5 | 评测 | Unit test 防 regression + Critic 第 7 scorer plan_correctness | unit test only / scorer only |
| 6 | 改进闭环 | Production self-correcting retry，max 2 轮 | 离线 prompt iteration only / 混合 |
| 7 | tool inventory 扩展 | P0 + P1 共 8 新 tool（11 接口）| 不扩展 / P0+P1+P2 共 18 接口 |
| 8 | 金融 SOP 形态 | Skill bundle (Anthropic Skills pattern) — 多文件结构 | Inline prompt / 单 md 文件 / Analytical tools (v0.8.6+) / 不加 |
| 9 | Skill bundle fidelity | (γ) 17 components: 11 methodology .md + 3 references (json/yaml) + 3 Python helpers | (α) 单 md / (β) 多 md only / (δ) bundle + loader infra (v0.9+) |

## § 3 架构 overview

5-agent 架构不增不减，Planner 内部从「自由生成 ResearchPlan」改为「constrained LLM router」+ LangGraph 加 retry edge + Critic 加第 7 scorer。

```
ResearchPlanner (LLM router)
  ↓ output: (plan_id, rationale)
  ↓ Python: PLAN_REGISTRY[plan_id] → instantiate Subtasks (template format with ts_code)
  ↓
DataCollector (subtasks deterministic) → Analyst → Writer
  ↓
Critic (7 scorers — 加 plan_correctness)
  ↓
plan_correctness >= 8.5 OR planner_retry_count >= 2?
  YES → END
  NO  → retry edge
        ResearchPlanner.run(state, feedback=critic.plan_correctness.rationale)
        ... loop max 2 轮
```

## § 4 Components 改动

### § 4.1 `backend/app/agents/research_planner.py` — 重写

**旧（v0.8.4）:**
- 调 LLM 生成 `ResearchPlan(subtasks=[Subtask(description=..., required_tools=...)])`
- 4 plan template emphasis 写在 prompt 里，LLM 自由组合 subtasks

**新（v0.8.5）:**
- 调 LLM 仅输出 `(plan_id, rationale)`（schema enum 强约束）
- prompt 明确：默认主映射（objective → plan_id 1:1）+ 5-7 条 override 例外（user_message 含特定 keyword 时偏离主映射）
- 收 critic feedback 时（`retry_count > 0`），prompt 多附 critic.rationale 让 LLM 知道为什么被打回

**Output schema:**

```python
PlanId = Literal["capital_preservation", "stable_growth", "balanced", "aggressive_growth"]

class ResearchPlan(BaseModel):
    plan_id: PlanId
    rationale: str = Field(max_length=200, description="LLM 解释为什么选这个 plan")
    # subtasks 字段 deprecated — instantiate from PLAN_REGISTRY by plan_id
```

### § 4.2 新建 `backend/app/agents/plan_registry.py`

```python
class SubtaskTemplate(BaseModel):
    description_template: str  # "评估 {target_name}（{ts_code}）的财务健康度"
    rationale: str
    required_tools: list[ToolName]  # ToolName = Literal[5 个 tool 名]

PLAN_REGISTRY: dict[PlanId, list[SubtaskTemplate]] = {
    "capital_preservation": [...],   # ~4 subtasks 偏风险/财务/股息/下行保护
    "stable_growth":         [...],
    "balanced":              [...],
    "aggressive_growth":     [...],  # ~4 subtasks 偏成长/估值/行业/催化
}

def instantiate_plan(plan_id: PlanId, target_name: str, ts_code: str) -> list[Subtask]:
    return [
        Subtask(
            description=tmpl.description_template.format(target_name=target_name, ts_code=ts_code),
            rationale=tmpl.rationale,
            required_tools=tmpl.required_tools,
        )
        for tmpl in PLAN_REGISTRY[plan_id]
    ]
```

**4 plan 各自的 subtask 内容（description_template / required_tools）由 implementer 在 implementation 阶段设计 + dogfood 验证 + iterate**（plan 故意不写完整内容 — 见 `feedback_plan_specificity`）。 spec 契约只规定：每 plan 含 ~4 subtask，required_tools 从 5 个固定 enum 选 0-3 个 per subtask。

### § 4.3 新建 `backend/app/agents/critic_subagents/plan_correctness_scorer.py`

LLM-as-judge:

- **Input:** ResearchRequest 6 字段 + user_message + 选定的 plan_id + 选定的 rationale + override 规则原文
- **Judge prompt:** "评判 plan_id 选择是否符合 6 字段 + user_message。 评分 0-10。"
- **Output:** `score: float (0-10)` + `reasoning: str`

继承 `_BaseScorer`（跟 6 个现有 scorer 同 pattern）。 `Critic.scorers` list 6 → 7。

### § 4.4 `backend/app/orchestration/research_graph.py` — 加 retry edge

```python
graph.add_conditional_edges(
    "critic_subgraph",
    _retry_router,
    {"retry": "research_planner_node", "continue": END},
)

def _retry_router(state: ResearchState) -> Literal["retry", "continue"]:
    plan_score = state.critic_report.get_score("plan_correctness")
    if plan_score is None:
        return "continue"
    if plan_score < 8.5 and state.planner_retry_count < 2:
        return "retry"
    return "continue"
```

### § 4.5 `backend/app/agents/schemas.py` — ResearchState 加字段

- `planner_retry_count: int = 0`
- `planner_critic_feedback: str | None = None`（retry 时传给 Planner 的 critic.rationale）

### § 4.6 Tool inventory 扩展（P0 + P1，8 新 tool / 11 tushare 接口）

新建 8 个 Tool class（`backend/app/tools/`）+ TushareService 加对应 method：

| 新 Tool | tushare 接口 | 服务于哪些 plan |
|---|---|---|
| `get_daily_basic` | `daily_basic`（PE/PB/PS/股息率/总市值/换手率） | 全 4 plan（估值与流动性核心）|
| `get_balance_sheet` | `balancesheet` | capital_preservation（偿债能力）|
| `get_cashflow` | `cashflow` | capital_preservation + stable_growth（盈利质量校验）|
| `get_pe_history` | `daily_basic` 历史聚合 → 计算分位 | aggressive_growth + capital_preservation（估值高低判断）|
| `get_forecast` | `forecast` + `express` | aggressive_growth（领先指标）|
| `get_dividend_history` | `dividend` | stable_growth（红利配置要点）|
| `get_holder_change` | `stk_holdernumber` | aggressive_growth（散户化指标）|
| `get_money_flow` | `moneyflow` + `top_list` | aggressive_growth（短期资金博弈）|

5 个原 tool 保留（`get_stock_quote / get_financials / get_news / web_search / kb_search`）。 总计 13 tool。

`ToolName` Literal 由 5 → 13。 `PLAN_REGISTRY` 的 `required_tools` 从 13 个里选组合。

### § 4.7 新建 `backend/app/skills/financial_research/` Skills bundle

按 **Anthropic Skills bundle pattern** 设计：methodology markdown + reference 数据 + Python helpers，**deterministic 计算与 LLM 解读职责分离**。 17 components：

```
backend/app/skills/financial_research/
├── SKILL.md                              # manifest: 入口/版本/触发条件/component list
├── methodology/                          # 11 个研究方法论文档（LLM 看的）
│   ├── solvency.md                       # § 1.1 偿债能力 — 阈值表 + 行业差异
│   ├── profitability.md                  # § 1.2 盈利能力 — ROE/ROA/毛利率
│   ├── growth.md                         # § 1.3 成长性 — 营收/净利同比 + 复合增长
│   ├── cashflow_quality.md               # § 1.4 现金流质量 — 经营/投资/筹资配比
│   ├── valuation.md                      # § 2.1 估值水平 — PE/PB/PS 历史分位
│   ├── industry.md                       # § 2.2 行业地位 — 申万分位 + 市占率
│   ├── shareholder_governance.md         # § 2.3 股东结构与治理 — 集中度 + 质押
│   ├── short_term_capital_flow.md        # § 2.4 短期资金面 — 龙虎榜 + 北向 + 大单
│   ├── event_driven.md                   # § 3.1 事件驱动 — 业绩预告/分红/解禁/回购
│   ├── risk_factors.md                   # § 3.2 风险因素 — 减持/变脸/质押/政策
│   └── decision_framework.md             # § 4 决策框架 — 评级 5 档触发条件
├── references/                           # 3 数据文件（独立版本化）
│   ├── industry_benchmarks.json          # 行业基准阈值 (公用事业/白酒/科技/...)
│   ├── recommendation_rules.yaml         # 5 档评级 if-else 规则
│   └── position_size_rules.yaml          # 仓位计算公式参数
└── scripts/                              # 3 Python helpers (deterministic)
    ├── compute_position_size.py          # 仓位计算 (pure function)
    ├── industry_benchmark_lookup.py      # 行业基准查询 (json lookup)
    └── recommendation_classifier.py      # 评级 5 档分类器 (if-else from yaml)
```

#### § 4.7.1 Deterministic 计算 vs LLM 解读 分离原则

**hardcode 在 Python**（不靠 LLM）— Writer / Critic 直接调用：
- `compute_position_size_pct(rec, risk, market_cap)` → 仓位百分比（公式：`base_pct × risk_multiplier × liquidity_haircut`，参数从 yaml 读）
- `classify_recommendation(metrics)` → 评级 5 档（基于阈值 if-else，规则从 yaml 读）
- `lookup_industry_benchmark(industry, indicator)` → 行业基准（json lookup）

**让 LLM 看 markdown 解读**：
- Analyst 写 narrative（"X 资产负债率 50% 在科技行业属于警戒水平"）
- Writer 综合性 narrative 拼装
- 行业差异判断（"白酒 ROE 33% 远超行业平均 18%"）

#### § 4.7.2 SKILL.md manifest 样例

```markdown
---
name: financial_research
description: 投资标的尽调研究员级 SOP — 偿债/盈利/成长/估值/行业/风险 11 维度方法论 + 评级与仓位决策框架
version: 0.8.5
trigger:
  - investment_due_diligence
loaded_by:
  - Analyst.build_prompt
  - Writer.build_prompt
component_count: 17
---

## Components

### Methodology (11 markdown docs)
1. solvency.md — 偿债能力评估
2. profitability.md — 盈利能力评估
... (其余 9 个)

### References (3 data files)
- industry_benchmarks.json — 行业基准阈值
- recommendation_rules.yaml — 评级触发规则
- position_size_rules.yaml — 仓位计算参数

### Scripts (3 Python helpers)
- compute_position_size.py — 仓位计算
- industry_benchmark_lookup.py — 行业基准查询
- recommendation_classifier.py — 评级分类

## Loader

```python
from app.skills.financial_research import load_skill
skill = load_skill()
skill.methodology["solvency"]  # str — markdown content
skill.references["industry_benchmarks"]  # dict — json data
skill.scripts.compute_position_size_pct(...)  # callable
```
```

#### § 4.7.3 SkillBundle 加载逻辑（最简）

```python
# backend/app/skills/financial_research/__init__.py
from pathlib import Path
import json
import yaml
from . import scripts

_SKILL_DIR = Path(__file__).parent
_METHODOLOGY = {
    f.stem: f.read_text(encoding="utf-8")
    for f in (_SKILL_DIR / "methodology").glob("*.md")
}
_REFERENCES = {
    "industry_benchmarks": json.loads((_SKILL_DIR / "references" / "industry_benchmarks.json").read_text()),
    "recommendation_rules": yaml.safe_load((_SKILL_DIR / "references" / "recommendation_rules.yaml").read_text()),
    "position_size_rules": yaml.safe_load((_SKILL_DIR / "references" / "position_size_rules.yaml").read_text()),
}

def load_skill() -> "SkillBundle":
    return SkillBundle(methodology=_METHODOLOGY, references=_REFERENCES, scripts=scripts)
```

无热加载、无版本检查（v0.9+ 才需要 (δ) loader infra）。 简单 module-level cache。

#### § 4.7.4 工程量分解

| Component 类别 | 数量 | 工期 |
|---|---|---|
| methodology .md（11 份每份 ~150-200 字符）| 11 | 2 天（含金融知识查阅）|
| references json/yaml（行业基准 + 规则）| 3 | 0.5 天 |
| Python helpers（仓位计算 + 分类器 + 基准查询）| 3 | 1 天（含 unit test）|
| SKILL.md manifest + loader | 2 | 0.3 天 |
| Analyst/Writer prompt 注入 | — | 0.2 天 |
| **合计** | **17 + 1 manifest** | **~1 周** |

#### § 4.7.5 简历 sell points

> v0.8.5 引入 **financial_research skill bundle (Anthropic Skills pattern)**:
> - **17 components**: 11 methodology markdown + 3 reference json/yaml + 3 Python helpers
> - **职责分离**: 仓位计算 + 评级分类 + 行业基准查询 完全 hardcode 在 Python（pure functions, deterministic, unit-testable）；LLM 仅负责 narrative 与行业差异判断
> - **数据与逻辑分离**: 行业基准 JSON / 评级规则 YAML 独立版本化，dogfood 时发现某行业基准错改 json 不动 markdown
> - 业界对照: **Bloomberg Terminal / Reuters Eikon** 的领域知识 hardcode + **Anthropic Skills** bundle pattern

## § 5 Testing strategy

### § 5.1 Unit tests（β 模式 deterministic ground truth）

```python
@pytest.mark.parametrize("objective,user_msg,expected_plan_id", [
    # 默认主映射 4 个
    ("capital_preservation", "", "capital_preservation"),
    ("stable_growth",        "", "stable_growth"),
    ("balanced",             "", "balanced"),
    ("aggressive_growth",    "", "aggressive_growth"),
    # override 例外 — balanced 配各种 user_message
    ("balanced", "想抓短期机会",        "aggressive_growth"),
    ("balanced", "担心下跌避险",         "capital_preservation"),
    ("balanced", "长期持有不在乎波动",   "stable_growth"),
    # ... 12-16 个 case 覆盖 4 objective × 主映射 / 3 override 关键词
])
def test_router_selects_expected_plan(objective, user_msg, expected_plan_id):
    plan = await router.select(...)
    assert plan.plan_id == expected_plan_id
```

LLM mock — `LLM_MODE=mock` + cassette 录制 (low cost, deterministic)。

### § 5.2 Integration test — retry edge

```python
async def test_planner_retry_when_plan_correctness_low():
    """模拟 Critic plan_correctness 第 1 次返回 5.0 → 触发 retry → 第 2 次返回 9.0 → continue."""
    # mock plan_correctness scorer 返回 [5.0, 9.0]
    # verify ResearchState.planner_retry_count == 1
    # verify final plan_id 跟 retry 后的 LLM 输出一致

async def test_planner_retry_max_2_no_infinite_loop():
    """连续 3 次 plan_correctness < threshold → max 2 retry 后 continue (不无限循环)."""
```

### § 5.3 Differential golden case

- 复用 v0.8.4 已有 3 case（capital_preservation / aggressive_growth / balanced）
- 新增 1 case：retry-trigger（user_message 故意构造让 LLM 第一次选错，verify retry 纠正）
- ic_score（v0.8.4 input_context_appropriateness）≥ 8.5 + plan_correctness ≥ 8.5

## § 6 Migration impact

### § 6.1 v0.8.4 sqlite 历史数据

dogfood 期间 v0.8.4 run 在 `backend/data/research.sqlite` 的 ResearchPlan 是旧 schema（subtasks 自由生成）。 v0.8.5 forward-compat shim：

- `_read_research_runs_from_sqlite` 读旧数据时按 `plan_id="balanced"` 默认填充
- 历史 list 显示 OK，详情页可见旧 subtasks 但缺 `plan_id` 字段（可接受，dogfood 阶段历史不重要）

### § 6.2 v0.8.4 cassette 重录

- e2e `test_b1_maotai_investment_dd_cassette.py` — Planner output schema 变 → 重录（cost ≈ ¥0.2）
- 3 differential golden cassettes — 同上（cost ≈ ¥0.6）
- 新增 1 retry-trigger cassette — 录制（cost ≈ ¥0.3）
- **总 cassette 重录 cost ≈ ¥1**

### § 6.3 单元测试影响

- v0.8.4 `test_research_planner.py` 改名 `test_research_router.py`，按 § 5.1 重写
- v0.8.4 `test_planner_objective_routing.py` 删除（已被 § 5.1 完整覆盖）
- 新增 `test_plan_registry.py` 验证 instantiate_plan() 正确性

## § 7 风险 + 缓解

| 风险 | 概率 | 缓解 |
|---|---|---|
| LLM router 在 user_message 含模糊信号时 plan_correctness 低 → 频繁 retry | 中 | dogfood 收集低分 case → 加 unit test → 调 prompt（β 模式正是为此设计） |
| 4 plan template 的 subtask 内容设计不合理 → 整体报告质量低 | 中 | implementation 阶段 dogfood 5 ts_code × 4 plan = 20 case 实跑，主观 review |
| retry 收 critic feedback 后仍选错（不收敛） | 低 | max 2 轮硬上限 + Critic 评分扣分透明显示，用户能看到 |
| LLM cost 平均增 30-60%（plan_correctness scorer + 偶发 retry） | 低 | scorer cost ≤ ¥0.005，retry 触发率预计 < 15% |
| schema 变更 break v0.8.4 cassette | 高（必发生） | § 6.2 一次性重录 cost ≈ ¥1 |
| 8 新 tool 的 tushare API rate limit / 数据缺失 | 中 | TushareService 加 tier-1 retry + tier-2 graceful degrade（tool 返回 error 字段，DataCollector 继续）|
| financial_research_sop.md 内容设计不专业 → LLM 用错 SOP 误导分析 | 中 | dogfood 时人审报告 narrative，发现 SOP 错误立即修订（单文件 git log 易追）|
| 新 tool 数据返回 schema 变化 break Analyst prompt | 低 | 每 tool 严格 Pydantic args/output schema，Analyst 看的是结构化字段不是 raw |

## § 8 Roadmap / scope

| 版本 | 范围 |
|---|---|
| **v0.8.5** | constrained router + plan_registry（4 plan × ~4 subtask）+ 8 新 tool + financial_research_sop.md skill + plan_correctness scorer + retry edge + cassette 重录 + dogfood + memory + PR |
| **v0.8.6** | C 端 5+1 use case batch + （演化方向）部分 analytical tools 替换 inline SOP（业界 Bloomberg-like 模式）|
| **v0.9** | D6 评测 dashboard + 简历 / project-story.md update + v1.0 ship gate |

## § 9 完成定义（DoD）

- [ ] `pytest backend/tests/unit/agents/test_research_router.py` 16+ test PASS
- [ ] `pytest backend/tests/integration/test_research_router_sse.py::test_planner_retry_*` PASS
- [ ] 4 differential golden case PASS（plan_correctness ≥ 8.5）
- [ ] 8 新 tool 各自 unit test PASS（mock tushare service + cassette）
- [ ] `financial_research/` skill bundle 17 components 完成（11 methodology .md + 3 references + 3 scripts + 1 SKILL.md + 1 loader）
- [ ] 3 Python helper 各自 unit test PASS（compute_position_size 公式 deterministic / classify_recommendation 5 档分支覆盖 / lookup_industry_benchmark 6+ 行业 lookup）
- [ ] Analyst / Writer prompt 真注入 methodology markdown（unit test grep prompt 中 "## 偿债能力评估" 等关键词存在）
- [ ] dogfood ≥10 ts_code 真跑，主观 review report quality ≥ v0.8.4 水平
- [ ] LLM router cost 平均（含 retry）≤ ¥0.5/run（v0.8.4 是 ¥0.3-0.4）
- [ ] `uv run poe ci` 全过 + nightly L3 eval 全过
- [ ] memory `project_v0.8.5_architecture_landed.md` 沉淀
- [ ] PR ship + v0.8.4 → v0.8.5 simply 叙事 update（README / project-story）

## § 10 简历叙事 / Elevator pitch

> **v0.8.5 把 plan-driven multi-agent 改造为 constrained LLM router with self-correcting retry + financial domain skill bundle：**
>
> - 4 个 plan template hardcode 在 `plan_registry`，LLM 仅作 4 选 1 router（受 schema enum + prompt override 规则强约束）
> - 产出后由第 7 个 Critic scorer (LLM-as-judge) 评 plan_correctness
> - < 8.5 触发 LangGraph retry edge，让 planner 收 critic feedback 重选；max 2 轮硬上限
> - 16 个 deterministic invariant unit test 防 prompt regression
> - **13 个 tool（5 现有 + 8 新增 from tushare P0/P1）**，每 plan 在 plan_registry 选差异化组合
> - **financial_research skill bundle (Anthropic Skills pattern)** — 17 components: 11 methodology markdown + 3 reference json/yaml + 3 Python helpers，**deterministic 计算（仓位/评级/行业基准）与 LLM 解读（narrative）职责分离**
>
> **兼顾 LLM judgment + production reproducibility + financial domain rigor。**
> **业界对照：Reflexion / Constitutional AI / LangGraph self-correcting agents tutorial / Anthropic Skills bundle / Bloomberg Terminal-style domain hardcoded reasoning。**

## § 11 Handoff

实施按 `docs/superpowers/plans/2026-05-05-v0.8.5-constrained-router-implementation.md`（待写），通过 `superpowers:writing-plans` 拆 task-level plan。 估计 10 个 task：

1. **TushareService 加 8 个 method**（balance_sheet / cashflow / daily_basic / pe_history / forecast / dividend / holder_change / money_flow）+ unit test
2. **8 个 Tool class**（含 args/output Pydantic schema + cassette）+ ToolRegistry 注册
3. **`financial_research` skill bundle 数据层**（references/industry_benchmarks.json + recommendation_rules.yaml + position_size_rules.yaml）+ 3 Python helpers + unit test
4. **`financial_research` skill bundle 知识层**（11 methodology markdown 写作 + SKILL.md manifest + loader）
5. **Analyst / Writer prompt 改造**：注入 skill methodology 内容；Writer 调 Python helper（compute_position_size_pct / classify_recommendation）替代 LLM 算数字
6. **`plan_registry.py` 建** — 4 plan × ~4 subtask 设计（13 tool 任意组合）+ instantiate_plan() helper + unit test
7. **ResearchPlanner 重写**为 constrained router（plan_id + rationale 输出，β 模式 prompt + override 规则）
8. **plan_correctness_scorer 加** + Critic scorer list 6→7
9. **retry edge** 加 graph + ResearchState 加 planner_retry_count / planner_critic_feedback 字段 + integration test + 16+ router unit test + 4 differential golden case 重录（含 1 retry-trigger case）
10. **dogfood ≥10 ts_code** + memory `project_v0.8.5_architecture_landed.md` + PR + README/project-story update
