# Session Handoff — 2026-05-01 Evening → 下次 session 起点

> **目的**:让下次 session 5 分钟内进入 v0.7 工作状态,不浪费 token 重新摸底。

---

## 今天(2026-05-01)在主对话中 ship 了什么

```
brainstorming session a4ce0864(2026-04-29)立的 roadmap → 今天连推 3 站:

  PR #6  v0   chat agent skeleton           (merged d91b573)  -7,038 LOC
  PR #7  v0.5 research mode                 (merged 7eb2a9e)  +3,683 LOC
  PR #8  v0.6 Bocha search integration      (merged c85db6a)  +4,276 LOC
                                             ─────────────────
                                             净 +921 LOC,tests 82 → 233(+151)
```

main HEAD 现在是 `c85db6a`。所有 spec / plan / 实施 / cassette 都在 repo 里。

---

## 同一个架构模式经 3 次验证(portfolio 故事点)

每个 spec 都是 **`Protocol + factory + env-var switch`** 模式应用到不同域:

| spec | Protocol | factory | env switch | mock 后端 | real 后端 |
|---|---|---|---|---|---|
| v0 (Plan B/D) | `ChatClient` | `LLMService(client=...)` | `LLM_MODE=mock\|real` | `MockLLMClient` | `OpenAIClient` |
| v0.5 SUT swap | `SUT` | `EvalRunner(sut=...)` | (DI,无 env) | `_LLMServiceSUT` | `ChatAgent` / `ResearchAgent` |
| v0.6 Bocha | `BochaService` | `build_bocha_service_from_env()` | `BOCHA_MODE=mock\|real` | `_MockBochaAdapter` | `ReliableBochaService(BochaClient + 4 layers)` |

**v0.7 应用同一模式给 KB 域**:`KbSearchService` Protocol + `build_kb_service_from_env()` + `KB_MODE=mock|real`,mock 后端是 `MockKbSearchTool`(已存在,v0.5 留),real 后端是 `MilvusKbClient + ReliableKbService`(v0.7 新)。

---

## v0.7 起点(brainstorming session a4ce0864 决议)

**v0.7 = 真 kb_search + Milvus 接入 + KB corpus ingest**

依赖就位(检查 memory `project_v0.6_architecture_landed`):
- ✅ `BochaService` Protocol 模式经 2 次验证(Mock + Real)— v0.7 可镜像
- ✅ 4 件 reliability 套件(`RateLimiter` / `CircuitBreaker` / `QuotaCounter` / `CostBudget`)直接复用
- ✅ `BOCHA_MODE` env var pattern 镜像 `KB_MODE`
- ✅ 测试模式(L0 unit + L1 httpx.MockTransport + L2 cassette)经 v0.6 验证
- ✅ pre-commit ruff 0.15.x 一致(0 format 撞车风险)
- ✅ legacy `MockKbSearchTool` 已就位(`backend/app/tools/mock_kb_search.py`),v0.7 重命名 → `kb_search` 走 Protocol

**v0.7 真正新工作(scope 大头)**:
- Milvus 部署(本地 docker / Zilliz Cloud free tier)
- KB corpus 准备 + ingest pipeline(embedding + chunking + 元数据)
- 检索调优(top_k / similarity threshold / reranking)
- 上述都是 **数据工程** 性质,不是 v0/v0.5/v0.6 的接口工程

**预估**:7-10h(v0/v0.5/v0.6 的 1.5-2x),scope 较大可能拆成 v0.7(Milvus + ingest 骨架)+ v0.8(检索调优 + 真 corpus)。**brainstorming 第一题就是 scope 拆法**。

---

## v0.7 brainstorming 第一题(预设答案)

我会问你的第一个 scope 决议:**v0.7 包多大?**

```
A. 全做(Milvus 部署 + corpus ingest + 检索调优)— 10-14h,可能爆
B. 仅 Milvus 部署 + 1 个 sample corpus ingest(~10 docs)+ 基础检索 — 7-9h,scope 边界清晰
C. 仅 KbSearchService Protocol + 真 Milvus 调用骨架(corpus 留下次 ship)— 4-5h,故事点弱
D. 别的拆法
```

我倾向 **B**(v0.7 = 骨架 + 一份小 corpus 跑通,真业务 corpus 留 v0.8 + 检索调优 spec)。

---

## 你新 session 的第一句话(推荐)

复制粘贴即可(根据当时心情删改):

> 起 v0.7 brainstorming。前置:v0+v0.5+v0.6 已 merge 到 main(c85db6a),memory 已完整沉淀(13 条)。v0.7 范围 = 真 kb_search + Milvus + KB corpus ingest,brainstorming session a4ce0864 已决议。读 `docs/superpowers/_session-handoff/2026-05-01-evening.md` 拿到完整 anchor,然后调 brainstorming skill,scope 决议先问 A/B/C。

新 session 应该:
1. Read 这个 handoff 文件 → 5 秒拿到全部上下文
2. 自动 load memory → 知道 v0.6 落地细节
3. 调 brainstorming skill → 进入设计模式
4. 你回答 A/B/C → 后续问题逐个推进

---

## 已知 debt / 留给 v0.7+ 的 follow-up

不影响 v0.7 起步,但应该知道:

- **Agent ABC 没 generic 化**:v0.5 5 处 + v0.6 几处 `# type: ignore[override]` 累积。v0.7 / v1 的 chore task 处理(memory `feedback_agent_abc_state_generic`)
- **`MockWebSearchTool` cassette stub 仍叫 `mock_web_search`**:v0.6 Task 8 重命名时为了 cassette replay 不破坏保留旧名(`backend/tests/e2e/test_research_agent_cassette.py:133`)。下次 cassette re-record 时清理
- **`backend/app/router/research.py` 仍 inline 一段 importlib hack 加载 MockBochaService 给 KbSearchTool**:v0.7 真接 Milvus 时这段直接删
- **`.pre-commit-config.yaml` ruff 已 0.15.12 跟 venv 一致**(v0.6 Task 1 修),v0.7 实施时不会再撞 format
- **`backend/.env` 有 `BOCHA_MODE` 没设默认 mock**(实际 production 用 real,需要 BOCHA_API_KEY)。v0.7 加 `KB_MODE` 时类似 pattern

---

## 数字记录(可选,作为 portfolio 写故事时的素材)

```
今日 (2026-05-01):
  - 主对话 token 消耗: ~200k+(3 个 spec→plan→impl 周期)
  - subagent dispatch: ~30 次(implementer + 5 spec reviewer)
  - LLM cost: ~¥0.35(3 cassette 录制 + nightly 多次)
  - tests: 82 → 233(+151)
  - LOC: 净 +921(瘦身 v0 -7k + 增 v0.5 +3.7k + v0.6 +4.3k)
  - memory 沉淀: 13 条(8 feedback + 5 project 其他类)

跨会话 (含今日之前):
  - 项目总 commits 在 main: ~30
  - 6 个 spec 文件 / 6 个 plan 文件
  - 5 个 cassette
  - 233 tests passing
```

---

**晚安。今天 3 spec ship 是高强度,睡好了再起 v0.7 brainstorming。**
