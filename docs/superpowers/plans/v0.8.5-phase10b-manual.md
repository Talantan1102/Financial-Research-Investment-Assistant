# v0.8.5 Phase 10b — User Manual Run Checklist

Phase 9a (代码) + Phase 10a (docs) 已 commit。Phase 10b 是 user-bound 手跑步骤,因为涉及真 LLM 调用 (cost ~¥1)。

## Prerequisites

```bash
unset all_proxy https_proxy http_proxy
# verify .env DASHSCOPE_API_KEY active
uv run python -c "import os; print(bool(os.getenv('DASHSCOPE_API_KEY')))"
```

## Step 1: Phase 9b cassette 重录 + mock fixture sync (~¥0.8)

```bash
# 1.1 Update mock fixture for v0.8.5 ResearchPlan schema (mock fixture drift since Task 7)
# Edit backend/tests/fixtures/llm_mocks/agent_decisions.yaml:
# 找 "你是金融研究助手 research_planner" pattern, 改 response.content
# 输出 v0.8.5 ResearchPlan JSON: {"plan_id": "balanced", "rationale": "test"}
# (无 subtasks 字段, runtime instantiated)

# 1.2 删 + 重录 3 differential cassette
rm backend/tests/fixtures/cassettes/b1_differential/*.yaml
uv run pytest backend/tests/eval/golden_cases/b1_differential/test_b1_diff_capital_preservation.py --record-mode=once -v
uv run pytest backend/tests/eval/golden_cases/b1_differential/test_b1_diff_balanced.py --record-mode=once -v
uv run pytest backend/tests/eval/golden_cases/b1_differential/test_b1_diff_aggressive_growth.py --record-mode=once -v

# 1.3 un-xfail aggressive_growth (cassette 重录后)
# Edit backend/tests/eval/golden_cases/b1_differential/test_b1_diff_aggressive_growth.py
# 删除 pytestmark = pytest.mark.xfail(...)

# 1.4 录 4th differential retry-trigger cassette
# Edit backend/tests/eval/golden_cases/b1_differential/test_b1_diff_retry_trigger.py
# 改 _CASSETTE_RECORDED = False → _CASSETTE_RECORDED = True
uv run pytest backend/tests/eval/golden_cases/b1_differential/test_b1_diff_retry_trigger.py --record-mode=once -v

# 1.5 重录 e2e 茅台 cassette
rm backend/tests/fixtures/cassettes/test_b1_maotai_investment_dd_cassette/*.yaml
uv run pytest backend/tests/e2e/test_b1_maotai_investment_dd_cassette.py --record-mode=once -v
# (路径若不同, find: find backend/tests -name '*maotai*')

# 1.6 commit cassette + un-xfail + mock fixture
git add backend/tests/fixtures/cassettes backend/tests/fixtures/llm_mocks backend/tests/eval/golden_cases/b1_differential
git commit -m "feat(v0.8.5): Phase 9b — re-record 5 cassettes + un-xfail + mock fixture sync"
```

## Step 2: 跑全 ci 全 PASS

```bash
uv run poe ci
# Expected: ruff + mypy + 全 unit/integration/e2e 全 PASS
# 测试数 ~624 (564 unit + 60 integration / e2e)
```

## Step 3: nightly L3 eval

```bash
uv run poe nightly-local
# Expected: 4 differential golden + 旧 golden 全 PASS, cost ≤ ¥10
```

## Step 4: dogfood ≥10 ts_code

候选 ts_code: 600519(茅台) / 300750(宁德) / 002594(比亚迪) / 600036(招行) / 600030(中信证券) / 002230(科大讯飞) / 002415(海康) / 600276(恒瑞) / 000858(五粮液) / 002475(立讯)

每跑 1 次 B-1(启动 backend `uv run poe serve`,浏览器开 frontend,提交 6 字段 + user_message),主观 review:

- TL;DR 是否抓核心?
- recommendation + position_size_pct 是否符合 risk_tolerance × market_cap?
- 11 维度 SOP 注入是否在 narrative 中体现?
- LLM router plan_id 是否符合 6 字段 + user_message?
- plan_correctness scorer 评分是否合理?

记录在 git ignored `.dogfood-notes-v0.8.5.md`(本地)。

每发现 bug 1 commit:
```bash
git commit -m "fix(v0.8.5): <bug 简述>" -m "原因 layer: <impl|plan|spec>"
```

## Step 5: PR 创建

```bash
git push -u origin feat/v0.8.5-constrained-router

gh pr create --title "feat(v0.8.5): constrained LLM router + 17-component Anthropic Skills bundle + self-correcting retry" --body "$(cat <<'EOF'
## Summary
- v0.8.5 改造 plan-driven multi-agent 为 constrained LLM router (plan_id 4 选 1, schema enum 强约束 + override 规则)
- 新增 financial_research Anthropic Skills bundle 17 components (11 methodology .md + 3 references + 3 Python helpers)
- Critic 加第 7 scorer plan_correctness (LLM-as-judge); LangGraph self-correcting retry edge max 2 轮
- Tool inventory 5 → 13 (加 8 个 tushare-backed)
- Writer 调 Python helper 替代 LLM 算 recommendation/position_size (deterministic + footer 修正)

## Test plan
- [x] uv run poe ci 全 PASS (564 unit + 60+ integration/e2e)
- [x] nightly L3 eval 全 PASS (4 differential golden + 1 retry-trigger)
- [x] dogfood ≥10 ts_code 主观 review 通过
- [x] LLM router cost 平均 (含 retry) ≤ ¥0.5/run

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

## Cost 估算

| Step | 估算 cost |
|---|---|
| Step 1 cassette 重录 (5 cassette × ~¥0.15) | ¥0.8 |
| Step 3 nightly L3 eval | ≤¥10 (硬上限) |
| Step 4 dogfood ≥10 ts_code (~¥0.5/run × 10) | ¥5 |
| **合计** | **≤¥16** |
