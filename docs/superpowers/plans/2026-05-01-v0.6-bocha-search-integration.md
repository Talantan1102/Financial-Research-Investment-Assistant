# v0.6 Bocha Search Integration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Land production-grade Bocha Search API integration — real `BochaClient` (raw httpx) + 4 reliability components(RateLimiter / CircuitBreaker / QuotaCounter / CostBudget reuse)+ 5 类错误分类 + retry 策略,把 v0/v0.5 mock 路径升级到真接入,通过 `BOCHA_MODE` env var 切换。

**Architecture:**
- **真 LLM 入口模式延续** —— `app.services.bocha_factory.build_bocha_service_from_env()` 类比 `build_llm_service_from_env()`,根据 `BOCHA_MODE=mock|real` 选择 `MockBochaService`(v0.5 留)或 `BochaClient`(v0.6 新)
- **`BochaService` Protocol** —— 共同接口,工具(`WebSearchTool` / `GetNewsTool`)依赖 Protocol 不依赖具体实现,production 测试零回归
- **Reliability layer 共享** —— 单实例 RateLimiter + CircuitBreaker + QuotaCounter,WebSearchTool 和 GetNewsTool 都用同一组(同一 API key 同一份 quota)
- **legacy 参考** —— `backend/app/service/news_collection_service.py:55` 有真 Bocha API 用法的现成代码(URL / payload / response shape),Task 0 spike 只需验证 API key 有效

**Tech Stack:**
- 已就位:Python 3.13 (uv), Pydantic v2, httpx (≥0.27), pytest-recording, asyncio-throttle 不引入(自写 RateLimiter)
- 不引入新 runtime deps;**chore Task 1**:pre-commit ruff 0.6.9 → 0.15.x 升级(消除 v0.5 4 次 format 撞车根因)

**Status inherited from v0/v0.5:**
- `app.tools.{base,registry,get_stock_quote,get_financials,get_news,mock_web_search,mock_kb_search}` 全在,Tool ABC 不变
- `app.service.mock_bocha_service.MockBochaService` 保留(v0.5 留),作为 `BOCHA_MODE=mock` 后端
- `app.services.cost_budget.CostBudget`(Plan D)直接复用,加 `bocha_search_monthly` 实例
- `app.services.openai_client.build_llm_service_from_env()`(v0 Task 11)是 `bocha_factory` 模式参考
- `app.router.{chat,research}` production wiring 现在用 `MockBochaService(...)` 直构造,Task 10 改用 factory
- `agent_decisions.yaml` 含 `mock_web_search` 名,Task 8 重命名 `web_search` 时同步改

**Memory inputs(v0 + v0.5 sediment):**
- `feedback_dev_tool_version_pin_alignment` — pre-commit ruff 0.6 vs venv 0.15 撞车,Task 1 一刀切
- `feedback_unguarded_imports_after_delete` — Task 8 重命名后 grep 残留 reference
- `feedback_third_party_plugin_defaults` — httpx / asyncio.Lock 默认行为 spike 验证
- `feedback_test_env_modeling` — `BOCHA_MODE` env var 用 monkeypatch.setenv 注入,L0 conftest autouse 处理
- `feedback_agent_abc_state_generic` — 与 v0.6 无关(本次不改 Agent ABC),v1 处理
- `project_v0.5_architecture_landed` — ResearchAgent + Critic + 5 sub-agents 已就位,production wiring 改一行 factory 调用即可
- `project_eval_pipeline_contract` — Plan C/D 的 SQLite 文件(`eval.sqlite`)复用,新加 `search_quota_log` 表

---

## File Structure

| Path | Purpose | Created/Modified/Deleted |
|---|---|---|
| **新增 — services/(reliability)** | | |
| `backend/app/services/bocha_errors.py` | 5 exception classes | Create |
| `backend/app/services/rate_limiter.py` | 滑动窗口 RateLimiter | Create |
| `backend/app/services/circuit_breaker.py` | 三态 CircuitBreaker | Create |
| `backend/app/services/quota_counter.py` | SQLite QuotaCounter | Create |
| `backend/app/services/bocha_client.py` | raw httpx BochaClient | Create |
| `backend/app/services/bocha_factory.py` | `BochaService` Protocol + `build_bocha_service_from_env` | Create |
| **改造 — tools/** | | |
| `backend/app/tools/mock_web_search.py` | **删除**(git mv 重命名为 web_search.py) | Delete |
| `backend/app/tools/web_search.py` | rename + use BochaService Protocol(原 MockWebSearchTool 改名 WebSearchTool) | Create |
| `backend/app/tools/get_news.py` | **Modify**:`__init__(bocha: BochaService)` + 走 Protocol | Modify |
| **改造 — router/** | | |
| `backend/app/router/chat.py` | **Modify**:`get_chat_graph` 用 `build_bocha_service_from_env()` | Modify |
| `backend/app/router/research.py` | **Modify**:`get_research_graph` 用 `build_bocha_service_from_env()` | Modify |
| **改造 — config** | | |
| `backend/.env.example` | `BOCHA_MODE=mock` 加注释 + `BOCHA_BASE_URL=https://api.bochaai.com/v1/web-search` 加 placeholder | Modify |
| `backend/.env` | 用户本地加 `BOCHA_MODE=real`(implementer 不改) | Skip |
| `pyproject.toml` | dev tier 不变 | Skip |
| `.pre-commit-config.yaml` | **Modify**:ruff `rev: v0.6.9` → `rev: v0.15.12` | Modify |
| **测试 — unit/** | | |
| `backend/tests/unit/test_bocha_errors.py` | L0:5 类异常 instance + str | Create |
| `backend/tests/unit/test_rate_limiter.py` | L0:窗口边界 / 并发 acquire | Create |
| `backend/tests/unit/test_circuit_breaker.py` | L0:三态转换 + race | Create |
| `backend/tests/unit/test_quota_counter.py` | L0:SQLite 持久化 + would_exceed | Create |
| `backend/tests/unit/test_bocha_client.py` | L0:httpx.MockTransport + 5 类错误触发 | Create |
| `backend/tests/unit/test_bocha_factory.py` | L0:Protocol 满足 + factory 选择 | Create |
| `backend/tests/unit/test_v0_tools.py` | **Modify**:`mock_web_search` → `web_search` 重命名同步 | Modify |
| **测试 — integration/** | | |
| `backend/tests/integration/test_web_search_real_path.py` | L1:WebSearchTool + BochaClient + reliability 协同(httpx.MockTransport) | Create |
| `backend/tests/integration/test_get_news_real_path.py` | L1:GetNewsTool + BochaClient | Create |
| **测试 — e2e/** | | |
| `backend/tests/e2e/test_bocha_client_cassette.py` | L2:1 次真 Bocha 请求录 + replay | Create |
| **MockLLMClient fixtures** | | |
| `backend/tests/fixtures/llm_mocks/agent_decisions.yaml` | **Modify**:`required_tools: ["mock_web_search"]` → `["web_search"]` | Modify |

> **Notes**: ~10 新文件 + 6 改造 + 1 删除(rename)。粗算 +1.8k LOC(reliability 类 + tests + cassette)。

---

## Pre-flight check (Task 0 — done before dispatching Task 1)

> **2 个 spike,合计 ≤ 5 分钟。** Bocha API specifics 大部分从 legacy `news_collection_service.py:55` 已知,spike 主要验证当前 API key + quota 状态。

### Spike 1: Bocha API 真接入验证

```bash
unset all_proxy https_proxy http_proxy
set -a && source backend/.env && set +a
uv run python - <<'PY'
import httpx, asyncio, json, os

async def go():
    url = "https://api.bochaai.com/v1/web-search"
    headers = {
        "Authorization": f"Bearer {os.environ['BOCHA_API_KEY']}",
        "Content-Type": "application/json",
    }
    payload = {"query": "贵州茅台 600519", "summary": True, "count": 3, "page": 1}
    async with httpx.AsyncClient(timeout=15.0) as client:
        r = await client.post(url, headers=headers, json=payload)
        print(f"STATUS: {r.status_code}")
        print(f"HEADERS: {dict(r.headers)}")
        if r.status_code == 200:
            data = r.json()
            wp = data.get("data", {}).get("webPages", {}).get("value", [])
            print(f"RESULTS_COUNT: {len(wp)}")
            if wp:
                print(f"FIRST_KEYS: {list(wp[0].keys())}")
                print(f"FIRST_TITLE: {wp[0].get('name', '')[:60]}")
        else:
            print(f"ERROR_BODY: {r.text[:500]}")

asyncio.run(go())
PY
```

期望:`STATUS: 200`,`RESULTS_COUNT >= 1`,response 有 `name` / `url` / `summary` / `snippet` / `siteName` / `datePublished` 字段(legacy 代码已展示这些)。

记录在 plan retrospective:
- Bocha API 真打通过(STATUS: 200)
- response shape 与 legacy 一致 / 不一致(列差异)
- rate limit headers(检查是否有 `X-RateLimit-Remaining` 等头部供 RateLimiter 参考;legacy 代码没用 headers,可能博查不暴露)

**3 种结果对应行为**:
- **A. STATUS=200 + 字段齐全** → BochaClient 直接按 legacy 形态实现
- **B. STATUS=200 + 字段名变了** → BochaClient response 解析逻辑按实际调整,Task 3 plan 代码同步
- **C. STATUS=401 / 403 / 其他** → API key 状态不对或 quota 用完,plan 阻塞,implementer 报告作者重新申请 key

### Spike 2: ruff 0.15.x 升级范围探查

```bash
# 装 ruff 0.15.x 到一个临时 venv,跑 format check 看会改多少行
uv pip install --upgrade ruff 2>&1 | tail -3  # 已经是 0.15.12,无需重装
uv run ruff format --check . 2>&1 | tail -5
uv run ruff format --diff . 2>&1 | head -50
```

期望:`format --check` 全通过(因为 venv 已 0.15.12),或 reformat 数量已知(Task 1 跑全 format 时一次性 fix)。

记录在 plan retrospective:
- 升级 pre-commit ruff 后总共会 reformat 多少行(估算 Task 1 commit 大小)

---

## Task 1: chore — pre-commit ruff 0.6.9 → 0.15.x 升级

> **目的**:消除 v0.5 4 次 format 撞车的根因(pre-commit pinned 0.6.9 vs venv 0.15.12)。这是技术债清理,不是 web_search 业务功能。

**Files:**
- Modify: `.pre-commit-config.yaml`
- 可能 Modify: 受 0.15.x format 影响的 *.py 文件(全 backend sweep 一次)

- [ ] **Step 1: Read current `.pre-commit-config.yaml`**

```bash
cat .pre-commit-config.yaml | head -30
```

定位 `repo: https://github.com/astral-sh/ruff-pre-commit` block 的 `rev: v0.6.9` 行。

- [ ] **Step 2: Bump ruff rev**

```yaml
- repo: https://github.com/astral-sh/ruff-pre-commit
  rev: v0.15.12  # was v0.6.9 — aligned with venv ruff per pyproject ">=0.6"
  hooks:
    - id: ruff-format
    - id: ruff
      args: [--fix]
```

- [ ] **Step 3: Run pre-commit on all files to apply new format**

```bash
uv run pre-commit run --all-files 2>&1 | tail -20
```

预期:可能有少量文件 reformat。如果 reformat 数量 > 20 文件或 > 200 行,**暂停 + 报告**(可能引入 unintended scope changes,需要单独 PR 处理)。

- [ ] **Step 4: Verify ci**

```bash
uv run poe ci 2>&1 | tail -5
```

预期:全绿。所有原有测试通过(format-only 改动,zero behavior change)。

- [ ] **Step 5: Commit**

```bash
git add .pre-commit-config.yaml
git add -u  # 加上所有 reformat 的 *.py
git commit -m "$(cat <<'EOF'
chore(pre-commit): bump ruff v0.6.9 → v0.15.12 to align with venv

Removes the version drift that caused 4× format-bounce commits in v0.5
(documented in feedback_dev_tool_version_pin_alignment memory). venv
ruff resolves to 0.15.12 per pyproject `ruff>=0.6`; pre-commit hook now
uses the same major version. Includes any format-only diffs from the
upgrade.

原因 layer: chore
EOF
)"
```

---

## Task 2: `BochaErrors` — 5 类异常

**Files:**
- Create: `backend/app/services/bocha_errors.py`
- Create: `backend/tests/unit/test_bocha_errors.py`

- [ ] **Step 1: Write the failing test**

```python
# backend/tests/unit/test_bocha_errors.py
"""L0 — Bocha error class hierarchy and instantiation."""

import pytest

from app.services.bocha_errors import (
    BochaAuthError,
    BochaClientError,
    BochaError,
    BochaNetworkError,
    BochaRateLimitError,
    BochaServerError,
)


def test_all_inherit_from_base() -> None:
    for cls in (BochaNetworkError, BochaRateLimitError, BochaAuthError, BochaServerError, BochaClientError):
        assert issubclass(cls, BochaError)


def test_can_instantiate_with_message() -> None:
    e = BochaNetworkError("connection reset")
    assert str(e) == "connection reset"


def test_can_chain_with_cause() -> None:
    try:
        try:
            raise OSError("dns failure")
        except OSError as orig:
            raise BochaNetworkError("wrap") from orig
    except BochaNetworkError as e:
        assert isinstance(e.__cause__, OSError)


def test_distinct_classes() -> None:
    """Each class is its own subtype — except blocks can target one without catching others."""
    classes = [BochaNetworkError, BochaRateLimitError, BochaAuthError, BochaServerError, BochaClientError]
    for i, c1 in enumerate(classes):
        for j, c2 in enumerate(classes):
            if i != j:
                assert not issubclass(c1, c2)
```

- [ ] **Step 2: Run — expect fail**

```bash
uv run pytest backend/tests/unit/test_bocha_errors.py -v
```
Expected: ImportError.

- [ ] **Step 3: Implement `bocha_errors.py`**

```python
# backend/app/services/bocha_errors.py
"""Bocha API error hierarchy.

Each error class corresponds to a distinct retry / circuit-breaker / user-facing behavior:

| Class                   | HTTP / cause          | Retry?  | Notes                                  |
|-------------------------|-----------------------|---------|----------------------------------------|
| BochaNetworkError       | DNS / TCP / TLS / r/w | yes     | exponential backoff in caller         |
| BochaRateLimitError     | HTTP 429              | NO      | rate_limiter should have prevented     |
| BochaAuthError          | HTTP 401 / 403        | NO      | re-auth needed                         |
| BochaServerError        | HTTP 5xx              | yes     | exponential backoff                    |
| BochaClientError        | HTTP 4xx (other)      | NO      | request itself is malformed            |
"""

from __future__ import annotations


class BochaError(Exception):
    """Base for all BochaClient errors."""


class BochaNetworkError(BochaError):
    """Network failure: DNS / connection / TLS / read timeout."""


class BochaRateLimitError(BochaError):
    """HTTP 429 — rate limit exceeded.

    rate_limiter.acquire() should have prevented this; if hit, signal that
    the limiter and the server disagree on quota state. Caller should
    treat as breaker-failure event but not retry.
    """


class BochaAuthError(BochaError):
    """HTTP 401 / 403 — invalid or revoked credentials."""


class BochaServerError(BochaError):
    """HTTP 5xx — Bocha-side server error, likely transient."""


class BochaClientError(BochaError):
    """HTTP 4xx (other than 401 / 403 / 429) — request itself is malformed."""
```

- [ ] **Step 4: Verify**

```bash
uv run pytest backend/tests/unit/test_bocha_errors.py -v
uv run mypy backend/app/services/bocha_errors.py
```
Expected: 4 PASS, mypy clean.

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/bocha_errors.py backend/tests/unit/test_bocha_errors.py
git commit -m "$(cat <<'EOF'
feat(services): add BochaError hierarchy (5 classes)

5 distinct exception classes: BochaNetworkError / BochaRateLimitError /
BochaAuthError / BochaServerError / BochaClientError. Each maps to a
distinct retry / breaker / fail-loud behavior used by BochaClient (Task 3)
and WebSearchTool (Task 8) callers.

原因 layer: services
EOF
)"
```

---

## Task 3: `BochaClient` — raw httpx wrapper

**Files:**
- Create: `backend/app/services/bocha_client.py`
- Create: `backend/tests/unit/test_bocha_client.py`

> **API specifics from Spike 1 + legacy `news_collection_service.py:55`**:
> - URL: `https://api.bochaai.com/v1/web-search`
> - Method: `POST`
> - Headers: `Authorization: Bearer <key>`, `Content-Type: application/json`
> - Body: `{"query": str, "summary": True, "count": int, "page": 1}`
> - Response: `{"data": {"webPages": {"value": [{"name", "url", "summary", "snippet", "siteName", "datePublished"}, ...]}}}`
> - `search_type` 字段博查 API **不接受**(legacy 没用,Spike 1 验证) — 调用方(WebSearchTool / GetNewsTool)用 keyword 自行区分 news vs general

- [ ] **Step 1: Write the failing test**

```python
# backend/tests/unit/test_bocha_client.py
"""L0 — BochaClient HTTP request shape + 5-class error mapping."""

from typing import Any

import httpx
import pytest

from app.services.bocha_client import BochaClient
from app.services.bocha_errors import (
    BochaAuthError,
    BochaClientError,
    BochaNetworkError,
    BochaRateLimitError,
    BochaServerError,
)


def _ok_response(payload: dict[str, Any] | None = None) -> httpx.Response:
    body = payload or {
        "data": {
            "webPages": {
                "value": [
                    {
                        "name": "茅台",
                        "url": "https://example.com/maotai",
                        "summary": "贵州茅台股价 1820 元",
                        "snippet": "片段",
                        "siteName": "示例",
                        "datePublished": "2026-04-30",
                    },
                ],
            },
        },
    }
    return httpx.Response(200, json=body)


@pytest.mark.asyncio
async def test_search_request_shape() -> None:
    """Verifies POST URL + auth header + body shape."""
    captured: dict[str, Any] = {}

    def handler(request: httpx.Request) -> httpx.Response:
        captured["url"] = str(request.url)
        captured["auth"] = request.headers.get("Authorization", "")
        captured["body"] = request.read().decode()
        return _ok_response()

    transport = httpx.MockTransport(handler)
    client = BochaClient(api_key="test-key", transport=transport)
    result = await client.search(query="茅台", count=3)
    await client.aclose()

    assert captured["url"] == "https://api.bochaai.com/v1/web-search"
    assert captured["auth"] == "Bearer test-key"
    assert '"query"' in captured["body"]
    assert '"count": 3' in captured["body"]
    n = len(result["items"])
    assert n == 1
    assert result["items"][0]["title"] == "茅台"


@pytest.mark.asyncio
async def test_search_429_raises_rate_limit() -> None:
    transport = httpx.MockTransport(lambda req: httpx.Response(429, json={"error": "rate limit"}))
    client = BochaClient(api_key="k", transport=transport)
    with pytest.raises(BochaRateLimitError):
        await client.search(query="x", count=1)
    await client.aclose()


@pytest.mark.asyncio
async def test_search_401_raises_auth() -> None:
    transport = httpx.MockTransport(lambda req: httpx.Response(401, json={"error": "unauthorized"}))
    client = BochaClient(api_key="k", transport=transport)
    with pytest.raises(BochaAuthError):
        await client.search(query="x", count=1)
    await client.aclose()


@pytest.mark.asyncio
async def test_search_403_raises_auth() -> None:
    transport = httpx.MockTransport(lambda req: httpx.Response(403, json={}))
    client = BochaClient(api_key="k", transport=transport)
    with pytest.raises(BochaAuthError):
        await client.search(query="x", count=1)
    await client.aclose()


@pytest.mark.asyncio
async def test_search_500_raises_server() -> None:
    transport = httpx.MockTransport(lambda req: httpx.Response(500, json={"error": "server"}))
    client = BochaClient(api_key="k", transport=transport)
    with pytest.raises(BochaServerError):
        await client.search(query="x", count=1)
    await client.aclose()


@pytest.mark.asyncio
async def test_search_400_raises_client() -> None:
    transport = httpx.MockTransport(lambda req: httpx.Response(400, json={"error": "bad request"}))
    client = BochaClient(api_key="k", transport=transport)
    with pytest.raises(BochaClientError):
        await client.search(query="x", count=1)
    await client.aclose()


@pytest.mark.asyncio
async def test_search_network_error_raises_network() -> None:
    def handler(req: httpx.Request) -> httpx.Response:
        raise httpx.ConnectError("dns failure")

    transport = httpx.MockTransport(handler)
    client = BochaClient(api_key="k", transport=transport)
    with pytest.raises(BochaNetworkError):
        await client.search(query="x", count=1)
    await client.aclose()


@pytest.mark.asyncio
async def test_search_returns_normalized_items() -> None:
    """All response fields normalized to consistent keys (title / url / summary / snippet / site_name / date)."""
    transport = httpx.MockTransport(lambda req: _ok_response())
    client = BochaClient(api_key="k", transport=transport)
    result = await client.search(query="x", count=1)
    item = result["items"][0]
    assert "title" in item
    assert "url" in item
    assert "summary" in item
    assert "snippet" in item
    assert "site_name" in item
    assert "date" in item
    await client.aclose()
```

- [ ] **Step 2: Run — expect fail**

```bash
uv run pytest backend/tests/unit/test_bocha_client.py -v
```

- [ ] **Step 3: Implement `bocha_client.py`**

```python
# backend/app/services/bocha_client.py
"""BochaClient — async raw httpx wrapper for Bocha Search API.

Endpoint: POST https://api.bochaai.com/v1/web-search
Auth:     Authorization: Bearer <api_key>

Response shape normalized to:
  {"items": [{"title", "url", "summary", "snippet", "site_name", "date"}, ...]}
"""

from __future__ import annotations

from typing import Any

import httpx

from app.services.bocha_errors import (
    BochaAuthError,
    BochaClientError,
    BochaNetworkError,
    BochaRateLimitError,
    BochaServerError,
)

_DEFAULT_BASE_URL = "https://api.bochaai.com/v1/web-search"
_DEFAULT_TIMEOUT_S = 15.0


class BochaClient:
    """Async client for Bocha Search API. Single-shot call, no retry / no rate limit
    (caller layers those on top via RateLimiter / CircuitBreaker / retry helper)."""

    def __init__(
        self,
        api_key: str,
        *,
        base_url: str = _DEFAULT_BASE_URL,
        timeout_s: float = _DEFAULT_TIMEOUT_S,
        transport: httpx.AsyncBaseTransport | None = None,
    ) -> None:
        self._api_key = api_key
        self._base_url = base_url
        self._client = httpx.AsyncClient(timeout=timeout_s, transport=transport)

    async def search(self, *, query: str, count: int) -> dict[str, Any]:
        """Single web-search call. Raises one of 5 BochaError subclasses on failure.

        Returns dict with shape:
            {"items": [{"title", "url", "summary", "snippet", "site_name", "date"}, ...]}
        """
        headers = {
            "Authorization": f"Bearer {self._api_key}",
            "Content-Type": "application/json",
        }
        payload = {"query": query, "summary": True, "count": count, "page": 1}

        try:
            response = await self._client.post(self._base_url, headers=headers, json=payload)
        except (httpx.ConnectError, httpx.ReadTimeout, httpx.WriteTimeout, httpx.PoolTimeout) as e:
            raise BochaNetworkError(f"network error: {e}") from e
        except httpx.HTTPError as e:
            raise BochaNetworkError(f"transport error: {e}") from e

        status = response.status_code
        if status == 200:
            return _normalize(response.json())
        if status == 429:
            raise BochaRateLimitError(f"rate limit (429): {response.text[:200]}")
        if status in (401, 403):
            raise BochaAuthError(f"auth error ({status}): {response.text[:200]}")
        if 500 <= status < 600:
            raise BochaServerError(f"server error ({status}): {response.text[:200]}")
        raise BochaClientError(f"client error ({status}): {response.text[:200]}")

    async def aclose(self) -> None:
        await self._client.aclose()


def _normalize(raw: dict[str, Any]) -> dict[str, Any]:
    """Bocha response → unified shape used by tools."""
    web_pages = raw.get("data", {}).get("webPages", {}).get("value", [])
    items: list[dict[str, Any]] = []
    for entry in web_pages:
        if not isinstance(entry, dict):
            continue
        items.append(
            {
                "title": entry.get("name", ""),
                "url": entry.get("url", ""),
                "summary": entry.get("summary", "") or entry.get("snippet", ""),
                "snippet": entry.get("snippet", ""),
                "site_name": entry.get("siteName", ""),
                "date": entry.get("datePublished", ""),
            }
        )
    return {"items": items}
```

- [ ] **Step 4: Verify**

```bash
uv run pytest backend/tests/unit/test_bocha_client.py -v
uv run mypy backend/app/services/bocha_client.py
```

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/bocha_client.py backend/tests/unit/test_bocha_client.py
git commit -m "$(cat <<'EOF'
feat(services): add BochaClient raw httpx wrapper

POST https://api.bochaai.com/v1/web-search with Bearer auth. Maps HTTP
status to 5 BochaError subclasses (Network / RateLimit / Auth / Server /
Client). Response normalized to {"items": [{title, url, summary,
snippet, site_name, date}, ...]} for downstream tool consumption. No
retry / rate limit logic here — those layers live in RateLimiter,
CircuitBreaker, and retry helper (Tasks 4 / 5 + WebSearchTool wiring).

原因 layer: services
EOF
)"
```

---

## Task 4: `RateLimiter` — sliding window

**Files:**
- Create: `backend/app/services/rate_limiter.py`
- Create: `backend/tests/unit/test_rate_limiter.py`

- [ ] **Step 1: Write the failing test**

```python
# backend/tests/unit/test_rate_limiter.py
"""L0 — RateLimiter sliding window enforcement."""

import asyncio
import time

import pytest

from app.services.rate_limiter import RateLimiter


@pytest.mark.asyncio
async def test_under_limit_no_wait() -> None:
    rl = RateLimiter(max_calls=5, window_s=1.0)
    started = time.monotonic()
    for _ in range(5):
        await rl.acquire()
    elapsed = time.monotonic() - started
    assert elapsed < 0.05  # 5 instant calls under limit


@pytest.mark.asyncio
async def test_over_limit_blocks() -> None:
    rl = RateLimiter(max_calls=2, window_s=0.3)
    await rl.acquire()
    await rl.acquire()
    started = time.monotonic()
    await rl.acquire()  # this one must wait until window slides
    elapsed = time.monotonic() - started
    assert elapsed >= 0.25  # roughly 0.3s wait (allow jitter)
    assert elapsed < 1.0


@pytest.mark.asyncio
async def test_window_slides() -> None:
    rl = RateLimiter(max_calls=2, window_s=0.2)
    await rl.acquire()
    await rl.acquire()
    await asyncio.sleep(0.25)  # window passes
    started = time.monotonic()
    await rl.acquire()
    elapsed = time.monotonic() - started
    assert elapsed < 0.05  # no wait — window slid past first 2


@pytest.mark.asyncio
async def test_concurrent_acquire_serialized() -> None:
    """Concurrent acquire calls don't interleave timestamps inconsistently."""
    rl = RateLimiter(max_calls=3, window_s=0.5)

    async def task() -> float:
        started = time.monotonic()
        await rl.acquire()
        return time.monotonic() - started

    elapsed = await asyncio.gather(*[task() for _ in range(5)])
    # First 3 should be near-zero; last 2 should be ≥ 0.4s
    elapsed.sort()
    n_short = sum(1 for t in elapsed if t < 0.05)
    n_long = sum(1 for t in elapsed if t >= 0.4)
    assert n_short == 3
    assert n_long == 2
```

- [ ] **Step 2: Run — expect fail**

- [ ] **Step 3: Implement `rate_limiter.py`**

```python
# backend/app/services/rate_limiter.py
"""RateLimiter — sliding-window async rate limiter.

Tracks last N call timestamps in a deque; on acquire(), if N >= max_calls,
sleeps until the oldest timestamp falls outside window_s.

Thread/task-safe via asyncio.Lock — concurrent acquire() calls serialize.
"""

from __future__ import annotations

import asyncio
import time
from collections import deque


class RateLimiter:
    def __init__(self, *, max_calls: int, window_s: float = 60.0) -> None:
        if max_calls <= 0:
            raise ValueError("max_calls must be positive")
        if window_s <= 0:
            raise ValueError("window_s must be positive")
        self._max_calls = max_calls
        self._window_s = window_s
        self._timestamps: deque[float] = deque()
        self._lock = asyncio.Lock()

    async def acquire(self) -> None:
        """Block (await) until a call is allowed, then record current timestamp."""
        async with self._lock:
            while True:
                now = time.monotonic()
                while self._timestamps and now - self._timestamps[0] > self._window_s:
                    self._timestamps.popleft()
                if len(self._timestamps) < self._max_calls:
                    self._timestamps.append(now)
                    return
                wait_s = self._window_s - (now - self._timestamps[0])
                # release lock during sleep so other awaiters can re-check
                self._lock.release()
                try:
                    await asyncio.sleep(wait_s)
                finally:
                    await self._lock.acquire()
```

- [ ] **Step 4: Verify + commit**

```bash
uv run pytest backend/tests/unit/test_rate_limiter.py -v
uv run mypy backend/app/services/rate_limiter.py
```

```bash
git add backend/app/services/rate_limiter.py backend/tests/unit/test_rate_limiter.py
git commit -m "$(cat <<'EOF'
feat(services): add RateLimiter (sliding window, asyncio.Lock-safe)

Sliding window of last N timestamps in a deque; acquire() awaits until
caller is within max_calls per window_s. Concurrent acquires serialize
via asyncio.Lock; lock released during sleep so other awaiters re-check
on wakeup.

原因 layer: services
EOF
)"
```

---

## Task 5: `CircuitBreaker` — 三态

**Files:**
- Create: `backend/app/services/circuit_breaker.py`
- Create: `backend/tests/unit/test_circuit_breaker.py`

- [ ] **Step 1: Write the failing test**

```python
# backend/tests/unit/test_circuit_breaker.py
"""L0 — CircuitBreaker 3-state transitions."""

import asyncio

import pytest

from app.services.circuit_breaker import CircuitBreaker, CircuitOpenError


@pytest.mark.asyncio
async def test_initial_state_closed() -> None:
    cb = CircuitBreaker(failure_threshold=3, cooldown_s=0.1)
    assert cb.state == "closed"


@pytest.mark.asyncio
async def test_success_keeps_closed() -> None:
    cb = CircuitBreaker(failure_threshold=3, cooldown_s=0.1)

    async def ok() -> int:
        return 42

    result = await cb.call(ok)
    assert result == 42
    assert cb.state == "closed"


@pytest.mark.asyncio
async def test_threshold_opens_breaker() -> None:
    cb = CircuitBreaker(failure_threshold=3, cooldown_s=0.1)

    async def fail() -> None:
        raise RuntimeError("boom")

    for _ in range(3):
        with pytest.raises(RuntimeError):
            await cb.call(fail)
    assert cb.state == "open"


@pytest.mark.asyncio
async def test_open_breaker_short_circuits() -> None:
    cb = CircuitBreaker(failure_threshold=1, cooldown_s=0.5)

    async def fail() -> None:
        raise RuntimeError("boom")

    with pytest.raises(RuntimeError):
        await cb.call(fail)
    assert cb.state == "open"

    async def ok() -> int:
        return 1

    # Should fail-fast with CircuitOpenError, NOT call ok()
    called = False

    async def tracker() -> int:
        nonlocal called
        called = True
        return 1

    with pytest.raises(CircuitOpenError):
        await cb.call(tracker)
    assert not called  # short-circuited


@pytest.mark.asyncio
async def test_cooldown_transitions_to_half_open() -> None:
    cb = CircuitBreaker(failure_threshold=1, cooldown_s=0.1)

    async def fail() -> None:
        raise RuntimeError("boom")

    with pytest.raises(RuntimeError):
        await cb.call(fail)
    assert cb.state == "open"

    await asyncio.sleep(0.15)
    # Reading state after cooldown should auto-transition (or first call does)
    async def ok() -> int:
        return 7

    # Single trial allowed in half_open
    result = await cb.call(ok)
    assert result == 7
    assert cb.state == "closed"  # success in half_open closes


@pytest.mark.asyncio
async def test_half_open_failure_reopens() -> None:
    cb = CircuitBreaker(failure_threshold=1, cooldown_s=0.1)

    async def fail() -> None:
        raise RuntimeError("boom")

    with pytest.raises(RuntimeError):
        await cb.call(fail)
    await asyncio.sleep(0.15)

    with pytest.raises(RuntimeError):
        await cb.call(fail)
    assert cb.state == "open"
```

- [ ] **Step 2: Run — expect fail**

- [ ] **Step 3: Implement `circuit_breaker.py`**

```python
# backend/app/services/circuit_breaker.py
"""CircuitBreaker — 3-state breaker (closed / open / half_open).

State transitions:
  closed → open      : N consecutive failures
  open   → half_open : cooldown_s elapsed since opening
  half_open → closed : 1 successful call
  half_open → open   : 1 failed call

Concurrent calls in half_open are serialized via asyncio.Lock — only one
trial at a time.
"""

from __future__ import annotations

import asyncio
import time
from collections.abc import Awaitable, Callable
from typing import Literal, TypeVar

T = TypeVar("T")

State = Literal["closed", "open", "half_open"]


class CircuitOpenError(Exception):
    """Raised when a call is short-circuited due to open breaker."""


class CircuitBreaker:
    def __init__(self, *, failure_threshold: int = 5, cooldown_s: float = 30.0) -> None:
        if failure_threshold <= 0:
            raise ValueError("failure_threshold must be positive")
        if cooldown_s <= 0:
            raise ValueError("cooldown_s must be positive")
        self._failure_threshold = failure_threshold
        self._cooldown_s = cooldown_s
        self._state: State = "closed"
        self._consecutive_failures = 0
        self._opened_at: float | None = None
        self._half_open_lock = asyncio.Lock()

    @property
    def state(self) -> State:
        if self._state == "open" and self._opened_at is not None:
            if time.monotonic() - self._opened_at >= self._cooldown_s:
                self._state = "half_open"
        return self._state

    async def call(self, coro_factory: Callable[[], Awaitable[T]]) -> T:
        current = self.state

        if current == "open":
            raise CircuitOpenError("circuit breaker is open")

        if current == "half_open":
            async with self._half_open_lock:
                # Re-check inside lock (state may have changed during await)
                if self._state == "open":
                    raise CircuitOpenError("circuit breaker is open")
                try:
                    result = await coro_factory()
                except Exception:
                    self._state = "open"
                    self._opened_at = time.monotonic()
                    self._consecutive_failures += 1
                    raise
                else:
                    self._state = "closed"
                    self._consecutive_failures = 0
                    self._opened_at = None
                    return result

        # closed
        try:
            result = await coro_factory()
        except Exception:
            self._consecutive_failures += 1
            if self._consecutive_failures >= self._failure_threshold:
                self._state = "open"
                self._opened_at = time.monotonic()
            raise
        else:
            self._consecutive_failures = 0
            return result
```

- [ ] **Step 4: Verify + commit**

```bash
uv run pytest backend/tests/unit/test_circuit_breaker.py -v
uv run mypy backend/app/services/circuit_breaker.py
```

```bash
git add backend/app/services/circuit_breaker.py backend/tests/unit/test_circuit_breaker.py
git commit -m "$(cat <<'EOF'
feat(services): add CircuitBreaker (3-state with asyncio.Lock for half_open)

States: closed / open / half_open. closed→open after N consecutive
failures; open→half_open after cooldown_s; half_open→closed on 1 success
or →open on 1 failure. half_open allows only 1 concurrent trial via
asyncio.Lock.

原因 layer: services
EOF
)"
```

---

## Task 6: `QuotaCounter` — SQLite 持久化

**Files:**
- Create: `backend/app/services/quota_counter.py`
- Create: `backend/tests/unit/test_quota_counter.py`

- [ ] **Step 1: Write the failing test**

```python
# backend/tests/unit/test_quota_counter.py
"""L0 — QuotaCounter SQLite-backed monthly counter."""

from datetime import datetime, timezone
from pathlib import Path

import pytest

from app.services.quota_counter import QuotaCounter


def test_initial_count_zero(tmp_path: Path) -> None:
    counter = QuotaCounter(db_path=tmp_path / "quota.sqlite", name="test", monthly_limit=100)
    assert counter.used_this_month() == 0
    assert counter.remaining() == 100
    assert not counter.would_exceed()


def test_increment_persists(tmp_path: Path) -> None:
    db = tmp_path / "quota.sqlite"
    counter1 = QuotaCounter(db_path=db, name="test", monthly_limit=100)
    counter1.increment()
    counter1.increment()
    counter1.increment()

    # New instance, same file → state preserved
    counter2 = QuotaCounter(db_path=db, name="test", monthly_limit=100)
    assert counter2.used_this_month() == 3
    assert counter2.remaining() == 97


def test_separate_names_isolated(tmp_path: Path) -> None:
    db = tmp_path / "quota.sqlite"
    web = QuotaCounter(db_path=db, name="web_search", monthly_limit=100)
    news = QuotaCounter(db_path=db, name="get_news", monthly_limit=50)
    web.increment()
    web.increment()
    news.increment()

    assert web.used_this_month() == 2
    assert news.used_this_month() == 1


def test_would_exceed_at_limit(tmp_path: Path) -> None:
    counter = QuotaCounter(db_path=tmp_path / "quota.sqlite", name="t", monthly_limit=2)
    assert not counter.would_exceed()
    counter.increment()
    assert not counter.would_exceed()
    counter.increment()
    assert counter.would_exceed()  # at limit
    counter.increment()  # over
    assert counter.would_exceed()


def test_only_counts_current_month(tmp_path: Path) -> None:
    """Inserts a row from previous month directly; current_month query should not include it."""
    import sqlite3

    db = tmp_path / "quota.sqlite"
    counter = QuotaCounter(db_path=db, name="t", monthly_limit=100)
    counter.increment()  # initialize schema

    # Manually insert a stale row from 60 days ago
    stale_ts = int((datetime.now(timezone.utc).timestamp() - 60 * 24 * 3600))
    with sqlite3.connect(str(db)) as conn:
        conn.execute(
            "INSERT INTO search_quota_log (name, ts) VALUES (?, ?)",
            ("t", stale_ts),
        )
        conn.commit()

    # current month count should still be 1 (the increment, not the stale row)
    assert counter.used_this_month() == 1
```

- [ ] **Step 2: Run — expect fail**

- [ ] **Step 3: Implement `quota_counter.py`**

```python
# backend/app/services/quota_counter.py
"""QuotaCounter — SQLite-persisted monthly call counter.

Stores one row per increment; queries SUM where timestamp falls in current calendar month.
Multiple counters share the same SQLite file via the `name` column for isolation.
"""

from __future__ import annotations

import sqlite3
import time
from datetime import datetime, timezone
from pathlib import Path

_SCHEMA = """
CREATE TABLE IF NOT EXISTS search_quota_log (
    name TEXT NOT NULL,
    ts INTEGER NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_search_quota_name_ts ON search_quota_log(name, ts);
"""


def _month_start_ts(now_utc: datetime | None = None) -> int:
    now = now_utc or datetime.now(timezone.utc)
    month_start = now.replace(day=1, hour=0, minute=0, second=0, microsecond=0)
    return int(month_start.timestamp())


class QuotaCounter:
    def __init__(self, *, db_path: Path, name: str, monthly_limit: int) -> None:
        if monthly_limit < 0:
            raise ValueError("monthly_limit must be non-negative")
        self._db_path = db_path
        self._name = name
        self._monthly_limit = monthly_limit
        self._db_path.parent.mkdir(parents=True, exist_ok=True)
        with sqlite3.connect(str(self._db_path)) as conn:
            conn.executescript(_SCHEMA)
            conn.commit()

    def used_this_month(self) -> int:
        start_ts = _month_start_ts()
        with sqlite3.connect(str(self._db_path)) as conn:
            cur = conn.execute(
                "SELECT COUNT(*) FROM search_quota_log WHERE name = ? AND ts >= ?",
                (self._name, start_ts),
            )
            row = cur.fetchone()
            return int(row[0]) if row else 0

    def remaining(self) -> int:
        return max(0, self._monthly_limit - self.used_this_month())

    def would_exceed(self) -> bool:
        return self.used_this_month() >= self._monthly_limit

    def increment(self) -> None:
        with sqlite3.connect(str(self._db_path)) as conn:
            conn.execute(
                "INSERT INTO search_quota_log (name, ts) VALUES (?, ?)",
                (self._name, int(time.time())),
            )
            conn.commit()
```

- [ ] **Step 4: Verify + commit**

```bash
uv run pytest backend/tests/unit/test_quota_counter.py -v
uv run mypy backend/app/services/quota_counter.py
```

```bash
git add backend/app/services/quota_counter.py backend/tests/unit/test_quota_counter.py
git commit -m "$(cat <<'EOF'
feat(services): add QuotaCounter (SQLite-persisted monthly counter)

Stores 1 row per increment; SUM query filters to current calendar month
(month start = day=1, hour=0, in UTC). Multiple counters isolated by
`name` column on shared SQLite file.

原因 layer: services
EOF
)"
```

---

## Task 7: `BochaService` Protocol + `bocha_factory`

**Files:**
- Create: `backend/app/services/bocha_factory.py`
- Create: `backend/tests/unit/test_bocha_factory.py`

> **Goal**:`BochaService` Protocol 让 `WebSearchTool` / `GetNewsTool` 不依赖具体实现;`build_bocha_service_from_env()` 根据 `BOCHA_MODE` 返回 `MockBochaService`(legacy)或 `BochaClient`(新)。

- [ ] **Step 1: Verify MockBochaService 接口**

```bash
grep -n "async def generate_search_results\|async def search\|def __init__" backend/app/service/mock_bocha_service.py | head
```

确认 `MockBochaService` 现有方法签名。**预期**:`async def generate_search_results(query, search_type, count, ...)`(v0.5 Task 2 用过)。Protocol 必须**兼容这个签名**OR 在 factory 层做 adapter。

- [ ] **Step 2: Write the failing test**

```python
# backend/tests/unit/test_bocha_factory.py
"""L0 — BochaService Protocol satisfaction + factory mode selection."""

import os
from typing import Any

import pytest

from app.services.bocha_factory import BochaService, build_bocha_service_from_env


def _supports_protocol(obj: Any) -> bool:
    """Duck-type Protocol check."""
    return hasattr(obj, "search") and callable(obj.search)


def test_factory_mock_default(monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.delenv("BOCHA_MODE", raising=False)
    monkeypatch.setenv("LLM_MODE", "mock")
    svc = build_bocha_service_from_env()
    assert _supports_protocol(svc)
    # mock returns LLM-driven mock data
    assert type(svc).__name__ in ("_MockBochaAdapter", "MockBochaService")


def test_factory_mock_explicit(monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.setenv("BOCHA_MODE", "mock")
    monkeypatch.setenv("LLM_MODE", "mock")
    svc = build_bocha_service_from_env()
    assert _supports_protocol(svc)


def test_factory_real(monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.setenv("BOCHA_MODE", "real")
    monkeypatch.setenv("BOCHA_API_KEY", "fake-key-for-test")
    svc = build_bocha_service_from_env()
    assert _supports_protocol(svc)
    assert type(svc).__name__ == "BochaClient"


def test_factory_unknown_mode_rejected(monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.setenv("BOCHA_MODE", "weird")
    with pytest.raises(ValueError, match="BOCHA_MODE"):
        build_bocha_service_from_env()


def test_factory_real_missing_key_rejected(monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.setenv("BOCHA_MODE", "real")
    monkeypatch.delenv("BOCHA_API_KEY", raising=False)
    with pytest.raises(KeyError, match="BOCHA_API_KEY"):
        build_bocha_service_from_env()
```

- [ ] **Step 3: Run — expect fail**

- [ ] **Step 4: Implement `bocha_factory.py`**

```python
# backend/app/services/bocha_factory.py
"""BochaService Protocol + factory.

Tools (WebSearchTool, GetNewsTool) depend on the Protocol, not on a
concrete class. The factory build_bocha_service_from_env() picks
MockBochaService (legacy mock) or BochaClient (real) based on
BOCHA_MODE env var.

For backward compatibility with v0/v0.5 MockBochaService.generate_search_results(...)
signature, a thin _MockBochaAdapter wraps it to expose .search() shape.
"""

from __future__ import annotations

import os
from typing import Any, Protocol, runtime_checkable

from app.services.bocha_client import BochaClient


@runtime_checkable
class BochaService(Protocol):
    """Common interface for MockBochaService + BochaClient (search-only).

    Returns dict shape {"items": [...]} with normalized fields per Task 3.
    """

    async def search(self, *, query: str, count: int) -> dict[str, Any]: ...


class _MockBochaAdapter:
    """Wraps legacy MockBochaService.generate_search_results into BochaService.search shape."""

    def __init__(self) -> None:
        # Lazy import to avoid app/service/__init__.py legacy import chain at module load
        from importlib.util import spec_from_file_location, module_from_spec
        from pathlib import Path

        path = Path(__file__).resolve().parents[1] / "service" / "mock_bocha_service.py"
        spec = spec_from_file_location("_mock_bocha_module", str(path))
        if spec is None or spec.loader is None:
            raise RuntimeError(f"cannot load {path}")
        module = module_from_spec(spec)
        spec.loader.exec_module(module)
        from app.services.openai_client import build_llm_service_from_env

        self._inner = module.MockBochaService(llm=build_llm_service_from_env())

    async def search(self, *, query: str, count: int) -> dict[str, Any]:
        # MockBochaService default search_type="news" (v0.5 Task 2 verified)
        items = await self._inner.generate_search_results(
            query=query, search_type="news", count=count
        )
        if isinstance(items, dict) and "items" in items:
            return items
        # Some legacy paths return list directly
        return {"items": items if isinstance(items, list) else []}


def build_bocha_service_from_env() -> BochaService:
    """Pick BochaService impl per env:
    - BOCHA_MODE unset or "mock" → _MockBochaAdapter (LLM-driven mock)
    - BOCHA_MODE = "real" → BochaClient (httpx, Bocha API)
    Anything else raises ValueError.
    """
    mode = os.environ.get("BOCHA_MODE", "mock").lower()
    if mode == "mock":
        return _MockBochaAdapter()
    if mode == "real":
        if "BOCHA_API_KEY" not in os.environ:
            raise KeyError("BOCHA_API_KEY required when BOCHA_MODE=real")
        base_url = os.environ.get("BOCHA_BASE_URL", "https://api.bochaai.com/v1/web-search")
        return BochaClient(api_key=os.environ["BOCHA_API_KEY"], base_url=base_url)
    raise ValueError(f"BOCHA_MODE must be 'mock' or 'real', got {mode!r}")
```

- [ ] **Step 5: Verify + commit**

```bash
uv run pytest backend/tests/unit/test_bocha_factory.py -v
uv run mypy backend/app/services/bocha_factory.py
```

```bash
git add backend/app/services/bocha_factory.py backend/tests/unit/test_bocha_factory.py
git commit -m "$(cat <<'EOF'
feat(services): add BochaService Protocol + build_bocha_service_from_env factory

Tools depend on Protocol, factory picks MockBocha (legacy, default) or
BochaClient (real) per BOCHA_MODE env var. _MockBochaAdapter bridges
legacy generate_search_results signature to .search shape.

原因 layer: services
EOF
)"
```

---

## Task 7b: `ReliableBochaService` wrapper(reliability orchestration)

**Files:**
- Create: `backend/app/services/reliable_bocha_service.py`
- Create: `backend/tests/unit/test_reliable_bocha_service.py`
- Modify: `backend/app/services/bocha_factory.py`(real mode 返回 ReliableBochaService 包装 BochaClient)

> **Goal**:把 4 件 reliability 套件(RateLimiter + CircuitBreaker + QuotaCounter + CostBudget)+ retry 逻辑封装成单一 `ReliableBochaService`,**Tools 仍只见 BochaService Protocol**(零知情)。

- [ ] **Step 1: Write the failing test**

```python
# backend/tests/unit/test_reliable_bocha_service.py
"""L0 — ReliableBochaService coordinates 4 reliability layers + retry."""

import asyncio
from pathlib import Path
from typing import Any
from unittest.mock import AsyncMock

import pytest

from app.services.bocha_errors import BochaNetworkError, BochaServerError
from app.services.circuit_breaker import CircuitBreaker, CircuitOpenError
from app.services.cost_budget import CostBudget
from app.services.quota_counter import QuotaCounter
from app.services.rate_limiter import RateLimiter
from app.services.reliable_bocha_service import ReliableBochaService


def _build(tmp_path: Path, *, retry_count: int = 3, fail_threshold: int = 3) -> tuple[ReliableBochaService, AsyncMock]:
    inner = AsyncMock()
    inner.search = AsyncMock(return_value={"items": [{"title": "ok"}]})
    inner.aclose = AsyncMock()
    svc = ReliableBochaService(
        inner=inner,
        rate_limiter=RateLimiter(max_calls=100, window_s=1.0),
        circuit_breaker=CircuitBreaker(failure_threshold=fail_threshold, cooldown_s=0.1),
        quota_counter=QuotaCounter(db_path=tmp_path / "q.sqlite", name="t", monthly_limit=100),
        cost_budget=CostBudget(limit_cny=10.0),
        per_call_cost_cny=0.01,
        max_retries=retry_count,
    )
    return svc, inner


@pytest.mark.asyncio
async def test_happy_path_increments_counters(tmp_path: Path) -> None:
    svc, inner = _build(tmp_path)
    result = await svc.search(query="x", count=3)
    assert result["items"][0]["title"] == "ok"
    inner.search.assert_called_once_with(query="x", count=3)
    assert svc.quota_counter.used_this_month() == 1


@pytest.mark.asyncio
async def test_quota_exceeded_blocks(tmp_path: Path) -> None:
    svc, inner = _build(tmp_path)
    # Fill quota
    for _ in range(100):
        svc.quota_counter.increment()
    with pytest.raises(Exception, match="quota"):
        await svc.search(query="x", count=1)
    # inner not called
    inner.search.assert_not_called()


@pytest.mark.asyncio
async def test_retry_on_network_error(tmp_path: Path) -> None:
    svc, inner = _build(tmp_path, retry_count=3)
    inner.search = AsyncMock(side_effect=[
        BochaNetworkError("dns"),
        BochaNetworkError("dns"),
        {"items": [{"title": "recovered"}]},
    ])
    # Use very small backoff in test (override via class const if needed)
    svc._base_delay_s = 0.01  # type: ignore[assignment]
    result = await svc.search(query="x", count=1)
    assert result["items"][0]["title"] == "recovered"
    assert inner.search.call_count == 3


@pytest.mark.asyncio
async def test_retry_exhausted_raises(tmp_path: Path) -> None:
    svc, inner = _build(tmp_path, retry_count=2)
    inner.search = AsyncMock(side_effect=BochaNetworkError("dns"))
    svc._base_delay_s = 0.01  # type: ignore[assignment]
    with pytest.raises(BochaNetworkError):
        await svc.search(query="x", count=1)
    assert inner.search.call_count == 2  # 2 retries (max_retries=2)


@pytest.mark.asyncio
async def test_breaker_opens_after_threshold(tmp_path: Path) -> None:
    svc, inner = _build(tmp_path, retry_count=1, fail_threshold=2)
    inner.search = AsyncMock(side_effect=BochaServerError("500"))
    svc._base_delay_s = 0.01  # type: ignore[assignment]
    # First failure
    with pytest.raises(BochaServerError):
        await svc.search(query="x", count=1)
    # Second failure → opens breaker
    with pytest.raises(BochaServerError):
        await svc.search(query="x", count=1)
    # Third call short-circuits
    with pytest.raises(CircuitOpenError):
        await svc.search(query="x", count=1)
```

- [ ] **Step 2: Run — expect fail**

- [ ] **Step 3: Implement `reliable_bocha_service.py`**

```python
# backend/app/services/reliable_bocha_service.py
"""ReliableBochaService — wraps BochaService inner with 4 reliability layers + retry.

Layers (in order):
  1. quota_counter / cost_budget pre-check (fail fast)
  2. circuit_breaker.call(...)
     within breaker:
       3. rate_limiter.acquire (await)
       4. inner.search(...)
       5. retry on (BochaNetworkError, BochaServerError) up to max_retries
  6. on success: quota_counter.increment + cost_budget.charge
"""

from __future__ import annotations

import asyncio
import random
from typing import Any

from app.services.bocha_errors import BochaNetworkError, BochaServerError
from app.services.circuit_breaker import CircuitBreaker
from app.services.cost_budget import CostBudget
from app.services.quota_counter import QuotaCounter
from app.services.rate_limiter import RateLimiter


class ReliableBochaService:
    """BochaService Protocol-compatible: provides .search(query, count) -> dict."""

    def __init__(
        self,
        *,
        inner: Any,  # BochaService Protocol-compatible (typically BochaClient)
        rate_limiter: RateLimiter,
        circuit_breaker: CircuitBreaker,
        quota_counter: QuotaCounter,
        cost_budget: CostBudget,
        per_call_cost_cny: float = 0.01,
        max_retries: int = 3,
    ) -> None:
        self._inner = inner
        self._rate_limiter = rate_limiter
        self._circuit_breaker = circuit_breaker
        self.quota_counter = quota_counter
        self._cost_budget = cost_budget
        self._per_call_cost_cny = per_call_cost_cny
        self._max_retries = max_retries
        self._base_delay_s = 1.0
        self._jitter_max_s = 0.2

    async def search(self, *, query: str, count: int) -> dict[str, Any]:
        # 1. quota pre-check (fail fast, no inner call)
        if self.quota_counter.would_exceed():
            raise RuntimeError(
                f"quota exceeded: used {self.quota_counter.used_this_month()} of "
                f"{self.quota_counter._monthly_limit} this month"
            )
        # 1b. cost budget pre-check via Plan D's assert_under_limit()
        # (raises BudgetExceeded if cumulative spend already > limit_cny)
        self._cost_budget.assert_under_limit()

        # 2-5. breaker → rate_limit → inner with retry
        async def _attempt() -> dict[str, Any]:
            await self._rate_limiter.acquire()
            return await self._do_with_retry(query=query, count=count)

        result = await self._circuit_breaker.call(_attempt)

        # 6. on success
        self.quota_counter.increment()
        self._cost_budget.track(self._per_call_cost_cny)
        return result

    async def _do_with_retry(self, *, query: str, count: int) -> dict[str, Any]:
        """Exponential backoff + jitter; only retries Network/Server errors."""
        last: Exception | None = None
        for attempt in range(self._max_retries):
            try:
                return await self._inner.search(query=query, count=count)
            except (BochaNetworkError, BochaServerError) as e:
                last = e
                if attempt < self._max_retries - 1:
                    delay = self._base_delay_s * (2**attempt) + random.uniform(0, self._jitter_max_s)
                    await asyncio.sleep(delay)
                continue
        if last is None:
            raise RuntimeError("retry loop exited without exception (impossible)")
        raise last

    async def aclose(self) -> None:
        if hasattr(self._inner, "aclose"):
            await self._inner.aclose()
```

> **Note on CostBudget API**(Plan D 实现):Plan D 的 `CostBudget` 没有 `name` / `would_exceed_after` / `charge`,只有:
> - `__init__(limit_cny: float)` — no name
> - `track(cost_cny)` — adds cost (incremental tracking)
> - `assert_under_limit()` — raises `BudgetExceeded` if `spent > limit`
> - `remaining_cny` / `spent_cny` properties
>
> Plan code uses `assert_under_limit()` for fail-fast and `track()` for post-success increment. Cost budget is **in-memory** (resets on restart) — monthly persistence enforced by `QuotaCounter`(SQLite). v1+ may add SQLite-backed cost ledger if needed.

- [ ] **Step 4: Modify `bocha_factory.py` real mode 返回 ReliableBochaService**

```python
# In bocha_factory.py, replace the "real" branch in build_bocha_service_from_env():
#
#     if mode == "real":
#         if "BOCHA_API_KEY" not in os.environ:
#             raise KeyError("BOCHA_API_KEY required when BOCHA_MODE=real")
#         base_url = os.environ.get("BOCHA_BASE_URL", "https://api.bochaai.com/v1/web-search")
#         return BochaClient(api_key=os.environ["BOCHA_API_KEY"], base_url=base_url)
#
# With:

    if mode == "real":
        if "BOCHA_API_KEY" not in os.environ:
            raise KeyError("BOCHA_API_KEY required when BOCHA_MODE=real")
        base_url = os.environ.get("BOCHA_BASE_URL", "https://api.bochaai.com/v1/web-search")
        from pathlib import Path
        from app.services.circuit_breaker import CircuitBreaker
        from app.services.cost_budget import CostBudget
        from app.services.quota_counter import QuotaCounter
        from app.services.rate_limiter import RateLimiter
        from app.services.reliable_bocha_service import ReliableBochaService

        client = BochaClient(api_key=os.environ["BOCHA_API_KEY"], base_url=base_url)
        return ReliableBochaService(
            inner=client,
            rate_limiter=RateLimiter(max_calls=60, window_s=60.0),
            circuit_breaker=CircuitBreaker(failure_threshold=5, cooldown_s=30.0),
            quota_counter=QuotaCounter(
                db_path=Path("backend/data/eval.sqlite"),
                name="bocha_search_monthly",
                monthly_limit=1000,
            ),
            cost_budget=CostBudget(limit_cny=20.0),  # Plan D CostBudget has no `name` param
            per_call_cost_cny=0.02,
            max_retries=3,
        )
```

- [ ] **Step 5: Update test_bocha_factory.py — real mode now returns ReliableBochaService**

```python
def test_factory_real(monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.setenv("BOCHA_MODE", "real")
    monkeypatch.setenv("BOCHA_API_KEY", "fake-key-for-test")
    svc = build_bocha_service_from_env()
    assert _supports_protocol(svc)
    assert type(svc).__name__ == "ReliableBochaService"  # was "BochaClient"
```

- [ ] **Step 6: Verify + commit**

```bash
uv run pytest backend/tests/unit/test_reliable_bocha_service.py backend/tests/unit/test_bocha_factory.py -v
uv run mypy backend/app/services/
```

```bash
git add backend/app/services/reliable_bocha_service.py backend/app/services/bocha_factory.py backend/tests/unit/test_reliable_bocha_service.py backend/tests/unit/test_bocha_factory.py
git commit -m "$(cat <<'EOF'
feat(services): add ReliableBochaService wrapping 4 reliability layers + retry

Wraps BochaClient with quota_counter pre-check, circuit_breaker, rate_limiter,
exponential-backoff retry, and post-success counter/budget increment. Tools
get a BochaService Protocol-compatible object — they call .search() and the
wrapper handles all reliability concerns transparently.

bocha_factory real mode now returns ReliableBochaService(BochaClient + 4 layers).
Mock mode unchanged (no reliability needed for LLM-driven mock).

原因 layer: services
EOF
)"
```

---

## Task 8: `WebSearchTool` rename + use Protocol

**Files:**
- Delete (via git mv): `backend/app/tools/mock_web_search.py` → `backend/app/tools/web_search.py`
- Modify: `backend/app/tools/web_search.py` (renamed, refactored)
- Modify: `backend/tests/unit/test_v0_tools.py` and `backend/tests/unit/test_mock_search_tools.py` (rename references)

- [ ] **Step 1: git mv**

```bash
git mv backend/app/tools/mock_web_search.py backend/app/tools/web_search.py
```

- [ ] **Step 2: Refactor the renamed file**

Open `backend/app/tools/web_search.py`. Replace contents with:

```python
"""web_search Tool — uses BochaService Protocol (mock or real per BOCHA_MODE).

v0.5 was MockWebSearchTool against MockBochaService;
v0.6 renamed + Protocol-injected for real Bocha API integration.
"""

from __future__ import annotations

from typing import Any, Literal

from pydantic import BaseModel, Field

from app.services.bocha_factory import BochaService
from app.tools.base import Tool, ToolError


class WebSearchArgs(BaseModel):
    query: str
    search_type: Literal["news", "industry", "report"] = "news"
    count: int = Field(default=5, ge=1, le=20)


class WebSearchTool(Tool):
    name = "web_search"
    description = "搜索网络获取最新资讯或一般信息(v0.6 真接 Bocha API,可通过 BOCHA_MODE=mock 切回 mock)"
    args_schema = WebSearchArgs

    def __init__(self, bocha: BochaService) -> None:
        self._bocha = bocha

    async def run(self, args: BaseModel) -> dict[str, Any]:
        if not isinstance(args, WebSearchArgs):
            raise ToolError(f"args must be WebSearchArgs, got {type(args).__name__}")
        # Real Bocha API doesn't have search_type; augment query for news
        effective_query = args.query
        if args.search_type == "news":
            effective_query = f"{args.query} 最近新闻"
        elif args.search_type == "report":
            effective_query = f"{args.query} 研报"
        return await self._bocha.search(query=effective_query, count=args.count)
```

- [ ] **Step 3: Update tests**

`backend/tests/unit/test_mock_search_tools.py`:

```python
# At top of file: rename imports
from app.tools.web_search import WebSearchArgs  # was: mock_web_search
# Keep KbSearchArgs import unchanged (mock_kb_search not renamed in v0.6)
from app.tools.mock_kb_search import KbSearchArgs
```

The `WebSearchArgs` tests are unchanged.

- [ ] **Step 4: Update agent_decisions.yaml fixture**

```bash
sed -i.bak 's/"mock_web_search"/"web_search"/g' backend/tests/fixtures/llm_mocks/agent_decisions.yaml
rm backend/tests/fixtures/llm_mocks/agent_decisions.yaml.bak
```

Verify:
```bash
grep -n "mock_web_search\|web_search" backend/tests/fixtures/llm_mocks/agent_decisions.yaml | head
```
Expected: only `web_search` strings, no `mock_web_search`.

- [ ] **Step 5: grep for stragglers**

```bash
grep -rn "mock_web_search\|MockWebSearchTool" backend/ --include="*.py" 2>&1
```
Expected: zero matches (all usages have been migrated). If matches, fix:
- imports of `MockWebSearchTool` → `WebSearchTool`
- imports from `app.tools.mock_web_search` → `app.tools.web_search`
- DataCollector default args mapping `"mock_web_search"` → `"web_search"`

Likely files:
- `backend/app/agents/data_collector.py:67-70` (the `_default_args_for("mock_web_search", ...)` branch — rename)
- `backend/app/router/research.py` (registry.register(...))
- Any tests referencing the old class

- [ ] **Step 6: Verify + commit**

```bash
uv run pytest backend/tests/ -v 2>&1 | tail -20  # full suite, expect zero regression
uv run mypy backend/app/tools/ backend/app/agents/data_collector.py
```

```bash
git add -A
git commit -m "$(cat <<'EOF'
refactor(tools): rename mock_web_search → web_search + use BochaService Protocol

WebSearchTool now depends on BochaService Protocol (Task 7 factory picks
mock or real). Real Bocha API doesn't expose search_type — caller
augments query with "最近新闻" / "研报" keywords per WebSearchArgs.search_type.

DataCollector / agent_decisions.yaml / tests updated to use new tool
name. Zero behavior change when BOCHA_MODE unset (default mock).

原因 layer: services
EOF
)"
```

---

## Task 9: `GetNewsTool` use BochaService Protocol

**Files:**
- Modify: `backend/app/tools/get_news.py`
- Possibly Modify: existing `test_v0_tools.py` if it tests GetNewsTool internals

- [ ] **Step 1: Read current `get_news.py`**

```bash
cat backend/app/tools/get_news.py
```

Confirm current `__init__(self, mock_bocha: ...)` signature and `run()` body.

- [ ] **Step 2: Refactor**

Replace `__init__` parameter and `run()` body to use `BochaService` Protocol:

```python
# backend/app/tools/get_news.py (Modify only the relevant parts)
"""get_news Tool — uses BochaService Protocol for news search."""

from __future__ import annotations

from typing import Any

from pydantic import BaseModel, Field

from app.services.bocha_factory import BochaService
from app.tools.base import Tool, ToolError


class NewsArgs(BaseModel):
    ts_code: str | None = None
    n: int = Field(default=5, ge=1, le=20)
    days_back: int = Field(default=7, ge=1, le=90)


class GetNewsTool(Tool):
    name = "get_news"
    description = "获取最近的财经资讯(可指定股票或大盘),走 Bocha 搜索"
    args_schema = NewsArgs

    def __init__(self, bocha: BochaService) -> None:
        self._bocha = bocha

    async def run(self, args: BaseModel) -> dict[str, Any]:
        if not isinstance(args, NewsArgs):
            raise ToolError(f"args must be NewsArgs, got {type(args).__name__}")
        # Build query: add ts_code if specified, plus "最近新闻" keyword
        parts: list[str] = []
        if args.ts_code:
            parts.append(args.ts_code)
        parts.append("最近新闻")
        query = " ".join(parts)
        return await self._bocha.search(query=query, count=args.n)
```

- [ ] **Step 3: Update any failing tests**

If existing `test_v0_tools.py` tests `GetNewsTool` with `mock_bocha=...` keyword — update to `bocha=...`.

- [ ] **Step 4: Verify + commit**

```bash
uv run pytest backend/tests/ -v 2>&1 | tail -10  # full suite, zero regression
uv run mypy backend/app/tools/get_news.py
```

```bash
git add backend/app/tools/get_news.py backend/tests/
git commit -m "$(cat <<'EOF'
refactor(tools): GetNewsTool uses BochaService Protocol

__init__ takes BochaService (factory picks mock/real per BOCHA_MODE).
Query augmented with "最近新闻" keyword for news intent (real Bocha API
has no search_type field).

原因 layer: services
EOF
)"
```

---

## Task 10: Production wiring + `.env.example` update

**Files:**
- Modify: `backend/app/router/chat.py` — `get_chat_graph()` use factory
- Modify: `backend/app/router/research.py` — `get_research_graph()` use factory
- Modify: `backend/.env.example` — add `BOCHA_MODE` placeholder

- [ ] **Step 1: Update `.env.example`**

Add at appropriate position (near existing `BOCHA_API_KEY`):

```
# Bocha 搜索 API 模式
# - mock: 用 MockBochaService(LLM 生成的假搜索结果)— 测试 / 开发默认
# - real: 真打 Bocha API(需要 BOCHA_API_KEY)
BOCHA_MODE=mock
BOCHA_BASE_URL=https://api.bochaai.com/v1/web-search
```

- [ ] **Step 2: Update `backend/app/router/chat.py`**

Find the `get_chat_graph()` function. Replace the `mock_bocha = MockBochaService(llm=...)` line:

```python
# Before:
# from app.service.mock_bocha_service import MockBochaService  # type: ignore[import]
# mock_bocha = MockBochaService(llm=llm)

# After:
from app.services.bocha_factory import build_bocha_service_from_env

bocha = build_bocha_service_from_env()
```

Then update `registry.register(GetNewsTool(mock_bocha=mock_bocha))` → `registry.register(GetNewsTool(bocha=bocha))`.

- [ ] **Step 3: Update `backend/app/router/research.py`**

Same pattern. Find `get_research_graph()` function, replace the `mock_bocha = MockBochaService(...)` line and the `MockWebSearchTool / MockKbSearchTool / GetNewsTool` registrations:

```python
# Before:
from app.service.mock_bocha_service import MockBochaService  # type: ignore[import]
mock_bocha = MockBochaService(llm=llm)
registry.register(GetNewsTool(mock_bocha=mock_bocha))
registry.register(MockWebSearchTool(mock_bocha=mock_bocha))
registry.register(MockKbSearchTool(mock_bocha=mock_bocha))

# After:
from app.services.bocha_factory import build_bocha_service_from_env

bocha = build_bocha_service_from_env()
registry.register(GetNewsTool(bocha=bocha))
registry.register(WebSearchTool(bocha=bocha))
registry.register(MockKbSearchTool(mock_bocha=mock_bocha))  # kb still legacy mock; v0.7 changes
```

> **Note on KbSearchTool**:v0.7 spec 会把它换 Protocol。v0.6 暂时保留 `MockBochaService` 直 inject(从 `_MockBochaAdapter._inner` 拿出?或者保持原写法)。**最简单**:KbSearchTool init 还是 `mock_bocha=MockBochaService_instance`,直接构造一次老 MockBochaService 给它。代码量微增,但 v0.7 一并清理。

具体 KbSearchTool wiring 实现:
```python
# Task 10 wiring (research.py only, since chat doesn't have KbSearch):
# Keep one MockBochaService instance for KbSearchTool legacy use
from importlib.util import spec_from_file_location, module_from_spec
from pathlib import Path

mock_path = Path(__file__).resolve().parents[1] / "service" / "mock_bocha_service.py"
mock_spec = spec_from_file_location("_legacy_mock_bocha", str(mock_path))
mock_module = module_from_spec(mock_spec)
mock_spec.loader.exec_module(mock_module)
legacy_mock_bocha = mock_module.MockBochaService(llm=llm)
registry.register(MockKbSearchTool(mock_bocha=legacy_mock_bocha))
```

(粗糙,v0.7 一刀切。)

- [ ] **Step 4: Verify**

Start uvicorn briefly to verify imports:
```bash
unset all_proxy https_proxy http_proxy
timeout 5 uv run uvicorn app.app_main:app --port 8765 --app-dir backend 2>&1 | tail -10
```

Expected: no ImportError. (App may fail readiness since /api/v0/chat needs a real call, but startup must succeed.)

```bash
uv run poe ci 2>&1 | tail -5
```

Expected: green.

- [ ] **Step 5: Commit**

```bash
git add backend/.env.example backend/app/router/chat.py backend/app/router/research.py
git commit -m "$(cat <<'EOF'
feat(router): production wiring uses BochaService factory (BOCHA_MODE env)

chat router + research router now build_bocha_service_from_env() and
inject the resulting BochaService into WebSearchTool / GetNewsTool.
Default BOCHA_MODE=mock keeps existing tests passing without env changes.

KbSearchTool stays on legacy MockBochaService directly for v0.6 — v0.7
spec will migrate it to its own Protocol.

原因 layer: services
EOF
)"
```

---

## Task 11: L1 + L2 cassette tests

**Files:**
- Create: `backend/tests/integration/test_web_search_real_path.py`
- Create: `backend/tests/integration/test_get_news_real_path.py`
- Create: `backend/tests/e2e/test_bocha_client_cassette.py`
- Create: `backend/tests/fixtures/cassettes/test_bocha_client_cassette/...yaml`

### Step 1: L1 `test_web_search_real_path.py`

```python
# backend/tests/integration/test_web_search_real_path.py
"""L1 — WebSearchTool against BochaClient (httpx.MockTransport) with reliability layers.

Verifies the full real path: WebSearchTool.run → BochaClient.search → 200 → normalized items.
Doesn't exercise rate_limiter / circuit_breaker (those have their own L0 tests).
"""

from typing import Any

import httpx
import pytest

from app.services.bocha_client import BochaClient
from app.tools.web_search import WebSearchArgs, WebSearchTool


@pytest.mark.asyncio
async def test_web_search_tool_real_path() -> None:
    captured_query: str = ""

    def handler(request: httpx.Request) -> httpx.Response:
        nonlocal captured_query
        body = request.read().decode()
        import json as _json
        captured_query = _json.loads(body).get("query", "")
        return httpx.Response(
            200,
            json={
                "data": {
                    "webPages": {
                        "value": [
                            {
                                "name": "茅台研报",
                                "url": "https://example.com/r1",
                                "summary": "summary text",
                                "snippet": "snip",
                                "siteName": "site",
                                "datePublished": "2026-04-30",
                            }
                        ]
                    }
                }
            },
        )

    client = BochaClient(api_key="test", transport=httpx.MockTransport(handler))
    tool = WebSearchTool(bocha=client)
    args = WebSearchArgs(query="茅台", search_type="news", count=3)

    result = await tool.run(args)
    n = len(result["items"])
    assert n == 1
    assert result["items"][0]["title"] == "茅台研报"
    # search_type="news" augments query with "最近新闻"
    assert "最近新闻" in captured_query
    await client.aclose()
```

### Step 2: L1 `test_get_news_real_path.py`

```python
# backend/tests/integration/test_get_news_real_path.py
"""L1 — GetNewsTool against BochaClient (httpx.MockTransport)."""

import json

import httpx
import pytest

from app.services.bocha_client import BochaClient
from app.tools.get_news import GetNewsTool, NewsArgs


@pytest.mark.asyncio
async def test_get_news_tool_real_path() -> None:
    captured: dict[str, str] = {}

    def handler(request: httpx.Request) -> httpx.Response:
        captured["query"] = json.loads(request.read().decode()).get("query", "")
        return httpx.Response(
            200,
            json={"data": {"webPages": {"value": [{"name": "新闻 1", "url": "u", "summary": "s", "snippet": "n", "siteName": "x", "datePublished": "2026-04-30"}]}}},
        )

    client = BochaClient(api_key="test", transport=httpx.MockTransport(handler))
    tool = GetNewsTool(bocha=client)

    result = await tool.run(NewsArgs(ts_code="600519.SH", n=3))
    assert "600519.SH" in captured["query"]
    assert "最近新闻" in captured["query"]
    n = len(result["items"])
    assert n == 1
    await client.aclose()
```

### Step 3: L2 cassette `test_bocha_client_cassette.py`

Read `backend/tests/e2e/test_chat_agent_cassette.py` for VCR pattern reference. Then create:

```python
# backend/tests/e2e/test_bocha_client_cassette.py
"""L2 cassette — BochaClient with real Bocha API, recorded via VCR."""

import os
from pathlib import Path

import pytest

from app.services.bocha_client import BochaClient


# vcr_config fixture pattern: refer to existing test_chat_agent_cassette.py
# and tests/conftest.py for cassette_dir + sanitize hooks.

@pytest.mark.asyncio
@pytest.mark.vcr
async def test_bocha_client_search_maotai() -> None:
    """1 real Bocha API call. Records once, replays offline."""
    api_key = os.environ.get("BOCHA_API_KEY", "")
    if not api_key:
        pytest.skip("BOCHA_API_KEY not set; skipping cassette record/replay")

    client = BochaClient(api_key=api_key)
    result = await client.search(query="贵州茅台 600519", count=3)
    n = len(result["items"])
    assert n >= 1
    item = result["items"][0]
    assert "title" in item
    assert "url" in item
    await client.aclose()
```

### Step 4: Record cassette

```bash
unset all_proxy https_proxy http_proxy
set -a && source backend/.env && set +a
VCR_RECORD_MODE=once uv run pytest backend/tests/e2e/test_bocha_client_cassette.py -v -s 2>&1 | tail -10
```

Expected:
- 1 real Bocha API call (~¥0.001)
- New cassette under `backend/tests/fixtures/cassettes/test_bocha_client_cassette/`
- Cassette ≤ 50KB

### Step 5: Verify cassette

```bash
wc -c backend/tests/fixtures/cassettes/test_bocha_client_cassette/*.yaml
grep -i "Bearer\|API_KEY" backend/tests/fixtures/cassettes/test_bocha_client_cassette/*.yaml
```
Expected: ≤ 50KB, no live key (sanitize hook should `Bearer REDACTED`).

### Step 6: Replay verify

```bash
unset VCR_RECORD_MODE
uv run pytest backend/tests/e2e/test_bocha_client_cassette.py -v
```
Expected: PASS in <1s.

### Step 7: Run full ci

```bash
uv run poe ci 2>&1 | tail -5
```
Expected: 200+ tests pass (191 v0.5 baseline + 10-15 new from Tasks 2-11).

### Step 8: Commit

```bash
git add backend/tests/integration/test_web_search_real_path.py backend/tests/integration/test_get_news_real_path.py backend/tests/e2e/test_bocha_client_cassette.py backend/tests/fixtures/cassettes/test_bocha_client_cassette/
git commit -m "$(cat <<'EOF'
test(integration+e2e): L1 real-path tests + L2 BochaClient cassette

L1: WebSearchTool / GetNewsTool against BochaClient with httpx.MockTransport
verifying request shape + query augmentation. L2: 1 real Bocha API call
recorded via VCR (~¥0.001 once), replays offline. Sanitized credentials.

原因 layer: services
EOF
)"
```

---

## Task 12: Final ci + retrospective

- [ ] **Step 1: Final ci sweep**

```bash
uv run poe ci 2>&1 | tail -5
```

Expected: ~200+ tests green (191 baseline + 10-15 new). mypy strict 200+ source files clean.

- [ ] **Step 2: nightly-local sweep**

```bash
set -a && source backend/.env && set +a
unset all_proxy https_proxy http_proxy
uv run poe nightly-local 2>&1 | tail -10
```

Expected: 5 cassettes validate (4 baseline + 1 new bocha cassette), 0 drifts.

- [ ] **Step 3: Manual production smoke test (optional)**

```bash
# In one terminal:
BOCHA_MODE=real uv run poe serve

# In another:
curl -N -X POST http://localhost:8000/api/v0.5/research \
  -H "Content-Type: application/json" \
  -d '{"user_message": "深度分析贵州茅台 600519.SH"}'
```

Expected: SSE stream with real search data flowing through DataCollector → Analyst → Writer → Critic.

If quota concern: limit count=2 to keep below ~20 search calls during smoke test.

- [ ] **Step 4: Fill spec retrospective**

Open `docs/superpowers/specs/2026-05-01-v0.6-bocha-search-integration-spec.md` and fill the `Retrospective` section at the end (3 right designs / 3 wrong / 3 next-spec rules / memory sediment / v0.7 trigger).

- [ ] **Step 5: Fill plan retrospective**

Same for `docs/superpowers/plans/2026-05-01-v0.6-bocha-search-integration.md` (Task 0 spike result + final retro).

- [ ] **Step 6: Memory sediment**

Write 1-3 new memory files based on what was learned:
- Suggested: `feedback_external_api_reliability_pattern` if this 4-layer pattern applies broadly
- Suggested: `project_v0.6_architecture_landed`
- Update `MEMORY.md` index

- [ ] **Step 7: Commit retrospective**

```bash
git add docs/superpowers/specs/2026-05-01-v0.6-bocha-search-integration-spec.md docs/superpowers/plans/2026-05-01-v0.6-bocha-search-integration.md
git commit -m "$(cat <<'EOF'
docs(spec+plan): v0.6 retrospective — Bocha search integration landed

(Implementer fills: total commits, LOC delta, test pass count, cassette
size, real API smoke-test result, plus the 3-right / 3-wrong / 3-next
sections in spec + plan retrospective.)

原因 layer: chore
EOF
)"
```

---

## Acceptance Criteria

- [ ] `uv run poe ci` green (~200+ tests)
- [ ] LangGraph + 5-agent pipeline still works (zero regression on v0/v0.5 tests)
- [ ] `BOCHA_MODE=mock` (default) unchanged behavior — all v0.5 mock-path tests pass without env changes
- [ ] `BOCHA_MODE=real` + valid BOCHA_API_KEY → real Bocha API request through WebSearchTool / GetNewsTool
- [ ] 5 BochaError classes covered by L0 unit tests
- [ ] RateLimiter / CircuitBreaker / QuotaCounter each ≥ 4 L0 tests covering happy + edge paths
- [ ] L1 integration: httpx.MockTransport verifies request shape + reliability layer wiring
- [ ] L2 cassette: 1 real Bocha call, replay <1s, ≤ 50KB, no live credentials
- [ ] pre-commit ruff bumped to 0.15.x (no more format ping-pong)
- [ ] `MockWebSearchTool` / `mock_web_search.py` fully removed (zero `grep -rn mock_web_search backend/`)
- [ ] services / agents / tools / orchestration 任意 .py 不 `import openai`(grep 确认;`app.services.openai_client` 是唯一例外)
- [ ] Plan retrospective + spec retrospective 填写
- [ ] 1-3 new memory entries added

---

## Test plan (run on completion)

- Local: `uv run poe ci`
- Local: `uv run poe nightly-local`(cassette replay,0 drift)
- Local: `BOCHA_MODE=real uv run poe serve` + curl POST /api/v0.5/research(真实搜索 + 研报全流程)
- Local: `unset VCR_RECORD_MODE && uv run pytest backend/tests/e2e/test_bocha_client_cassette.py -v`(replay-only)
- Cloud: PR `pr.yml` ≤ 5min ci 全绿(包括 v0.6 新 cassette test)
- Cloud: 下次 nightly cron 跑 `nightly.yml`,所有 cassettes drift = 0

---

## Notes for the implementer

- **Task 0 spike 必跑**(Bocha API key 验证),如果 STATUS != 200 立刻报告作者(可能 quota 用完或 key 失效)
- **ruff format 注意**(v0.5 4 次撞车教训):assert 写 single-line `assert n >= 1, msg`,Task 1 完成后应该不再有 format 撞车
- **Plan B/C/D + v0/v0.5 契约保持是 acceptance criteria**:LLMService.chat / Judge.score / EvalRunner.run_one / Tool ABC / Agent ABC 都不能破坏
- **ToolRegistry.execute 已有 ToolError 自动 wrap**(v0 Task 4 实现),Bocha 错误最终会被它包成 ToolResult(success=False);DataCollector 的"partial failure 不阻塞其他 tool"模式不变

---

## Plan retrospective — Task 0 spike result

> Filled in by implementer at Task 0.

### Spike 1: Bocha API real call verification
- **Date probed**: 2026-05-01
- **STATUS**: **200**(API key 有效,quota 充足)
- **RESULTS_COUNT**: 3
- **Field shape**: 与 legacy `news_collection_service.py:55` 一致 + 多了几个字段(`cachedPageUrl` / `dateLastCrawled` / `displayUrl` / `id` / `isFamilyFriendly` / `isNavigational` / `language` / `siteIcon`)。**v0.6 BochaClient 只用 `name` / `url` / `summary` / `snippet` / `siteName` / `datePublished` 6 个核心字段**(_normalize 已写),其余 ignore
- **Rate-limit headers**: 不暴露(博查 API response 没有 `X-RateLimit-Remaining` 等头)→ RateLimiter 用 client-side max_calls=60/min 默认,不基于 server hint
- **Action taken**: BochaClient 实现 = plan 草拟代码,无需调整。Task 3 直接进入 implementation。

### Spike 2: ruff 0.15 upgrade scope
- **Date probed**: 2026-05-01
- **venv ruff version**: 0.15.12
- **Files needing reformat by 0.15**: **0**(`format --check` 全 179 文件 clean) — 因为 v0.5 implementer 已经 manual 处理过 0.15 兼容(v0.5 4 次 chore(format) 跟进 commit 实质上把全 codebase 升级了)
- **Action taken**: Task 1 commit size 估算 = **仅 .pre-commit-config.yaml 1 文件 1 行改动**(`rev: v0.6.9` → `rev: v0.15.12`),无 *.py reformat 涟漪。最干净的 chore commit。

---

## Retrospective

> Filled in by implementer at Task 12.

**Implementation completion date**:
**Branch**:
**Total commits**:
**Total time**:

### 对的设计(3 条)

### 错的设计 / plan 漏了什么(3 条)

### 下个 plan 要避免(3 条)

### 沉淀到 memory

### Subagent-driven-development 节奏复盘

### v0.7 启动条件
