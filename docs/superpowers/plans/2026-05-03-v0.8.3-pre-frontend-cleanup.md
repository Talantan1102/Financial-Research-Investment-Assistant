# v0.8.3-pre Frontend Cleanup + 项目个人化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 清理 156 文件 legacy company copyright header + 砍 bidding 页 + 写后端 LEGACY_LAYOUT.md / 前端 page mapping / design tokens 文档 + 设计语言对齐(B/C 双 theme + Antd ConfigProvider)+ rebrand 到 "AI 投研 copilot"

**Architecture:** B 路线(中度清理 + 评估 + 文档化), 不动业务代码. 单 PR / 7 task / 5 commit / 2.5-4 天 wall time

**Tech Stack:** bash + sed + git tools + Antd 5 ConfigProvider + React 19 + Vite 6 + TypeScript 5.7 + uv (Python 3.11+)

**Spec**: `docs/superpowers/specs/2026-05-03-v0.8.3-pre-frontend-cleanup-design.md`

**Branch**: `feat/v0.8.3-pre-cleanup`(从 main `0dcc091` 开)

---

## Task 1 — 156 文件 legacy copyright header 批量删除

**Files:**
- Create: `scripts/cleanup_legacy_copyright.sh`(留 repo 作 audit trail)
- Modify: 197 code files(audit 结果, 见下;原估 156 是 spec 写时未包括 .scss/.js/.css)
  - .py: 73(2 行 `#` comment 格式)
  - .tsx: 44 / .ts: 39 / .scss: 39 / .js: 1 / .css: 1(共 124, 4 行 `/** */` 块格式)
  - .md: 1(`docs/superpowers/plans/2026-05-03-v0.8.3-pre-frontend-cleanup.md` body text 含 string, 由 script 自 exclude)
  - 自身脚本 `scripts/cleanup_legacy_copyright.sh` 也含 string, 由 script 自 exclude

**Pre-task: 切分支**

- [ ] **Step 0: 从 main `0dcc091` 切分支**

```bash
git checkout main
git pull --ff-only
git log -1 --oneline  # 验证 HEAD = 0dcc091
git checkout -b feat/v0.8.3-pre-cleanup
```

Expected: branch created, HEAD 跟 main 一致

- [ ] **Step 1: 写 cleanup 脚本**

Create `scripts/cleanup_legacy_copyright.sh`:

```bash
#!/usr/bin/env bash
# Cleanup legacy company copyright header from tracked code files.
#
# Two header formats handled (per audit 2026-05-03):
#   .py files (2-line, # comment style):
#       # Copyright © 2026 深圳市深维智见教育科技有限公司 版权所有
#       # 未经授权,禁止转售或仿制。
#   .tsx/.ts/.jsx/.js/.scss/.css/.html files (4-line, /** */ block):
#       /**
#        * Copyright © 2026 深圳市深维智见教育科技有限公司 版权所有
#        * 未经授权,禁止转售或仿制。
#        */
#
# Self-excluded:
#   - This script itself (LEGACY_STRING value contains the string)
#   - docs/superpowers/plans/* (plan docs legitimately contain the string for documentation)
#
# Other .md files with the string are SKIPPED (no handler defined).
#
# Usage:
#   bash scripts/cleanup_legacy_copyright.sh           # dry-run
#   bash scripts/cleanup_legacy_copyright.sh --apply   # actually edit files

set -euo pipefail

LEGACY_STRING="深圳市深维智见教育科技有限公司"
APPLY=0
[[ "${1:-}" == "--apply" ]] && APPLY=1

# Get all tracked files containing the legacy string,
# excluding self + plan docs.
mapfile -t FILES < <(
  git ls-files \
  | grep -vE '^docs/superpowers/plans/|^scripts/cleanup_legacy_copyright\.sh$' \
  | xargs -I{} grep -l "$LEGACY_STRING" {} 2>/dev/null \
  || true
)

echo "Found ${#FILES[@]} code files containing legacy copyright (excluding plan docs + self)."

if [[ ${#FILES[@]} -eq 0 ]]; then
  echo "Nothing to do."
  exit 0
fi

if [[ $APPLY -eq 0 ]]; then
  echo "DRY-RUN. Files (first 20):"
  printf '  %s\n' "${FILES[@]:0:20}"
  [[ ${#FILES[@]} -gt 20 ]] && echo "  ... and $((${#FILES[@]} - 20)) more"
  echo ""
  echo "Re-run with --apply to actually edit."
  exit 0
fi

CLEANED=0
SKIPPED=0
for f in "${FILES[@]}"; do
  case "$f" in
    *.py)
      # 2-line # comment block (lines 1-2)
      head -n 2 "$f" | grep -q "$LEGACY_STRING" || {
        echo "SKIP (.py legacy not in first 2 lines): $f"
        SKIPPED=$((SKIPPED+1))
        continue
      }
      sed -i.bak '1,2d' "$f"
      rm "${f}.bak"
      CLEANED=$((CLEANED+1))
      ;;
    *.tsx|*.ts|*.jsx|*.js|*.scss|*.css|*.html)
      # 4-line /** ... */ block (lines 1-4)
      head -n 4 "$f" | grep -q "$LEGACY_STRING" || {
        echo "SKIP (legacy not in first 4 lines): $f"
        SKIPPED=$((SKIPPED+1))
        continue
      }
      sed -i.bak '1,4d' "$f"
      rm "${f}.bak"
      CLEANED=$((CLEANED+1))
      ;;
    *.md)
      echo "SKIP (md no handler): $f"
      SKIPPED=$((SKIPPED+1))
      ;;
    *)
      echo "SKIP (unknown extension): $f"
      SKIPPED=$((SKIPPED+1))
      ;;
  esac
done

echo ""
echo "Cleaned: $CLEANED files"
echo "Skipped: $SKIPPED files"
echo "Run 'git diff --stat' to review."
```

- [ ] **Step 2: chmod + dry-run**

```bash
chmod +x scripts/cleanup_legacy_copyright.sh
bash scripts/cleanup_legacy_copyright.sh
```

Expected: `Found 197 code files containing legacy copyright (excluding plan docs + self).` + DRY-RUN message + first 20 file paths.

If count is not 197, **STOP** and investigate (legacy string variant? new files added? script self-exclusion regex broken?). Resume only after count matches expectation OR controller acknowledges divergence.

- [ ] **Step 3: Apply + view diff**

```bash
bash scripts/cleanup_legacy_copyright.sh --apply
git diff --stat | tail -5
```

Expected: `Cleaned: 197 files` + `Skipped: 0 files` + `git diff --stat` summary showing 197 files modified.

Per-file deletion counts (mixed):
- 73 `.py` files: -2 lines each (`# comment` 2 行 header)
- 124 other code files (`.tsx`/`.ts`/`.scss`/`.js`/`.css`): -4 lines each (`/** */` 4 行 block)
- Total: 73*2 + 124*4 = 146 + 496 = **642 total deletions, 0 insertions**

If counts deviate, STOP and investigate.

- [ ] **Step 4: Verify zero footprint + 4-line per file invariant**

```bash
# Code files (excluding plan + script self) should be 0 hits
git ls-files \
  | grep -vE '^docs/superpowers/plans/|^scripts/cleanup_legacy_copyright\.sh$' \
  | xargs grep -l "深圳市深维智见教育科技有限公司" 2>/dev/null \
  | wc -l

# Total diff stats
git diff --shortstat

# Per-file deletion sanity:
# - .py files: -2 each
# - other code files: -4 each
git diff --numstat | grep '\.py$' | awk '$1 != 0 || $2 != 2 { print "WARN .py " $0 }' | head -10
git diff --numstat | grep -E '\.(tsx|ts|jsx|js|scss|css|html)$' | awk '$1 != 0 || $2 != 4 { print "WARN code " $0 }' | head -10
```

Expected:
- First command: `0`(zero hits in code files; plan + script self still contain the string for documentation purposes)
- Second command: `197 files changed, 642 deletions(-)`(approx — exact 642 = 73*2 + 124*4)
- Third + fourth command: empty(no WARN lines)

If WARN lines, investigate — sed may have over/under-deleted. Use `git checkout -- <file>` to revert specific files and re-investigate.

- [ ] **Step 5: 跑 backend mypy + ruff**

```bash
cd backend
uv run poe ci 2>&1 | tail -20
cd ..
```

Expected: All checks passed(mypy 0 error, ruff 0 error). 删 comment 不应影响 type/lint check.

- [ ] **Step 6: 跑 backend test**

```bash
cd backend
uv run pytest -x --tb=short 2>&1 | tail -10
cd ..
```

Expected: 基线 289 passed + 1 skipped(同 v0.8.2 ship 时基线)

- [ ] **Step 7: 前端 lint + build**

```bash
cd frontend
npm run lint 2>&1 | tail -10
npm run build 2>&1 | tail -10
cd ..
```

Expected: lint 不增加 error/warning(基线 ≤ 94 errors / 10 warnings, 全部 pre-existing), build success(`✓ built in ...s`)

- [ ] **Step 8: 前端 dev server smoke test**

```bash
cd frontend
npm run dev &
DEV_PID=$!
sleep 5
curl -sf http://localhost:5173/ >/dev/null && echo "[OK] dev server up" || echo "FAIL"
kill $DEV_PID 2>/dev/null || true
cd ..
```

Expected: `[OK] dev server up`

- [ ] **Step 9: Commit**

```bash
git add scripts/cleanup_legacy_copyright.sh
git add -u  # all 156 modified files
git status --short | head -5
git status --short | wc -l  # should be 198 (197 modified code files + 1 new script)

git commit -m "$(cat <<'EOF'
chore(cleanup): remove legacy company copyright header from 197 code files

Batch sed cleanup via scripts/cleanup_legacy_copyright.sh (kept in repo as
audit trail). Pre-cleanup baseline: 197 code files contained the legacy
copyright; post-cleanup: 0 hits in code files.

Two header formats handled:
- 73 .py files: 2-line # comment block
- 124 .tsx/.ts/.scss/.js/.css files: 4-line /** */ block
- Total deletions: 642 (73*2 + 124*4)

Self-excluded from cleanup (legitimately contain the string for documentation):
- docs/superpowers/plans/* (plan docs)
- scripts/cleanup_legacy_copyright.sh (self)

No code logic touched.

Spec: docs/superpowers/specs/2026-05-03-v0.8.3-pre-frontend-cleanup-design.md (T1)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git log --oneline -1
```

Expected: 1 new commit, message above. Pre-commit hook should pass.

---

## Task 2 — 砍 frontend bidding 页 + legacy data 评估

**Files:**
- Delete: `frontend/src/pages/bidding/`(整个目录)
- Modify: `frontend/src/router/routes.tsx`(删 bidding 路由)
- Modify: `frontend/src/layout/base/nav.tsx` 或 `nav-item.tsx`(删 nav 项)
- Evaluate(maybe modify): `frontend/src/configs/data/news.ts` / `host.ts`

- [ ] **Step 1: grep 找 bidding 引用**

```bash
grep -rEn "bidding|招标" frontend/src/ 2>/dev/null
```

Expected: 列出 pages/bidding/* 自身 + router / nav 中的引用。记下每处 file:line, 后续 step 一并清理。

- [ ] **Step 2: 删 bidding pages 目录**

```bash
rm -rf frontend/src/pages/bidding/
ls frontend/src/pages/  # should not contain bidding
```

Expected: bidding/ 不在 list 中.

- [ ] **Step 3: 删 router 中 bidding 路由**

Read `frontend/src/router/routes.tsx`, locate the route entry containing `bidding`(可能形如 `path: '/bidding'` or `import { Bidding } from '@/pages/bidding'`)。Use Edit tool to remove that entry + the import line.

After edit, verify:

```bash
grep -nE "bidding|Bidding" frontend/src/router/routes.tsx
```

Expected: 0 hits.

- [ ] **Step 4: 删 nav 中 bidding 项**

```bash
grep -nE "bidding|招标" frontend/src/layout/base/nav.tsx frontend/src/layout/base/nav-item.tsx
```

Locate the nav item entry. Edit to remove the entry + any import.

Verify after edit:

```bash
grep -rEn "bidding|招标" frontend/src/ 2>/dev/null
```

Expected: 0 hits (excluding backend `app/models/news.py` which has `BiddingInfo` ORM class — that's backend legacy and stays per LEGACY_LAYOUT, T3).

- [ ] **Step 5: 评估 configs/data/news.ts**

```bash
cat frontend/src/configs/data/news.ts
```

Decision based on content:
- If file contains hardcoded mock industry news data unused by v0.8.x main path → **delete**
- If file contains shared constants still referenced(grep `import.*from.*configs/data/news` frontend/src/) → **keep**, add inline comment `// legacy data; v0.8.x main path consumes Bocha API instead`

Apply the decision. If kept, run grep to confirm references; if deleted, grep to confirm no orphan imports:

```bash
grep -rE "from.*configs/data/news" frontend/src/
```

If file deleted, expected: 0 hits.

- [ ] **Step 6: 评估 configs/data/host.ts**

```bash
cat frontend/src/configs/data/host.ts
```

Decision:
- If contains API host configuration still in use(grep references)→ **keep**(usually production config)
- If unused → delete

Apply. Verify:

```bash
grep -rE "from.*configs/data/host" frontend/src/
```

- [ ] **Step 7: dev server smoke test**

```bash
cd frontend
npm run dev &
DEV_PID=$!
sleep 5
curl -sf http://localhost:5173/ >/dev/null && echo "[OK] index page up" || echo "FAIL"
# Try chat page
curl -sf http://localhost:5173/chat >/dev/null && echo "[OK] chat page up" || echo "WARN chat unreachable"
kill $DEV_PID 2>/dev/null || true
cd ..
```

Expected: `[OK] index page up`. Chat may require auth, WARN OK.

- [ ] **Step 8: 前端 lint + build verify**

```bash
cd frontend
npm run lint 2>&1 | tail -5
npm run build 2>&1 | tail -5
cd ..
```

Expected: lint 不增加 error/warning(基线 ≤ 94 errors / 10 warnings, 全部 pre-existing), build success.

- [ ] **Step 9: Commit**

```bash
git add -A frontend/
git status --short

git commit -m "$(cat <<'EOF'
chore(frontend): remove bidding page + legacy data audit

- Delete frontend/src/pages/bidding/ (招标 page, unrelated to "AI 投研 copilot" scope)
- Remove bidding route from router/routes.tsx + nav item
- Evaluate configs/data/news.ts and host.ts (kept/deleted per usage audit)

Spec: docs/superpowers/specs/2026-05-03-v0.8.3-pre-frontend-cleanup-design.md (T2)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git log --oneline -1
```

---

## Task 3 — backend LEGACY_LAYOUT.md + 4 个 __init__.py docstring

**Files:**
- Create: `backend/LEGACY_LAYOUT.md`
- Modify: `backend/app/core/__init__.py`
- Modify: `backend/app/config/__init__.py`
- Modify: `backend/app/models/__init__.py`
- Modify: `backend/app/service/__init__.py`

- [ ] **Step 1: 写 backend/LEGACY_LAYOUT.md**

Create `backend/LEGACY_LAYOUT.md`:

```markdown
# Backend Legacy 模块清单 + v1.x 演进计划

本文档列出 backend 里跟 v0.8.x 主路径(`app/services/*` 复数 / `app/agents/*` / `app/orchestration/*` / `app/tools/*` / `app/kb/*`)并存的 legacy 模块, 以及未来演进计划。

## 开发规则

- **新功能优先 import `app/services/*`(复数)主线**
- 不要给 legacy 模块加新功能
- legacy 模块仅作 v0.8.x 主路径的"基础设施"使用

## Legacy 模块清单

| 模块 | 用途 | 当前依赖方 | v1.x 演进计划 |
|---|---|---|---|
| `app/core/database.py` | SQLAlchemy `Base` + `engine` + `get_db` + `SessionLocal` | router 13 个 + `app_main` + 多个 service | v1.1.0 改 Postgres + tenant_id, 保留位置 |
| `app/core/security.py` | OAuth/JWT helper | `auth_router` | v1.1.1 OAuth 升级时重写 |
| `app/core/redis_client.py` | Redis cache wrapper | `research_router` | v1.1.1 Redis 缓存升级时扩 |
| `app/config/llm_config.py` | LLM 配置 + `get_config` | `services/openai_client`(v0.8.x 主路径)+ 多个 router | v0.8.x 主路径仍在用, 不变 |
| `app/config/industry_config.py` | 行业配置 | `news_router` / `news_collection_service` | legacy, v0.8.x 不用 |
| `app/config/stock_mapping.py` | 股票名 → 代码映射 | `deep_research_v2`(legacy) | legacy, 可能被新 tools 替代 |
| `app/models/user.py` | User ORM | router(`auth` / `database` / `memory` / `attachment` / `knowledge` / `session`) | v1.1.0 加 tenant_id 列 |
| `app/models/chat.py` | ChatMessage / ChatSession / LongTermMemory / ChatAttachment ORM | router(`session` / `attachment` / `memory`)+ `memory_service` | v1.1.0 加 tenant_id 列 |
| `app/models/knowledge.py` | Document / KnowledgeBase ORM | `knowledge_router` | v1.1.0 加 tenant_id 列 |
| `app/models/news.py` | BiddingInfo / IndustryNews / NewsCollectionTask ORM | `news_collection_service` | legacy(BiddingInfo 不再用), v1.x 评估砍 |
| `app/models/industry_data.py` | CompanyData / IndustryStats / PolicyData ORM | `seed_industry_data` script | legacy, v1.x 评估砍 |
| `app/models/research.py` | ResearchCheckpoint ORM | `checkpoint_service` | legacy(v0.8.x 不用 checkpoint), v1.x 评估砍 |
| `app/service/checkpoint_service.py` | LangGraph checkpoint 持久化 | (待 grep 看是否被 agents 用) | v0.8.x 主路径用 SqliteSaver, 这个 service 可能 unused |
| `app/service/scheduler_service.py` | 调度任务 | (待 grep) | v0.8.3 持仓预警可能复用其调度框架, 否则 legacy |
| `app/service/news_collection_service.py` | 新闻采集 batch | (待 grep) | legacy, v0.8.x 用 Bocha 实时 |
| `app/service/memory_service.py` | LongTermMemory CRUD | `memory_router` | v0.8.4 Memory 子系统 v1 实施时合并到 `app/services/memory_*` |
| `app/service/mock_bocha_service.py` | Mock Bocha API | (历史) | v0.6 真接 Bocha 后已 deprecated, 可删 |
| `app/service/mock_tushare_service.py` | Mock tushare API | `app/tools/get_financials.py` / `get_stock_quote.py`(try/except guard) | v0.8.3 真接 tushare 后 deprecated |
| `app/service/deep_research_v2/*` | 上一代 research subgraph | (legacy 替代品 = `app/orchestration/research_*`) | legacy, v1.x 可整删 |

## 边界

本文档只列**当前** legacy 状态; v0.8.3 / v0.8.4 / v0.8.5 / v1.1 实施时按"演进计划"列分阶段迁移。
```

- [ ] **Step 2: 加 app/core/__init__.py docstring**

Read current content of `backend/app/core/__init__.py`. If it's empty or contains only minor imports, prepend the docstring. If it has complex content, add the docstring at the very top above existing content.

Edit to set content to:

```python
"""Legacy module — see backend/LEGACY_LAYOUT.md for module-by-module status and v1.x evolution plan.

New features should prefer importing from `app/services/*` (plural, v0.8.x main path).
"""
```

(If existing imports/code follows, keep them after the docstring.)

- [ ] **Step 3: 加 app/config/__init__.py docstring**

Same pattern as Step 2 for `backend/app/config/__init__.py`:

```python
"""Legacy module — see backend/LEGACY_LAYOUT.md for module-by-module status and v1.x evolution plan.

New features should prefer importing from `app/services/*` (plural, v0.8.x main path).
"""
```

- [ ] **Step 4: 加 app/models/__init__.py docstring**

Same for `backend/app/models/__init__.py`:

```python
"""Legacy module — see backend/LEGACY_LAYOUT.md for module-by-module status and v1.x evolution plan.

New features should prefer importing from `app/services/*` (plural, v0.8.x main path).
"""
```

- [ ] **Step 5: 加 app/service/__init__.py docstring**

Same for `backend/app/service/__init__.py`(注意 service 单数, 不是 services):

```python
"""Legacy module — see backend/LEGACY_LAYOUT.md for module-by-module status and v1.x evolution plan.

New features should prefer importing from `app/services/*` (plural, v0.8.x main path).
"""
```

- [ ] **Step 6: 跑 mypy + ruff verify**

```bash
cd backend
uv run poe ci 2>&1 | tail -10
cd ..
```

Expected: mypy 0 error, ruff 0 error. Docstring 加上不应破坏 ci.

If ruff D rule complains about docstring format, adjust(usually D200 short single-line / D205 1 blank between summary and body — current docstring is 2 paragraphs, D205 should be satisfied with the blank line).

If mypy complains about untyped `__init__.py`, that's pre-existing not new — verify by `git stash` + re-running `uv run poe ci` and check if error count is the same.

---

## Task 4 — `docs/frontend-page-mapping.md`

**Files:**
- Create: `docs/frontend-page-mapping.md`

- [ ] **Step 1: 写 frontend-page-mapping.md**

Create `docs/frontend-page-mapping.md`:

```markdown
# Frontend Pages → v0.8.x Use Case Mapping

本文档列出 `frontend/src/pages/` 当前所有 page 跟 v0.8.x 5 个 use case(B-1 信贷调查报告 / B-3 持仓预警 / C-1 个股研究 / C-2 自选股监控 / C-3 NL 选股 + 通用对话)的映射关系, 作为 v0.8.3 / v0.8.4 / v0.8.5 implementation spec 的前端基础参考。

**Spec anchor**: `docs/superpowers/specs/2026-05-03-business-clarification-v1-design.md`

## 现有 Pages

| 现有 page | 当前用途 | v0.8.x mapping | 行动 / 实施 sub-version |
|---|---|---|---|
| `pages/index/` | 首页(legacy) | landing page | v0.8.5 改造(显示 5 use case 入口卡片, B/C 各 ~) |
| `pages/auth/login` | 登录页 | v1.1.1 OAuth 升级前的占位 | 暂留, v1.1.1 升级 |
| `pages/chat/` | 通用对话 | **B-7 + C 端通用对话** | v0.8.5 改造扩 reference link |
| `pages/knowledge/` | KB 管理 | KB 管理 + 信贷调查报告 viewer | v0.8.5 拆: KB 管理 + 报告 viewer |
| `pages/memory/` | memory 页 | v0.8.4 Memory v1 实施时复用 | v0.8.4 适配新 API |
| `pages/news/` | 新闻 | v0.8.x 不直接需要(Bocha 集成在 chat 内) | v0.8.5 评估砍 or 留作 dashboard |
| `pages/database/` | 数据库管理 | legacy, 不在 v0.8.x scope | 暂留, v1.x 评估砍 |
| `pages/bidding/` | 招标 | legacy, 完全砍 | **v0.8.3-pre T2 已砍** |
| `pages/404/` | 404 | 通用 | 留 |

## 新建 Pages(对应 v0.8.x 5 use case)

| 新建 page | 对应 use case | 实施 sub-version | 入口 theme |
|---|---|---|---|
| `pages/credit-report/` | B-1 信贷调查报告 viewer | v0.8.5(后端 v0.8.2 已 ship, 前端待) | banking |
| `pages/portfolio-monitoring/` | B-3 持仓预警 监控列表 + alert 详情 | v0.8.3 | banking |
| `pages/ticker/` | C-1 个股研究(Perplexity Finance 风) | v0.8.5 | retail |
| `pages/watchlist/` | C-2 自选股监控 | v0.8.5 | retail |
| `pages/screener/` | C-3 NL 选股(同花顺 i问财 风) | v0.8.5 | retail |

## 设计语言映射

按 `docs/design-tokens.md` 双 theme:
- banking 入口(`/credit-report` / `/portfolio-monitoring` / 任何 `/monitoring/*`):深色 navy + 金色 accent + monospace 数字
- retail 入口(`/ticker` / `/watchlist` / `/screener` / `/chat`):浅色 + 蓝色 accent + sans-serif 现代
- landing page (`/`):默认 retail theme

## Edge case
- chat 页同时服务 B-7 (审批人对报告追问) 和 C 端通用对话: 默认 retail theme, 但跳转自 banking 入口时(URL 带 `?from=banking`)切换 banking theme(v0.8.5 实施细节)
```

---

## Task 5 — Commit T3 + T4

- [ ] **Step 1: Stage + verify file count**

```bash
git status --short
```

Expected:
- `A backend/LEGACY_LAYOUT.md`
- `M backend/app/core/__init__.py`
- `M backend/app/config/__init__.py`
- `M backend/app/models/__init__.py`
- `M backend/app/service/__init__.py`
- `A docs/frontend-page-mapping.md`

(6 entries)

- [ ] **Step 2: Commit**

```bash
git add backend/LEGACY_LAYOUT.md backend/app/core/__init__.py backend/app/config/__init__.py backend/app/models/__init__.py backend/app/service/__init__.py docs/frontend-page-mapping.md

git commit -m "$(cat <<'EOF'
docs: add LEGACY_LAYOUT.md + frontend-page-mapping.md

- backend/LEGACY_LAYOUT.md: list all backend legacy modules (app/core/* /
  app/config/* / app/models/* / app/service/*) with current usage and v1.x
  evolution plan. Audit reveals these are not unused — they are v0.8.x main
  path dependencies (router 13 + app_main + multiple services), so we cannot
  delete them, only document.
- 4 __init__.py module docstrings pointing to LEGACY_LAYOUT.md.
- docs/frontend-page-mapping.md: map current pages/ + new pages needed in
  v0.8.3/v0.8.4/v0.8.5 sub-versions, with banking/retail theme assignment.

Spec: docs/superpowers/specs/2026-05-03-v0.8.3-pre-frontend-cleanup-design.md (T3, T4)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git log --oneline -1
```

---

## Task 6 — Design tokens + Antd theme switching + design-tokens.md

**Files:**
- Create: `frontend/src/themes/tokens-base.ts`
- Create: `frontend/src/themes/tokens-banking.ts`
- Create: `frontend/src/themes/tokens-retail.ts`
- Create: `frontend/src/themes/index.ts`
- Modify: `frontend/src/App.tsx`(包 ConfigProvider)
- Create: `docs/design-tokens.md`

- [ ] **Step 1: 创 tokens-base.ts**

Create `frontend/src/themes/tokens-base.ts`:

```typescript
/**
 * Base design tokens — shared across banking and retail themes.
 *
 * See docs/design-tokens.md for the design philosophy.
 */

export const baseTokens = {
  // Spacing scale (8px base)
  spacing: {
    xs: 4,
    sm: 8,
    md: 16,
    lg: 24,
    xl: 32,
    xxl: 48,
  },
  // Border radius
  radius: {
    sm: 4,
    md: 8,
    lg: 12,
  },
  // Motion durations (ms)
  motion: {
    fast: 150,
    base: 250,
    slow: 400,
  },
  // Font stack
  fontFamily: {
    sans: '-apple-system, "Helvetica Neue", "PingFang SC", "Microsoft YaHei", sans-serif',
    mono: '"SF Mono", "JetBrains Mono", "Cascadia Code", Consolas, monospace',
  },
  // Font sizes
  fontSize: {
    xs: 12,
    sm: 14,
    base: 16,
    lg: 18,
    xl: 24,
    xxl: 32,
  },
  // Semantic colors (shared)
  semantic: {
    success: '#52c41a',
    warning: '#faad14',
    error: '#f5222d',
    info: '#1890ff',
    // 涨跌专用 (中国标准: 红涨绿跌)
    up: '#f5222d',
    down: '#52c41a',
  },
} as const;

export type BaseTokens = typeof baseTokens;
```

- [ ] **Step 2: 创 tokens-banking.ts**

Create `frontend/src/themes/tokens-banking.ts`:

```typescript
/**
 * Banking theme — Hebbia / Bloomberg-inspired dark professional style.
 * For B-1 信贷调查报告 / B-3 持仓预警 / 监控列表 entries.
 */

import { theme } from 'antd';
import type { ThemeConfig } from 'antd';
import { baseTokens } from './tokens-base';

export const bankingTokens = {
  // Color palette
  background: {
    primary: '#0a1929', // Deep navy
    secondary: '#132f4c',
    tertiary: '#1e3a5f',
  },
  text: {
    primary: '#e7ebf0',
    secondary: '#b2bac2',
    tertiary: '#788896',
  },
  border: {
    base: '#1e3a5f',
    strong: '#2d4f7c',
  },
  accent: {
    primary: '#d4af37', // Gold
    hover: '#e0c151',
  },
  // Numerical display: monospace for data density
  numFontFamily: baseTokens.fontFamily.mono,
} as const;

export const bankingThemeConfig: ThemeConfig = {
  algorithm: theme.darkAlgorithm,
  token: {
    colorPrimary: bankingTokens.accent.primary,
    colorBgContainer: bankingTokens.background.secondary,
    colorBgLayout: bankingTokens.background.primary,
    colorText: bankingTokens.text.primary,
    colorTextSecondary: bankingTokens.text.secondary,
    colorBorder: bankingTokens.border.base,
    fontFamily: baseTokens.fontFamily.sans,
    borderRadius: baseTokens.radius.sm,
  },
  components: {
    Table: {
      headerBg: bankingTokens.background.tertiary,
      borderColor: bankingTokens.border.base,
    },
    Card: {
      colorBgContainer: bankingTokens.background.secondary,
    },
  },
};
```

- [ ] **Step 3: 创 tokens-retail.ts**

Create `frontend/src/themes/tokens-retail.ts`:

```typescript
/**
 * Retail theme — Perplexity-inspired light modern style.
 * For C-1 个股研究 / C-2 自选股 / C-3 NL 选股 / 通用对话 entries.
 */

import { theme } from 'antd';
import type { ThemeConfig } from 'antd';
import { baseTokens } from './tokens-base';

export const retailTokens = {
  background: {
    primary: '#ffffff',
    secondary: '#f7f8fa',
    tertiary: '#eef0f3',
  },
  text: {
    primary: '#1a1d21',
    secondary: '#5d6975',
    tertiary: '#8a96a3',
  },
  border: {
    base: '#e5e8eb',
    strong: '#c9cfd5',
  },
  accent: {
    primary: '#1890ff',
    hover: '#40a9ff',
  },
  numFontFamily: baseTokens.fontFamily.sans,
} as const;

export const retailThemeConfig: ThemeConfig = {
  algorithm: theme.defaultAlgorithm,
  token: {
    colorPrimary: retailTokens.accent.primary,
    colorBgContainer: retailTokens.background.primary,
    colorBgLayout: retailTokens.background.secondary,
    colorText: retailTokens.text.primary,
    colorTextSecondary: retailTokens.text.secondary,
    colorBorder: retailTokens.border.base,
    fontFamily: baseTokens.fontFamily.sans,
    borderRadius: baseTokens.radius.md,
  },
  components: {
    Card: {
      colorBgContainer: retailTokens.background.primary,
      borderRadiusLG: baseTokens.radius.lg,
    },
  },
};
```

- [ ] **Step 4: 创 themes/index.ts(theme 选择 hook)**

Create `frontend/src/themes/index.ts`:

```typescript
/**
 * Theme provider hook + route-based theme selection.
 *
 * Banking entries (深色, B 端):
 *   /credit-report/* | /portfolio-monitoring/* | /monitoring/*
 *
 * Retail entries (浅色, C 端):
 *   /ticker/* | /watchlist/* | /screener/* | /chat/* | / (default)
 */

import { useLocation } from 'react-router-dom';
import { bankingThemeConfig } from './tokens-banking';
import { retailThemeConfig } from './tokens-retail';

const BANKING_ROUTE_PREFIXES = [
  '/credit-report',
  '/portfolio-monitoring',
  '/monitoring',
];

export function useActiveTheme() {
  const { pathname } = useLocation();
  const isBanking = BANKING_ROUTE_PREFIXES.some((p) => pathname.startsWith(p));
  return isBanking ? bankingThemeConfig : retailThemeConfig;
}

export { bankingThemeConfig, retailThemeConfig };
export { baseTokens } from './tokens-base';
export { bankingTokens } from './tokens-banking';
export { retailTokens } from './tokens-retail';
```

- [ ] **Step 5: 集成到 App.tsx**

Read current `frontend/src/App.tsx`. Locate where the routing tree is rendered. Wrap the Routes with `ConfigProvider` using `useActiveTheme()`.

If current `App.tsx` looks roughly like:

```tsx
function App() {
  return (
    <Router>
      <Routes>
        ...
      </Routes>
    </Router>
  );
}
```

Refactor to:

```tsx
import { ConfigProvider } from 'antd';
import { BrowserRouter as Router, Routes } from 'react-router-dom';
import { useActiveTheme } from '@/themes';

function ThemedApp() {
  const themeConfig = useActiveTheme();
  return (
    <ConfigProvider theme={themeConfig}>
      <Routes>
        ...
      </Routes>
    </ConfigProvider>
  );
}

function App() {
  return (
    <Router>
      <ThemedApp />
    </Router>
  );
}

export default App;
```

(`useLocation` requires the component to be inside `<Router>`, hence the `ThemedApp` wrapper.)

If existing `App.tsx` already imports `ConfigProvider` (e.g., for Antd locale), merge — pass both `theme={themeConfig}` and existing props to the same `ConfigProvider`.

- [ ] **Step 6: spike — verify theme switching with dev server**

```bash
cd frontend
npm run dev &
DEV_PID=$!
sleep 5
echo "Open http://localhost:5173/ in browser — should be retail theme (light, blue accent)"
echo "Open http://localhost:5173/credit-report — should be banking theme (dark, gold accent)"
echo "Press enter when verified..."
read
kill $DEV_PID 2>/dev/null || true
cd ..
```

Expected:
- `/` → light background, blue primary button color
- `/credit-report` → dark navy background, gold accent

(Note: `/credit-report` route doesn't exist yet — page won't render content, but the theme should still apply since `useLocation` runs before route resolution. If theme doesn't switch, debug `useActiveTheme` hook + ConfigProvider integration.)

If theme conflicts with existing `frontend/src/antd.scss` overrides, identify the conflicting selectors and either:
- Remove the SCSS override (preferred — let ConfigProvider be source of truth)
- Add `!important` to ConfigProvider tokens (last resort)

- [ ] **Step 7: 写 docs/design-tokens.md**

Create `docs/design-tokens.md`:

```markdown
# Design Tokens

本文档说明 frontend 的 design tokens 设计哲学 + B/C 双 theme 配色规范, 跟业务梳理 spec § 1.2 立论 (B 长报告 / C 短回答+列表 各自体验最优) 自然对齐。

**Source**: `frontend/src/themes/`(`tokens-base.ts` / `tokens-banking.ts` / `tokens-retail.ts` / `index.ts`)

## 设计哲学

按业务梳理 spec § 1.2 "Writer schema 分叉 + 入口 UI 分叉" — B/C 两端共用底层(底座 + 5 agent + 数据), 但 deliverable 形态(长报告 vs 短回答+列表)+ 入口 UI 风格分叉。Design tokens 同样:

- **共用 tokens(base)**: spacing scale / border radius / motion 时长 / font stack / font sizes / 语义色(success/warning/error/info, 涨跌色)
- **B 端 tokens(banking)**: Hebbia / Bloomberg-inspired 深色 navy + 金色 accent + monospace 数字。banking workflow 数据密度大, 深色减疲劳, 金色 accent 强调关键决策。
- **C 端 tokens(retail)**: Perplexity-inspired 浅色 + 蓝色 accent + sans-serif 现代。retail 用户偏好轻量交互, 浅色亲和力强, 蓝色 accent 中性。

## Theme 切换机制

路由级判断(`frontend/src/themes/index.ts` `useActiveTheme()`)。无用户主题切换 toggle (scope 不要 creep)。

| 路由前缀 | Theme |
|---|---|
| `/credit-report/*` | banking |
| `/portfolio-monitoring/*` | banking |
| `/monitoring/*` | banking |
| `/ticker/*` | retail |
| `/watchlist/*` | retail |
| `/screener/*` | retail |
| `/chat/*` | retail |
| `/`(landing) | retail(default) |

## 配色规范

### Banking 调色板(深色)

| 用途 | 值 |
|---|---|
| 背景主 | `#0a1929`(deep navy) |
| 背景次 | `#132f4c` |
| 背景三 | `#1e3a5f` |
| 文本主 | `#e7ebf0` |
| 文本次 | `#b2bac2` |
| 边框 | `#1e3a5f` |
| Accent | `#d4af37`(gold) |

### Retail 调色板(浅色)

| 用途 | 值 |
|---|---|
| 背景主 | `#ffffff` |
| 背景次 | `#f7f8fa` |
| 背景三 | `#eef0f3` |
| 文本主 | `#1a1d21` |
| 文本次 | `#5d6975` |
| 边框 | `#e5e8eb` |
| Accent | `#1890ff` |

### 涨跌色(共用, 中国标准)

| 状态 | 值 |
|---|---|
| 上涨 | `#f5222d`(red) |
| 下跌 | `#52c41a`(green) |

(注: 跟国际标准相反, 但符合 A 股用户习惯)

## Typography

### 字体栈

- **Sans-serif**(默认): `-apple-system, "Helvetica Neue", "PingFang SC", "Microsoft YaHei", sans-serif`
- **Monospace**(banking 数字): `"SF Mono", "JetBrains Mono", "Cascadia Code", Consolas, monospace`

### 字号 scale

| Token | 值 | 用途 |
|---|---|---|
| `xs` | 12px | tag / label |
| `sm` | 14px | secondary text |
| `base` | 16px | body |
| `lg` | 18px | subtitle |
| `xl` | 24px | section title |
| `xxl` | 32px | page title |

## Spacing scale

8px base, sequence: 4 / 8 / 16 / 24 / 32 / 48

## Border radius

- `sm` 4px (banking 默认, 严肃感)
- `md` 8px (retail 默认)
- `lg` 12px (retail card)

## Motion 时长

- fast 150ms (微交互)
- base 250ms (默认)
- slow 400ms (页面切换)

## 不在本文档范围

- 完整 component library — 用 Antd 5 默认 + 上面 ConfigProvider 注入即可
- Storybook — scope 不要 creep, v1.x 评估
- Light/dark 用户切换 — 只走路由判断
```

- [ ] **Step 8: lint + build verify**

```bash
cd frontend
npm run lint 2>&1 | tail -5
npm run build 2>&1 | tail -5
cd ..
```

Expected: lint 不增加 error/warning(基线 ≤ 94 errors / 10 warnings, 全部 pre-existing), build success.

- [ ] **Step 9: Commit**

```bash
git add frontend/src/themes/ frontend/src/App.tsx docs/design-tokens.md
git status --short

git commit -m "$(cat <<'EOF'
feat(frontend): design tokens + Antd dual theme switching

Implements design language alignment per spec T5: B 端 banking theme (Hebbia /
Bloomberg-inspired dark navy + gold) and C 端 retail theme (Perplexity-inspired
light + blue), sharing base tokens (spacing / typography / motion / 涨跌色).

Theme selection is route-based via useActiveTheme() hook — no user toggle, no
SSR/client mismatch concern. Banking routes: /credit-report /portfolio-monitoring
/monitoring. Retail routes: /ticker /watchlist /screener /chat / (default).

Tokens live in frontend/src/themes/ (tokens-base / tokens-banking / tokens-retail
/ index). App.tsx wraps Routes with ConfigProvider+useActiveTheme.

Spec: docs/superpowers/specs/2026-05-03-v0.8.3-pre-frontend-cleanup-design.md (T5)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git log --oneline -1
```

---

## Task 7 — Rebrand to "AI 投研 copilot" + verify gate + commit

**Files:**
- Modify: `frontend/.env`
- Modify: `frontend/.env.example`(if exists)
- Modify: `frontend/package.json`
- Rewrite: `frontend/README.md`

- [ ] **Step 1: 改 frontend/.env VITE_TITLE**

Read `frontend/.env`. Locate `VITE_TITLE = 行业咨询助手`. Edit to `VITE_TITLE = AI 投研 copilot`.

```bash
grep "VITE_TITLE" frontend/.env
```

Expected: `VITE_TITLE = AI 投研 copilot`

- [ ] **Step 2: 改 frontend/.env.example(if exists)**

```bash
ls frontend/.env.example 2>/dev/null && grep "VITE_TITLE" frontend/.env.example
```

If file exists, edit `VITE_TITLE` line same way as .env. If not exists, skip.

- [ ] **Step 3: 改 frontend/package.json name**

Edit `frontend/package.json`, change `"name": "gsk"` to `"name": "financial-research-frontend"`.

```bash
grep '"name"' frontend/package.json | head -1
```

Expected: `"name": "financial-research-frontend",`

- [ ] **Step 4: 重写 frontend/README.md**

Replace entire content of `frontend/README.md` with:

```markdown
# AI 投研 copilot — 前端

个人 portfolio 项目, 服务"投资生命周期"3 阶段(投前评估 / 投后监控 / 标的发现), 同时支持 B 端 banking 用户(信贷调查报告 / 持仓预警)和 C 端散户(个股研究 / 自选股监控 / NL 选股)。

详见 [业务梳理 spec](../docs/superpowers/specs/2026-05-03-business-clarification-v1-design.md)。

## 技术栈

| 类别 | 技术 |
|---|---|
| Framework | React 19 + Vite 6 |
| Language | TypeScript 5.7 |
| UI | Antd 5(双 theme: banking / retail, 路由级切换) |
| Charts | ECharts 5 |
| Routing | React Router 6 |
| State | valtio |
| HTTP | axios |
| Markdown | marked |

## 环境要求

- Node.js >= 18
- npm >= 9

## 启动

```bash
# 1. 安装依赖
npm install --legacy-peer-deps

# 2. 启动开发服务器
npm run dev
```

访问 http://localhost:5173/

## Build

```bash
npm run build
```

构建产物在 `dist/`.

## Lint

```bash
npm run lint
```

## Design tokens

双 theme(B 端 banking 深色 / C 端 retail 浅色)tokens 在 `src/themes/`, 设计规范 + 配色 / typography / spacing 详见 [docs/design-tokens.md](../docs/design-tokens.md).

## Page mapping

现有 pages 跟 v0.8.x 5 use case 的映射 + 待新建 pages 见 [docs/frontend-page-mapping.md](../docs/frontend-page-mapping.md).

## 跟 backend 协同

- backend 默认起在 `http://localhost:8000`
- frontend dev server proxy 配置见 `vite.config.ts`
- API 类型定义在 `src/api/`
```

- [ ] **Step 5: grep verify rebrand**

```bash
grep -nE "行业咨询|gsk" frontend/.env frontend/.env.example frontend/package.json frontend/README.md 2>/dev/null
```

Expected: 0 hits (all rebrand changes applied).

If hits, investigate which file still has the legacy string and edit.

- [ ] **Step 6: Run full verify gate(spec § 9)**

```bash
echo "=== legacy footprint ==="
git ls-files | xargs grep -l "深圳市深维智见教育科技有限公司" 2>/dev/null | wc -l
# expected: 0

echo "=== backend mypy + ruff ==="
cd backend && uv run poe ci 2>&1 | tail -10 && cd ..

echo "=== backend test ==="
cd backend && uv run pytest -x --tb=short 2>&1 | tail -5 && cd ..

echo "=== frontend lint ==="
cd frontend && npm run lint 2>&1 | tail -5 && cd ..

echo "=== frontend build ==="
cd frontend && npm run build 2>&1 | tail -5 && cd ..

echo "=== docs review ==="
ls -la backend/LEGACY_LAYOUT.md docs/frontend-page-mapping.md docs/design-tokens.md
```

Expected:
- legacy footprint: `0`
- mypy + ruff: `All checks passed`
- backend test: 289 passed + 1 skipped (or current baseline)
- frontend lint: 不增加 error/warning(基线 ≤ 94 errors / 10 warnings, 全部 pre-existing)
- frontend build: success
- docs review: 3 files exist with non-zero size

If any gate fails, **STOP**, fix the failing item, re-run the gate. Do not proceed to commit until all gates pass.

- [ ] **Step 7: dev server full smoke**

```bash
cd frontend
npm run dev &
DEV_PID=$!
sleep 5

# Index page (should show new title in browser tab via VITE_TITLE)
curl -sf http://localhost:5173/ >/dev/null && echo "[OK] /" || echo "FAIL /"

# /chat page
curl -sf http://localhost:5173/chat >/dev/null && echo "[OK] /chat (or auth gate)" || echo "WARN /chat"

# Banking route (theme should switch to dark; route may 404 since page not built yet)
curl -s http://localhost:5173/credit-report -o /dev/null -w "%{http_code}\n"
# 200 (route renders 404 page in retail theme — that's OK for now) or 404 acceptable

kill $DEV_PID 2>/dev/null || true
cd ..
```

Expected: `/` returns 200, `/chat` returns 200 or auth-redirect, `/credit-report` returns 200/404. No 500.

If you can manually open the browser, also verify:
- Browser tab title = "AI 投研 copilot"
- Index page shows retail theme (light, blue accent)
- (Optional) Navigate to a banking-prefixed URL — theme switches dark even if page not built

- [ ] **Step 8: Commit**

```bash
git add frontend/.env frontend/.env.example frontend/package.json frontend/README.md
git status --short

git commit -m "$(cat <<'EOF'
chore: rebrand to "AI 投研 copilot" + verify gate pass

- frontend/.env: VITE_TITLE = "AI 投研 copilot" (was "行业咨询助手")
- frontend/.env.example: same
- frontend/package.json: name = "financial-research-frontend" (was "gsk")
- frontend/README.md: rewrite with new product name + tech stack + setup guide
  + links to design-tokens.md and frontend-page-mapping.md

All v0.8.3-pre verify gates passed:
- legacy company footprint: 0 (down from 156)
- backend mypy + ruff + pytest: all green
- frontend lint + build: green
- 3 new docs (LEGACY_LAYOUT / frontend-page-mapping / design-tokens) all in place
- Theme switching verified via dev server smoke

Spec: docs/superpowers/specs/2026-05-03-v0.8.3-pre-frontend-cleanup-design.md (T6, T7)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git log --oneline -5
```

Expected: 5 new commits on `feat/v0.8.3-pre-cleanup` branch since `0dcc091`.

---

## Task 8 — Final verification + push branch

- [ ] **Step 1: Re-run full verify gate(safety belt)**

```bash
echo "=== legacy footprint ==="
git ls-files | xargs grep -l "深圳市深维智见教育科技有限公司" 2>/dev/null | wc -l
# expected: 0

echo "=== backend ==="
cd backend && uv run poe ci 2>&1 | tail -5 && uv run pytest -x --tb=short 2>&1 | tail -5 && cd ..

echo "=== frontend ==="
cd frontend && npm run lint 2>&1 | tail -3 && npm run build 2>&1 | tail -3 && cd ..

echo "=== git log ==="
git log --oneline main..HEAD
```

Expected:
- legacy footprint: 0
- backend: all green
- frontend: all green
- git log: 5 commits on branch since main

If any check fails, **STOP** — branch not ready to ship.

- [ ] **Step 2: Push branch**

```bash
git push -u origin feat/v0.8.3-pre-cleanup
```

Expected: branch pushed, GitHub returns PR creation URL.

- [ ] **Step 3: 准备 PR description(human review 完后由 user 创建 PR)**

PR title 建议:
```
chore(v0.8.3-pre): frontend cleanup + 项目个人化 + backend legacy 标识 + 设计语言对齐
```

PR body 建议(用 gh pr create 时):

```markdown
## Summary

业务梳理 spec § 7.1 的 implementation, 7 task / 5 commit / 2.5-4 天 wall time.

- T1: 156 文件 legacy copyright header 批量删除 (sed 脚本 + repo audit trail)
- T2: 砍 frontend bidding 页 + legacy data 评估
- T3: backend LEGACY_LAYOUT.md(legacy 模块清单 + v1.x 演进计划)+ 4 个 __init__.py docstring
- T4: docs/frontend-page-mapping.md(现有 pages → v0.8.x 5 use case 映射)
- T5: 设计语言对齐 — 混合风(B 端 Bloomberg 深色 + C 端 Perplexity 浅色, 共用 design tokens, Antd theme switching)
- T6: rebrand to "AI 投研 copilot"(VITE_TITLE / package.json name / README)
- T7: verify gate

## Test plan

- [x] backend mypy + ruff pass
- [x] backend pytest baseline(289 passed + 1 skipped)
- [x] frontend lint(error/warning 不增加, 基线 = 94 errors / 10 warnings 全部 pre-existing)
- [x] frontend build pass
- [x] dev server smoke(`/` 浏览器标签 = "AI 投研 copilot", retail theme;`/credit-report` banking theme 切换)
- [x] legacy footprint = 0(down from 156)

## Spec

`docs/superpowers/specs/2026-05-03-v0.8.3-pre-frontend-cleanup-design.md`

## Plan

`docs/superpowers/plans/2026-05-03-v0.8.3-pre-frontend-cleanup.md`
```

(由 user 决定何时 + 是否用 `gh pr create` 创建 PR;本 plan 不自动 create PR.)

---

## Plan complete

5 commits ready on `feat/v0.8.3-pre-cleanup` branch:

1. `chore(cleanup): remove legacy company copyright header from 156 files`
2. `chore(frontend): remove bidding page + legacy data audit`
3. `docs: add LEGACY_LAYOUT.md + frontend-page-mapping.md`
4. `feat(frontend): design tokens + Antd dual theme switching`
5. `chore: rebrand to "AI 投研 copilot" + verify gate pass`
