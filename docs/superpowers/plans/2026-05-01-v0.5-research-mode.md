# v0.5 Research Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Land the v0.5 research mode skeleton — 5 typed agents(`ResearchPlanner` / `DataCollector` / `Analyst` / `Writer` / `Critic`)+ Critic 内部 5 维并行评分(`DispatchSubAgent` 首次真启用,LangGraph Send API fan-out)+ markdown + chart placeholder 输出 + `POST /api/v0.5/research` SSE 流式 + ResearchAgent 满足 `SUT` Protocol 接入 EvalRunner。

**Architecture:**
- **混合 LangGraph 拓扑(spec 决策 GT3)**:扁平 5 节点 top-level graph(planner→collector→analyst→writer→critic),其中 `critic_node` 是个 LangGraph subgraph,内部用 Send API fan-out 5 个 scoring sub-agent(`FactualityScorer` / `CoverageScorer` / `InsightScorer` / `StructureScorer` / `ConcisenessScorer`),reduce 阶段聚合到 `CriticReport`
- **Critic DispatchSubAgent 真启用**:`Critic.__init__(llm, scorers=[...])` 构造时注入(spec 决策 DS1),`Critic.dispatch_subagent(name, state)` override v0 base.py 的 `NotImplementedError` 占位
- **B 路径(prompt-engineered JSON)延续 v0**:5 个 agent 都通过 `LLMService.chat(prompt, tier, request_id)` 调,不破坏 v0 契约,不加 `tools=` 参数
- **Memory v0.5 = LangGraph SqliteSaver only**(spec 决策 § 8),thread_id = `research:{user_id}:{request_id}`(对话用 `chat:{user_id}:{session_id}`,前缀区分)

**Tech Stack:**
- 已就位(Plan A→D + v0):Python 3.13(uv),Pydantic v2 frozen,LangGraph 1.1.10,langgraph-checkpoint-sqlite 3.0.3,FastAPI SSE,pytest-recording,mypy strict on `app.{services,agents,tools,orchestration}.*`,poethepoet,pre-commit
- v0.5 第一次真用:**LangGraph Send API**(`from langgraph.types import Send`)
- v0.5 不引入新 runtime deps

**Status inherited from v0(PR #6):**
- `app.agents.base.Agent` ABC + `dispatch_subagent` placeholder(NotImplementedError)
- `app.agents.schemas` 含 `ToolCall` / `ToolResult` / `StepResult`(直接复用)
- `app.tools.{base,registry,get_stock_quote,get_financials,get_news}`(直接复用)
- `app.orchestration.checkpointer.make_chat_checkpointer(db_path)`(复用)
- `app.services.openai_client.build_llm_service_from_env()`(真 LLM 入口)
- `app.services.{llm_service,trace_service,eval_runner,judge}`(Plan B/C/D 全部稳定)
- `app.services.eval_models.SUTOutput`(v0 Task 10 加;v0.5 复用 + 不破坏)
- `app.services.eval_runner.SUT` Protocol(v0 Task 12 加;ResearchAgent 直接满足)
- `MockTushareService(llm)` / `MockBochaService` 走 LLMService(v0 Task 5/11 改造完)
- `tests/fixtures/llm_mocks/agent_decisions.yaml`(v0 加 ChatPlanner / Responder pattern)

**Memory inputs(v0 retrospective + spec):**
- `feedback_unguarded_imports_after_delete` — v0.5 几乎没删除,本次不太相关,但保持原则
- `feedback_dev_tool_version_pin_alignment` — **必须避免** ruff 0.6/0.15 撞车;assert 写 single-line 或 `n = ...; assert n >= 1, msg`
- `feedback_plan_specificity` — 写意图不写最终配置内容
- `feedback_third_party_plugin_defaults` — Send API 第一次用,**Task 0 spike 必跑**
- `project_v0_architecture_landed` — 引用 v0 实际 module path + 版本号
- `project_eval_pipeline_contract` — 加 `report_markdown_quality` 维度 additive,Plan C/D 老测试零回归

---

## File Structure

| Path | Purpose | Created/Modified |
|---|---|---|
| **新增 — agents/** | | |
| `backend/app/agents/research_planner.py` | `ResearchPlanner(Agent)` | Create |
| `backend/app/agents/data_collector.py` | `DataCollector(Agent)`(asyncio.gather 并行调 tools) | Create |
| `backend/app/agents/analyst.py` | `Analyst(Agent)` | Create |
| `backend/app/agents/writer.py` | `Writer(Agent)`(产 markdown + chart_specs) | Create |
| `backend/app/agents/critic.py` | `Critic(Agent)` + `dispatch_subagent` 真实现 | Create |
| `backend/app/agents/research_agent.py` | `ResearchAgent` SUT-friendly wrapper | Create |
| `backend/app/agents/critic_subagents/__init__.py` | package marker | Create |
| `backend/app/agents/critic_subagents/_base_scorer.py` | 共享 `_BASE_SCORER_TEMPLATE` + `_BaseScorer(Agent)` 抽象 | Create |
| `backend/app/agents/critic_subagents/factuality.py` | `FactualityScorer(_BaseScorer)` | Create |
| `backend/app/agents/critic_subagents/coverage.py` | `CoverageScorer(_BaseScorer)` | Create |
| `backend/app/agents/critic_subagents/insight.py` | `InsightScorer(_BaseScorer)` | Create |
| `backend/app/agents/critic_subagents/structure.py` | `StructureScorer(_BaseScorer)` | Create |
| `backend/app/agents/critic_subagents/conciseness.py` | `ConcisenessScorer(_BaseScorer)` | Create |
| `backend/app/agents/schemas.py` | **Modify**:加 `Subtask` / `ResearchPlan` / `Insight` / `ChartSpec` / `CriticDimensionScore` / `CriticReport` / `ResearchState` | Modify |
| **新增 — tools/** | | |
| `backend/app/tools/mock_web_search.py` | `MockWebSearchTool` + `WebSearchArgs` | Create |
| `backend/app/tools/mock_kb_search.py` | `MockKbSearchTool` + `KbSearchArgs` | Create |
| **新增 — orchestration/** | | |
| `backend/app/orchestration/research_nodes.py` | 5 个 outer node 函数 | Create |
| `backend/app/orchestration/critic_subgraph.py` | Critic 内部 subgraph + Send API fan-out + reduce | Create |
| `backend/app/orchestration/research_graph.py` | `build_research_graph(...)` 装配 | Create |
| **新增 — router/** | | |
| `backend/app/router/research.py` | `POST /api/v0.5/research` SSE 流式 | Create |
| `backend/app/app_main.py` | **Modify**:加 `from app.router import research` + `app.include_router(research.router)` | Modify |
| **修改 — services/** | | |
| `backend/app/services/judge.py` | **Modify**:`Judge.score(..., report_markdown=None)` 加 `report_markdown_quality` 维度 | Modify |
| `backend/app/services/eval_models.py` | **Modify**:`GoldenCase` 加 `expected_topics: list[str] = Field(default_factory=list)` | Modify |
| **测试 — unit/** | | |
| `backend/tests/unit/test_research_schemas.py` | L0:Subtask / ResearchPlan / Insight / CriticDimensionScore / CriticReport invariants | Create |
| `backend/tests/unit/test_mock_search_tools.py` | L0:WebSearchArgs / KbSearchArgs validation | Create |
| `backend/tests/unit/test_research_planner.py` | L0:prompt 构造 + ResearchPlan 解析 | Create |
| `backend/tests/unit/test_data_collector.py` | L0:asyncio.gather 并行调 tools + 失败容错 | Create |
| `backend/tests/unit/test_analyst.py` | L0:prompt 构造 + Insight list 解析 | Create |
| `backend/tests/unit/test_writer.py` | L0:prompt 构造 + markdown + chart_specs 解析 | Create |
| `backend/tests/unit/test_critic_subagents.py` | L0:5 个 scorer 各自 prompt + score 解析 | Create |
| `backend/tests/unit/test_critic.py` | L0:`Critic.dispatch_subagent` 真实现 + aggregate | Create |
| `backend/tests/unit/test_critic_subgraph.py` | L0:Send API fan-out + reduce 装配 | Create |
| `backend/tests/unit/test_research_graph.py` | L0:graph 装配正确性(节点 + 边) | Create |
| **测试 — integration/** | | |
| `backend/tests/integration/test_research_agent_e2e_mock.py` | L1:ResearchAgent 端到端跑(MockLLMClient) | Create |
| `backend/tests/integration/test_eval_runner_research_agent_sut.py` | L1:EvalRunner SUT = ResearchAgent,3 GoldenCase 跑通 | Create |
| `backend/tests/integration/test_research_router_sse.py` | L1:HTTP SSE 流式正确(FastAPI TestClient) | Create |
| **测试 — e2e/** | | |
| `backend/tests/e2e/test_research_agent_cassette.py` | L2:ResearchAgent 真打 LLM cassette 录 + replay | Create |
| **GoldenCase fixtures** | | |
| `backend/tests/eval/golden/research_basic.jsonl` | 3-5 条 research_* category case | Create |
| **MockLLMClient fixtures** | | |
| `backend/tests/fixtures/llm_mocks/agent_decisions.yaml` | **Modify**:加 ResearchPlanner / Analyst / Writer / 5 Scorer 的 pattern entry | Modify |

> **Notes**: v0.5 plan 涉及 ~25 新文件 + 4 修改 + 0 删除。粗算 +2.5-3k 行(agent + 5 scorer + subgraph + tests)。

---

## Pre-flight check (Task 0 — done before dispatching Task 1)

> **2 个 spike,合计 ≤ 3 分钟**。Plan B/C/D 沉淀的 `feedback_third_party_plugin_defaults` 教训应用到 LangGraph Send API。

### Spike 1: LangGraph Send API fan-out + reduce 行为

```bash
unset all_proxy https_proxy http_proxy
uv run python - <<'PY'
from typing import TypedDict, Annotated
import operator
from langgraph.graph import StateGraph, START, END
from langgraph.types import Send

class S(TypedDict):
    msg: str
    scores: Annotated[list[int], operator.add]  # reduce: 各 sub-agent 的 score 累加

def planner(s: S) -> dict:
    # fan-out:返回 list[Send],每个 Send 触发一个 sub-agent
    return [Send("scorer", {"msg": s["msg"], "scorer_id": i}) for i in range(3)]

def scorer(s: dict) -> dict:
    return {"scores": [s["scorer_id"] * 10]}

def aggregator(s: S) -> dict:
    return {"msg": s["msg"] + f" total={sum(s['scores'])}"}

g = StateGraph(S)
g.add_node("scorer", scorer)
g.add_node("aggregator", aggregator)
g.add_conditional_edges(START, planner, ["scorer"])
g.add_edge("scorer", "aggregator")
g.add_edge("aggregator", END)

app = g.compile()
final = app.invoke({"msg": "test", "scores": []})
print("FINAL:", final)
print("EXPECTED scores=[0,10,20] sum=30")
PY
```

期望:`FINAL: {'msg': 'test total=30', 'scores': [0, 10, 20]}`(顺序可能不同,sum 一定 = 30)。

**3 种结果对应行为**:
- **A. 通过且行为符合上述描述** → Task 9 critic_subgraph 直接用 Send API + Annotated[list, operator.add] reducer
- **B. Send API 在 1.1.10 中 import 路径不同** → 改 import,其他代码同
- **C. reducer 语义不同** → plan 阶段调整 schema(把 list reduce 改 dict[name, score] reduce),Task 9 同步改

记录结果在 plan 末尾"Task 0 spike result"区。

### Spike 2: LangGraph subgraph 内嵌到 top-level graph 的状态传递

```bash
uv run python - <<'PY'
from typing import TypedDict
from langgraph.graph import StateGraph, START, END

class TopState(TypedDict):
    msg: str
    sub_result: str

class SubState(TypedDict):
    msg: str
    sub_result: str

def sub_node(s: SubState) -> dict:
    return {"sub_result": "done"}

# 内层 subgraph
sub_g = StateGraph(SubState)
sub_g.add_node("sub_node", sub_node)
sub_g.add_edge(START, "sub_node")
sub_g.add_edge("sub_node", END)
sub_app = sub_g.compile()

# 外层 graph,把 subgraph 当节点
top_g = StateGraph(TopState)
top_g.add_node("subgraph_as_node", sub_app)
top_g.add_edge(START, "subgraph_as_node")
top_g.add_edge("subgraph_as_node", END)
top_app = top_g.compile()

final = top_app.invoke({"msg": "hello", "sub_result": ""})
print("FINAL:", final)
PY
```

期望:`FINAL: {'msg': 'hello', 'sub_result': 'done'}` —— subgraph 状态合并回 top-level state,共享字段名直接对接。

如果 subgraph state schema 与 top-level state schema 不能直接对接,Task 9 critic_subgraph 需要在边界做 state 转换。

记录结果在 plan 末尾。

---

## Task 1: Pydantic schemas extension

**Files:**
- Modify: `backend/app/agents/schemas.py`
- Create: `backend/tests/unit/test_research_schemas.py`

- [ ] **Step 1: Write the failing test**

```python
# backend/tests/unit/test_research_schemas.py
"""L0 — research-mode I/O schemas."""

import pytest
from pydantic import ValidationError

from app.agents.schemas import (
    ChartSpec,
    CriticDimensionScore,
    CriticReport,
    Insight,
    ResearchPlan,
    ResearchState,
    Subtask,
)


def test_subtask_minimal() -> None:
    s = Subtask(subtask_id="overview", description="x", required_tools=["get_stock_quote"], rationale="r")
    assert s.subtask_id == "overview"


def test_research_plan_minimal() -> None:
    p = ResearchPlan(
        subtasks=[Subtask(subtask_id="overview", description="x", required_tools=[], rationale="r")],
        target_entity="600519.SH",
        research_style="comprehensive",
        reasoning="single subtask",
    )
    assert len(p.subtasks) == 1
    assert p.research_style == "comprehensive"


def test_research_plan_invalid_style_rejected() -> None:
    with pytest.raises(ValidationError):
        ResearchPlan(
            subtasks=[],
            target_entity="x",
            research_style="weekly",  # type: ignore[arg-type]
            reasoning="r",
        )


def test_insight_confidence_levels() -> None:
    for c in ("high", "medium", "low"):
        Insight(subtask_id="s", finding="f", supporting_data=[], confidence=c)


def test_critic_dimension_score_range() -> None:
    s = CriticDimensionScore(
        dimension="factuality", score=8.5, evidence="cite x", sub_agent_request_id="r"
    )
    assert s.score == 8.5


def test_critic_dimension_score_out_of_range_rejected() -> None:
    with pytest.raises(ValidationError):
        CriticDimensionScore(dimension="factuality", score=11.0, evidence="x", sub_agent_request_id="r")


def test_critic_report_5_dims() -> None:
    dims = [
        CriticDimensionScore(dimension=d, score=8.0, evidence="e", sub_agent_request_id="r")
        for d in ("factuality", "coverage", "insight", "structure", "conciseness")
    ]
    r = CriticReport(dimensions=dims, overall_score=8.0, summary_markdown="ok")
    assert len(r.dimensions) == 5


def test_chart_spec_minimal() -> None:
    c = ChartSpec(chart_id="c1", chart_type="line", title="t", data=[{"x": 1, "y": 2}])
    assert c.chart_type == "line"


def test_research_state_minimal() -> None:
    s = ResearchState(
        user_id="u",
        session_id="s",
        user_message="深度分析茅台",
        request_id="req-test1234",
    )
    assert s.plan is None
    assert s.tool_results == []
    assert s.report_markdown is None
    assert s.critic_report is None
```

- [ ] **Step 2: Run — expect fail**

```bash
uv run pytest backend/tests/unit/test_research_schemas.py -v
```
Expected: FAIL with `ImportError`(schemas 还没加).

- [ ] **Step 3: Extend `app/agents/schemas.py`**

在文件末尾追加(不动现有 v0 schemas):

```python
class Subtask(BaseModel):
    """ResearchPlanner 输出的子任务。"""
    model_config = ConfigDict(frozen=True)

    subtask_id: str
    description: str
    required_tools: list[str]
    rationale: str


class ResearchPlan(BaseModel):
    """ResearchPlanner 输出。"""
    model_config = ConfigDict(frozen=True)

    subtasks: list[Subtask]
    target_entity: str
    research_style: Literal["concise", "comprehensive"]
    reasoning: str


class Insight(BaseModel):
    """Analyst 输出的单个分析洞察。"""
    model_config = ConfigDict(frozen=True)

    subtask_id: str
    finding: str
    supporting_data: list[dict[str, Any]]
    confidence: Literal["high", "medium", "low"]


class ChartSpec(BaseModel):
    """Writer 产出的图占位元信息(v0.5 不渲染,留 v0.6 frontend)。"""
    model_config = ConfigDict(frozen=True)

    chart_id: str
    chart_type: Literal["line", "bar", "pie", "table"]
    title: str
    data: list[dict[str, Any]]
    x_axis: str | None = None
    y_axis: str | None = None


CriticDimension = Literal["factuality", "coverage", "insight", "structure", "conciseness"]


class CriticDimensionScore(BaseModel):
    """Critic 单维度评分。"""
    model_config = ConfigDict(frozen=True)

    dimension: CriticDimension
    score: float = Field(ge=0.0, le=10.0)
    evidence: str
    sub_agent_request_id: str


class CriticReport(BaseModel):
    """Critic 聚合输出。"""
    model_config = ConfigDict(frozen=True)

    dimensions: list[CriticDimensionScore]
    overall_score: float = Field(ge=0.0, le=10.0)
    summary_markdown: str


class ResearchState(BaseModel):
    """LangGraph 研报状态(类比 v0 GraphState)。"""
    model_config = ConfigDict(arbitrary_types_allowed=True)

    user_id: str
    session_id: str
    user_message: str
    request_id: str

    plan: ResearchPlan | None = None
    tool_results: list[ToolResult] = Field(default_factory=list)
    insights: list[Insight] = Field(default_factory=list)
    report_markdown: str | None = None
    chart_specs: list[ChartSpec] = Field(default_factory=list)
    critic_report: CriticReport | None = None

    span_stack: list[str] = Field(default_factory=list)
```

注意:`Literal` 已经在 schemas.py 顶部 `from typing import Any, Literal` 引入(v0 已有);如未引入,补上。

- [ ] **Step 4: Verify**

```bash
uv run pytest backend/tests/unit/test_research_schemas.py -v
uv run mypy backend/app/agents/schemas.py
```
Expected: 9 PASS, mypy clean.

- [ ] **Step 5: Commit**

```bash
git add backend/app/agents/schemas.py backend/tests/unit/test_research_schemas.py
git commit -m "$(cat <<'EOF'
feat(agents): add v0.5 research-mode schemas (ResearchPlan/Subtask/Insight/CriticReport/ResearchState/ChartSpec)

原因 layer: services
EOF
)"
```

---

## Task 2: 2 个新 mock tools(`mock_web_search` + `mock_kb_search`)

**Files:**
- Create: `backend/app/tools/mock_web_search.py`
- Create: `backend/app/tools/mock_kb_search.py`
- Create: `backend/tests/unit/test_mock_search_tools.py`

- [ ] **Step 1: Write the L0 test**

```python
# backend/tests/unit/test_mock_search_tools.py
"""L0 — args validation for mock_web_search / mock_kb_search."""

import pytest
from pydantic import ValidationError

from app.tools.mock_web_search import WebSearchArgs
from app.tools.mock_kb_search import KbSearchArgs


def test_web_search_args_default() -> None:
    args = WebSearchArgs(query="茅台")
    assert args.query == "茅台"
    assert args.search_type == "general"
    assert args.count == 5


def test_web_search_args_news_type() -> None:
    args = WebSearchArgs(query="茅台", search_type="news")
    assert args.search_type == "news"


def test_web_search_args_invalid_type_rejected() -> None:
    with pytest.raises(ValidationError):
        WebSearchArgs(query="x", search_type="weekly")  # type: ignore[arg-type]


def test_web_search_args_negative_count_rejected() -> None:
    with pytest.raises(ValidationError):
        WebSearchArgs(query="x", count=-1)


def test_kb_search_args_minimal() -> None:
    args = KbSearchArgs(query="茅台")
    assert args.query == "茅台"
    assert args.top_k == 5


def test_kb_search_args_top_k_zero_rejected() -> None:
    with pytest.raises(ValidationError):
        KbSearchArgs(query="x", top_k=0)
```

- [ ] **Step 2: Implement `mock_web_search.py`**

```python
# backend/app/tools/mock_web_search.py
"""mock_web_search — uses MockBochaService LLM-driven mock for v0.5.
Real Bocha API integration deferred to v0.6.
"""

from __future__ import annotations

from typing import Any, Literal, TYPE_CHECKING

from pydantic import BaseModel, Field

from app.tools.base import Tool, ToolError

if TYPE_CHECKING:
    from app.service.mock_bocha_service import MockBochaService


class WebSearchArgs(BaseModel):
    query: str
    search_type: Literal["general", "news"] = "general"
    count: int = Field(default=5, ge=1, le=20)


class MockWebSearchTool(Tool):
    name = "mock_web_search"
    description = "搜索网络获取最新资讯或一般信息(v0.5 mock,v0.6 真接入)"
    args_schema = WebSearchArgs

    def __init__(self, mock_bocha: "MockBochaService | None" = None) -> None:
        self._mock_bocha = mock_bocha

    async def run(self, args: BaseModel) -> dict[str, Any]:
        if not isinstance(args, WebSearchArgs):
            raise ToolError(f"args must be WebSearchArgs, got {type(args).__name__}")
        if self._mock_bocha is None:
            raise ToolError("MockBochaService not injected; cannot run web_search")
        items = await self._mock_bocha.generate_search_results(
            query=args.query,
            search_type=args.search_type,
            count=args.count,
        )
        return {"items": items}
```

- [ ] **Step 3: Implement `mock_kb_search.py`**

```python
# backend/app/tools/mock_kb_search.py
"""mock_kb_search — uses MockBochaService for KB-style search v0.5.
Real Milvus integration deferred to v0.6.
"""

from __future__ import annotations

from typing import Any, TYPE_CHECKING

from pydantic import BaseModel, Field

from app.tools.base import Tool, ToolError

if TYPE_CHECKING:
    from app.service.mock_bocha_service import MockBochaService


class KbSearchArgs(BaseModel):
    query: str
    top_k: int = Field(default=5, ge=1, le=20)


class MockKbSearchTool(Tool):
    name = "mock_kb_search"
    description = "搜索内部知识库(v0.5 mock,v0.6 真接 Milvus)"
    args_schema = KbSearchArgs

    def __init__(self, mock_bocha: "MockBochaService | None" = None) -> None:
        self._mock_bocha = mock_bocha

    async def run(self, args: BaseModel) -> dict[str, Any]:
        if not isinstance(args, KbSearchArgs):
            raise ToolError(f"args must be KbSearchArgs, got {type(args).__name__}")
        if self._mock_bocha is None:
            raise ToolError("MockBochaService not injected; cannot run kb_search")
        items = await self._mock_bocha.generate_search_results(
            query=args.query,
            search_type="docs",
            count=args.top_k,
        )
        return {"items": items}
```

> **Note**:`MockBochaService.generate_search_results(...)` 真实签名以代码为准(grep 一下)。如签名不同,implementer 调整传参。

- [ ] **Step 4: Verify + commit**

```bash
uv run pytest backend/tests/unit/test_mock_search_tools.py -v
uv run mypy backend/app/tools/
```

```bash
git add backend/app/tools/mock_web_search.py backend/app/tools/mock_kb_search.py backend/tests/unit/test_mock_search_tools.py
git commit -m "$(cat <<'EOF'
feat(tools): add mock_web_search + mock_kb_search for v0.5 DataCollector

Both use MockBochaService (LLM-driven mock); real Bocha API + Milvus
integration deferred to v0.6 spec.

原因 layer: services
EOF
)"
```

---

## Task 3: `ResearchPlanner` agent

**Files:**
- Create: `backend/app/agents/research_planner.py`
- Create: `backend/tests/unit/test_research_planner.py`
- Modify: `backend/tests/fixtures/llm_mocks/agent_decisions.yaml`

- [ ] **Step 1: Write the L0 test**

```python
# backend/tests/unit/test_research_planner.py
"""L0 — ResearchPlanner prompt construction + ResearchPlan parsing."""

import pytest

from app.agents.research_planner import ResearchPlanner, build_planner_prompt
from app.agents.schemas import ResearchPlan, ResearchState, Subtask
from app.services.llm_mock_client import MockLLMClient
from app.services.llm_service import LLMService


def _state(target: str, msg: str) -> ResearchState:
    return ResearchState(
        user_id="u",
        session_id="s",
        user_message=msg,
        request_id="req-test1234",
    )


def test_build_prompt_includes_target_and_message() -> None:
    prompt = build_planner_prompt(target_entity="600519.SH", user_message="深度分析茅台基本面")
    assert "你是金融研究助手 research_planner" in prompt
    assert "600519.SH" in prompt
    assert "深度分析茅台基本面" in prompt
    assert "subtasks" in prompt  # JSON schema mentioned


def test_planner_step_returns_plan(mock_llm_client: MockLLMClient, monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.setenv("LLM_MODE", "mock")
    svc = LLMService(client=mock_llm_client)
    planner = ResearchPlanner(llm=svc)

    state = _state("600519.SH", "深度分析茅台基本面")
    sr = planner.step(state)

    assert "plan" in sr.state_update
    plan = sr.state_update["plan"]
    assert isinstance(plan, ResearchPlan)
    assert len(plan.subtasks) >= 1
    assert plan.target_entity == "600519.SH" or plan.target_entity in state.user_message
```

- [ ] **Step 2: Run — expect fail**

- [ ] **Step 3: Implement `research_planner.py`**

```python
# backend/app/agents/research_planner.py
"""ResearchPlanner — decomposes user research intent into subtasks."""

from __future__ import annotations

import json
import re
from typing import Any

from app.agents.base import Agent
from app.agents.schemas import ResearchPlan, ResearchState, StepResult, Subtask
from app.services.llm_response import Tier
from app.services.llm_service import LLMService


_SYSTEM_PROMPT = """你是金融研究助手 research_planner。

任务:把用户的研报需求拆成 2-5 个子任务,每个子任务对应一组数据收集点。

可用数据点(后续 DataCollector 会调对应工具):
- 行情数据(get_stock_quote)
- 财务数据(get_financials)
- 财经新闻(get_news)
- 网络搜索(mock_web_search)
- 知识库搜索(mock_kb_search)

输出格式:仅输出 JSON,符合以下 schema:
{
  "subtasks": [
    {"subtask_id": "...", "description": "...", "required_tools": ["..."], "rationale": "..."}
  ],
  "target_entity": "<股票代码或公司名>",
  "research_style": "comprehensive",
  "reasoning": "<1-2 句拆分思路>"
}
"""


def build_planner_prompt(target_entity: str, user_message: str) -> str:
    return (
        _SYSTEM_PROMPT
        + f"\n\n# 目标实体\n{target_entity}\n\n# 用户研报需求\n{user_message}\n\n请输出 JSON。"
    )


def _parse_plan(content: str) -> ResearchPlan:
    """Strip code fences, json.loads, ResearchPlan validate."""
    cleaned = re.sub(r"^```(?:json)?\s*", "", content.strip(), flags=re.MULTILINE)
    cleaned = re.sub(r"\s*```\s*$", "", cleaned)
    data = json.loads(cleaned)
    return ResearchPlan.model_validate(data)


class ResearchPlanner(Agent):
    name = "ResearchPlanner"
    model_tier: Tier = "balanced"

    def step(self, state: ResearchState) -> StepResult:
        # 简化:从 user_message 抽 target_entity 留 LLM 自己识别(prompt 里说明);
        # 如果用户消息不含明显实体,LLM 会基于上下文决定。
        target = _extract_target_or_default(state.user_message)
        prompt = build_planner_prompt(target_entity=target, user_message=state.user_message)
        r = self._llm.chat(prompt=prompt, tier=self.model_tier, request_id=state.request_id)
        plan = _parse_plan(r.content)
        return StepResult(
            state_update={"plan": plan},
            span_metadata={"agent": "ResearchPlanner", "model": r.model, "cost_cny": r.cost_cny},
        )


def _extract_target_or_default(user_message: str) -> str:
    """Heuristic: 提取 .SH/.SZ 股票代码;否则返回空让 LLM 自己识别。"""
    m = re.search(r"\b\d{6}\.(SH|SZ)\b", user_message)
    return m.group(0) if m else ""
```

- [ ] **Step 4: Add MockLLMClient fixture entry**

打开 `backend/tests/fixtures/llm_mocks/agent_decisions.yaml`,在 ChatPlanner pattern 之前(更具体的优先)加:

```yaml
- pattern: '^你是金融研究助手 research_planner'
  response:
    content: |
      {"subtasks":[{"subtask_id":"overview","description":"公司概况","required_tools":["get_stock_quote"],"rationale":"行情基线"},{"subtask_id":"financial_health","description":"财务健康度","required_tools":["get_financials"],"rationale":"基本面"}],"target_entity":"600519.SH","research_style":"comprehensive","reasoning":"双子任务拆分"}
```

- [ ] **Step 5: Verify + commit**

```bash
uv run pytest backend/tests/unit/test_research_planner.py -v
uv run mypy backend/app/agents/research_planner.py
```

```bash
git add backend/app/agents/research_planner.py backend/tests/unit/test_research_planner.py backend/tests/fixtures/llm_mocks/agent_decisions.yaml
git commit -m "$(cat <<'EOF'
feat(agents): add ResearchPlanner with prompt-engineered ResearchPlan JSON

Decomposes user research intent into subtasks. Uses balanced tier; B path
(prompt + JSON parse) per v0 contract; no LLMService extension.

原因 layer: services
EOF
)"
```

---

## Task 4: `DataCollector` agent(asyncio.gather 并行)

**Files:**
- Create: `backend/app/agents/data_collector.py`
- Create: `backend/tests/unit/test_data_collector.py`

> **关键决策(plan 内决)**:DataCollector **并行**调 tools(asyncio.gather),失败容错 = 单个 tool 失败不阻塞其他 tool,失败的 ToolResult.success=False 仍存进 state.tool_results。

- [ ] **Step 1: Write the L0 test**

```python
# backend/tests/unit/test_data_collector.py
"""L0 — DataCollector parallel tool execution + failure resilience."""

from typing import Any

import pytest
from pydantic import BaseModel

from app.agents.data_collector import DataCollector
from app.agents.schemas import ResearchPlan, ResearchState, Subtask
from app.services.llm_mock_client import MockLLMClient
from app.services.llm_service import LLMService
from app.tools.base import Tool, ToolError
from app.tools.registry import ToolRegistry


class _Args(BaseModel):
    x: int


class _OkTool(Tool):
    name = "ok"
    description = "Returns x doubled"
    args_schema = _Args

    async def run(self, args: BaseModel) -> dict[str, Any]:
        if not isinstance(args, _Args):
            raise ToolError("type")
        return {"y": args.x * 2}


class _FailTool(Tool):
    name = "fail"
    description = "Always fails"
    args_schema = _Args

    async def run(self, args: BaseModel) -> dict[str, Any]:
        raise ToolError("intended")


@pytest.mark.asyncio
async def test_data_collector_parallel_success(
    mock_llm_client: MockLLMClient, monkeypatch: pytest.MonkeyPatch
) -> None:
    monkeypatch.setenv("LLM_MODE", "mock")
    reg = ToolRegistry()
    reg.register(_OkTool())

    plan = ResearchPlan(
        subtasks=[
            Subtask(subtask_id="s1", description="d1", required_tools=["ok"], rationale="r"),
            Subtask(subtask_id="s2", description="d2", required_tools=["ok"], rationale="r"),
        ],
        target_entity="x",
        research_style="comprehensive",
        reasoning="r",
    )
    state = ResearchState(
        user_id="u", session_id="s", user_message="m", request_id="r-1", plan=plan
    )

    collector = DataCollector(llm=LLMService(client=mock_llm_client), registry=reg)
    sr = collector.step(state)

    results = sr.state_update["tool_results"]
    successes = [r for r in results if r.success]
    assert len(successes) >= 2  # 2 subtasks each call 1 tool


@pytest.mark.asyncio
async def test_data_collector_partial_failure_does_not_block(
    mock_llm_client: MockLLMClient, monkeypatch: pytest.MonkeyPatch
) -> None:
    monkeypatch.setenv("LLM_MODE", "mock")
    reg = ToolRegistry()
    reg.register(_OkTool())
    reg.register(_FailTool())

    plan = ResearchPlan(
        subtasks=[
            Subtask(subtask_id="s1", description="d1", required_tools=["ok", "fail"], rationale="r"),
        ],
        target_entity="x",
        research_style="comprehensive",
        reasoning="r",
    )
    state = ResearchState(
        user_id="u", session_id="s", user_message="m", request_id="r-2", plan=plan
    )

    collector = DataCollector(llm=LLMService(client=mock_llm_client), registry=reg)
    sr = collector.step(state)

    results = sr.state_update["tool_results"]
    assert len(results) == 2
    # 1 success, 1 fail — both recorded
    n_success = sum(1 for r in results if r.success)
    n_fail = sum(1 for r in results if not r.success)
    assert n_success == 1
    assert n_fail == 1
```

- [ ] **Step 2: Run — expect fail**

- [ ] **Step 3: Implement `data_collector.py`**

```python
# backend/app/agents/data_collector.py
"""DataCollector — runs ResearchPlan.subtasks' required_tools in parallel.

Uses asyncio.gather for fan-out; failures of individual tools are caught
and recorded as ToolResult(success=False) so other tools still run.
"""

from __future__ import annotations

import asyncio
from typing import Any

from app.agents.base import Agent
from app.agents.schemas import ResearchState, StepResult, ToolCall, ToolResult
from app.services.llm_response import Tier
from app.services.llm_service import LLMService
from app.tools.registry import ToolRegistry


class DataCollector(Agent):
    name = "DataCollector"
    model_tier: Tier = "fast"  # nominal; doesn't actually call LLM directly

    def __init__(self, llm: LLMService, registry: ToolRegistry) -> None:
        super().__init__(llm)
        self._registry = registry

    def step(self, state: ResearchState) -> StepResult:
        if state.plan is None:
            return StepResult(state_update={"tool_results": []}, span_metadata={"agent": "DataCollector"})
        # Build flat list of ToolCalls from subtasks.required_tools
        calls: list[ToolCall] = []
        for sub in state.plan.subtasks:
            for tool_name in sub.required_tools:
                args = _default_args_for(tool_name, state.plan.target_entity, state.user_message)
                calls.append(ToolCall(tool_name=tool_name, args=args, rationale=sub.rationale))

        # Parallel execute
        results = asyncio.run(_execute_all(calls, self._registry))

        return StepResult(
            state_update={"tool_results": results},
            span_metadata={"agent": "DataCollector", "n_calls": len(calls)},
        )


async def _execute_all(calls: list[ToolCall], registry: ToolRegistry) -> list[ToolResult]:
    tasks = [registry.execute(c) for c in calls]
    return await asyncio.gather(*tasks)


def _default_args_for(tool_name: str, target: str, user_message: str) -> dict[str, Any]:
    """Heuristic default args based on tool name. Real-world v1+ would have
    a proper agent decide args per subtask; v0.5 uses simple rules."""
    if tool_name == "get_stock_quote":
        return {"ts_code": target}
    if tool_name == "get_financials":
        return {"ts_code": target, "period": "latest"}
    if tool_name == "get_news":
        return {"ts_code": target if target else None, "n": 5, "days_back": 7}
    if tool_name == "mock_web_search":
        return {"query": user_message, "search_type": "general", "count": 5}
    if tool_name == "mock_kb_search":
        return {"query": user_message, "top_k": 5}
    return {}
```

> **Note**:`DataCollector.step` 是 sync(同 v0 Agent ABC),内部用 `asyncio.run` 跑并行 await。**Caveat**:LangGraph node 已经在 async event loop 里时,`asyncio.run` 会抛 `RuntimeError`。Task 9 实施时 `data_collector_node` 写成 async function,直接 `await _execute_all(...)`,不经过 `step()` 同步入口。这就要求 `Agent` ABC 给 `DataCollector` 一个 async-friendly 的旁路。

> **Plan 内决议**:`DataCollector.step` 提供 sync 路径(给 unit test 直接调用),`DataCollector.collect_async(state)` 提供 async 路径(给 LangGraph node 调用)。两者共享 `_execute_all` 实现。Task 9 用 async 路径。

修订 implementation:

```python
# 在 DataCollector class 内补充:
    async def collect_async(self, state: ResearchState) -> StepResult:
        """LangGraph node-friendly async entry. Bypasses asyncio.run."""
        if state.plan is None:
            return StepResult(state_update={"tool_results": []}, span_metadata={"agent": "DataCollector"})
        calls: list[ToolCall] = []
        for sub in state.plan.subtasks:
            for tool_name in sub.required_tools:
                args = _default_args_for(tool_name, state.plan.target_entity, state.user_message)
                calls.append(ToolCall(tool_name=tool_name, args=args, rationale=sub.rationale))
        results = await _execute_all(calls, self._registry)
        return StepResult(
            state_update={"tool_results": results},
            span_metadata={"agent": "DataCollector", "n_calls": len(calls)},
        )
```

`step()` 用于 sync 单元测试(`asyncio.run` 包装);`collect_async()` 用于 LangGraph 节点。

- [ ] **Step 4: Verify + commit**

```bash
uv run pytest backend/tests/unit/test_data_collector.py -v
uv run mypy backend/app/agents/data_collector.py
```

```bash
git add backend/app/agents/data_collector.py backend/tests/unit/test_data_collector.py
git commit -m "$(cat <<'EOF'
feat(agents): add DataCollector with parallel tool execution + failure resilience

Sync step() wraps asyncio.run for unit tests; collect_async() entry for
LangGraph nodes. Failed tools recorded as ToolResult(success=False), do
not block other tool calls.

原因 layer: services
EOF
)"
```

---

## Task 5: `Analyst` agent

**Files:**
- Create: `backend/app/agents/analyst.py`
- Create: `backend/tests/unit/test_analyst.py`
- Modify: `backend/tests/fixtures/llm_mocks/agent_decisions.yaml`

- [ ] **Step 1: Write the L0 test**

```python
# backend/tests/unit/test_analyst.py
"""L0 — Analyst prompt construction + Insight list parsing."""

import pytest

from app.agents.analyst import Analyst, build_analyst_prompt
from app.agents.schemas import (
    Insight,
    ResearchPlan,
    ResearchState,
    Subtask,
    ToolResult,
)
from app.services.llm_mock_client import MockLLMClient
from app.services.llm_service import LLMService


def _state_with_data() -> ResearchState:
    plan = ResearchPlan(
        subtasks=[Subtask(subtask_id="overview", description="d", required_tools=["get_stock_quote"], rationale="r")],
        target_entity="600519.SH",
        research_style="comprehensive",
        reasoning="r",
    )
    tr = ToolResult(
        tool_name="get_stock_quote", args={"ts_code": "600519.SH"},
        success=True, output={"price": 1820.5}, latency_ms=300,
    )
    return ResearchState(
        user_id="u", session_id="s", user_message="m", request_id="req-test1234",
        plan=plan, tool_results=[tr],
    )


def test_build_prompt_includes_subtasks_and_data() -> None:
    state = _state_with_data()
    prompt = build_analyst_prompt(state)
    assert "你是金融研究助手 analyst" in prompt
    assert "overview" in prompt
    assert "1820.5" in prompt or "get_stock_quote" in prompt
    assert "insights" in prompt


def test_analyst_step_returns_insights(mock_llm_client: MockLLMClient, monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.setenv("LLM_MODE", "mock")
    svc = LLMService(client=mock_llm_client)
    analyst = Analyst(llm=svc)

    state = _state_with_data()
    sr = analyst.step(state)

    assert "insights" in sr.state_update
    insights = sr.state_update["insights"]
    assert len(insights) >= 1
    assert all(isinstance(i, Insight) for i in insights)
```

- [ ] **Step 2: Implement `analyst.py`** + fixture entry

```python
# backend/app/agents/analyst.py
"""Analyst — synthesizes tool_results into structured Insights."""

from __future__ import annotations

import json
import re

from app.agents.base import Agent
from app.agents.schemas import Insight, ResearchState, StepResult
from app.services.llm_response import Tier
from app.services.llm_service import LLMService


_SYSTEM_PROMPT = """你是金融研究助手 analyst。

任务:基于 ResearchPlan 的子任务和 DataCollector 收集到的工具结果,产出 list[Insight]。
每个 Insight 关联一个 subtask_id,提炼 1-2 句关键发现 + 引用支持数据。

输出 JSON:
{
  "insights": [
    {"subtask_id":"...", "finding":"...", "supporting_data":[...], "confidence":"high|medium|low"}
  ]
}
"""


def build_analyst_prompt(state: ResearchState) -> str:
    plan_summary = ""
    if state.plan is not None:
        for sub in state.plan.subtasks:
            plan_summary += f"- [{sub.subtask_id}] {sub.description}\n"
    tool_summary = "\n".join(
        f"- {tr.tool_name}({tr.args}) → success={tr.success} output={tr.output}"
        for tr in state.tool_results
    )
    return (
        _SYSTEM_PROMPT
        + f"\n\n# Subtasks\n{plan_summary}\n# Tool Results\n{tool_summary}\n\n请输出 JSON。"
    )


def _parse_insights(content: str) -> list[Insight]:
    cleaned = re.sub(r"^```(?:json)?\s*", "", content.strip(), flags=re.MULTILINE)
    cleaned = re.sub(r"\s*```\s*$", "", cleaned)
    data = json.loads(cleaned)
    return [Insight.model_validate(i) for i in data["insights"]]


class Analyst(Agent):
    name = "Analyst"
    model_tier: Tier = "balanced"

    def step(self, state: ResearchState) -> StepResult:
        prompt = build_analyst_prompt(state)
        r = self._llm.chat(prompt=prompt, tier=self.model_tier, request_id=state.request_id)
        insights = _parse_insights(r.content)
        return StepResult(
            state_update={"insights": insights},
            span_metadata={"agent": "Analyst", "model": r.model, "cost_cny": r.cost_cny},
        )
```

Fixture entry:
```yaml
- pattern: '^你是金融研究助手 analyst'
  response:
    content: |
      {"insights":[{"subtask_id":"overview","finding":"茅台当前股价 1820.5 元,处于高位震荡","supporting_data":[{"price":1820.5}],"confidence":"high"},{"subtask_id":"financial_health","finding":"营收稳健增长","supporting_data":[],"confidence":"medium"}]}
```

- [ ] **Step 3: Verify + commit**

```bash
git add backend/app/agents/analyst.py backend/tests/unit/test_analyst.py backend/tests/fixtures/llm_mocks/agent_decisions.yaml
git commit -m "$(cat <<'EOF'
feat(agents): add Analyst for tool_results → Insight synthesis

原因 layer: services
EOF
)"
```

---

## Task 6: `Writer` agent

**Files:**
- Create: `backend/app/agents/writer.py`
- Create: `backend/tests/unit/test_writer.py`
- Modify: `backend/tests/fixtures/llm_mocks/agent_decisions.yaml`

- [ ] **Step 1: Write the L0 test**

```python
# backend/tests/unit/test_writer.py
"""L0 — Writer prompt construction + markdown + chart_specs parsing."""

import pytest

from app.agents.writer import Writer, build_writer_prompt
from app.agents.schemas import ChartSpec, Insight, ResearchPlan, ResearchState, Subtask
from app.services.llm_mock_client import MockLLMClient
from app.services.llm_service import LLMService


def _state_with_insights() -> ResearchState:
    plan = ResearchPlan(
        subtasks=[Subtask(subtask_id="overview", description="d", required_tools=[], rationale="r")],
        target_entity="600519.SH",
        research_style="comprehensive",
        reasoning="r",
    )
    insights = [Insight(subtask_id="overview", finding="x", supporting_data=[], confidence="high")]
    return ResearchState(
        user_id="u", session_id="s", user_message="m", request_id="req-test1234",
        plan=plan, insights=insights,
    )


def test_build_prompt_includes_insights() -> None:
    state = _state_with_insights()
    prompt = build_writer_prompt(state)
    assert "你是金融研究助手 writer" in prompt
    assert "overview" in prompt
    assert "chart_id" in prompt
    assert "chart://" in prompt


def test_writer_step_returns_markdown(mock_llm_client: MockLLMClient, monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.setenv("LLM_MODE", "mock")
    svc = LLMService(client=mock_llm_client)
    writer = Writer(llm=svc)

    state = _state_with_insights()
    sr = writer.step(state)

    md = sr.state_update["report_markdown"]
    assert isinstance(md, str)
    n = len(md)
    assert n > 50, f"report markdown too short ({n} chars)"
    chart_specs = sr.state_update.get("chart_specs", [])
    assert isinstance(chart_specs, list)
```

- [ ] **Step 2: Implement `writer.py`** + fixture

```python
# backend/app/agents/writer.py
"""Writer — synthesizes Insights into Markdown report with chart placeholders."""

from __future__ import annotations

import json
import re
from typing import Any

from app.agents.base import Agent
from app.agents.schemas import ChartSpec, ResearchState, StepResult
from app.services.llm_response import Tier
from app.services.llm_service import LLMService


_SYSTEM_PROMPT = """你是金融研究助手 writer。

任务:基于 Insights 写成 Markdown 研报。可在合适位置插入图占位符 ![描述](chart://{chart_id})。
chart_id 使用 c1/c2/... 格式。

输出 JSON:
{
  "report_markdown": "...",
  "chart_specs": [
    {"chart_id":"c1","chart_type":"line|bar|pie|table","title":"...","data":[...],"x_axis":"...","y_axis":"..."}
  ]
}
"""


def build_writer_prompt(state: ResearchState) -> str:
    insights_str = "\n".join(
        f"- [{i.subtask_id}] {i.finding} (confidence={i.confidence})"
        for i in state.insights
    )
    return (
        _SYSTEM_PROMPT
        + f"\n\n# Insights\n{insights_str}\n# 用户原始需求\n{state.user_message}\n\n请输出 JSON。"
    )


def _parse_writer_output(content: str) -> tuple[str, list[ChartSpec]]:
    cleaned = re.sub(r"^```(?:json)?\s*", "", content.strip(), flags=re.MULTILINE)
    cleaned = re.sub(r"\s*```\s*$", "", cleaned)
    data: dict[str, Any] = json.loads(cleaned)
    md = str(data.get("report_markdown", ""))
    specs = [ChartSpec.model_validate(s) for s in data.get("chart_specs", [])]
    return md, specs


class Writer(Agent):
    name = "Writer"
    model_tier: Tier = "balanced"

    def step(self, state: ResearchState) -> StepResult:
        prompt = build_writer_prompt(state)
        r = self._llm.chat(prompt=prompt, tier=self.model_tier, request_id=state.request_id)
        md, specs = _parse_writer_output(r.content)
        return StepResult(
            state_update={"report_markdown": md, "chart_specs": specs},
            span_metadata={"agent": "Writer", "model": r.model, "cost_cny": r.cost_cny},
        )
```

Fixture(JSON 输出 markdown 含 chart placeholder):
```yaml
- pattern: '^你是金融研究助手 writer'
  response:
    content: |
      {"report_markdown":"# 茅台(600519.SH)研报\n\n## 概况\n茅台当前股价 1820.5 元。\n\n![价格走势](chart://c1)\n\n## 财务\n营收稳健增长。\n","chart_specs":[{"chart_id":"c1","chart_type":"line","title":"价格走势","data":[{"date":"2026-04-30","price":1820.5}],"x_axis":"date","y_axis":"price"}]}
```

- [ ] **Step 3: Verify + commit**

```bash
git add backend/app/agents/writer.py backend/tests/unit/test_writer.py backend/tests/fixtures/llm_mocks/agent_decisions.yaml
git commit -m "$(cat <<'EOF'
feat(agents): add Writer for Insights → markdown + chart_specs synthesis

原因 layer: services
EOF
)"
```

---

## Task 7: 5 个 Critic sub-agents

**Files:**
- Create: `backend/app/agents/critic_subagents/__init__.py`(empty)
- Create: `backend/app/agents/critic_subagents/_base_scorer.py`
- Create: `backend/app/agents/critic_subagents/factuality.py`
- Create: `backend/app/agents/critic_subagents/coverage.py`
- Create: `backend/app/agents/critic_subagents/insight.py`
- Create: `backend/app/agents/critic_subagents/structure.py`
- Create: `backend/app/agents/critic_subagents/conciseness.py`
- Create: `backend/tests/unit/test_critic_subagents.py`
- Modify: `backend/tests/fixtures/llm_mocks/agent_decisions.yaml`

- [ ] **Step 1: Write the L0 test**

```python
# backend/tests/unit/test_critic_subagents.py
"""L0 — 5 Critic sub-agents prompt construction + score parsing."""

import pytest

from app.agents.critic_subagents.coverage import CoverageScorer
from app.agents.critic_subagents.factuality import FactualityScorer
from app.agents.critic_subagents.insight import InsightScorer
from app.agents.critic_subagents.structure import StructureScorer
from app.agents.critic_subagents.conciseness import ConcisenessScorer
from app.agents.schemas import (
    CriticDimensionScore,
    Insight,
    ResearchPlan,
    ResearchState,
    Subtask,
)
from app.services.llm_mock_client import MockLLMClient
from app.services.llm_service import LLMService


def _state_with_report() -> ResearchState:
    return ResearchState(
        user_id="u", session_id="s", user_message="m", request_id="req-test1234",
        report_markdown="# 茅台研报\n\n股价 1820.5。",
        insights=[Insight(subtask_id="overview", finding="x", supporting_data=[], confidence="high")],
        plan=ResearchPlan(
            subtasks=[Subtask(subtask_id="overview", description="d", required_tools=[], rationale="r")],
            target_entity="600519.SH", research_style="comprehensive", reasoning="r",
        ),
    )


@pytest.mark.parametrize("ScorerCls,dim", [
    (FactualityScorer, "factuality"),
    (CoverageScorer, "coverage"),
    (InsightScorer, "insight"),
    (StructureScorer, "structure"),
    (ConcisenessScorer, "conciseness"),
])
def test_scorer_returns_dimension_score(
    ScorerCls: type, dim: str,
    mock_llm_client: MockLLMClient, monkeypatch: pytest.MonkeyPatch
) -> None:
    monkeypatch.setenv("LLM_MODE", "mock")
    svc = LLMService(client=mock_llm_client)
    scorer = ScorerCls(llm=svc)

    state = _state_with_report()
    sr = scorer.step(state)

    score = sr.state_update[f"{dim}_score"]
    assert isinstance(score, CriticDimensionScore)
    assert score.dimension == dim
    assert 0.0 <= score.score <= 10.0
```

- [ ] **Step 2: Implement `_base_scorer.py`**

```python
# backend/app/agents/critic_subagents/_base_scorer.py
"""Shared base for Critic dimension scorers — common prompt template + parse."""

from __future__ import annotations

import json
import re
from abc import abstractmethod

from app.agents.base import Agent
from app.agents.schemas import (
    CriticDimension,
    CriticDimensionScore,
    ResearchState,
    StepResult,
)
from app.services.llm_response import Tier


_BASE_SCORER_TEMPLATE = """你是金融研究助手 critic_{dimension}_scorer。

任务:为以下研报评分(0-10 分),仅评估 {dimension} 维度。

# 评分标准({dimension})
{rubric}

# 研报全文
{report_markdown}

# 上下文(研报子任务和 insights)
{context}

输出 JSON:
{{"score": <0-10>, "evidence": "<1-2 句话支持依据>"}}
"""


class _BaseScorer(Agent):
    """Subclass must set:
    - name (e.g. "FactualityScorer")
    - dimension: CriticDimension (e.g. "factuality")
    - rubric: str (criteria for scoring)
    - state_field: str (e.g. "factuality_score")
    """

    dimension: CriticDimension
    rubric: str
    state_field: str
    model_tier: Tier = "fast"

    def step(self, state: ResearchState) -> StepResult:
        prompt = _BASE_SCORER_TEMPLATE.format(
            dimension=self.dimension,
            rubric=self.rubric,
            report_markdown=state.report_markdown or "(empty)",
            context=_build_context(state),
        )
        r = self._llm.chat(prompt=prompt, tier=self.model_tier, request_id=state.request_id)
        score = _parse_score(r.content, dimension=self.dimension, request_id=r.request_id or state.request_id)
        return StepResult(
            state_update={self.state_field: score},
            span_metadata={"agent": self.name, "dimension": self.dimension, "model": r.model, "cost_cny": r.cost_cny},
        )


def _build_context(state: ResearchState) -> str:
    parts = []
    if state.plan is not None:
        parts.append("Subtasks: " + ", ".join(s.subtask_id for s in state.plan.subtasks))
    if state.insights:
        parts.append(f"Insights count: {len(state.insights)}")
    return "\n".join(parts)


def _parse_score(content: str, *, dimension: CriticDimension, request_id: str) -> CriticDimensionScore:
    cleaned = re.sub(r"^```(?:json)?\s*", "", content.strip(), flags=re.MULTILINE)
    cleaned = re.sub(r"\s*```\s*$", "", cleaned)
    data = json.loads(cleaned)
    return CriticDimensionScore(
        dimension=dimension,
        score=float(data["score"]),
        evidence=str(data.get("evidence", "")),
        sub_agent_request_id=request_id,
    )
```

- [ ] **Step 3: Implement 5 scorers**

```python
# backend/app/agents/critic_subagents/factuality.py
from app.agents.critic_subagents._base_scorer import _BaseScorer

class FactualityScorer(_BaseScorer):
    name = "FactualityScorer"
    dimension = "factuality"
    state_field = "factuality_score"
    rubric = (
        "10 = 所有数据点准确;9-7 = 多数准确;6-4 = 部分准确;3-1 = 多处错误;0 = 严重失实。"
    )
```

```python
# backend/app/agents/critic_subagents/coverage.py
from app.agents.critic_subagents._base_scorer import _BaseScorer

class CoverageScorer(_BaseScorer):
    name = "CoverageScorer"
    dimension = "coverage"
    state_field = "coverage_score"
    rubric = (
        "10 = 所有 subtask 都覆盖;9-7 = 大部分;6-4 = 覆盖一半左右;3-1 = 覆盖少;0 = 完全偏题。"
    )
```

```python
# backend/app/agents/critic_subagents/insight.py
from app.agents.critic_subagents._base_scorer import _BaseScorer

class InsightScorer(_BaseScorer):
    name = "InsightScorer"
    dimension = "insight"
    state_field = "insight_score"
    rubric = (
        "10 = 深度分析,有非显然结论;9-7 = 有分析;6-4 = 表层分析;3-1 = 数据流水账;0 = 无任何分析。"
    )
```

```python
# backend/app/agents/critic_subagents/structure.py
from app.agents.critic_subagents._base_scorer import _BaseScorer

class StructureScorer(_BaseScorer):
    name = "StructureScorer"
    dimension = "structure"
    state_field = "structure_score"
    rubric = (
        "10 = 章节清晰、引用恰当、markdown 格式正确;9-7 = 多数 OK;"
        "6-4 = 章节模糊;3-1 = 杂乱无章;0 = 无结构。"
    )
```

```python
# backend/app/agents/critic_subagents/conciseness.py
from app.agents.critic_subagents._base_scorer import _BaseScorer

class ConcisenessScorer(_BaseScorer):
    name = "ConcisenessScorer"
    dimension = "conciseness"
    state_field = "conciseness_score"
    rubric = (
        "10 = 长度合理无冗余;9-7 = 略有重复;6-4 = 啰嗦;3-1 = 严重冗余;0 = 全篇水文。"
    )
```

- [ ] **Step 4: Add fixture entry(共享一个 pattern,所有 scorer 命中相同回复)**

```yaml
- pattern: '^你是金融研究助手 critic_'
  response:
    content: |
      {"score": 8.0, "evidence": "数据准确,结构清晰,符合预期。"}
```

(所有 5 维评分共用同一返回,score=8.0,简单测试足够。真打时 LLM 会按 dimension 给不同分。)

- [ ] **Step 5: Verify + commit**

```bash
uv run pytest backend/tests/unit/test_critic_subagents.py -v
uv run mypy backend/app/agents/critic_subagents/
```

```bash
git add backend/app/agents/critic_subagents/ backend/tests/unit/test_critic_subagents.py backend/tests/fixtures/llm_mocks/agent_decisions.yaml
git commit -m "$(cat <<'EOF'
feat(agents): add 5 Critic sub-agents (factuality/coverage/insight/structure/conciseness)

Shared _BaseScorer template + per-dimension rubric. All 5 satisfy Agent
ABC; ready for Critic.dispatch_subagent invocation in Task 8.

原因 layer: services
EOF
)"
```

---

## Task 8: `Critic` agent + DispatchSubAgent 真实现

**Files:**
- Create: `backend/app/agents/critic.py`
- Create: `backend/tests/unit/test_critic.py`

- [ ] **Step 1: Write the L0 test**

```python
# backend/tests/unit/test_critic.py
"""L0 — Critic.dispatch_subagent real implementation + aggregate."""

import pytest

from app.agents.critic import Critic, aggregate_scores
from app.agents.critic_subagents.factuality import FactualityScorer
from app.agents.critic_subagents.coverage import CoverageScorer
from app.agents.critic_subagents.insight import InsightScorer
from app.agents.critic_subagents.structure import StructureScorer
from app.agents.critic_subagents.conciseness import ConcisenessScorer
from app.agents.schemas import (
    CriticDimensionScore,
    CriticReport,
    ResearchState,
)
from app.services.llm_mock_client import MockLLMClient
from app.services.llm_service import LLMService


def _state() -> ResearchState:
    return ResearchState(
        user_id="u", session_id="s", user_message="m", request_id="req-test1234",
        report_markdown="# 测试研报",
    )


def test_critic_dispatch_subagent_real(mock_llm_client: MockLLMClient, monkeypatch: pytest.MonkeyPatch) -> None:
    """v0.5: Critic.dispatch_subagent override v0 base.py 的 NotImplementedError 占位。"""
    monkeypatch.setenv("LLM_MODE", "mock")
    svc = LLMService(client=mock_llm_client)
    scorers = [
        FactualityScorer(llm=svc), CoverageScorer(llm=svc), InsightScorer(llm=svc),
        StructureScorer(llm=svc), ConcisenessScorer(llm=svc),
    ]
    critic = Critic(llm=svc, scorers=scorers)

    sr = critic.dispatch_subagent(name="FactualityScorer", state=_state())
    assert "factuality_score" in sr.state_update


def test_critic_dispatch_subagent_unknown_raises() -> None:
    svc = LLMService(client=MockLLMClient.from_fixture_dir(__import__("pathlib").Path("backend/tests/fixtures/llm_mocks")))
    critic = Critic(llm=svc, scorers=[])
    with pytest.raises(KeyError, match="not found"):
        critic.dispatch_subagent(name="NonExistent", state=_state())


def test_aggregate_scores_5_dims() -> None:
    dims = [
        CriticDimensionScore(dimension=d, score=8.0, evidence="e", sub_agent_request_id="r")
        for d in ("factuality", "coverage", "insight", "structure", "conciseness")
    ]
    report = aggregate_scores(dims, summary="ok")
    assert isinstance(report, CriticReport)
    assert report.overall_score == 8.0
```

- [ ] **Step 2: Implement `critic.py`**

```python
# backend/app/agents/critic.py
"""Critic — orchestrates 5-dim parallel scoring via LangGraph subgraph (Send API).

The actual fan-out + reduce happens at the orchestration layer
(critic_subgraph.py). Critic class itself owns the sub_agents list and
provides:
  - dispatch_subagent(name, state) → real implementation (override v0
    base.py's NotImplementedError placeholder)
  - aggregate_scores helper for the subgraph reduce phase
"""

from __future__ import annotations

from app.agents.base import Agent
from app.agents.schemas import (
    CriticDimensionScore,
    CriticReport,
    ResearchState,
    StepResult,
)
from app.services.llm_response import Tier
from app.services.llm_service import LLMService


class Critic(Agent):
    name = "Critic"
    model_tier: Tier = "balanced"  # nominal; aggregate doesn't call LLM

    def __init__(self, llm: LLMService, scorers: list[Agent]) -> None:
        super().__init__(llm)
        self._scorers = scorers

    @property
    def scorers(self) -> list[Agent]:
        return list(self._scorers)

    def step(self, state: ResearchState) -> StepResult:
        """Critic.step is not the entry point; the LangGraph subgraph (Task 9) is.
        This sync entry exists for unit testability of Agent contract.
        """
        # Single-process dispatch fallback (used by unit tests, not LangGraph)
        scores: list[CriticDimensionScore] = []
        for scorer in self._scorers:
            sr = scorer.step(state)
            for v in sr.state_update.values():
                if isinstance(v, CriticDimensionScore):
                    scores.append(v)
        report = aggregate_scores(scores, summary="(unit-test aggregation)")
        return StepResult(
            state_update={"critic_report": report},
            span_metadata={"agent": "Critic", "n_scorers": len(self._scorers)},
        )

    def dispatch_subagent(self, name: str, state: ResearchState) -> StepResult:
        """Override v0 base.py 的 NotImplementedError 占位。"""
        target = next((s for s in self._scorers if s.name == name), None)
        if target is None:
            raise KeyError(f"sub-agent {name!r} not found in Critic._scorers")
        return target.step(state)


def aggregate_scores(dims: list[CriticDimensionScore], *, summary: str = "") -> CriticReport:
    """Compute overall_score = mean(dim scores)."""
    if not dims:
        return CriticReport(dimensions=[], overall_score=0.0, summary_markdown=summary)
    overall = sum(d.score for d in dims) / len(dims)
    return CriticReport(dimensions=dims, overall_score=overall, summary_markdown=summary)
```

- [ ] **Step 3: Verify + commit**

```bash
uv run pytest backend/tests/unit/test_critic.py -v
uv run mypy backend/app/agents/critic.py
```

```bash
git add backend/app/agents/critic.py backend/tests/unit/test_critic.py
git commit -m "$(cat <<'EOF'
feat(agents): add Critic with DispatchSubAgent real implementation + aggregate

Critic owns 5 scorers via constructor injection (DI per spec § 4 DS1).
dispatch_subagent overrides v0 base.py NotImplementedError placeholder.
aggregate_scores helper computes overall_score = mean(dim scores).

The actual parallel fan-out happens in critic_subgraph (Task 9 via
LangGraph Send API); Critic.step provides sync fallback for unit tests.

原因 layer: services
EOF
)"
```

---

## Task 9: `critic_subgraph` + `research_nodes`

**Files:**
- Create: `backend/app/orchestration/critic_subgraph.py`
- Create: `backend/app/orchestration/research_nodes.py`
- Create: `backend/tests/unit/test_critic_subgraph.py`

- [ ] **Step 1: Implement `research_nodes.py`** (5 outer nodes)

```python
# backend/app/orchestration/research_nodes.py
"""Outer-graph node functions for v0.5 research mode."""

from __future__ import annotations

from typing import Any

from app.agents.analyst import Analyst
from app.agents.critic import Critic
from app.agents.data_collector import DataCollector
from app.agents.research_planner import ResearchPlanner
from app.agents.schemas import ResearchState
from app.agents.writer import Writer


async def research_planner_node(state: ResearchState, planner: ResearchPlanner) -> dict[str, Any]:
    sr = planner.step(state)
    return sr.state_update


async def data_collector_node(state: ResearchState, collector: DataCollector) -> dict[str, Any]:
    sr = await collector.collect_async(state)
    return sr.state_update


async def analyst_node(state: ResearchState, analyst: Analyst) -> dict[str, Any]:
    sr = analyst.step(state)
    return sr.state_update


async def writer_node(state: ResearchState, writer: Writer) -> dict[str, Any]:
    sr = writer.step(state)
    return sr.state_update
```

(critic_node 不在这里;由 critic_subgraph 提供 compiled subgraph 直接 add_node。)

- [ ] **Step 2: Implement `critic_subgraph.py`**

> 参考 Task 0 Spike 1/2 结果。如果 Send API 行为符合预期(case A),用以下代码。

```python
# backend/app/orchestration/critic_subgraph.py
"""Critic 内部 subgraph — Send API fan-out 5 sub-agents + reduce + aggregate."""

from __future__ import annotations

from typing import Annotated, Any

from langgraph.graph import StateGraph, START, END
from langgraph.types import Send
from pydantic import BaseModel, ConfigDict, Field

from app.agents.critic import Critic, aggregate_scores
from app.agents.schemas import CriticDimensionScore, CriticReport, ResearchState


class _CriticSubState(BaseModel):
    """子图内部 state — top-level state 字段共享 + scores 累积 list (reducer)."""
    model_config = ConfigDict(arbitrary_types_allowed=True)

    user_id: str
    session_id: str
    user_message: str
    request_id: str
    report_markdown: str | None = None
    insights: list[Any] = Field(default_factory=list)
    plan: Any = None
    tool_results: list[Any] = Field(default_factory=list)

    # reducer field — 5 scorer 各自 append 一个,框架自动 concat
    collected_scores: Annotated[list[CriticDimensionScore], _list_extend] = Field(default_factory=list)


def _list_extend(left: list, right: list) -> list:
    """Reducer: concat two lists (LangGraph Send API → reduce phase)."""
    return left + right


def _scorer_node_factory(critic: Critic, scorer_name: str) -> Any:
    """Wrap scorer.step into a node function that appends its CriticDimensionScore to collected_scores."""
    async def node(state: _CriticSubState) -> dict[str, Any]:
        # Convert _CriticSubState back to ResearchState for scorer
        rs = ResearchState(
            user_id=state.user_id, session_id=state.session_id,
            user_message=state.user_message, request_id=state.request_id,
            report_markdown=state.report_markdown, insights=state.insights,
            plan=state.plan, tool_results=state.tool_results,
        )
        sr = critic.dispatch_subagent(name=scorer_name, state=rs)
        scores = [v for v in sr.state_update.values() if isinstance(v, CriticDimensionScore)]
        return {"collected_scores": scores}
    return node


def _planner_router(state: _CriticSubState) -> list[Send]:
    """Fan-out to 5 scorer nodes via Send API."""
    return [
        Send("scorer_factuality", state.model_dump()),
        Send("scorer_coverage", state.model_dump()),
        Send("scorer_insight", state.model_dump()),
        Send("scorer_structure", state.model_dump()),
        Send("scorer_conciseness", state.model_dump()),
    ]


async def _aggregate_node(state: _CriticSubState) -> dict[str, Any]:
    report = aggregate_scores(
        state.collected_scores,
        summary=f"5-dim parallel scoring; overall={sum(d.score for d in state.collected_scores)/max(len(state.collected_scores),1):.2f}",
    )
    return {"critic_report": report}


def build_critic_subgraph(critic: Critic) -> Any:
    """Build the Critic subgraph; compile into a node-friendly object."""
    g: StateGraph = StateGraph(_CriticSubState)

    # 5 scorer nodes
    g.add_node("scorer_factuality", _scorer_node_factory(critic, "FactualityScorer"))
    g.add_node("scorer_coverage", _scorer_node_factory(critic, "CoverageScorer"))
    g.add_node("scorer_insight", _scorer_node_factory(critic, "InsightScorer"))
    g.add_node("scorer_structure", _scorer_node_factory(critic, "StructureScorer"))
    g.add_node("scorer_conciseness", _scorer_node_factory(critic, "ConcisenessScorer"))

    # Aggregate node
    g.add_node("aggregate", _aggregate_node)

    # Edges: START → fan-out (via planner_router) → all 5 scorer nodes → aggregate → END
    g.add_conditional_edges(START, _planner_router, [
        "scorer_factuality", "scorer_coverage", "scorer_insight",
        "scorer_structure", "scorer_conciseness",
    ])
    for n in ["scorer_factuality", "scorer_coverage", "scorer_insight", "scorer_structure", "scorer_conciseness"]:
        g.add_edge(n, "aggregate")
    g.add_edge("aggregate", END)

    return g.compile()
```

> **Spike fallback**:如果 Spike 1 显示 Send API + Annotated reducer 行为不符合预期,改用如下 sequential 方案(性能损失 5x latency,但保证可跑):

```python
# Fallback if Send API doesn't work as expected
async def critic_node_sequential(state: ResearchState, critic: Critic) -> dict[str, Any]:
    """Sequential fallback for environments where Send API isn't usable."""
    sr = critic.step(state)  # uses Critic.step's sequential dispatch
    return sr.state_update
```

- [ ] **Step 3: Write the L0 test for subgraph**

```python
# backend/tests/unit/test_critic_subgraph.py
"""L0 — critic_subgraph builds, runs 5 scorers, returns CriticReport."""

import pytest

from app.agents.critic import Critic
from app.agents.critic_subagents.coverage import CoverageScorer
from app.agents.critic_subagents.factuality import FactualityScorer
from app.agents.critic_subagents.insight import InsightScorer
from app.agents.critic_subagents.structure import StructureScorer
from app.agents.critic_subagents.conciseness import ConcisenessScorer
from app.agents.schemas import CriticReport
from app.orchestration.critic_subgraph import build_critic_subgraph
from app.services.llm_mock_client import MockLLMClient
from app.services.llm_service import LLMService


@pytest.mark.asyncio
async def test_critic_subgraph_runs_5_scorers(
    mock_llm_client: MockLLMClient, monkeypatch: pytest.MonkeyPatch
) -> None:
    monkeypatch.setenv("LLM_MODE", "mock")
    svc = LLMService(client=mock_llm_client)
    scorers = [
        FactualityScorer(llm=svc), CoverageScorer(llm=svc), InsightScorer(llm=svc),
        StructureScorer(llm=svc), ConcisenessScorer(llm=svc),
    ]
    critic = Critic(llm=svc, scorers=scorers)
    sub_app = build_critic_subgraph(critic)

    initial = {
        "user_id": "u", "session_id": "s", "user_message": "m", "request_id": "req-test1234",
        "report_markdown": "# 测试研报", "insights": [], "plan": None, "tool_results": [],
        "collected_scores": [],
    }
    final = await sub_app.ainvoke(initial)
    assert "critic_report" in final
    report = final["critic_report"]
    assert isinstance(report, CriticReport)
    n = len(report.dimensions)
    assert n == 5, f"Expected 5 dimensions, got {n}"
```

- [ ] **Step 4: Verify + commit**

```bash
uv run pytest backend/tests/unit/test_critic_subgraph.py -v
uv run mypy backend/app/orchestration/
```

```bash
git add backend/app/orchestration/research_nodes.py backend/app/orchestration/critic_subgraph.py backend/tests/unit/test_critic_subgraph.py
git commit -m "$(cat <<'EOF'
feat(orchestration): add research nodes + Critic subgraph (Send API fan-out)

Top-level nodes: research_planner / data_collector / analyst / writer.
Critic subgraph uses LangGraph Send API to fan out to 5 scorer nodes in
parallel, reducer concats CriticDimensionScores, aggregate node produces
final CriticReport.

原因 layer: services
EOF
)"
```

---

## Task 10: `research_graph.py` + `ResearchAgent` SUT wrapper

**Files:**
- Create: `backend/app/orchestration/research_graph.py`
- Create: `backend/app/agents/research_agent.py`
- Create: `backend/tests/unit/test_research_graph.py`
- Create: `backend/tests/integration/test_research_agent_e2e_mock.py`

- [ ] **Step 1: Implement `research_graph.py`**

```python
# backend/app/orchestration/research_graph.py
"""Top-level v0.5 research mode StateGraph builder."""

from __future__ import annotations

from functools import partial
from pathlib import Path
from typing import Any

from langgraph.graph import StateGraph, START, END

from app.agents.analyst import Analyst
from app.agents.critic import Critic
from app.agents.data_collector import DataCollector
from app.agents.research_planner import ResearchPlanner
from app.agents.schemas import ResearchState
from app.agents.writer import Writer
from app.orchestration.checkpointer import make_chat_checkpointer
from app.orchestration.critic_subgraph import build_critic_subgraph
from app.orchestration.research_nodes import (
    analyst_node, data_collector_node, research_planner_node, writer_node,
)


def build_research_graph(
    planner: ResearchPlanner,
    collector: DataCollector,
    analyst: Analyst,
    writer: Writer,
    critic: Critic,
    *,
    db_path: Path | None = None,
) -> Any:
    g: StateGraph = StateGraph(ResearchState)

    g.add_node("research_planner_node", partial(research_planner_node, planner=planner))
    g.add_node("data_collector_node", partial(data_collector_node, collector=collector))
    g.add_node("analyst_node", partial(analyst_node, analyst=analyst))
    g.add_node("writer_node", partial(writer_node, writer=writer))

    # Critic 是子图,直接作为节点
    critic_subapp = build_critic_subgraph(critic)
    g.add_node("critic_node", critic_subapp)

    g.add_edge(START, "research_planner_node")
    g.add_edge("research_planner_node", "data_collector_node")
    g.add_edge("data_collector_node", "analyst_node")
    g.add_edge("analyst_node", "writer_node")
    g.add_edge("writer_node", "critic_node")
    g.add_edge("critic_node", END)

    checkpointer = make_chat_checkpointer(db_path) if db_path else None
    return g.compile(checkpointer=checkpointer)
```

- [ ] **Step 2: Implement `research_agent.py`**

```python
# backend/app/agents/research_agent.py
"""ResearchAgent — SUT-friendly wrapper for the research LangGraph."""

from __future__ import annotations

from typing import Any

from app.agents.schemas import ResearchPlan, ResearchState, ToolCall
from app.services.eval_models import SUTOutput
from app.services.trace_service import TraceService


class ResearchAgent:
    def __init__(
        self,
        graph: Any,  # CompiledStateGraph (langgraph 1.x typing)
        trace_service: TraceService | None = None,
    ) -> None:
        self._graph = graph
        self._trace = trace_service

    async def run(self, user_input: str, request_id: str) -> SUTOutput:
        config = {"configurable": {"thread_id": f"research:eval:{request_id}"}}
        initial = ResearchState(
            user_id="eval",
            session_id=request_id,
            user_message=user_input,
            request_id=request_id,
        )
        final: dict[str, Any] = await self._graph.ainvoke(initial.model_dump(), config=config)

        plan_val = final.get("plan")
        if isinstance(plan_val, dict):
            plan_val = ResearchPlan.model_validate(plan_val)

        # Extract tool_calls from tool_results (reverse-engineer for eval)
        tool_calls = []
        for tr in (final.get("tool_results") or []):
            tool_calls.append(ToolCall(
                tool_name=tr.tool_name if hasattr(tr, "tool_name") else tr["tool_name"],
                args=tr.args if hasattr(tr, "args") else tr["args"],
                rationale="",
            ))

        return SUTOutput(
            request_id=request_id,
            response_text=final.get("report_markdown") or "",
            tool_calls=tool_calls,
        )
```

- [ ] **Step 3: Write tests + commit**

`test_research_graph.py` 测 `build_research_graph` 返回 compiled object。

`test_research_agent_e2e_mock.py` 是 L1 集成,完整跑 5-agent + Critic subgraph,assert `report_markdown` 非空 + `critic_report.dimensions` len = 5。注意:e2e 需要构造 5 个 scorer + 5 mocks(已 Task 7 fixture 通用 pattern 覆盖)。

```bash
git add backend/app/orchestration/research_graph.py backend/app/agents/research_agent.py backend/tests/unit/test_research_graph.py backend/tests/integration/test_research_agent_e2e_mock.py
git commit -m "$(cat <<'EOF'
feat(orchestration+agents): build_research_graph + ResearchAgent SUT wrapper

Top-level graph wires 4 outer agents + Critic subgraph (Send API).
ResearchAgent.run satisfies SUT Protocol; report_markdown becomes
SUTOutput.response_text; tool_results reverse-engineered to tool_calls
for EvalRunner tool_correctness scoring.

原因 layer: services
EOF
)"
```

---

## Task 11: HTTP `POST /api/v0.5/research` SSE 路由

**Files:**
- Create: `backend/app/router/research.py`
- Modify: `backend/app/app_main.py`
- Create: `backend/tests/integration/test_research_router_sse.py`

> 类比 v0 chat router,但事件类型改为 7 种(plan / data_progress / insight / report_chunk / critic_score / done / error)。

- [ ] **Step 1: Implement `research.py`** (FastAPI router with SSE)

```python
# backend/app/router/research.py
"""POST /api/v0.5/research SSE streaming endpoint."""

from __future__ import annotations

import json
from typing import Any, AsyncIterator, Literal
from uuid import uuid4

from fastapi import APIRouter, Depends
from fastapi.responses import StreamingResponse
from pydantic import BaseModel, Field

from app.agents.schemas import ResearchState
from app.router.chat import _AnonUser, get_current_user  # reuse v0 stub auth

router = APIRouter()


class ResearchRequest(BaseModel):
    target_entity: str = ""
    user_message: str
    research_style: Literal["concise", "comprehensive"] = "comprehensive"


class ResearchStreamEvent(BaseModel):
    type: Literal[
        "plan", "data_progress", "insight", "report_chunk",
        "critic_score", "done", "error",
    ]
    data: dict[str, Any] = Field(default_factory=dict)


def _adapt_event(ev: dict[str, Any]) -> ResearchStreamEvent | None:
    """LangGraph event → ResearchStreamEvent (or None to skip)."""
    et = ev.get("event")
    name = ev.get("name", "")

    if et == "on_chain_end":
        if name == "research_planner_node":
            return ResearchStreamEvent(type="plan", data={"name": name})
        if name == "data_collector_node":
            return ResearchStreamEvent(type="data_progress", data={"name": name})
        if name == "analyst_node":
            return ResearchStreamEvent(type="insight", data={"name": name})
        if name == "writer_node":
            return ResearchStreamEvent(type="report_chunk", data={"name": name})
        if name.startswith("scorer_"):
            return ResearchStreamEvent(type="critic_score", data={"scorer": name})
        if name == "LangGraph":
            return ResearchStreamEvent(type="done", data={})
    return None


_RESEARCH_GRAPH_SINGLETON: Any = None


def get_research_graph() -> Any:
    """DI factory: build the research graph singleton at first request (lazy init).

    v0 chat router pattern延续(`backend/app/router/chat.py:get_chat_graph`);
    implementer 实施 Task 11 时,如果 v0 router 的 build helper 抽出来了 reuse,
    否则在此 inline。"""
    global _RESEARCH_GRAPH_SINGLETON
    if _RESEARCH_GRAPH_SINGLETON is not None:
        return _RESEARCH_GRAPH_SINGLETON

    from app.agents.analyst import Analyst
    from app.agents.critic import Critic
    from app.agents.critic_subagents.coverage import CoverageScorer
    from app.agents.critic_subagents.factuality import FactualityScorer
    from app.agents.critic_subagents.insight import InsightScorer
    from app.agents.critic_subagents.structure import StructureScorer
    from app.agents.critic_subagents.conciseness import ConcisenessScorer
    from app.agents.data_collector import DataCollector
    from app.agents.research_planner import ResearchPlanner
    from app.agents.writer import Writer
    from app.orchestration.research_graph import build_research_graph
    from app.services.openai_client import build_llm_service_from_env
    from app.tools.get_financials import GetFinancialsTool
    from app.tools.get_news import GetNewsTool
    from app.tools.get_stock_quote import StockQuoteTool
    from app.tools.mock_kb_search import MockKbSearchTool
    from app.tools.mock_web_search import MockWebSearchTool
    from app.tools.registry import ToolRegistry
    from app.service.mock_bocha_service import MockBochaService  # type: ignore[import]
    from app.service.mock_tushare_service import MockTushareService  # type: ignore[import]

    llm = build_llm_service_from_env()
    mock_tushare = MockTushareService(llm=llm)
    mock_bocha = MockBochaService(llm=llm)

    registry = ToolRegistry()
    registry.register(StockQuoteTool(mock_tushare=mock_tushare))
    registry.register(GetFinancialsTool(mock_tushare=mock_tushare))
    registry.register(GetNewsTool(mock_bocha=mock_bocha))
    registry.register(MockWebSearchTool(mock_bocha=mock_bocha))
    registry.register(MockKbSearchTool(mock_bocha=mock_bocha))

    planner = ResearchPlanner(llm=llm)
    collector = DataCollector(llm=llm, registry=registry)
    analyst = Analyst(llm=llm)
    writer = Writer(llm=llm)
    scorers = [
        FactualityScorer(llm=llm), CoverageScorer(llm=llm), InsightScorer(llm=llm),
        StructureScorer(llm=llm), ConcisenessScorer(llm=llm),
    ]
    critic = Critic(llm=llm, scorers=scorers)

    _RESEARCH_GRAPH_SINGLETON = build_research_graph(
        planner=planner, collector=collector, analyst=analyst,
        writer=writer, critic=critic,
        db_path=Path("backend/data/research.sqlite"),
    )
    return _RESEARCH_GRAPH_SINGLETON


@router.post("/api/v0.5/research")
async def research(
    req: ResearchRequest,
    user: _AnonUser = Depends(get_current_user),
    graph: Any = Depends(get_research_graph),
) -> StreamingResponse:
    return StreamingResponse(
        _stream_research(req, user, graph),
        media_type="text/event-stream",
        headers={"Cache-Control": "no-cache", "X-Accel-Buffering": "no"},
    )


async def _stream_research(req: ResearchRequest, user: _AnonUser, graph: Any) -> AsyncIterator[str]:
    request_id = f"research-{uuid4().hex[:12]}"
    initial = ResearchState(
        user_id=user.id, session_id=request_id,
        user_message=req.user_message, request_id=request_id,
    )
    config = {"configurable": {"thread_id": f"research:{user.id}:{request_id}"}}

    try:
        async for ev in graph.astream_events(initial.model_dump(), config=config, version="v2"):
            adapted = _adapt_event(ev)
            if adapted is not None:
                yield f"data: {adapted.model_dump_json()}\n\n"
    except Exception as e:
        err = ResearchStreamEvent(type="error", data={"message": str(e)})
        yield f"data: {err.model_dump_json()}\n\n"
```

> **DI wiring**:`get_research_graph` 在 plan 实施时由 implementer 用 `build_llm_service_from_env() + 5 tools + 5 agents + build_research_graph(...)` 构造。可放在 `app_main.py` lifespan startup,或 lazily 第一次请求时构建。

- [ ] **Step 2: Modify `app_main.py`**

加入 `from app.router import research` 和 `app.include_router(research.router)`。

- [ ] **Step 3: L1 SSE test + commit**

```python
# backend/tests/integration/test_research_router_sse.py
"""L1 — Research SSE router framing + event types."""

import json

import pytest
from fastapi.testclient import TestClient

# Test setup: instantiate FastAPI app with mock-injected research graph
# (similar pattern to v0 test_chat_router_sse.py)
```

```bash
uv run pytest backend/tests/integration/test_research_router_sse.py -v
```

```bash
git add backend/app/router/research.py backend/app/app_main.py backend/tests/integration/test_research_router_sse.py
git commit -m "$(cat <<'EOF'
feat(router): POST /api/v0.5/research SSE streaming endpoint

7 event types (plan / data_progress / insight / report_chunk /
critic_score / done / error) adapt LangGraph astream_events.

原因 layer: services
EOF
)"
```

---

## Task 12: Judge extension + GoldenCase extension + EvalRunner ResearchAgent SUT 测试

**Files:**
- Modify: `backend/app/services/judge.py`
- Modify: `backend/app/services/eval_models.py`
- Create: `backend/tests/integration/test_eval_runner_research_agent_sut.py`
- Modify: `backend/tests/fixtures/llm_mocks/agent_decisions.yaml`

- [ ] **Step 1: Extend `judge.py`** (additive: 加 `report_markdown` param + `report_markdown_quality` 维度)

```python
# 在 Judge.score signature 加(default None,Plan C/D 老调用零修改):
def score(
    self, *, case: GoldenCase, sut_response: str, trace_summary: str,
    tool_calls: list[ToolCall] | None = None,
    report_markdown: str | None = None,
) -> tuple[JudgeScores, dict[str, Any]]:
    # 现有逻辑不动;若 report_markdown 提供,prompt 末尾加 markdown_quality 段落
    ...
```

`JudgeScores` 加 `report_markdown_quality: float | None = None`(additive)。

- [ ] **Step 2: Extend `eval_models.py`** — `GoldenCase` 加 `expected_topics: list[str] = Field(default_factory=list)`(additive)。

- [ ] **Step 3: L1 test 验证 ResearchAgent SUT** + commit

```python
# backend/tests/integration/test_eval_runner_research_agent_sut.py
"""L1 — EvalRunner with ResearchAgent SUT, verifying report_markdown_quality emitted."""

import pytest
from app.services.eval_runner import EvalRunner
# ... build full ResearchAgent stack with mocks
# Assert: scores.report_markdown_quality is not None (because SUT is ResearchAgent)
# Assert: scores.tool_correctness is not None (because tool_calls provided)
```

```bash
git add backend/app/services/judge.py backend/app/services/eval_models.py backend/tests/integration/test_eval_runner_research_agent_sut.py backend/tests/fixtures/llm_mocks/agent_decisions.yaml
git commit -m "$(cat <<'EOF'
feat(eval): add report_markdown_quality dim + GoldenCase.expected_topics

Judge.score gains optional report_markdown param (additive); when
provided, prompt includes markdown quality rubric. GoldenCase gains
expected_topics: list[str] (additive) for research-mode coverage check.

ResearchAgent SUT integration verified — both tool_correctness and
report_markdown_quality emit non-None scores.

原因 layer: services
EOF
)"
```

---

## Task 13: 3-5 条 research_* GoldenCase fixtures

**Files:**
- Create: `backend/tests/eval/golden/research_basic.jsonl`

3-5 条 case,例如:
```json
{"case_id":"r-stock-overview-basic","category":"research_overview","user_input":"深度分析茅台(600519.SH)基本面","expected_topics":["股价","财务","行业地位"],"expected_behavior":{"min_topics_covered":2}}
{"case_id":"r-financial-deepdive","category":"research_financial","user_input":"600519.SH 的财务健康度怎么样?","expected_topics":["营收","利润","现金流"],"expected_behavior":{"min_topics_covered":2}}
{"case_id":"r-macro-context","category":"research_macro","user_input":"白酒行业最近表现如何?","expected_topics":["行业","主要公司","趋势"],"expected_behavior":{"min_topics_covered":1}}
```

```bash
git add backend/tests/eval/golden/research_basic.jsonl
git commit -m "$(cat <<'EOF'
test(eval): add 3 research-mode golden cases

3 categories: research_overview / research_financial / research_macro.
Each carries expected_topics for v0.5 coverage scoring.

原因 layer: services
EOF
)"
```

---

## Task 14: L2 cassette + final integration

**Files:**
- Create: `backend/tests/e2e/test_research_agent_cassette.py`
- Create: `backend/tests/fixtures/cassettes/test_research_agent_cassette/...yaml`

L2 cassette 录全 research path,~8-12 LLM calls,~¥0.05-0.10 录一次。Replay ≤ 2s。

```bash
unset all_proxy https_proxy http_proxy
VCR_RECORD_MODE=once uv run pytest backend/tests/e2e/test_research_agent_cassette.py -v -s
```

验证:cassette ≤ 100KB / 无 credential 泄露 / replay 通过。

```bash
git add backend/tests/e2e/test_research_agent_cassette.py backend/tests/fixtures/cassettes/test_research_agent_cassette/
git commit -m "$(cat <<'EOF'
test(e2e): add L2 cassette for ResearchAgent with real DashScope LLM

Records 8-12 real LLM round-trips (planner + analyst + writer + 5
Critic scorers) at ~¥0.05-0.10 once; replays offline in <2s.

原因 layer: services
EOF
)"
```

---

## Task 15: Final ci + retrospective

- [ ] `uv run poe ci` 全绿(预估 165+ tests:143 baseline + 22 新)
- [ ] `uv run poe nightly-local` 通过(0 drift)
- [ ] 跑 EvalRunner over 3 research GoldenCase,记录 `report_markdown_quality` 平均分
- [ ] 填 spec retrospective(同 v0 模板:对的 3 / 错的 3 / 下个 3 / memory 沉淀 / subagent 节奏 / v0.6 启动条件)

---

## Acceptance Criteria

- [ ] `uv run poe ci` green
- [ ] LangGraph Send API in critic_subgraph correctly fans out to 5 scorers in parallel (L0 test verified)
- [ ] `Critic.dispatch_subagent(name, state)` overrides v0 base.py NotImplementedError (L0 test verified)
- [ ] services/agents/tools/orchestration 任意 .py 不 `import openai` (grep 确认;`app.services.openai_client` 是唯一例外)
- [ ] research checkpoint thread_id 隔离正确(`research:{user_id}:{request_id}` vs chat 的 `chat:{user_id}:{session_id}`)
- [ ] HTTP SSE 7 事件类型框架正确(L1 test verified)
- [ ] EvalRunner ResearchAgent SUT 跑通,`report_markdown_quality != None`
- [ ] EvalRunner ChatAgent SUT + bare LLMService SUT 仍兼容
- [ ] L2 cassette ≤ 100KB,sanitized,replay ≤ 2s
- [ ] 5 sub-agent prompt 共享 `_BASE_SCORER_TEMPLATE`,各 dimension 用各自 rubric
- [ ] Plan retrospective + spec retrospective 填写

---

## Test plan (run on completion)

- Local: `uv run poe ci`
- Local: `uv run poe nightly-local`
- Local: `uv run poe serve` 起 uvicorn,curl `POST /api/v0.5/research` 真流(深度分析茅台)
- Local: `unset VCR_RECORD_MODE && uv run pytest backend/tests/e2e/test_research_agent_cassette.py -v`(replay-only)
- Cloud: PR 触发 `pr.yml` ≤ 5min ci 全绿(包括 research cassette test)
- Cloud: 下次 nightly cron 跑 `nightly.yml`,`EvalResult` 表里多 N 行 ResearchAgent SUT 评分

---

## Notes for the implementer

- **Task 0 spike 必跑**(LangGraph Send API + subgraph 嵌套);后续 Task 9 critic_subgraph 代码结构依赖 spike 结果
- **ruff format 注意**(Plan C/D 沉淀):assert 写 `n = ...; assert n >= 1, msg`,避免 `assert (... ) , msg` 多行 wrap pattern(pre-commit ruff 0.6 vs venv 0.15 disagree)
- **SUT Protocol 复用**:Plan C/D + v0 测试零回归;ResearchAgent.run 直接满足现有 Protocol(无需扩 SUT 接口)
- **Plan B/C/D 契约保持是 acceptance criteria**:LLMService.chat / Judge.score / EvalRunner.run_one 都不能破坏

---

## Plan retrospective — Task 0 spike result

> Filled in by implementer at Task 0.

### Spike 1: LangGraph Send API fan-out + reduce
- **Date probed**: 2026-05-01
- **LangGraph version**: 1.1.10(同 v0)
- **Result**: **A**(完全符合预期)
- **Output**: `FINAL: {'msg': 'test total=30', 'scores': [0, 10, 20]}` —— 3 个 scorer 通过 Send API 并行触发,`Annotated[list[int], operator.add]` reducer 正确累加
- **Import path**: `from langgraph.types import Send`(同 plan 假设)
- **Action taken**: Task 9 `critic_subgraph` 直接按 plan 写法实施(Send API + Annotated reducer);`_list_extend` reducer 函数等价于 `operator.add`(都是 list concat),plan 代码合法。

### Spike 2: LangGraph subgraph state passing
- **Date probed**: 2026-05-01
- **State 共享行为**: TopState 与 SubState 字段名相同时,subgraph 输出自动合并回 top-level state(`sub_result` 从 "" → "done" 经 subgraph 后正确传回 top)
- **Action taken**: Task 9 `_CriticSubState` schema 设计正确(共享字段 + reducer 累加 collected_scores);Task 10 `research_graph` 把 `critic_subapp` 直接 `g.add_node("critic_node", critic_subapp)` 即可,LangGraph 自动 bridge state。

### Spike outcome summary
- 两个 spike 都符合 plan 假设,**plan 无需调整**。
- pyproject deps 不变(`langgraph>=1.0,<2` + `langgraph-checkpoint-sqlite>=3.0,<4` 已 v0 加好)。
- Task 9 critic_subgraph 按 plan 草拟代码实施。

---

## Retrospective

> Filled in by implementer at Task 15.

**Implementation completion date**: 2026-05-01
**Branch**: `feat/v0.5-research-mode`
**Total commits**: 18(13 个 feat/refactor/test + 4 个 chore format/fix + 1 个 spike result)
**Total LOC delta**: 43 文件 / +3,704 / -21 = **净 +3,683 行**(主要新增 5 agents + 5 sub-agents + critic_subgraph + research_graph + tests + cassette)
**Total time**: ~3 小时主对话(subagent-driven cadence,15 个 subagent 派发,实施 + 单 review)
**Final ci**: 191 tests pass(143 v0 baseline + 48 新),mypy strict 192 源文件干净,ruff 干净,L2 cassette 84KB / replay 0.33s / nightly drift 0(¥0.0014)

### 对的设计(3 条)

1. **混合拓扑(GT3)是正确选择** — Top-level 5 节点扁平 + Critic 用 LangGraph subgraph + Send API。**收益**:故事点强(Send API + parallel scoring)+ 简单 agent(Planner/Collector/Analyst/Writer)不被 subgraph 过度抽象;**实测**:4 个简单 agent 各 1 节点清晰可读,Critic subgraph 5-fan-out 在 trace 中清楚分明。**反例验证**:如果走 GT2 全 subgraph,会有 4 个 1-node-subgraph 显得冗余,trace 也变深。

2. **per-Critic 构造注入(DS1)而不是全局 registry** — `Critic.__init__(llm, scorers=[FactualityScorer(...), ...])`。**收益**:测试 mock 直接传 stub scorers,没有全局 mutable 状态。**实测**:Task 8 的 unit tests 5 行能验证 `dispatch_subagent` 真启用 + 错误路径 + aggregate 聚合,没出过任何 fixture 互相污染问题。**对比 v0 ChatPlanner / Responder DI 风格**:延续了一致的 constructor 注入模式,招聘官眼里 = "DI 习惯一致 + 没引入新模式"。

3. **Task 0 spike 真值 5 分钟,plan 完全 intact** — 2 个 spike(Send API fan-out + reduce / subgraph state passing)各 60 秒跑完,**结果都是 A**,plan 未调整。**对比 v0**:v0 spike 发现 LangGraph 1.x vs 0.2.x 大版本差异,plan 中等程度调整;v0.5 spike 因为 v0 已经趟过 LangGraph 1.x 的雷,这次没意外。**收益**:plan 写得越具体 + spike 越早,后续 task 越机械,subagent 越不需要 judgement。

### 错的设计 / plan 漏了什么(3 条)

1. **`Agent.step(state: GraphState)` ABC 与 `ResearchState` 不兼容** — v0.5 5 个 agent 全用 `# type: ignore[override]` 跳过 mypy Liskov 检查。这是 v0 spec 设计 ABC 时**没预见到 v0.5 会引入新 state 类型**。后续:v1 把 Agent ABC 改成 generic(`Agent[StateT]`),消除所有 `type:ignore[override]`。**Plan 漏了什么**:plan 阶段应该 explicit 标注"Agent ABC 用 type:ignore[override] 接受 ResearchState",或者把 generic 化加进 v0.5 task。我选了"加 type:ignore"路径,scope 不爆但留了 5 处 ignore。

2. **LangGraph 1.x Send API 行为细节没 plan 完全到位** — Task 9 实施时 implementer 发现 2 个 quirks:(a) Send 传给 sub-node 的是 raw dict 不是 Pydantic instance;(b) subgraph state schema 必须包含 output 字段(`critic_report`),否则 LangGraph 自动 discard。**Plan 漏了什么**:Task 0 Spike 1 只验证了 Send + reducer 基本行为,**没验证 subgraph 的 state passing 细节**(Spike 2 只验证 simple subgraph,没验证 subgraph + Send 组合)。下次 spec/plan:Spike 必须 cover **生产形态**(Send + Pydantic state + reducer + subgraph)的最小可工作样例,不是孤立 API 验证。

3. **ruff format 0.6 vs 0.15 撞车 4 次了** — Task 10 / 12 / 14 的 chore(format) 跟进 commit,plan 已写明"避免 multi-line assert pattern"但 implementer 仍写了 3 次。这是 plan 的 **negative constraint 强度不够**:把"必须用 single-line assert + n=...; assert n>=1, msg"作为 commit-msg gate / pre-commit hook 的硬约束更彻底。**深层问题**:`.pre-commit-config.yaml` ruff pin v0.6.9 至今没更新到 v0.15.x,根因是项目 ci infra debt。下个 spec(v0.6)应该有一个 chore task 把 pre-commit ruff 升级到 0.15.x,一劳永逸。

### 下个 plan 要避免(3 条)

1. **新 state 类型先 generic Agent ABC,再写 agent** — 如果 v0.6 / v1+ 又引入新 state(比如 `MemoryQueryState`),先一个 chore task 把 Agent ABC 改 generic,再写新 agent。避免 `type:ignore[override]` 继续累积。

2. **LangGraph 高级特性 spike 必须验证生产形态** — 不能只验证孤立 API。例如:Send + Pydantic + reducer + subgraph 组合的最小 working sample。Plan B/C/D 沉淀的 `feedback_third_party_plugin_defaults` 已经强调过,v0.5 这次某种程度上还是踩了相邻坑。

3. **格式 / 工具版本对齐 进 spec 第 0 节** — `feedback_dev_tool_version_pin_alignment` memory 写过,但 v0.5 spec 没在第 0 节"开发环境"段落 explicit 标注 ruff 0.6 vs 0.15 状态 + 一刀切的"single-line assert" 强约束。下个 spec 第 0 节应有"开发约束 checklist",哪怕 5 条也要写。

### 沉淀到 memory

需要新加的 memories:
- **`feedback_agent_abc_state_generic`**(feedback)— 引入新 state 类型时,先 generic 化 Agent ABC,避免 `type:ignore[override]` 累积(v0.5 5 处教训)。
- **`feedback_langgraph_send_subgraph_quirks`**(feedback)— LangGraph 1.x Send API 传 raw dict 给 sub-node + subgraph state schema 必须含 output 字段,plan 阶段 spike 必须组合验证。
- 更新 **`project_v0_architecture_landed`** → **`project_v0.5_architecture_landed`** 或新建 v0.5 entry:研报模式 5-agent + Critic subgraph(Send API)+ ResearchAgent SUT 已落地;`POST /api/v0.5/research` SSE 7 event;cassette 84KB。
- 更新 **`project_eval_pipeline_contract`**:加 `report_markdown_quality` 维度 + `GoldenCase.expected_topics` 字段。

### Subagent-driven-development 节奏复盘

- **15 个 task 全部 sonnet 单 implementer 派发**(无 spec reviewer 单独派发),主对话独立 verify(`git log` + `poe ci`)。**没有任务被 spec reviewer catch 出 BLOCKING**,也就是说本次 plan 写得够具体,implementer 自我执行率高。**对比 v0**:v0 Task 1 + Task 5 reviewer 真 catch 到问题(unguarded import + _llm_adapter 位置),v0.5 没有这种漏。**原因**:v0.5 plan 是 v0 retrospective 修过的,具体度上一档。
- **3 个 chore(format) follow-up commits 是浪费** — 占了主对话 ~10 分钟。预防:plan 第 0 节加硬约束"single-line assert / `n = ...; assert n >= 1`"。下个 plan 必加。
- **Send API 行为 quirks(Task 9)是 v0.5 唯一一次 implementer judgement call** — implementer 自己发现 raw dict + state schema 字段缺失问题,inline 修了。**这是 subagent 的价值**:fresh context 没被前面 task 的 mental model 污染,LangGraph 行为不符合预期时直接 trace 看出来。

### v0.6 启动条件

v0.5 完成后,v0.6 真接入 web_search/kb_search + frontend chart 渲染 spec 可以起草。依赖就位:
- ✅ ResearchAgent + 5-agent pipeline 已用过
- ✅ Critic + DispatchSubAgent + Send API subgraph 模式已建立(Memory v1 spec 时如果有类似 fan-out 场景可复用)
- ✅ Mock web_search / kb_search 工具骨架在,真 API 接入是 1 行 import 改造(同 v0 mock_tushare → real Tushare 模式)
- ✅ ChartSpec 元信息已生成,frontend 接 chart_id → chart_data 是 v0.6 frontend spec 工作
- ✅ EvalRunner SUT swap 模式已 2 次验证(ChatAgent + ResearchAgent),Protocol 设计稳固
- ✅ SSE 流式 7 event 经验已积累(v0.6 frontend 实施时直接接入)
