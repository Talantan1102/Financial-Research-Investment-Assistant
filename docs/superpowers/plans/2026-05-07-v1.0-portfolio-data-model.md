# v1.0 持仓监控 — 数据模型 + Onboarding Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地 v1.0 持仓监控的**数据模型层 + Onboarding API**(product spec § 9.2 第 1 条 plan):Trade 表 + Position 表(materialized) + 三态机校验 + Trade write 同事务 recompute Position + REST API(POST/DELETE/PATCH /trades + GET /positions + POST /onboarding)。

**Architecture:** Trade 是 SoT(用户录入,immutable except initial),Position 是 materialized(单事务内 UPSERT)。recompute 是纯函数(`recompute_position_from_trades`):输入该 (user_id, ts_code) 的 Trade 全集,fold 出 Position 状态;TradeService 任何 write 路径同事务调用。三态机 guard 在 Service 层(决策 2),不引入 DB trigger。Position 表预留 `last_quote_price`/`last_quote_at`/`is_silenced` 字段 — quote 写入由后续监控引擎 plan 调用 `PositionService.update_quote()`。

**Tech Stack:** SQLAlchemy 2.0 ORM + `Base.metadata.create_all()` (沿用 v0.9.x 标准模式,不依赖 PR-A alembic);Pydantic v2 schemas;FastAPI router(prefix `/portfolio`);pytest L0/L1 with sqlite override + L2 e2e with `pg_test_container` fixture。

**Spec:** `docs/superpowers/specs/2026-05-07-v1.0-portfolio-data-model-engineering-design.md` — § 1 决策 1/2/3,§ 2 数据模型,§ 3 流程,§ 5 测试场景。
**Product anchor:** `docs/superpowers/specs/2026-05-07-v1.0-portfolio-monitoring-design.md` § 4(数据模型)+ § 4.2(Onboarding 雪球做法)+ § 4.3(三态机)。

---

## File Structure

**Created:**
- `backend/app/models/trade.py` — Trade SQLAlchemy model + `TradeType` enum
- `backend/app/models/position.py` — Position SQLAlchemy model
- `backend/app/services/portfolio_exceptions.py` — `ImmutableTradeError`, `ExpiredDeletionError`
- `backend/app/services/portfolio_recompute.py` — pure function `recompute_position_from_trades(trades) -> PositionState`
- `backend/app/services/trade_service.py` — TradeService(create / delete / update),内部调用 recompute + UPSERT Position
- `backend/app/services/position_service.py` — PositionService(list_for_user / update_quote)
- `backend/app/schemas/portfolio.py` — Pydantic schemas(TradeCreate, TradeRead, PositionRead, OnboardingRequest, OnboardingResponse, error responses)
- `backend/app/router/portfolio_router.py` — FastAPI router(prefix `/portfolio`)
- `backend/tests/unit/services/test_portfolio_recompute.py` — fold 算法 TDD
- `backend/tests/unit/services/test_trade_service.py` — TradeService CRUD + 三态机 guard
- `backend/tests/unit/services/test_position_service.py` — PositionService read + update_quote
- `backend/tests/integration/services/test_trade_position_consistency.py` — Trade ↔ Position 强一致性(sqlite-override + repo)
- `backend/tests/integration/services/test_three_state_machine.py` — 三态机 e2e 路径(sqlite-override)
- `backend/tests/e2e/test_portfolio_onboarding_e2e.py` — Onboarding 完整链路(PG container)

**Modified:**
- `backend/app/models/__init__.py` — barrel import 加 `Trade`, `TradeType`, `Position`
- `backend/app/app_main.py` — `include_router(portfolio_router)` + 顺序与 reports_router 一致
- `README.md`(根目录)— "持久化" 段落补 `trades` / `positions` 表名

---

## Task 0: Setup feature branch from main

**Files:**
- N/A(git operation)

- [ ] **Step 1: Switch to main and confirm clean tree**

```bash
git checkout main
git status
```

Expected:`On branch main, nothing to commit, working tree clean`。
如果有未提交改动:**STOP** —— 改动属于其他 in-progress 工作,不能动;改用 worktree 隔离(`git worktree add ../wt-feat-portfolio -b feat/v1.0-portfolio-data-model main`)。

- [ ] **Step 2: Pull latest main**

```bash
git pull --ff-only
git log -1 --format='%h %s'
```

Expected:HEAD = `0951cf0` 或更新(main 上 v1.0 portfolio engineering spec 已合入,见 PR #27 / #28)。

- [ ] **Step 3: Create feature branch**

```bash
git checkout -b feat/v1.0-portfolio-data-model
```

Expected:`Switched to a new branch 'feat/v1.0-portfolio-data-model'`。

- [ ] **Step 4: Confirm spec + plan files visible**

```bash
ls docs/superpowers/specs/2026-05-07-v1.0-portfolio-data-model-engineering-design.md
ls docs/superpowers/plans/2026-05-07-v1.0-portfolio-data-model.md
```

Expected:两份文件都存在。

---

## Task 1: Add `Trade` SQLAlchemy model + `TradeType` enum

**Files:**
- Create:`backend/app/models/trade.py`
- Modify:`backend/app/models/__init__.py`(barrel import)
- Test:`backend/tests/unit/models/test_trade_model.py`

**Spec ref:** § 2.1 Trade 表。

- [ ] **Step 1: Write failing test for Trade model basic fields**

Create `backend/tests/unit/models/test_trade_model.py`:

```python
"""Trade SQLAlchemy model basic field tests (sqlite-override)."""

from __future__ import annotations

from datetime import date, datetime
from decimal import Decimal
from uuid import uuid4

import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import Session

from app.core.database import Base
from app.models.trade import Trade, TradeType
from app.models.user import User


@pytest.fixture
def session() -> Session:
    engine = create_engine("sqlite:///:memory:")
    Base.metadata.create_all(engine)
    with Session(engine) as s:
        yield s


def _make_user(session: Session) -> User:
    user = User(
        id=str(uuid4()),
        email=f"u-{uuid4().hex[:8]}@test",
        password_hash="x",
        is_active=True,
    )
    session.add(user)
    session.flush()
    return user


def test_trade_can_be_persisted(session: Session) -> None:
    user = _make_user(session)
    trade = Trade(
        id=str(uuid4()),
        user_id=user.id,
        ts_code="600519.SH",
        name="贵州茅台",
        type=TradeType.INITIAL,
        quantity=200,
        price=Decimal("1450.00"),
        trade_date=date(2024, 6, 1),
        note="initial position",
    )
    session.add(trade)
    session.commit()

    fetched = session.query(Trade).filter_by(ts_code="600519.SH").one()
    assert fetched.type == TradeType.INITIAL
    assert fetched.quantity == 200
    assert fetched.price == Decimal("1450.00")
    assert fetched.note == "initial position"


def test_trade_type_enum_accepts_all_three_values(session: Session) -> None:
    user = _make_user(session)
    for ttype in [TradeType.INITIAL, TradeType.BUY, TradeType.SELL]:
        session.add(
            Trade(
                id=str(uuid4()),
                user_id=user.id,
                ts_code="600519.SH",
                name="贵州茅台",
                type=ttype,
                quantity=10,
                price=Decimal("1500.00"),
                trade_date=date.today(),
            )
        )
    session.commit()
    assert session.query(Trade).count() == 3
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd backend && uv run pytest tests/unit/models/test_trade_model.py -v
```

Expected:FAIL — `ModuleNotFoundError: No module named 'app.models.trade'`。

- [ ] **Step 3: Implement `Trade` model + `TradeType` enum**

Create `backend/app/models/trade.py`:

```python
"""Trade SQLAlchemy model — v1.0 持仓监控 SoT(交易记录,immutable except type=initial).

设计 ref: docs/superpowers/specs/2026-05-07-v1.0-portfolio-data-model-engineering-design.md § 2.1

类型选型:
- 生产 = PostgreSQL,使用 UUID native + Numeric
- 单元测试 = sqlite in-memory,通过 with_variant 降级 String(36)
"""

from __future__ import annotations

import enum
from datetime import date, datetime
from decimal import Decimal

from sqlalchemy import (
    Column,
    Date,
    DateTime,
    Enum,
    ForeignKey,
    Index,
    Integer,
    Numeric,
    String,
    Text,
)
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import relationship

from app.core.database import Base


class TradeType(enum.Enum):
    """三态:initial(虚拟初始仓位,始终可改)/ buy / sell。"""

    INITIAL = "initial"
    BUY = "buy"
    SELL = "sell"


class Trade(Base):
    """用户录入的单笔交易记录(SoT)。Position 表从 Trade 全集 fold 出。"""

    __tablename__ = "trades"

    id = Column(String(36), primary_key=True)

    user_id = Column(
        UUID(as_uuid=True).with_variant(String(36), "sqlite"),
        ForeignKey("users.id", ondelete="CASCADE"),
        nullable=False,
        index=True,
    )

    ts_code = Column(String(10), nullable=False, index=True)
    name = Column(String(50), nullable=False)
    type = Column(Enum(TradeType), nullable=False)
    quantity = Column(Integer, nullable=False)
    price = Column(Numeric(12, 4), nullable=False)
    trade_date = Column(Date, nullable=False, default=date.today)
    note = Column(Text, nullable=True)
    created_at = Column(DateTime, nullable=False, default=datetime.utcnow, index=True)

    user = relationship("User", backref="trades")

    __table_args__ = (
        Index("ix_trades_user_tscode_date", "user_id", "ts_code", "trade_date"),
    )
```

- [ ] **Step 4: Wire model into barrel**

Modify `backend/app/models/__init__.py`:

```python
from .trade import Trade, TradeType  # add this line above User import alphabetically
```

And add `"Trade"` and `"TradeType"` to `__all__` list.

- [ ] **Step 5: Run test to verify it passes**

```bash
cd backend && uv run pytest tests/unit/models/test_trade_model.py -v
```

Expected:both tests PASS。

- [ ] **Step 6: Run mypy on backend to ensure no type regression**

```bash
cd backend && uv run mypy app/models/trade.py app/models/__init__.py
```

Expected:`Success: no issues found`。

- [ ] **Step 7: Commit**

```bash
git add backend/app/models/trade.py backend/app/models/__init__.py backend/tests/unit/models/test_trade_model.py
git commit -m "feat(v1.0): Trade SQLAlchemy model + TradeType enum

Trade is SoT (source of truth) for portfolio monitoring data layer.
Three states (initial/buy/sell) per product spec § 4.1.

Sister: docs/superpowers/specs/2026-05-07-v1.0-portfolio-data-model-engineering-design.md § 2.1

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: Add `Position` SQLAlchemy model

**Files:**
- Create:`backend/app/models/position.py`
- Modify:`backend/app/models/__init__.py`(barrel)
- Test:`backend/tests/unit/models/test_position_model.py`

**Spec ref:** § 2.2 Position 表。

- [ ] **Step 1: Write failing test for Position model**

Create `backend/tests/unit/models/test_position_model.py`:

```python
"""Position SQLAlchemy model basic field tests (sqlite-override)."""

from __future__ import annotations

from datetime import datetime
from decimal import Decimal
from uuid import uuid4

import pytest
from sqlalchemy import create_engine
from sqlalchemy.exc import IntegrityError
from sqlalchemy.orm import Session

from app.core.database import Base
from app.models.position import Position
from app.models.user import User


@pytest.fixture
def session() -> Session:
    engine = create_engine("sqlite:///:memory:")
    Base.metadata.create_all(engine)
    with Session(engine) as s:
        yield s


def _make_user(session: Session) -> User:
    user = User(
        id=str(uuid4()),
        email=f"u-{uuid4().hex[:8]}@test",
        password_hash="x",
        is_active=True,
    )
    session.add(user)
    session.flush()
    return user


def test_position_can_be_persisted(session: Session) -> None:
    user = _make_user(session)
    pos = Position(
        id=str(uuid4()),
        user_id=user.id,
        ts_code="600519.SH",
        name="贵州茅台",
        quantity=220,
        avg_cost=Decimal("1460.00"),
        total_cost=Decimal("321200.00"),
        realized_pnl=Decimal("4200.00"),
        last_quote_price=None,
        last_quote_at=None,
        is_silenced=False,
    )
    session.add(pos)
    session.commit()

    fetched = session.query(Position).filter_by(ts_code="600519.SH").one()
    assert fetched.quantity == 220
    assert fetched.avg_cost == Decimal("1460.00")
    assert fetched.is_silenced is False


def test_position_unique_user_tscode(session: Session) -> None:
    user = _make_user(session)
    common = dict(
        user_id=user.id,
        ts_code="600519.SH",
        name="贵州茅台",
        quantity=100,
        avg_cost=Decimal("1450.00"),
        total_cost=Decimal("145000.00"),
        realized_pnl=Decimal("0.00"),
    )
    session.add(Position(id=str(uuid4()), **common))
    session.commit()
    session.add(Position(id=str(uuid4()), **common))
    with pytest.raises(IntegrityError):
        session.commit()
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd backend && uv run pytest tests/unit/models/test_position_model.py -v
```

Expected:FAIL — `ModuleNotFoundError`。

- [ ] **Step 3: Implement `Position` model**

Create `backend/app/models/position.py`:

```python
"""Position SQLAlchemy model — v1.0 持仓监控 materialized 状态(决策 1).

设计 ref: docs/superpowers/specs/2026-05-07-v1.0-portfolio-data-model-engineering-design.md § 2.2

不变量:Trade 写完后 Position 跟 Trade 严格一致(单事务保证)。
清仓再回来:quantity 重算到 0 时保留行(realized_pnl 累计有价值),
用户再买入是同一行 UPDATE 而不是新建。
"""

from __future__ import annotations

from datetime import datetime
from decimal import Decimal

from sqlalchemy import (
    Boolean,
    Column,
    DateTime,
    ForeignKey,
    Integer,
    Numeric,
    String,
    UniqueConstraint,
)
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import relationship

from app.core.database import Base


class Position(Base):
    """用户在某只股上的当前状态快照(quantity / avg_cost / realized_pnl / 现价 cache)。"""

    __tablename__ = "positions"

    id = Column(String(36), primary_key=True)

    user_id = Column(
        UUID(as_uuid=True).with_variant(String(36), "sqlite"),
        ForeignKey("users.id", ondelete="CASCADE"),
        nullable=False,
        index=True,
    )

    ts_code = Column(String(10), nullable=False, index=True)
    name = Column(String(50), nullable=False)
    quantity = Column(Integer, nullable=False, default=0)
    avg_cost = Column(Numeric(12, 4), nullable=False, default=Decimal("0"))
    total_cost = Column(Numeric(14, 2), nullable=False, default=Decimal("0"))
    realized_pnl = Column(Numeric(14, 2), nullable=False, default=Decimal("0"))

    # 决策 3:监控引擎 30min 周期写入,dashboard 读取算浮盈
    last_quote_price = Column(Numeric(12, 4), nullable=True)
    last_quote_at = Column(DateTime, nullable=True)

    # v1.x 静默仓位口子(spec § 8.2)
    is_silenced = Column(Boolean, nullable=False, default=False)

    updated_at = Column(
        DateTime, nullable=False, default=datetime.utcnow, onupdate=datetime.utcnow
    )

    user = relationship("User", backref="positions")

    __table_args__ = (
        UniqueConstraint("user_id", "ts_code", name="uq_positions_user_tscode"),
    )
```

- [ ] **Step 4: Wire model into barrel**

Modify `backend/app/models/__init__.py`:加 `from .position import Position` + `__all__` 加 `"Position"`。

- [ ] **Step 5: Run test to verify it passes**

```bash
cd backend && uv run pytest tests/unit/models/test_position_model.py -v
```

Expected:both tests PASS。

- [ ] **Step 6: Run mypy**

```bash
cd backend && uv run mypy app/models/position.py app/models/__init__.py
```

Expected:`Success`。

- [ ] **Step 7: Commit**

```bash
git add backend/app/models/position.py backend/app/models/__init__.py backend/tests/unit/models/test_position_model.py
git commit -m "feat(v1.0): Position SQLAlchemy model (materialized 状态)

Position is materialized aggregate over Trade(决策 1).
Includes last_quote_price/last_quote_at(决策 3 quote cache)
and is_silenced (v1.x 口子)。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: Add portfolio exceptions

**Files:**
- Create:`backend/app/services/portfolio_exceptions.py`
- Test:`backend/tests/unit/services/test_portfolio_exceptions.py`

**Spec ref:** § 3.3 三态机异常 + 决策 2。

- [ ] **Step 1: Write failing test**

Create `backend/tests/unit/services/test_portfolio_exceptions.py`:

```python
"""Portfolio exceptions raise + message correctness tests."""

from __future__ import annotations

import pytest

from app.services.portfolio_exceptions import (
    ExpiredDeletionError,
    ImmutableTradeError,
    PortfolioError,
)


def test_immutable_trade_error_inherits_portfolio_error() -> None:
    err = ImmutableTradeError("常规交易不可改字段,过 24h 也不可")
    assert isinstance(err, PortfolioError)
    assert "不可改" in str(err)


def test_expired_deletion_error_inherits_portfolio_error() -> None:
    err = ExpiredDeletionError("超 24h 不可删,请录反向交易抵消")
    assert isinstance(err, PortfolioError)
    assert "24h" in str(err)


def test_can_catch_both_via_base_class() -> None:
    with pytest.raises(PortfolioError):
        raise ImmutableTradeError("x")
    with pytest.raises(PortfolioError):
        raise ExpiredDeletionError("y")
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd backend && uv run pytest tests/unit/services/test_portfolio_exceptions.py -v
```

Expected:FAIL — `ModuleNotFoundError`。

- [ ] **Step 3: Implement exceptions**

Create `backend/app/services/portfolio_exceptions.py`:

```python
"""Portfolio service domain exceptions(三态机 guard)。

Spec ref: docs/superpowers/specs/2026-05-07-v1.0-portfolio-data-model-engineering-design.md
§ 1 决策 2 + § 3.3。

API 层捕获 PortfolioError 子类返回 4xx + 用户友好消息。
"""

from __future__ import annotations


class PortfolioError(Exception):
    """Portfolio domain exception base — all portfolio service errors inherit."""


class ImmutableTradeError(PortfolioError):
    """Attempted to update fields on a non-initial Trade(常规 trade 任何时候不可改字段)。"""


class ExpiredDeletionError(PortfolioError):
    """Attempted to delete a Trade older than 24h(超 24h 不可删,需录反向交易抵消)。"""
```

- [ ] **Step 4: Run test to verify it passes**

```bash
cd backend && uv run pytest tests/unit/services/test_portfolio_exceptions.py -v
```

Expected:3 PASS。

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/portfolio_exceptions.py backend/tests/unit/services/test_portfolio_exceptions.py
git commit -m "feat(v1.0): portfolio_exceptions 模块(ImmutableTradeError + ExpiredDeletionError)

三态机 guard 异常类型,API 层捕获返回 4xx。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: Add `recompute_position_from_trades` pure function

**Files:**
- Create:`backend/app/services/portfolio_recompute.py`
- Test:`backend/tests/unit/services/test_portfolio_recompute.py`

**Spec ref:** § 3.1 fold 算法 + § 5 测试场景 1/2/6。

这是整套数据模型的核心算法,纯函数,无 DB 依赖,TDD 必须覆盖 spec § 5 全部 fold 场景。

- [ ] **Step 1: Write failing test — 基本聚合场景(spec § 5 测试场景 1)**

Create `backend/tests/unit/services/test_portfolio_recompute.py`:

```python
"""recompute_position_from_trades fold 算法测试 (纯函数,无 DB)。

Spec ref: docs/superpowers/specs/2026-05-07-v1.0-portfolio-data-model-engineering-design.md
§ 3.1 fold 算法 + § 5 测试场景。
"""

from __future__ import annotations

from dataclasses import dataclass
from datetime import date
from decimal import Decimal

import pytest

from app.models.trade import TradeType
from app.services.portfolio_recompute import (
    PositionState,
    TradeInput,
    recompute_position_from_trades,
)


def _t(ttype: TradeType, qty: int, price: str, ymd: tuple[int, int, int]) -> TradeInput:
    return TradeInput(
        type=ttype,
        quantity=qty,
        price=Decimal(price),
        trade_date=date(*ymd),
    )


# ---- Spec § 5 测试场景 1:基本聚合 ------------------------------------------


def test_basic_aggregate_initial_buy_sell() -> None:
    """initial 200@1450 → buy 50@1500 → sell 30@1600。

    fold 推算(spec § 5 场景 1):
      after initial: qty=200, total=290000, avg=1450
      after buy:     qty=250, total=365000, avg=1460
      after sell:    realized += 30*(1600-1460)=4200, total -= 30*1460=43800
      → qty=220, total=321200, avg=1460(321200/220=1460), realized=4200
    """
    trades = [
        _t(TradeType.INITIAL, 200, "1450.00", (2024, 6, 1)),
        _t(TradeType.BUY, 50, "1500.00", (2026, 1, 15)),
        _t(TradeType.SELL, 30, "1600.00", (2026, 4, 20)),
    ]
    state = recompute_position_from_trades(trades)
    assert state.quantity == 220
    assert state.avg_cost == Decimal("1460.0000")
    assert state.total_cost == Decimal("321200.00")
    assert state.realized_pnl == Decimal("4200.00")
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd backend && uv run pytest tests/unit/services/test_portfolio_recompute.py::test_basic_aggregate_initial_buy_sell -v
```

Expected:FAIL — `ModuleNotFoundError: app.services.portfolio_recompute`。

- [ ] **Step 3: Implement minimal `recompute_position_from_trades`**

Create `backend/app/services/portfolio_recompute.py`:

```python
"""Pure fold algorithm: trades → PositionState(决策 1 / spec § 3.1)。

无 DB / 无 ORM 依赖,可独立 unit test 全部 fold 场景。
"""

from __future__ import annotations

from dataclasses import dataclass
from datetime import date
from decimal import Decimal
from typing import Iterable

from app.models.trade import TradeType


@dataclass(frozen=True)
class TradeInput:
    """fold 算法输入,刻意不直接吃 ORM Trade 以保持纯函数。"""

    type: TradeType
    quantity: int
    price: Decimal
    trade_date: date


@dataclass(frozen=True)
class PositionState:
    """fold 输出 — 跟 Position 表字段对齐(quote 字段不在 fold 范围内)。"""

    quantity: int
    avg_cost: Decimal
    total_cost: Decimal
    realized_pnl: Decimal


def recompute_position_from_trades(trades: Iterable[TradeInput]) -> PositionState:
    """对 (user_id, ts_code) 的 Trade 全集按 trade_date asc 跑 fold。

    Spec § 3.1 算法:
      initial / buy → total_cost += q*p; quantity += q; avg_cost = total/qty (if qty>0)
      sell          → realized_pnl += q*(p - avg_cost)
                     total_cost -= q*avg_cost
                     quantity -= q
                     avg_cost stays (sell 不改 avg_cost,清仓后保留最后非零值)

    返回的 avg_cost / total_cost / realized_pnl 都是 Decimal 精度。
    """
    sorted_trades = sorted(trades, key=lambda t: t.trade_date)
    quantity = 0
    total_cost = Decimal("0")
    avg_cost = Decimal("0")
    realized_pnl = Decimal("0")

    for tr in sorted_trades:
        if tr.type in (TradeType.INITIAL, TradeType.BUY):
            total_cost += Decimal(tr.quantity) * tr.price
            quantity += tr.quantity
            if quantity > 0:
                avg_cost = total_cost / Decimal(quantity)
        elif tr.type == TradeType.SELL:
            realized_pnl += Decimal(tr.quantity) * (tr.price - avg_cost)
            total_cost -= Decimal(tr.quantity) * avg_cost
            quantity -= tr.quantity
            # avg_cost 保留(spec § 2.2 — quantity=0 保留最后非零值)

    # 量化精度:avg_cost 4 位小数,其他 2 位
    return PositionState(
        quantity=quantity,
        avg_cost=avg_cost.quantize(Decimal("0.0001")),
        total_cost=total_cost.quantize(Decimal("0.01")),
        realized_pnl=realized_pnl.quantize(Decimal("0.01")),
    )
```

- [ ] **Step 4: Run test to verify it passes**

```bash
cd backend && uv run pytest tests/unit/services/test_portfolio_recompute.py::test_basic_aggregate_initial_buy_sell -v
```

Expected:PASS。

- [ ] **Step 5: Add scenario 2 — 删 Trade 自动 reverse(fold 序列变短)**

Append to `test_portfolio_recompute.py`:

```python
def test_delete_buy_reverses_position() -> None:
    """删上面的 buy 后,fold 序列变 initial 200@1450 → sell 30@1600。

    fold 推算(spec § 5 场景 2):
      after initial: qty=200, total=290000, avg=1450
      after sell:    realized += 30*(1600-1450)=4500, total -= 30*1450=43500
      → qty=170, total=246500, avg=1450, realized=4500
    """
    trades = [
        _t(TradeType.INITIAL, 200, "1450.00", (2024, 6, 1)),
        _t(TradeType.SELL, 30, "1600.00", (2026, 4, 20)),
    ]
    state = recompute_position_from_trades(trades)
    assert state.quantity == 170
    assert state.avg_cost == Decimal("1450.0000")
    assert state.total_cost == Decimal("246500.00")
    assert state.realized_pnl == Decimal("4500.00")
```

Run:
```bash
cd backend && uv run pytest tests/unit/services/test_portfolio_recompute.py::test_delete_buy_reverses_position -v
```

Expected:PASS(纯函数已实现,无需改 impl)。

- [ ] **Step 6: Add scenario 6 — 清仓再回来(qty=0 后再 buy)**

```python
def test_clear_then_rebuy_keeps_realized_pnl() -> None:
    """initial 100@1500 → sell 100@1600 → buy 50@1400。

    fold:
      after initial: qty=100, total=150000, avg=1500
      after sell 100: realized += 100*(1600-1500)=10000, total -= 100*1500=150000
                     → qty=0, total=0, avg=1500(保留最后非零值), realized=10000
      after buy 50: qty=50, total=50*1400=70000, avg=70000/50=1400
                    realized 不动(只 buy 不动 realized)
    """
    trades = [
        _t(TradeType.INITIAL, 100, "1500.00", (2024, 6, 1)),
        _t(TradeType.SELL, 100, "1600.00", (2026, 1, 15)),
        _t(TradeType.BUY, 50, "1400.00", (2026, 4, 1)),
    ]
    state = recompute_position_from_trades(trades)
    assert state.quantity == 50
    assert state.avg_cost == Decimal("1400.0000")
    assert state.total_cost == Decimal("70000.00")
    assert state.realized_pnl == Decimal("10000.00")
```

Run:
```bash
cd backend && uv run pytest tests/unit/services/test_portfolio_recompute.py::test_clear_then_rebuy_keeps_realized_pnl -v
```

Expected:PASS。

- [ ] **Step 7: Add edge case — 空 trades 输入**

```python
def test_empty_trades_returns_zeros() -> None:
    state = recompute_position_from_trades([])
    assert state.quantity == 0
    assert state.avg_cost == Decimal("0.0000")
    assert state.total_cost == Decimal("0.00")
    assert state.realized_pnl == Decimal("0.00")
```

Run:
```bash
cd backend && uv run pytest tests/unit/services/test_portfolio_recompute.py::test_empty_trades_returns_zeros -v
```

Expected:PASS。

- [ ] **Step 8: Add edge case — 乱序输入按 trade_date 排序后再 fold**

```python
def test_unsorted_trades_get_sorted_before_fold() -> None:
    """乱序输入应按 trade_date asc 排序后 fold,结果跟 Step 1 一致。"""
    trades = [
        _t(TradeType.SELL, 30, "1600.00", (2026, 4, 20)),  # 最后一笔放最前
        _t(TradeType.INITIAL, 200, "1450.00", (2024, 6, 1)),
        _t(TradeType.BUY, 50, "1500.00", (2026, 1, 15)),
    ]
    state = recompute_position_from_trades(trades)
    assert state.quantity == 220
    assert state.avg_cost == Decimal("1460.0000")
    assert state.total_cost == Decimal("321200.00")
    assert state.realized_pnl == Decimal("4200.00")
```

Run:
```bash
cd backend && uv run pytest tests/unit/services/test_portfolio_recompute.py -v
```

Expected:5 PASS。

- [ ] **Step 9: Run mypy on the new module**

```bash
cd backend && uv run mypy app/services/portfolio_recompute.py
```

Expected:`Success`。

- [ ] **Step 10: Commit**

```bash
git add backend/app/services/portfolio_recompute.py backend/tests/unit/services/test_portfolio_recompute.py
git commit -m "feat(v1.0): portfolio_recompute pure fold algorithm

5 fold 场景 TDD 全覆盖(spec § 5 场景 1/2/6 + 空输入 + 乱序),
为后续 TradeService 同事务调用提供算法保证。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: Add `TradeService.create` — Trade write 同事务 UPSERT Position

**Files:**
- Create:`backend/app/services/trade_service.py`
- Test:`backend/tests/unit/services/test_trade_service.py`

**Spec ref:** § 3.1 流程 + 决策 1。

- [ ] **Step 1: Write failing test for create + auto Position UPSERT**

Create `backend/tests/unit/services/test_trade_service.py`:

```python
"""TradeService.create 测试 — 单事务 Trade insert + Position UPSERT。"""

from __future__ import annotations

from datetime import date
from decimal import Decimal
from uuid import uuid4

import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import Session

from app.core.database import Base
from app.models.position import Position
from app.models.trade import Trade, TradeType
from app.models.user import User
from app.services.trade_service import TradeService


@pytest.fixture
def session() -> Session:
    engine = create_engine("sqlite:///:memory:")
    Base.metadata.create_all(engine)
    with Session(engine) as s:
        yield s


@pytest.fixture
def user(session: Session) -> User:
    u = User(
        id=str(uuid4()),
        email=f"u-{uuid4().hex[:8]}@test",
        password_hash="x",
        is_active=True,
    )
    session.add(u)
    session.flush()
    return u


def test_create_initial_trade_creates_position_row(session: Session, user: User) -> None:
    svc = TradeService(session)
    trade = svc.create(
        user_id=user.id,
        ts_code="600519.SH",
        name="贵州茅台",
        ttype=TradeType.INITIAL,
        quantity=200,
        price=Decimal("1450.00"),
        trade_date=date(2024, 6, 1),
        note="initial",
    )
    session.commit()
    assert trade.id is not None

    pos = session.query(Position).filter_by(user_id=user.id, ts_code="600519.SH").one()
    assert pos.quantity == 200
    assert pos.avg_cost == Decimal("1450.0000")
    assert pos.total_cost == Decimal("290000.00")
    assert pos.realized_pnl == Decimal("0.00")
    assert pos.name == "贵州茅台"


def test_create_sequence_initial_buy_sell_matches_spec_scenario_1(
    session: Session, user: User
) -> None:
    """spec § 5 测试场景 1 的 e2e 重现 — TradeService 链式调用后 Position 数值。"""
    svc = TradeService(session)
    svc.create(
        user_id=user.id, ts_code="600519.SH", name="贵州茅台",
        ttype=TradeType.INITIAL, quantity=200, price=Decimal("1450.00"),
        trade_date=date(2024, 6, 1),
    )
    svc.create(
        user_id=user.id, ts_code="600519.SH", name="贵州茅台",
        ttype=TradeType.BUY, quantity=50, price=Decimal("1500.00"),
        trade_date=date(2026, 1, 15),
    )
    svc.create(
        user_id=user.id, ts_code="600519.SH", name="贵州茅台",
        ttype=TradeType.SELL, quantity=30, price=Decimal("1600.00"),
        trade_date=date(2026, 4, 20),
    )
    session.commit()

    pos = session.query(Position).filter_by(user_id=user.id, ts_code="600519.SH").one()
    assert pos.quantity == 220
    assert pos.avg_cost == Decimal("1460.0000")
    assert pos.total_cost == Decimal("321200.00")
    assert pos.realized_pnl == Decimal("4200.00")
    assert session.query(Trade).filter_by(user_id=user.id).count() == 3


def test_create_does_not_leak_to_other_user(session: Session, user: User) -> None:
    """Multi-account 隔离 — user_a 的 create 不影响 user_b 的 Position(spec § 5 场景 8)。"""
    svc = TradeService(session)
    user_b = User(
        id=str(uuid4()),
        email=f"u-{uuid4().hex[:8]}@test",
        password_hash="x",
        is_active=True,
    )
    session.add(user_b)
    session.flush()

    svc.create(
        user_id=user.id, ts_code="600519.SH", name="贵州茅台",
        ttype=TradeType.INITIAL, quantity=200, price=Decimal("1450.00"),
        trade_date=date(2024, 6, 1),
    )
    session.commit()

    assert session.query(Position).filter_by(user_id=user_b.id).count() == 0
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd backend && uv run pytest tests/unit/services/test_trade_service.py -v
```

Expected:FAIL — `ModuleNotFoundError: app.services.trade_service`。

- [ ] **Step 3: Implement minimal `TradeService.create`**

Create `backend/app/services/trade_service.py`:

```python
"""TradeService — Trade write 入口,单事务保证 Trade ↔ Position 一致(决策 1)。

Spec ref: § 3.1 流程 + § 3.3 三态机 guard(本文件 create + delete + update)。
"""

from __future__ import annotations

from datetime import date, datetime, timedelta
from decimal import Decimal
from uuid import uuid4

from sqlalchemy.orm import Session

from app.models.position import Position
from app.models.trade import Trade, TradeType
from app.services.portfolio_exceptions import (
    ExpiredDeletionError,
    ImmutableTradeError,
)
from app.services.portfolio_recompute import (
    TradeInput,
    recompute_position_from_trades,
)


class TradeService:
    """Trade CRUD + 同事务 Position recompute。

    所有 write 路径(create / delete / update)在调用方提交前都同事务 UPSERT Position。
    Caller 负责 session.commit(),本类只 add / flush / merge。
    """

    def __init__(self, session: Session) -> None:
        self._session = session

    def create(
        self,
        *,
        user_id: str,
        ts_code: str,
        name: str,
        ttype: TradeType,
        quantity: int,
        price: Decimal,
        trade_date: date,
        note: str | None = None,
    ) -> Trade:
        trade = Trade(
            id=str(uuid4()),
            user_id=user_id,
            ts_code=ts_code,
            name=name,
            type=ttype,
            quantity=quantity,
            price=price,
            trade_date=trade_date,
            note=note,
        )
        self._session.add(trade)
        self._session.flush()
        self._recompute_position(user_id=user_id, ts_code=ts_code, name=name)
        return trade

    def _recompute_position(self, *, user_id: str, ts_code: str, name: str) -> None:
        """fold 该 (user_id, ts_code) 全部 Trade → UPSERT Position 行。"""
        trades = (
            self._session.query(Trade)
            .filter_by(user_id=user_id, ts_code=ts_code)
            .all()
        )
        inputs = [
            TradeInput(
                type=t.type,
                quantity=t.quantity,
                price=t.price,
                trade_date=t.trade_date,
            )
            for t in trades
        ]
        state = recompute_position_from_trades(inputs)

        pos = (
            self._session.query(Position)
            .filter_by(user_id=user_id, ts_code=ts_code)
            .one_or_none()
        )
        if pos is None:
            pos = Position(
                id=str(uuid4()),
                user_id=user_id,
                ts_code=ts_code,
                name=name,
                quantity=state.quantity,
                avg_cost=state.avg_cost,
                total_cost=state.total_cost,
                realized_pnl=state.realized_pnl,
            )
            self._session.add(pos)
        else:
            pos.quantity = state.quantity
            pos.avg_cost = state.avg_cost
            pos.total_cost = state.total_cost
            pos.realized_pnl = state.realized_pnl
            pos.name = name  # 同步名称变更
        self._session.flush()
```

- [ ] **Step 4: Run test to verify it passes**

```bash
cd backend && uv run pytest tests/unit/services/test_trade_service.py -v
```

Expected:3 PASS。

- [ ] **Step 5: Run mypy**

```bash
cd backend && uv run mypy app/services/trade_service.py
```

Expected:`Success`。

- [ ] **Step 6: Commit**

```bash
git add backend/app/services/trade_service.py backend/tests/unit/services/test_trade_service.py
git commit -m "feat(v1.0): TradeService.create + 同事务 Position recompute

实现决策 1 核心 — Trade write → fold 全集 → UPSERT Position(单事务)。
Spec § 5 场景 1(基本聚合)+ 场景 8(multi-account 隔离)TDD 覆盖。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: Add `TradeService.delete` with 24h guard

**Files:**
- Modify:`backend/app/services/trade_service.py`(append `delete` method)
- Modify:`backend/tests/unit/services/test_trade_service.py`(append delete tests)

**Spec ref:** § 3.3 三态机 + § 5 测试场景 2 / 4。

- [ ] **Step 1: Write failing test for delete within 24h + auto reverse Position**

Append to `test_trade_service.py`:

```python
from datetime import datetime, timedelta

from app.services.portfolio_exceptions import ExpiredDeletionError


def test_delete_within_24h_reverses_position(session: Session, user: User) -> None:
    """spec § 5 场景 2 — 删 buy → Position 回退到 initial+sell 序列结果。"""
    svc = TradeService(session)
    svc.create(
        user_id=user.id, ts_code="600519.SH", name="贵州茅台",
        ttype=TradeType.INITIAL, quantity=200, price=Decimal("1450.00"),
        trade_date=date(2024, 6, 1),
    )
    buy = svc.create(
        user_id=user.id, ts_code="600519.SH", name="贵州茅台",
        ttype=TradeType.BUY, quantity=50, price=Decimal("1500.00"),
        trade_date=date(2026, 1, 15),
    )
    svc.create(
        user_id=user.id, ts_code="600519.SH", name="贵州茅台",
        ttype=TradeType.SELL, quantity=30, price=Decimal("1600.00"),
        trade_date=date(2026, 4, 20),
    )
    session.commit()

    svc.delete(buy.id)
    session.commit()

    pos = session.query(Position).filter_by(user_id=user.id, ts_code="600519.SH").one()
    assert pos.quantity == 170
    assert pos.avg_cost == Decimal("1450.0000")
    assert pos.total_cost == Decimal("246500.00")
    assert pos.realized_pnl == Decimal("4500.00")


def test_delete_after_24h_raises_expired(session: Session, user: User) -> None:
    svc = TradeService(session)
    trade = svc.create(
        user_id=user.id, ts_code="600519.SH", name="贵州茅台",
        ttype=TradeType.INITIAL, quantity=100, price=Decimal("1450.00"),
        trade_date=date(2024, 6, 1),
    )
    # 强制把 created_at 改到 25h 前(模拟过期)
    trade.created_at = datetime.utcnow() - timedelta(hours=25)
    session.flush()

    with pytest.raises(ExpiredDeletionError):
        svc.delete(trade.id)
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd backend && uv run pytest tests/unit/services/test_trade_service.py::test_delete_within_24h_reverses_position tests/unit/services/test_trade_service.py::test_delete_after_24h_raises_expired -v
```

Expected:FAIL — `AttributeError: 'TradeService' object has no attribute 'delete'`。

- [ ] **Step 3: Implement `TradeService.delete`**

Append to `app/services/trade_service.py`:

```python
    def delete(self, trade_id: str) -> None:
        trade = self._session.query(Trade).filter_by(id=trade_id).one()
        if datetime.utcnow() - trade.created_at > timedelta(hours=24):
            raise ExpiredDeletionError("超 24h 不可删,请录反向交易抵消")
        user_id = trade.user_id
        ts_code = trade.ts_code
        name = trade.name
        self._session.delete(trade)
        self._session.flush()
        self._recompute_position(user_id=user_id, ts_code=ts_code, name=name)
```

- [ ] **Step 4: Run test to verify it passes**

```bash
cd backend && uv run pytest tests/unit/services/test_trade_service.py -v
```

Expected:5 PASS(3 已有 + 2 新增)。

- [ ] **Step 5: Run mypy**

```bash
cd backend && uv run mypy app/services/trade_service.py
```

Expected:`Success`。

- [ ] **Step 6: Commit**

```bash
git add backend/app/services/trade_service.py backend/tests/unit/services/test_trade_service.py
git commit -m "feat(v1.0): TradeService.delete + 24h guard + 自动 reverse Position

spec § 5 场景 2(删 Trade 自动 reverse)+ 场景 4(过 24h 抛 ExpiredDeletionError)。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 7: Add `TradeService.update` with `initial`-only guard

**Files:**
- Modify:`backend/app/services/trade_service.py`(append `update` method)
- Modify:`backend/tests/unit/services/test_trade_service.py`(append update tests)

**Spec ref:** § 3.3 + § 5 测试场景 3 / 5。

- [ ] **Step 1: Write failing tests**

Append to `test_trade_service.py`:

```python
from app.services.portfolio_exceptions import ImmutableTradeError


def test_update_initial_trade_succeeds_anytime(session: Session, user: User) -> None:
    """spec § 5 场景 5 — initial trade 任何时候可改字段。"""
    svc = TradeService(session)
    trade = svc.create(
        user_id=user.id, ts_code="600519.SH", name="贵州茅台",
        ttype=TradeType.INITIAL, quantity=200, price=Decimal("1450.00"),
        trade_date=date(2024, 6, 1),
    )
    # 强制 48h 前(模拟旧 trade)
    trade.created_at = datetime.utcnow() - timedelta(hours=48)
    session.flush()

    updated = svc.update(trade.id, price=Decimal("1455.00"))
    session.commit()
    assert updated.price == Decimal("1455.00")

    # Position 应跟着 recompute(total_cost 变了)
    pos = session.query(Position).filter_by(user_id=user.id, ts_code="600519.SH").one()
    assert pos.total_cost == Decimal("291000.00")  # 200 * 1455
    assert pos.avg_cost == Decimal("1455.0000")


def test_update_buy_trade_raises_immutable(session: Session, user: User) -> None:
    """spec § 5 场景 3 — 常规 trade(buy/sell)字段不可改。"""
    svc = TradeService(session)
    svc.create(
        user_id=user.id, ts_code="600519.SH", name="贵州茅台",
        ttype=TradeType.INITIAL, quantity=200, price=Decimal("1450.00"),
        trade_date=date(2024, 6, 1),
    )
    buy = svc.create(
        user_id=user.id, ts_code="600519.SH", name="贵州茅台",
        ttype=TradeType.BUY, quantity=50, price=Decimal("1500.00"),
        trade_date=date(2026, 1, 15),
    )
    session.commit()

    with pytest.raises(ImmutableTradeError):
        svc.update(buy.id, price=Decimal("1499.00"))


def test_update_sell_trade_raises_immutable(session: Session, user: User) -> None:
    svc = TradeService(session)
    svc.create(
        user_id=user.id, ts_code="600519.SH", name="贵州茅台",
        ttype=TradeType.INITIAL, quantity=200, price=Decimal("1450.00"),
        trade_date=date(2024, 6, 1),
    )
    sell = svc.create(
        user_id=user.id, ts_code="600519.SH", name="贵州茅台",
        ttype=TradeType.SELL, quantity=30, price=Decimal("1600.00"),
        trade_date=date(2026, 4, 20),
    )
    session.commit()

    with pytest.raises(ImmutableTradeError):
        svc.update(sell.id, quantity=20)
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd backend && uv run pytest tests/unit/services/test_trade_service.py -v -k "update"
```

Expected:FAIL — `AttributeError: 'TradeService' object has no attribute 'update'`。

- [ ] **Step 3: Implement `TradeService.update`**

Append to `app/services/trade_service.py`:

```python
    _INITIAL_UPDATABLE = {"ts_code", "name", "quantity", "price", "trade_date", "note"}

    def update(self, trade_id: str, **fields: object) -> Trade:
        trade = self._session.query(Trade).filter_by(id=trade_id).one()
        if trade.type != TradeType.INITIAL:
            raise ImmutableTradeError("常规交易不可改字段,过 24h 也不可")

        unknown = set(fields.keys()) - self._INITIAL_UPDATABLE
        if unknown:
            raise ValueError(f"unknown fields for update: {unknown}")

        for k, v in fields.items():
            setattr(trade, k, v)
        self._session.flush()
        self._recompute_position(
            user_id=trade.user_id, ts_code=trade.ts_code, name=trade.name
        )
        return trade
```

- [ ] **Step 4: Run test to verify it passes**

```bash
cd backend && uv run pytest tests/unit/services/test_trade_service.py -v
```

Expected:8 PASS(5 已有 + 3 新增)。

- [ ] **Step 5: Run mypy**

```bash
cd backend && uv run mypy app/services/trade_service.py
```

Expected:`Success`。

- [ ] **Step 6: Commit**

```bash
git add backend/app/services/trade_service.py backend/tests/unit/services/test_trade_service.py
git commit -m "feat(v1.0): TradeService.update + initial-only guard

spec § 5 场景 3(buy/sell 字段不可改 → ImmutableTradeError)
+ 场景 5(initial 48h 后仍可改)。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 8: Add `PositionService` (read + update_quote)

**Files:**
- Create:`backend/app/services/position_service.py`
- Test:`backend/tests/unit/services/test_position_service.py`

**Spec ref:** § 3.2 监控周期写 quote(决策 3 实现)。

- [ ] **Step 1: Write failing tests**

Create `backend/tests/unit/services/test_position_service.py`:

```python
"""PositionService read + update_quote 测试。"""

from __future__ import annotations

from datetime import date, datetime
from decimal import Decimal
from uuid import uuid4

import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import Session

from app.core.database import Base
from app.models.position import Position
from app.models.trade import TradeType
from app.models.user import User
from app.services.position_service import PositionService
from app.services.trade_service import TradeService


@pytest.fixture
def session() -> Session:
    engine = create_engine("sqlite:///:memory:")
    Base.metadata.create_all(engine)
    with Session(engine) as s:
        yield s


@pytest.fixture
def user(session: Session) -> User:
    u = User(
        id=str(uuid4()),
        email=f"u-{uuid4().hex[:8]}@test",
        password_hash="x",
        is_active=True,
    )
    session.add(u)
    session.flush()
    return u


def test_list_for_user_returns_only_active_positions(
    session: Session, user: User
) -> None:
    """list_for_user 返回该 user 的全部 Position(包括 quantity=0 的清仓行)。"""
    trade_svc = TradeService(session)
    trade_svc.create(
        user_id=user.id, ts_code="600519.SH", name="贵州茅台",
        ttype=TradeType.INITIAL, quantity=200, price=Decimal("1450.00"),
        trade_date=date(2024, 6, 1),
    )
    trade_svc.create(
        user_id=user.id, ts_code="000001.SZ", name="平安银行",
        ttype=TradeType.INITIAL, quantity=1000, price=Decimal("12.00"),
        trade_date=date(2024, 6, 1),
    )
    session.commit()

    pos_svc = PositionService(session)
    positions = pos_svc.list_for_user(user.id)
    codes = {p.ts_code for p in positions}
    assert codes == {"600519.SH", "000001.SZ"}


def test_update_quote_sets_price_and_timestamp(session: Session, user: User) -> None:
    """spec § 3.2 — 监控周期写 last_quote_price + last_quote_at。"""
    trade_svc = TradeService(session)
    trade_svc.create(
        user_id=user.id, ts_code="600519.SH", name="贵州茅台",
        ttype=TradeType.INITIAL, quantity=200, price=Decimal("1450.00"),
        trade_date=date(2024, 6, 1),
    )
    session.commit()

    now = datetime.utcnow()
    pos_svc = PositionService(session)
    pos_svc.update_quote(
        user_id=user.id,
        ts_code="600519.SH",
        price=Decimal("1580.00"),
        at=now,
    )
    session.commit()

    pos = session.query(Position).filter_by(user_id=user.id, ts_code="600519.SH").one()
    assert pos.last_quote_price == Decimal("1580.0000")
    assert pos.last_quote_at == now


def test_update_quote_for_unknown_position_raises_lookup(
    session: Session, user: User
) -> None:
    pos_svc = PositionService(session)
    with pytest.raises(LookupError):
        pos_svc.update_quote(
            user_id=user.id,
            ts_code="000001.SZ",
            price=Decimal("12.00"),
            at=datetime.utcnow(),
        )
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd backend && uv run pytest tests/unit/services/test_position_service.py -v
```

Expected:FAIL — `ModuleNotFoundError`。

- [ ] **Step 3: Implement `PositionService`**

Create `backend/app/services/position_service.py`:

```python
"""PositionService — Position 读 API + 监控引擎写 quote 入口(决策 3)。

Read 路径:dashboard / 详情卡。
Write 路径:仅 update_quote(监控引擎调用),其他 Position 字段写入由 TradeService 拥有。
"""

from __future__ import annotations

from datetime import datetime
from decimal import Decimal

from sqlalchemy.orm import Session

from app.models.position import Position


class PositionService:
    def __init__(self, session: Session) -> None:
        self._session = session

    def list_for_user(self, user_id: str) -> list[Position]:
        return (
            self._session.query(Position)
            .filter_by(user_id=user_id)
            .order_by(Position.ts_code.asc())
            .all()
        )

    def get(self, user_id: str, ts_code: str) -> Position | None:
        return (
            self._session.query(Position)
            .filter_by(user_id=user_id, ts_code=ts_code)
            .one_or_none()
        )

    def update_quote(
        self,
        *,
        user_id: str,
        ts_code: str,
        price: Decimal,
        at: datetime,
    ) -> None:
        pos = self.get(user_id=user_id, ts_code=ts_code)
        if pos is None:
            raise LookupError(
                f"no Position for user_id={user_id} ts_code={ts_code}"
            )
        pos.last_quote_price = price
        pos.last_quote_at = at
        self._session.flush()
```

- [ ] **Step 4: Run test to verify it passes**

```bash
cd backend && uv run pytest tests/unit/services/test_position_service.py -v
```

Expected:3 PASS。

- [ ] **Step 5: Run mypy**

```bash
cd backend && uv run mypy app/services/position_service.py
```

Expected:`Success`。

- [ ] **Step 6: Commit**

```bash
git add backend/app/services/position_service.py backend/tests/unit/services/test_position_service.py
git commit -m "feat(v1.0): PositionService(list_for_user / get / update_quote)

update_quote 是决策 3 在监控周期对 Position 写现价的入口,
后续监控引擎扩展 plan 调用本接口而不直接 SQL。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 9: Add Pydantic schemas

**Files:**
- Create:`backend/app/schemas/portfolio.py`
- Test:`backend/tests/unit/schemas/test_portfolio_schemas.py`

**Spec ref:** product spec § 4.1 + impl spec § 2.1/2.2(字段定义)。

- [ ] **Step 1: Write failing tests**

Create `backend/tests/unit/schemas/test_portfolio_schemas.py`(目录可能需要先建):

```python
"""Pydantic schema validation tests for portfolio endpoints."""

from __future__ import annotations

from datetime import date
from decimal import Decimal

import pytest
from pydantic import ValidationError

from app.schemas.portfolio import (
    OnboardingRequest,
    PositionRead,
    TradeCreate,
    TradeUpdate,
)


def test_trade_create_accepts_valid_initial() -> None:
    payload = {
        "ts_code": "600519.SH",
        "name": "贵州茅台",
        "type": "initial",
        "quantity": 200,
        "price": "1450.00",
        "trade_date": "2024-06-01",
        "note": "looks good",
    }
    tc = TradeCreate(**payload)
    assert tc.type == "initial"
    assert tc.quantity == 200
    assert tc.price == Decimal("1450.00")


def test_trade_create_rejects_negative_quantity() -> None:
    with pytest.raises(ValidationError):
        TradeCreate(
            ts_code="600519.SH",
            name="贵州茅台",
            type="initial",
            quantity=-5,
            price=Decimal("1450.00"),
            trade_date=date(2024, 6, 1),
        )


def test_trade_create_rejects_zero_price() -> None:
    with pytest.raises(ValidationError):
        TradeCreate(
            ts_code="600519.SH",
            name="贵州茅台",
            type="initial",
            quantity=10,
            price=Decimal("0"),
            trade_date=date(2024, 6, 1),
        )


def test_trade_update_only_accepts_initial_updatable_fields() -> None:
    """update 仅允许 ts_code / name / quantity / price / trade_date / note;type 不可改。"""
    valid = TradeUpdate(price=Decimal("1455.00"))
    assert valid.price == Decimal("1455.00")

    with pytest.raises(ValidationError):
        TradeUpdate(type="buy")  # type 不在 schema fields 中


def test_position_read_serializes_quote_fields() -> None:
    pr = PositionRead(
        id="abc",
        ts_code="600519.SH",
        name="贵州茅台",
        quantity=220,
        avg_cost=Decimal("1460.00"),
        total_cost=Decimal("321200.00"),
        realized_pnl=Decimal("4200.00"),
        last_quote_price=Decimal("1580.00"),
        last_quote_at=None,
        is_silenced=False,
    )
    assert pr.quantity == 220


def test_onboarding_request_validates_at_least_one_initial() -> None:
    with pytest.raises(ValidationError):
        OnboardingRequest(trades=[])
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd backend && uv run pytest tests/unit/schemas/test_portfolio_schemas.py -v
```

Expected:FAIL — `ModuleNotFoundError: app.schemas.portfolio`(also tests/unit/schemas dir may need create)。

- [ ] **Step 3: Implement schemas**

Create `backend/app/schemas/portfolio.py`:

```python
"""Pydantic schemas for portfolio endpoints — v1.0 持仓监控。

Spec ref: docs/superpowers/specs/2026-05-07-v1.0-portfolio-data-model-engineering-design.md § 2。
"""

from __future__ import annotations

from datetime import date, datetime
from decimal import Decimal
from typing import Literal

from pydantic import BaseModel, ConfigDict, Field


TradeTypeStr = Literal["initial", "buy", "sell"]


class TradeCreate(BaseModel):
    """POST /portfolio/trades 入参。"""

    model_config = ConfigDict(extra="forbid")

    ts_code: str = Field(min_length=1, max_length=10)
    name: str = Field(min_length=1, max_length=50)
    type: TradeTypeStr
    quantity: int = Field(gt=0)
    price: Decimal = Field(gt=Decimal("0"))
    trade_date: date
    note: str | None = None


class TradeUpdate(BaseModel):
    """PATCH /portfolio/trades/{id} 入参 — 仅 INITIAL trade 可调用,且 type 不可改。"""

    model_config = ConfigDict(extra="forbid")

    ts_code: str | None = Field(default=None, min_length=1, max_length=10)
    name: str | None = Field(default=None, min_length=1, max_length=50)
    quantity: int | None = Field(default=None, gt=0)
    price: Decimal | None = Field(default=None, gt=Decimal("0"))
    trade_date: date | None = None
    note: str | None = None


class TradeRead(BaseModel):
    """Trade 出参(POST 创建后 / GET 列表)。"""

    model_config = ConfigDict(from_attributes=True)

    id: str
    ts_code: str
    name: str
    type: TradeTypeStr
    quantity: int
    price: Decimal
    trade_date: date
    note: str | None
    created_at: datetime


class PositionRead(BaseModel):
    """GET /portfolio/positions 单条出参。"""

    model_config = ConfigDict(from_attributes=True)

    id: str
    ts_code: str
    name: str
    quantity: int
    avg_cost: Decimal
    total_cost: Decimal
    realized_pnl: Decimal
    last_quote_price: Decimal | None
    last_quote_at: datetime | None
    is_silenced: bool


class OnboardingRequest(BaseModel):
    """POST /portfolio/onboarding 入参 — 一次性录入多笔 INITIAL trade(雪球做法)。"""

    model_config = ConfigDict(extra="forbid")

    trades: list[TradeCreate] = Field(min_length=1)


class OnboardingResponse(BaseModel):
    """onboarding 完成后回传插入的 trades + 生成的 positions。"""

    trades: list[TradeRead]
    positions: list[PositionRead]
```

- [ ] **Step 4: Ensure tests/unit/schemas dir exists**

```bash
mkdir -p backend/tests/unit/schemas
touch backend/tests/unit/schemas/__init__.py
```

- [ ] **Step 5: Run test to verify it passes**

```bash
cd backend && uv run pytest tests/unit/schemas/test_portfolio_schemas.py -v
```

Expected:5 PASS。

- [ ] **Step 6: Run mypy**

```bash
cd backend && uv run mypy app/schemas/portfolio.py
```

Expected:`Success`。

- [ ] **Step 7: Commit**

```bash
git add backend/app/schemas/portfolio.py backend/tests/unit/schemas/__init__.py backend/tests/unit/schemas/test_portfolio_schemas.py
git commit -m "feat(v1.0): portfolio Pydantic schemas

TradeCreate / TradeUpdate / TradeRead / PositionRead / OnboardingRequest+Response。
extra=forbid 严格禁止 unknown 字段(spec § 4.3 type 不可改通过 schema 阻止)。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 10: Add `/portfolio/trades` CRUD endpoints (POST / DELETE / PATCH)

**Files:**
- Create:`backend/app/router/portfolio_router.py`
- Test:`backend/tests/unit/router/test_portfolio_router_trades.py`

**Spec ref:** product spec § 6.5 + impl spec § 3.3 三态机调用方。

- [ ] **Step 1: Write failing test for POST /portfolio/trades**

Create `backend/tests/unit/router/test_portfolio_router_trades.py`:

```python
"""POST/DELETE/PATCH /portfolio/trades endpoint tests (with TestClient + sqlite override)。

Auth 模式跟 reports.py 同 — get_current_user_required(JWT)。
"""

from __future__ import annotations

from datetime import date, datetime, timedelta
from decimal import Decimal
from uuid import uuid4

import pytest
from fastapi import FastAPI
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import Session, sessionmaker

from app.core.database import Base, get_db
from app.models.position import Position
from app.models.trade import Trade, TradeType
from app.models.user import User
from app.router.auth_router import get_current_user_required
from app.router.portfolio_router import router as portfolio_router


@pytest.fixture
def app_and_session() -> tuple[FastAPI, Session, User]:
    engine = create_engine("sqlite:///:memory:", connect_args={"check_same_thread": False})
    Base.metadata.create_all(engine)
    Session_ = sessionmaker(bind=engine)
    session = Session_()

    user = User(
        id=str(uuid4()),
        email="test@portfolio",
        password_hash="x",
        is_active=True,
    )
    session.add(user)
    session.commit()

    app = FastAPI()
    app.include_router(portfolio_router)

    def _override_get_db() -> Session:
        try:
            yield session
        finally:
            pass  # 不 close,留给 fixture cleanup

    app.dependency_overrides[get_db] = _override_get_db
    app.dependency_overrides[get_current_user_required] = lambda: user

    yield app, session, user
    session.close()


def test_post_trades_creates_trade_and_position(
    app_and_session: tuple[FastAPI, Session, User],
) -> None:
    app, session, user = app_and_session
    client = TestClient(app)
    resp = client.post(
        "/portfolio/trades",
        json={
            "ts_code": "600519.SH",
            "name": "贵州茅台",
            "type": "initial",
            "quantity": 200,
            "price": "1450.00",
            "trade_date": "2024-06-01",
            "note": "first",
        },
    )
    assert resp.status_code == 201, resp.text
    body = resp.json()
    assert body["ts_code"] == "600519.SH"
    assert body["quantity"] == 200

    pos = session.query(Position).filter_by(user_id=user.id).one()
    assert pos.quantity == 200


def test_delete_trade_within_24h_returns_204(
    app_and_session: tuple[FastAPI, Session, User],
) -> None:
    app, session, user = app_and_session
    client = TestClient(app)
    create_resp = client.post(
        "/portfolio/trades",
        json={
            "ts_code": "600519.SH",
            "name": "贵州茅台",
            "type": "initial",
            "quantity": 200,
            "price": "1450.00",
            "trade_date": "2024-06-01",
        },
    )
    assert create_resp.status_code == 201
    trade_id = create_resp.json()["id"]

    del_resp = client.delete(f"/portfolio/trades/{trade_id}")
    assert del_resp.status_code == 204
    assert session.query(Trade).count() == 0


def test_delete_trade_after_24h_returns_409(
    app_and_session: tuple[FastAPI, Session, User],
) -> None:
    app, session, user = app_and_session
    client = TestClient(app)
    create_resp = client.post(
        "/portfolio/trades",
        json={
            "ts_code": "600519.SH",
            "name": "贵州茅台",
            "type": "initial",
            "quantity": 200,
            "price": "1450.00",
            "trade_date": "2024-06-01",
        },
    )
    trade_id = create_resp.json()["id"]
    # 强制把 created_at 改老
    trade = session.query(Trade).filter_by(id=trade_id).one()
    trade.created_at = datetime.utcnow() - timedelta(hours=25)
    session.commit()

    del_resp = client.delete(f"/portfolio/trades/{trade_id}")
    assert del_resp.status_code == 409
    assert "24h" in del_resp.json()["detail"]


def test_patch_initial_trade_succeeds(
    app_and_session: tuple[FastAPI, Session, User],
) -> None:
    app, session, user = app_and_session
    client = TestClient(app)
    create_resp = client.post(
        "/portfolio/trades",
        json={
            "ts_code": "600519.SH",
            "name": "贵州茅台",
            "type": "initial",
            "quantity": 200,
            "price": "1450.00",
            "trade_date": "2024-06-01",
        },
    )
    trade_id = create_resp.json()["id"]

    patch_resp = client.patch(
        f"/portfolio/trades/{trade_id}",
        json={"price": "1455.00"},
    )
    assert patch_resp.status_code == 200
    assert patch_resp.json()["price"] == "1455.00"


def test_patch_buy_trade_returns_409(
    app_and_session: tuple[FastAPI, Session, User],
) -> None:
    app, session, user = app_and_session
    client = TestClient(app)
    client.post(
        "/portfolio/trades",
        json={
            "ts_code": "600519.SH",
            "name": "贵州茅台",
            "type": "initial",
            "quantity": 200,
            "price": "1450.00",
            "trade_date": "2024-06-01",
        },
    )
    buy_resp = client.post(
        "/portfolio/trades",
        json={
            "ts_code": "600519.SH",
            "name": "贵州茅台",
            "type": "buy",
            "quantity": 50,
            "price": "1500.00",
            "trade_date": "2026-01-15",
        },
    )
    buy_id = buy_resp.json()["id"]

    patch_resp = client.patch(
        f"/portfolio/trades/{buy_id}",
        json={"price": "1499.00"},
    )
    assert patch_resp.status_code == 409
    assert "不可改" in patch_resp.json()["detail"]
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd backend && uv run pytest tests/unit/router/test_portfolio_router_trades.py -v
```

Expected:FAIL — `ModuleNotFoundError: app.router.portfolio_router`。

- [ ] **Step 3: Implement portfolio router with POST/DELETE/PATCH /trades**

Create `backend/app/router/portfolio_router.py`:

```python
"""Portfolio router — v1.0 持仓监控 REST API(Trade CRUD + Position read + Onboarding)。

Endpoints:
  POST   /portfolio/trades        — 单笔 trade 录入(initial / buy / sell)
  DELETE /portfolio/trades/{id}   — 24h 内删除(spec § 3.3)
  PATCH  /portfolio/trades/{id}   — initial trade 字段更新
  GET    /portfolio/positions     — 当前 user 的全部 positions
  POST   /portfolio/onboarding    — 批量录入 initial trades(雪球做法,spec § 4)

Auth: get_current_user_required(JWT,跟 reports.py 同模式)。
错误:PortfolioError 子类映射到 409 Conflict。
"""

from __future__ import annotations

import logging
from typing import Annotated

from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.orm import Session

from app.core.database import get_db
from app.models.trade import TradeType
from app.models.user import User
from app.router.auth_router import get_current_user_required
from app.schemas.portfolio import (
    OnboardingRequest,
    OnboardingResponse,
    PositionRead,
    TradeCreate,
    TradeRead,
    TradeUpdate,
)
from app.services.portfolio_exceptions import (
    ExpiredDeletionError,
    ImmutableTradeError,
)
from app.services.position_service import PositionService
from app.services.trade_service import TradeService

logger = logging.getLogger(__name__)

router = APIRouter(prefix="/portfolio", tags=["portfolio-v1.0"])


def _trade_type_from_str(s: str) -> TradeType:
    return TradeType(s)


@router.post(
    "/trades",
    response_model=TradeRead,
    status_code=status.HTTP_201_CREATED,
)
def create_trade(
    payload: TradeCreate,
    db: Annotated[Session, Depends(get_db)],
    user: Annotated[User, Depends(get_current_user_required)],
) -> TradeRead:
    svc = TradeService(db)
    trade = svc.create(
        user_id=str(user.id),
        ts_code=payload.ts_code,
        name=payload.name,
        ttype=_trade_type_from_str(payload.type),
        quantity=payload.quantity,
        price=payload.price,
        trade_date=payload.trade_date,
        note=payload.note,
    )
    db.commit()
    db.refresh(trade)
    return TradeRead.model_validate(trade)


@router.delete(
    "/trades/{trade_id}",
    status_code=status.HTTP_204_NO_CONTENT,
)
def delete_trade(
    trade_id: str,
    db: Annotated[Session, Depends(get_db)],
    user: Annotated[User, Depends(get_current_user_required)],
) -> None:
    svc = TradeService(db)
    try:
        svc.delete(trade_id)
        db.commit()
    except ExpiredDeletionError as exc:
        db.rollback()
        raise HTTPException(
            status_code=status.HTTP_409_CONFLICT, detail=str(exc)
        ) from exc


@router.patch("/trades/{trade_id}", response_model=TradeRead)
def update_trade(
    trade_id: str,
    payload: TradeUpdate,
    db: Annotated[Session, Depends(get_db)],
    user: Annotated[User, Depends(get_current_user_required)],
) -> TradeRead:
    svc = TradeService(db)
    fields = payload.model_dump(exclude_unset=True)
    try:
        trade = svc.update(trade_id, **fields)
        db.commit()
        db.refresh(trade)
        return TradeRead.model_validate(trade)
    except ImmutableTradeError as exc:
        db.rollback()
        raise HTTPException(
            status_code=status.HTTP_409_CONFLICT, detail=str(exc)
        ) from exc
```

- [ ] **Step 4: Run test to verify it passes**

```bash
cd backend && uv run pytest tests/unit/router/test_portfolio_router_trades.py -v
```

Expected:5 PASS。

- [ ] **Step 5: Run mypy**

```bash
cd backend && uv run mypy app/router/portfolio_router.py
```

Expected:`Success`。

- [ ] **Step 6: Commit**

```bash
git add backend/app/router/portfolio_router.py backend/tests/unit/router/test_portfolio_router_trades.py
git commit -m "feat(v1.0): /portfolio/trades CRUD endpoints (POST/DELETE/PATCH)

ImmutableTradeError + ExpiredDeletionError 映射 409 Conflict。
JWT auth 跟 reports.py 同模式。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 11: Add `GET /portfolio/positions` + `POST /portfolio/onboarding`

**Files:**
- Modify:`backend/app/router/portfolio_router.py`(append GET / POST onboarding)
- Test:`backend/tests/unit/router/test_portfolio_router_positions.py`(NEW)

**Spec ref:** product § 4.2(雪球 onboarding)+ § 6.1(dashboard 读 positions)。

- [ ] **Step 1: Write failing tests**

Create `backend/tests/unit/router/test_portfolio_router_positions.py`:

```python
"""GET /portfolio/positions + POST /portfolio/onboarding 测试。"""

from __future__ import annotations

from uuid import uuid4

import pytest
from fastapi import FastAPI
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import Session, sessionmaker

from app.core.database import Base, get_db
from app.models.user import User
from app.router.auth_router import get_current_user_required
from app.router.portfolio_router import router as portfolio_router


@pytest.fixture
def client_and_session() -> tuple[TestClient, Session, User]:
    engine = create_engine("sqlite:///:memory:", connect_args={"check_same_thread": False})
    Base.metadata.create_all(engine)
    Session_ = sessionmaker(bind=engine)
    session = Session_()
    user = User(
        id=str(uuid4()),
        email="t@p",
        password_hash="x",
        is_active=True,
    )
    session.add(user)
    session.commit()

    app = FastAPI()
    app.include_router(portfolio_router)
    app.dependency_overrides[get_db] = lambda: (yield session)
    app.dependency_overrides[get_current_user_required] = lambda: user

    yield TestClient(app), session, user
    session.close()


def test_get_positions_empty_returns_empty_list(
    client_and_session: tuple[TestClient, Session, User],
) -> None:
    client, _, _ = client_and_session
    resp = client.get("/portfolio/positions")
    assert resp.status_code == 200
    assert resp.json() == []


def test_post_onboarding_creates_initial_trades_and_positions(
    client_and_session: tuple[TestClient, Session, User],
) -> None:
    client, session, user = client_and_session
    payload = {
        "trades": [
            {
                "ts_code": "600519.SH", "name": "贵州茅台",
                "type": "initial", "quantity": 200, "price": "1450.00",
                "trade_date": "2024-06-01",
                "note": "白酒龙头",
            },
            {
                "ts_code": "000001.SZ", "name": "平安银行",
                "type": "initial", "quantity": 1000, "price": "12.00",
                "trade_date": "2024-06-01",
            },
        ]
    }
    resp = client.post("/portfolio/onboarding", json=payload)
    assert resp.status_code == 201, resp.text
    body = resp.json()
    assert len(body["trades"]) == 2
    assert len(body["positions"]) == 2

    list_resp = client.get("/portfolio/positions")
    assert list_resp.status_code == 200
    items = list_resp.json()
    assert len(items) == 2
    codes = {p["ts_code"] for p in items}
    assert codes == {"600519.SH", "000001.SZ"}


def test_post_onboarding_rejects_non_initial_trade_type(
    client_and_session: tuple[TestClient, Session, User],
) -> None:
    client, _, _ = client_and_session
    resp = client.post(
        "/portfolio/onboarding",
        json={
            "trades": [
                {
                    "ts_code": "600519.SH", "name": "贵州茅台",
                    "type": "buy", "quantity": 50, "price": "1500.00",
                    "trade_date": "2026-01-15",
                }
            ]
        },
    )
    assert resp.status_code == 422
    assert "initial" in resp.text.lower()


def test_post_onboarding_rejects_empty_trades(
    client_and_session: tuple[TestClient, Session, User],
) -> None:
    client, _, _ = client_and_session
    resp = client.post("/portfolio/onboarding", json={"trades": []})
    assert resp.status_code == 422
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd backend && uv run pytest tests/unit/router/test_portfolio_router_positions.py -v
```

Expected:FAIL — endpoint not found。

- [ ] **Step 3: Append GET /positions + POST /onboarding to router**

Append to `backend/app/router/portfolio_router.py`:

```python
@router.get("/positions", response_model=list[PositionRead])
def list_positions(
    db: Annotated[Session, Depends(get_db)],
    user: Annotated[User, Depends(get_current_user_required)],
) -> list[PositionRead]:
    svc = PositionService(db)
    positions = svc.list_for_user(str(user.id))
    return [PositionRead.model_validate(p) for p in positions]


@router.post(
    "/onboarding",
    response_model=OnboardingResponse,
    status_code=status.HTTP_201_CREATED,
)
def onboarding(
    payload: OnboardingRequest,
    db: Annotated[Session, Depends(get_db)],
    user: Annotated[User, Depends(get_current_user_required)],
) -> OnboardingResponse:
    """雪球做法:批量录入 INITIAL trade,all-or-nothing 单事务。"""
    # 严格只接受 initial type(雪球语义)
    for tc in payload.trades:
        if tc.type != "initial":
            raise HTTPException(
                status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
                detail="onboarding 仅接受 type='initial' trades",
            )

    trade_svc = TradeService(db)
    pos_svc = PositionService(db)
    created_trades = []
    for tc in payload.trades:
        trade = trade_svc.create(
            user_id=str(user.id),
            ts_code=tc.ts_code,
            name=tc.name,
            ttype=_trade_type_from_str(tc.type),
            quantity=tc.quantity,
            price=tc.price,
            trade_date=tc.trade_date,
            note=tc.note,
        )
        created_trades.append(trade)
    db.commit()
    for t in created_trades:
        db.refresh(t)

    positions = pos_svc.list_for_user(str(user.id))
    return OnboardingResponse(
        trades=[TradeRead.model_validate(t) for t in created_trades],
        positions=[PositionRead.model_validate(p) for p in positions],
    )
```

- [ ] **Step 4: Run test to verify it passes**

```bash
cd backend && uv run pytest tests/unit/router/test_portfolio_router_positions.py -v
```

Expected:4 PASS。

- [ ] **Step 5: Run all unit + router tests for portfolio**

```bash
cd backend && uv run pytest tests/unit/services/test_portfolio_recompute.py tests/unit/services/test_trade_service.py tests/unit/services/test_position_service.py tests/unit/services/test_portfolio_exceptions.py tests/unit/schemas/test_portfolio_schemas.py tests/unit/router/test_portfolio_router_trades.py tests/unit/router/test_portfolio_router_positions.py -v
```

Expected:全部 PASS。

- [ ] **Step 6: Run mypy on entire portfolio surface**

```bash
cd backend && uv run mypy app/services/portfolio_recompute.py app/services/trade_service.py app/services/position_service.py app/services/portfolio_exceptions.py app/schemas/portfolio.py app/router/portfolio_router.py
```

Expected:`Success`。

- [ ] **Step 7: Commit**

```bash
git add backend/app/router/portfolio_router.py backend/tests/unit/router/test_portfolio_router_positions.py
git commit -m "feat(v1.0): GET /portfolio/positions + POST /portfolio/onboarding

雪球做法 onboarding:批量 initial trades,严格拒绝 buy/sell type。
List positions 走 PositionService.list_for_user。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 12: Wire `portfolio_router` into `app_main`

**Files:**
- Modify:`backend/app/app_main.py`(include_router 调用)

- [ ] **Step 1: Read current `include_router` block to find insertion point**

```bash
grep -n "include_router" backend/app/app_main.py
```

Confirm 当前已有 `app.include_router(reports_router)` 行(reference)。

- [ ] **Step 2: Find import block**

```bash
grep -n "from app.router" backend/app/app_main.py
```

- [ ] **Step 3: Add portfolio_router import + include_router**

Modify `backend/app/app_main.py`:

In the imports section near other `from app.router.*` imports, add:

```python
from app.router.portfolio_router import router as portfolio_router
```

In the `include_router` block, after `app.include_router(reports_router)`,add:

```python
app.include_router(portfolio_router)  # v1.0 — portfolio data model + onboarding
```

- [ ] **Step 4: Smoke test app import**(spec memory `feedback_serve_path_no_ci_coverage`)

```bash
cd backend && uv run python -c "from app.app_main import app; print('routes:', [r.path for r in app.routes if '/portfolio' in r.path])"
```

Expected:打印出 `['/portfolio/trades', '/portfolio/trades/{trade_id}', '/portfolio/trades/{trade_id}', '/portfolio/positions', '/portfolio/onboarding']` 5 条(POST/DELETE/PATCH/GET/POST,顺序可能不同)。

如果 import 失败,排查 `app/router/portfolio_router.py` 是否能独立 import。

- [ ] **Step 5: Run mypy on app_main**

```bash
cd backend && uv run mypy app/app_main.py
```

Expected:`Success`。

- [ ] **Step 6: Commit**

```bash
git add backend/app/app_main.py
git commit -m "feat(v1.0): wire portfolio_router into app_main lifespan

5 endpoints exposed:POST/DELETE/PATCH /portfolio/trades + GET /positions + POST /onboarding。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 13: e2e test (PG container) — Onboarding 完整链路 + 监控写 quote

**Files:**
- Create:`backend/tests/e2e/test_portfolio_onboarding_e2e.py`

**Spec ref:** § 5 测试场景 7(监控周期写 quote)+ Onboarding 链路。

参考已有 `backend/tests/e2e/test_pg_serve_path_e2e.py` 的 `pg_test_container` fixture 和 lifespan trigger 模式(memory `feedback_pg_test_container_pattern`)。

- [ ] **Step 1: Read existing e2e fixture conventions**

```bash
grep -n "pg_test_container\|lifespan\|TestClient(app)" backend/tests/e2e/test_pg_serve_path_e2e.py | head -20
grep -n "pg_test_container" backend/tests/conftest.py | head -10
```

记下 fixture 名称 + 用法(`with TestClient(app) as client:` context manager 必须用,memory `feedback_serve_path_no_ci_coverage` 提到)。

- [ ] **Step 2: Write failing e2e test**

Create `backend/tests/e2e/test_portfolio_onboarding_e2e.py`:

```python
"""E2E:Onboarding 完整链路 + 监控周期写 quote → dashboard 读浮盈(PG container)。

Fixture 依赖 conftest.py 的 pg_test_container + register endpoint。
"""

from __future__ import annotations

from datetime import datetime
from decimal import Decimal

import pytest
from fastapi.testclient import TestClient

from app.app_main import app
from app.core.database import SessionLocal
from app.models.position import Position
from app.services.position_service import PositionService


@pytest.mark.e2e
def test_onboarding_then_quote_then_list_e2e(
    pg_test_container: object,  # autouse fixture sets DATABASE_URL
) -> None:
    with TestClient(app) as client:
        # 1. Register a fresh user
        reg = client.post(
            "/auth/register",
            json={"email": "portfolio-e2e@test", "password": "Pa55word!!"},
        )
        assert reg.status_code == 201, reg.text
        token = reg.json()["access_token"]
        auth = {"Authorization": f"Bearer {token}"}

        # 2. POST /portfolio/onboarding 雪球做法
        ob_resp = client.post(
            "/portfolio/onboarding",
            headers=auth,
            json={
                "trades": [
                    {
                        "ts_code": "600519.SH", "name": "贵州茅台",
                        "type": "initial", "quantity": 200,
                        "price": "1450.00", "trade_date": "2024-06-01",
                    },
                    {
                        "ts_code": "000001.SZ", "name": "平安银行",
                        "type": "initial", "quantity": 1000,
                        "price": "12.00", "trade_date": "2024-06-01",
                    },
                ]
            },
        )
        assert ob_resp.status_code == 201, ob_resp.text

        # 3. 直连 DB 用 PositionService 模拟监控引擎写 quote(spec § 3.2)
        with SessionLocal() as db:
            user_id_resp = client.get("/auth/me", headers=auth)
            user_id = user_id_resp.json()["id"]
            pos_svc = PositionService(db)
            pos_svc.update_quote(
                user_id=user_id,
                ts_code="600519.SH",
                price=Decimal("1580.00"),
                at=datetime.utcnow(),
            )
            db.commit()

        # 4. GET /portfolio/positions 验证 quote 字段
        list_resp = client.get("/portfolio/positions", headers=auth)
        assert list_resp.status_code == 200
        items = list_resp.json()
        maotai = next(p for p in items if p["ts_code"] == "600519.SH")
        assert maotai["quantity"] == 200
        assert Decimal(maotai["last_quote_price"]) == Decimal("1580.00")
        # avg_cost 1450 → 浮盈 = (1580-1450) * 200 = 26000
        # 浮盈率 = (1580-1450) / 1450 ≈ 8.97%
```

- [ ] **Step 3: Run test to verify fail or skip**

```bash
cd backend && uv run pytest tests/e2e/test_portfolio_onboarding_e2e.py -v -m e2e
```

Expected:在没起 PG container 的本地可能 SKIP;若 fixture 跑起来,会 FAIL("/auth/me" 是否存在 + register endpoint 跟 PR #21 测试一致)。如果 register endpoint 不存在,降级为直接用 `User.__table__.create()` 然后 issue token 的方式(参考 PR #21 commit `7668b0f`)。

**注:** 如果发现 PR #21 路径必须跳过双轨 schema 的 workaround 仍存在,本测试可以 hold 到 PR-A ship 后再 enable;在那之前 mark `pytest.mark.skip("blocked on PR-A — double-schema workaround")` + 注释引用此 plan。

- [ ] **Step 4: 在 CI 起 PG 后跑测试**

```bash
docker compose up -d postgres
sleep 3
cd backend && DATABASE_URL=postgresql://postgres:postgres@localhost:5432/industry_assistant_test uv run pytest tests/e2e/test_portfolio_onboarding_e2e.py -v -m e2e
```

Expected:PASS。

- [ ] **Step 5: Commit**

```bash
git add backend/tests/e2e/test_portfolio_onboarding_e2e.py
git commit -m "test(v1.0): e2e portfolio onboarding + quote update + positions list

PG container 跑完整链路 — auth register → onboarding → 模拟监控写 quote → 验 quote 字段。
spec § 5 场景 7 在 e2e 层验证。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 14: Acceptance — README 同步 + 全套测试 + PR 准备

**Files:**
- Modify:`README.md`(根目录,持久化段落补 trades / positions 表名)

**Spec ref:** memory `feedback_refresh_readme_per_version`。

- [ ] **Step 1: Read README 的持久化 / 表清单段落**

```bash
grep -nE "(持久化|表|trade|position)" README.md | head -20
```

定位"持久化"或"数据库"段。

- [ ] **Step 2: Append trades / positions 到表清单**

Modify `README.md`:在持久化段表清单加:

```markdown
- `trades` — v1.0 持仓监控:用户录入交易记录(SoT)
- `positions` — v1.0 持仓监控:从 Trade fold 出的 materialized 状态
```

- [ ] **Step 3: 跑完整 backend 测试套确认无 regression**

```bash
cd backend && uv run poe ci
```

Expected:全部 PASS(包含本 plan 新增的全部 unit / router 测试,以及历史已有套件)。

如果某个 historical 测试 fail,**STOP** 排查 — 通常是 model barrel 顺序导致的 metadata 冲突。

- [ ] **Step 4: 跑 mypy 全 backend**

```bash
cd backend && uv run mypy app
```

Expected:`Success`(memory `feedback_type_ignore_with_typed_signature` — mypy 必须全 backend 跑)。

- [ ] **Step 5: Commit README + 准备 PR**

```bash
git add README.md
git commit -m "docs(readme): v1.0 持仓监控 trades/positions 表登记

第 1 条 implementation plan 落地 — 数据模型 + Onboarding API。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

- [ ] **Step 6: Push branch + 用 gh CLI 起 PR(若用户授权)**

```bash
git push -u origin feat/v1.0-portfolio-data-model
```

PR body 模板:

```markdown
## Summary
- 落地 v1.0 持仓监控产品 spec § 9.2 第 1 条 plan(数据模型 + Onboarding)
- Trade(SoT)+ Position(materialized 决策 1)+ 三态机 service guard(决策 2)+ Position.last_quote_price 决策 3 字段
- 5 endpoints:POST/DELETE/PATCH /portfolio/trades + GET /portfolio/positions + POST /portfolio/onboarding
- 不依赖 PR-A,沿用 v0.9.x with_variant FK + create_all 模式

## Test plan
- [ ] L0 unit:portfolio_recompute fold 5 场景 + TradeService CRUD + 三态机 guard + PositionService update_quote
- [ ] L1 router:5 endpoint integration tests with sqlite override
- [ ] L2 e2e:Onboarding → 监控 quote → list_positions(PG container)
- [ ] mypy backend 全跑无 regression
- [ ] poe ci 全套 PASS
```

---

## 自审(Plan Self-Review)

按 writing-plans skill 流程,检查 4 维度:

**1. Spec coverage:**
- spec § 1 决策 1(materialized Position)→ Task 2 + Task 4 + Task 5
- spec § 1 决策 2(service guard)→ Task 6 + Task 7
- spec § 1 决策 3(quote 字段 + monitor 写)→ Task 2 + Task 8 + Task 13
- spec § 2.1 Trade → Task 1
- spec § 2.2 Position → Task 2
- spec § 3.1 fold 算法 → Task 4
- spec § 3.1 Trade write 同事务 recompute → Task 5
- spec § 3.2 监控周期写 quote → Task 8(+ Task 13 e2e)
- spec § 3.3 三态机异常 → Task 3 + Task 6 + Task 7
- spec § 4 Onboarding 雪球做法 → Task 11(POST /onboarding 仅接受 initial)
- spec § 5 测试场景 1-9 → 各 task TDD 步骤覆盖,但场景 9(事务原子性)未显式覆盖 → 见后文修
- spec § 6 衔接(model barrel + create_all + research_report 参考)→ Task 1/2 Step 4

**Gap:** 测试场景 9(事务原子性 — Trade write 失败 Position 不变)未在 plan 中显式覆盖。原因:在 ORM 层模拟 DB error 注入工程量大,且场景 9 的不变量已经由 Task 5/6/7 中 `_recompute_position` 跟 trade write 在同一 session 隐含保证。**接受 gap**:在 e2e Task 13 注释里加一行说明,真正的事务原子性靠"Trade insert + recompute_position 同 session"的代码契约保证,不引入 mock-based 单测。

**2. Placeholder scan:**
- 全文搜 TBD/TODO/FIXME — 仅 Task 13 Step 3 有 "如果 register endpoint 不存在,降级为..." 的 conditional fallback,这是 implementer 在依赖现有 v0.9.x auth 时的运行时分叉,不是 plan 占位。可以接受。
- 无 "implement later" 类红旗。

**3. Type consistency:**
- `TradeService.create` 入参 `ttype: TradeType`(避开关键字 `type`)— Task 5 / 10 / 11 调用一致 ✅
- `PositionState` / `TradeInput` dataclass 在 Task 4 定义,Task 5 `_recompute_position` 内部消费 ✅
- `recompute_position_from_trades(trades) -> PositionState` 签名稳定 ✅
- `OnboardingRequest.trades: list[TradeCreate]` 跟 router endpoint 调用一致 ✅
- `PositionRead.last_quote_price: Decimal | None` 跟 model 一致 ✅

**4. Ambiguity:**
- "spec § 5 场景 9 事务原子性" — 已在 § Spec coverage gap 段说明
- "v0.9.x 已 ship 的 register endpoint" — Task 13 Step 3 已 explicit fallback
- "TradeService 调用方负责 commit" — Task 5 Step 3 docstring 显式声明
- 无其他歧义

Plan 自审通过。
