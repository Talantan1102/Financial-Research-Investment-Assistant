# v0.8.2 Implementation Plan — Credit Investigation Report Schema + Writer Refactor

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 ResearchAgent 在 B-1 信贷尽调场景下产出符合银行信贷调查报告 6-section 模板的结构化报告(每 section 带 evidence chunk_id),贵州茅台 dogfood 跑通。

**Architecture:** 引入 `CreditInvestigationReport` Pydantic schema 作为 source of truth;扩展 LLMService.chat 的 schema 参数支持 Pydantic class 自动转换 + parse;Writer agent 改用扩展后 API 产出 schema-conformant JSON;新增 markdown renderer 从 schema 推 markdown(兼容旧 SSE / 前端);新增 ResearchState `credit_report` field;录 B-1 茅台端到端 cassette。

**Tech Stack:** Python 3.11 / uv / Pydantic v2 / LangGraph / OpenAI SDK(compatible-mode)/ pytest / pytest-recording (VCR.py) / Milvus(KB)/ Bocha(web search)。

**Spec ref:** `docs/superpowers/specs/2026-05-03-v0.8.2-credit-research-report-design.md`
**Roadmap anchor:** `docs/superpowers/specs/2026-05-03-product-positioning-v1-roadmap.md` § 9 v0.8.2-β

**Branch:** `feat/v0.8.2-credit-research-report`(已建,基于 origin/main `05f8420`,已带 2 个 spec commit)

---

## File Structure

| 文件 | 责任 | 状态 |
|---|---|---|
| `backend/app/agents/credit_report_schema.py` | `CreditInvestigationReport` 主 schema + 6 section 子 schema + 通用 dataclass(`FinancialMetric` / `RiskItem`) | Create |
| `backend/app/agents/credit_report_renderer.py` | 把 `CreditInvestigationReport` 渲染成 markdown(章节标题 / 表格 / 列表 / evidence 注脚) | Create |
| `backend/tests/unit/test_credit_report_schema.py` | schema validation 单测(必填 / 枚举 / evidence 非空) | Create |
| `backend/tests/unit/test_credit_report_renderer.py` | renderer 单测(包含 anchor / idempotent) | Create |
| `backend/tests/unit/test_llm_service_pydantic_schema.py` | LLMService 扩展单测(dict 路径 + Pydantic class 路径 + parsed 自动 validate) | Create |
| `backend/tests/integration/test_writer_credit_report.py` | Writer L1(mock LLM 返 schema JSON,断言 state_update 含 credit_report) | Create |
| `backend/tests/e2e/test_b1_maotai_credit_report_cassette.py` | B-1 端到端 e2e(KB_MODE=real / BOCHA_MODE=real / cassette) | Create |
| `backend/tests/fixtures/cassettes/test_b1_maotai_credit_report_cassette/*.yaml` | cassette(录制) | Create |
| `backend/app/services/llm_service.py:72` | `chat(schema=...)` 接受 `dict \| type[BaseModel]`,自动 `model_json_schema()` + Pydantic validate | Modify |
| `backend/app/services/llm_response.py:18` | `parsed: dict[str, Any] \| BaseModel \| None`(扩 union) | Modify |
| `backend/app/agents/writer.py` | step 改造:用扩展后 LLMService API + schema=CreditInvestigationReport;build_writer_prompt → build_credit_report_prompt | Modify |
| `backend/app/agents/schemas.py:152 ResearchState` | 加 `credit_report: CreditInvestigationReport \| None = None`,加 import | Modify |
| `backend/app/orchestration/critic_subgraph.py:32` | 加 `credit_report` 透传字段(default None) | Modify |
| `backend/app/orchestration/research_nodes.py` | writer node 输出适配(state_update 已 Writer.step 处理,只需 verify SSE 流) | Modify |

**不动**:`planner.py` / `data_collector.py` / `analyst.py` / `critic.py` / `responder.py`(responder 不参与 research) / `KbSearchService` / `BochaService` / `Milvus` / `pricing` / `tier_router` / `router/research.py:123 Writer 构造`(只输出形态变,构造不变)。

---

## Task 0 — Branch + 前置 sanity

**Files:** None

- [ ] **Step 1: Verify branch 状态**

```bash
git branch --show-current
git log --oneline -3
```

Expected:
```
feat/v0.8.2-credit-research-report
3f578bd docs(spec): v0.8.2 — credit investigation report schema + Writer refactor
c2910a7 docs(spec): product positioning v1.0 roadmap — Agent 投研助手
05f8420 feat(v0.8.1): revert token-plan + finish v0.7 Task 13/14 cassette (#10)
```

不一致 → 停止排查 branch state。

- [ ] **Step 2: env + 连通性 sanity**

```bash
unset all_proxy https_proxy http_proxy
set -a && source backend/.env && set +a
echo "base_url=$DASHSCOPE_BASE_URL"
echo "key prefix=${DASHSCOPE_API_KEY:0:6}...${DASHSCOPE_API_KEY: -4}"
```

Expected:
- `base_url=https://dashscope.aliyuncs.com/compatible-mode/v1`
- `key prefix=sk-14e...326b`

不一致 → 停止,排查 .env 漂移。

- [ ] **Step 3: Milvus 容器 + 数据状态(参考 v0.8.1 plan Task 4)**

```bash
docker ps --format 'table {{.Names}}\t{{.Status}}' | grep milvus
cd backend
uv run python - <<'PYEOF'
from pymilvus import connections, utility, Collection
connections.connect(host="127.0.0.1", port="19530")
total = 0
for c in ("kb_research", "kb_financial", "kb_policy"):
    if c not in utility.list_collections():
        print(f"{c}: MISSING")
        continue
    Collection(c).load()
    n = Collection(c).num_entities
    total += n
    print(f"{c}: {n} chunks")
print(f"TOTAL: {total}")
PYEOF
```

Expected: TOTAL ≥ 5000(memory v0.7 真 ingest 6016 chunks)。
若 < 5000 → `uv run python -m app.scripts.ingest_kb --all` 重 ingest。

- [ ] **Step 4: 全测试 baseline 绿**

```bash
cd backend
uv run pytest tests/unit tests/integration -q 2>&1 | tail -5
```

Expected: all PASS(从 v0.8.1 main 一致)。失败 → 先排查不动 v0.8.2 工作。

- [ ] **Step 5: No commit(纯 verify)**

---

## Task 1 — `CreditInvestigationReport` Pydantic schema(TDD)

**Files:**
- Create: `backend/app/agents/credit_report_schema.py`
- Create: `backend/tests/unit/test_credit_report_schema.py`

- [ ] **Step 1: 写 failing test 文件**

写 `backend/tests/unit/test_credit_report_schema.py`,内容:

```python
"""Unit test for CreditInvestigationReport Pydantic schema."""

from __future__ import annotations

from datetime import datetime

import pytest
from pydantic import ValidationError

from app.agents.credit_report_schema import (
    CompanyOverview,
    CreditInvestigationReport,
    CreditRecommendation,
    FinancialAnalysis,
    FinancialMetric,
    IndustryAnalysis,
    LegalQualification,
    RiskAssessment,
    RiskItem,
)


def _minimal_report() -> CreditInvestigationReport:
    """Minimal valid report — all required fields filled, all optionals empty."""
    return CreditInvestigationReport(
        company_name="测试公司",
        request_id="req-test-001",
        generated_at=datetime(2026, 5, 3, 12, 0, 0),
        company_overview=CompanyOverview(
            narrative="测试综述。",
            main_business="测试主营。",
            evidence=["c1::0"],
        ),
        legal_qualification=LegalQualification(
            narrative="主体合规。",
            legal_status="合规",
            business_qualifications=[],
            adverse_records=[],
            evidence=["c1::1"],
        ),
        financial_analysis=FinancialAnalysis(
            narrative="财务稳定。",
            key_metrics=[
                FinancialMetric(name="营业收入", value="100 亿元", period="2024 H1"),
            ],
            solvency_analysis="偿债能力强。",
            profitability_analysis="盈利稳定。",
            cash_flow_analysis="现金流健康。",
            evidence=["c1::2"],
        ),
        industry_analysis=IndustryAnalysis(
            narrative="行业景气。",
            industry_name="白酒",
            industry_outlook="稳定增长",
            competitive_position="龙头",
            key_competitors=[],
            policy_impact="无重大不利政策。",
            evidence=["c1::3"],
        ),
        risk_assessment=RiskAssessment(
            narrative="整体风险可控。",
            operational_risks=[],
            financial_risks=[],
            industry_risks=[],
            compliance_risks=[],
            overall_risk_level="low",
            evidence=["c1::4"],
        ),
        credit_recommendation=CreditRecommendation(
            narrative="建议批准。",
            decision="approve",
            guarantee_requirements=[],
            conditions=[],
            evidence=["c1::5"],
        ),
    )


def test_minimal_valid_report() -> None:
    r = _minimal_report()
    assert r.company_name == "测试公司"
    assert r.credit_recommendation.decision == "approve"


def test_empty_evidence_rejected() -> None:
    """每个 section evidence 至少 1 个 chunk_id(min_length=1)。"""
    with pytest.raises(ValidationError):
        CompanyOverview(narrative="x", main_business="y", evidence=[])


def test_decision_enum_strict() -> None:
    """decision 必须是三值之一。"""
    with pytest.raises(ValidationError):
        CreditRecommendation(
            narrative="x",
            decision="maybe",  # type: ignore[arg-type]
            guarantee_requirements=[],
            conditions=[],
            evidence=["c1::0"],
        )


def test_overall_risk_level_enum_strict() -> None:
    with pytest.raises(ValidationError):
        RiskAssessment(
            narrative="x",
            operational_risks=[],
            financial_risks=[],
            industry_risks=[],
            compliance_risks=[],
            overall_risk_level="catastrophic",  # type: ignore[arg-type]
            evidence=["c1::0"],
        )


def test_risk_item_severity_enum_strict() -> None:
    with pytest.raises(ValidationError):
        RiskItem(
            title="x",
            description="y",
            severity="moderate",  # type: ignore[arg-type]
        )


def test_required_field_missing_rejected() -> None:
    """company_overview 缺 main_business 必拒。"""
    with pytest.raises(ValidationError):
        CompanyOverview(narrative="x", evidence=["c1::0"])  # type: ignore[call-arg]


def test_json_roundtrip() -> None:
    r = _minimal_report()
    j = r.model_dump_json()
    r2 = CreditInvestigationReport.model_validate_json(j)
    assert r2 == r


def test_json_schema_generation() -> None:
    """`model_json_schema()` 不抛异常,作为 LLMService.chat schema= 入参形态。"""
    schema = CreditInvestigationReport.model_json_schema()
    assert schema["type"] == "object"
    assert "company_name" in schema["properties"]
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd backend
uv run pytest tests/unit/test_credit_report_schema.py -v 2>&1 | tail -10
```

Expected: ImportError(`app.agents.credit_report_schema` 不存在)。

- [ ] **Step 3: 写 schema 文件**

写 `backend/app/agents/credit_report_schema.py`,内容:

```python
"""信贷调查报告 Pydantic schema(B-1 use case)。

设计 ref: docs/superpowers/specs/2026-05-03-v0.8.2-credit-research-report-design.md § 2
"""

from __future__ import annotations

from datetime import datetime
from typing import Literal

from pydantic import BaseModel, ConfigDict, Field

# ── 通用 dataclass ─────────────────────────────────────────────────────────────


class FinancialMetric(BaseModel):
    """单条财务指标。"""

    model_config = ConfigDict(extra="forbid")

    name: str = Field(description="指标名,如 '营业收入' / '资产负债率'")
    value: str = Field(description="指标值,如 '150 亿元' / '12.5%'(字符串,不强制单位)")
    period: str = Field(description="期间,如 '2024 H1' / '2024 全年'")
    yoy_change: str | None = Field(default=None, description="同比变化,如 '同比 +15%'")


class RiskItem(BaseModel):
    """单条风险项。"""

    model_config = ConfigDict(extra="forbid")

    title: str = Field(description="风险标题")
    description: str = Field(description="风险描述")
    severity: Literal["low", "medium", "high"] = Field(description="单项严重度")
    mitigations: list[str] = Field(default_factory=list, description="可缓释措施")


# ── Section schemas ────────────────────────────────────────────────────────────


class CompanyOverview(BaseModel):
    """§ 1 基本信息。"""

    model_config = ConfigDict(extra="forbid")

    narrative: str = Field(description="100-300 字综述")
    unified_credit_code: str | None = Field(default=None, description="统一社会信用代码")
    registered_capital: str | None = Field(default=None, description="注册资本")
    main_business: str = Field(description="主营业务一句话")
    controlling_shareholder: str | None = Field(default=None, description="实际控制人")
    listing_status: str | None = Field(default=None, description="上市/非上市 + 板块")
    evidence: list[str] = Field(min_length=1, description="引用的 chunk_id 列表(至少 1 个)")


class LegalQualification(BaseModel):
    """§ 2 主体资格。"""

    model_config = ConfigDict(extra="forbid")

    narrative: str = Field(description="200-400 字综述")
    legal_status: str = Field(description="法律主体合规情况")
    business_qualifications: list[str] = Field(description="经营资质列表(空 list 也行)")
    adverse_records: list[str] = Field(description="不良记录(空 list 也行)")
    evidence: list[str] = Field(min_length=1)


class FinancialAnalysis(BaseModel):
    """§ 3 财务分析。"""

    model_config = ConfigDict(extra="forbid")

    narrative: str = Field(description="400-800 字深度分析")
    key_metrics: list[FinancialMetric] = Field(description="关键财务指标")
    solvency_analysis: str = Field(description="偿债能力")
    profitability_analysis: str = Field(description="盈利能力")
    cash_flow_analysis: str = Field(description="现金流分析")
    year_over_year_summary: str | None = Field(default=None, description="同比变化(若多年数据)")
    evidence: list[str] = Field(min_length=1)


class IndustryAnalysis(BaseModel):
    """§ 4 行业分析。"""

    model_config = ConfigDict(extra="forbid")

    narrative: str = Field(description="300-600 字")
    industry_name: str = Field(description="所属行业")
    industry_outlook: str = Field(description="景气度判断")
    competitive_position: str = Field(description="在行业中的竞争地位")
    key_competitors: list[str] = Field(description="主要竞争对手(空 list 也行)")
    policy_impact: str = Field(description="相关政策影响")
    evidence: list[str] = Field(min_length=1)


class RiskAssessment(BaseModel):
    """§ 5 风险评估。"""

    model_config = ConfigDict(extra="forbid")

    narrative: str = Field(description="300-500 字")
    operational_risks: list[RiskItem] = Field(description="经营风险列表(空 list 也行)")
    financial_risks: list[RiskItem] = Field(description="财务风险列表")
    industry_risks: list[RiskItem] = Field(description="行业风险列表")
    compliance_risks: list[RiskItem] = Field(description="合规风险列表")
    overall_risk_level: Literal["low", "medium", "high", "very_high"] = Field(
        description="整体风险等级"
    )
    evidence: list[str] = Field(min_length=1)


class CreditRecommendation(BaseModel):
    """§ 6 信贷建议。"""

    model_config = ConfigDict(extra="forbid")

    narrative: str = Field(description="200-400 字综合建议")
    decision: Literal["approve", "reject", "approve_with_conditions"] = Field(
        description="决策建议"
    )
    recommended_credit_limit: str | None = Field(default=None, description="建议额度")
    recommended_term: str | None = Field(default=None, description="建议期限")
    recommended_rate_range: str | None = Field(default=None, description="建议利率区间")
    guarantee_requirements: list[str] = Field(description="担保要求(空 list 也行)")
    conditions: list[str] = Field(description="附加条件(approve_with_conditions 时填)")
    evidence: list[str] = Field(min_length=1)


# ── 主 schema ──────────────────────────────────────────────────────────────────


class CreditInvestigationReport(BaseModel):
    """信贷调查报告(B-1 use case 主输出)。"""

    model_config = ConfigDict(extra="forbid")

    # Header
    company_name: str = Field(description="企业名")
    request_id: str = Field(description="关联 trace request_id")
    generated_at: datetime = Field(description="生成时间")

    # Sections(全必填)
    company_overview: CompanyOverview
    legal_qualification: LegalQualification
    financial_analysis: FinancialAnalysis
    industry_analysis: IndustryAnalysis
    risk_assessment: RiskAssessment
    credit_recommendation: CreditRecommendation
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
cd backend
uv run pytest tests/unit/test_credit_report_schema.py -v 2>&1 | tail -15
```

Expected: 8 PASS。

- [ ] **Step 5: 跑 mypy 检查**

```bash
cd backend
uv run mypy app/agents/credit_report_schema.py 2>&1 | tail -5
```

Expected: Success: no issues found(strict mode 全 backend 在 ci 跑,本步只 spot check 文件)。

- [ ] **Step 6: Commit**

```bash
git add backend/app/agents/credit_report_schema.py backend/tests/unit/test_credit_report_schema.py
git commit -m "$(cat <<'EOF'
feat(agents): CreditInvestigationReport Pydantic schema (v0.8.2)

6 sections (CompanyOverview / LegalQualification / FinancialAnalysis /
IndustryAnalysis / RiskAssessment / CreditRecommendation), each with
required `evidence: list[chunk_id]` (min_length=1) for D1 strong
citation. Decision + risk_level enums strict.

Spec: 2026-05-03-v0.8.2-credit-research-report-design.md § 2

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2 — LLMService.chat schema 参数支持 Pydantic class(TDD,前置扩展)

**Why now:** Task 5 Writer 改造需要 `chat(schema=PydanticClass)` API。先扩 LLMService,再用。

**Files:**
- Modify: `backend/app/services/llm_service.py:72`
- Modify: `backend/app/services/llm_response.py:18`
- Create: `backend/tests/unit/test_llm_service_pydantic_schema.py`

- [ ] **Step 1: 写 failing test 文件**

写 `backend/tests/unit/test_llm_service_pydantic_schema.py`,内容:

```python
"""Unit test for LLMService.chat schema= accepting Pydantic class (v0.8.2)."""

from __future__ import annotations

from typing import Any

import pytest
from pydantic import BaseModel

from app.services.llm_response import LLMResponse
from app.services.llm_service import LLMService
from app.services.openai_client import _RawClientResponse


class _StubBookSchema(BaseModel):
    title: str
    pages: int


class _StubChatClient:
    """Capture the schema arg passed to client.chat() for assertion."""

    def __init__(self, response_content: str) -> None:
        self.response_content = response_content
        self.captured_schema: Any = None

    def chat(self, prompt: str, model: str, schema: Any) -> _RawClientResponse:
        self.captured_schema = schema
        return _RawClientResponse(
            content=self.response_content,
            prompt_tokens=10,
            completion_tokens=20,
        )


@pytest.fixture
def stub_client_book() -> _StubChatClient:
    return _StubChatClient(response_content='{"title": "Hello", "pages": 42}')


def _build_service(client: _StubChatClient) -> LLMService:
    from app.config.llm_config import LlmConfig
    from app.services.tier_router import TierRouter

    cfg = LlmConfig.default()
    return LLMService(client=client, tier_router=TierRouter(cfg), trace_service=None, budget=None)


def test_dict_schema_legacy_path(stub_client_book: _StubChatClient) -> None:
    """传 dict 仍按老路径走,parsed=None。"""
    svc = _build_service(stub_client_book)
    schema_dict: dict[str, Any] = {"type": "object", "properties": {"title": {"type": "string"}}}
    r = svc.chat(prompt="hi", schema=schema_dict)
    assert isinstance(r, LLMResponse)
    assert stub_client_book.captured_schema == schema_dict
    assert r.parsed is None


def test_pydantic_class_auto_converted(stub_client_book: _StubChatClient) -> None:
    """传 Pydantic class:client 收到的 schema 是 model_json_schema() dict。"""
    svc = _build_service(stub_client_book)
    r = svc.chat(prompt="hi", schema=_StubBookSchema)
    assert isinstance(r, LLMResponse)
    assert stub_client_book.captured_schema == _StubBookSchema.model_json_schema()
    assert isinstance(r.parsed, _StubBookSchema)
    assert r.parsed.title == "Hello"
    assert r.parsed.pages == 42


def test_pydantic_class_invalid_content_raises() -> None:
    """LLM 返不合 schema 的 JSON → Pydantic ValidationError 抛出(让上游决定 retry)。"""
    bad_client = _StubChatClient(response_content='{"title": "Hello"}')  # 缺 pages
    from pydantic import ValidationError

    svc = _build_service(bad_client)
    with pytest.raises(ValidationError):
        svc.chat(prompt="hi", schema=_StubBookSchema)


def test_no_schema_unchanged(stub_client_book: _StubChatClient) -> None:
    """schema=None 走老路径,parsed=None,client 收到 None。"""
    svc = _build_service(stub_client_book)
    r = svc.chat(prompt="hi", schema=None)
    assert stub_client_book.captured_schema is None
    assert r.parsed is None
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd backend
uv run pytest tests/unit/test_llm_service_pydantic_schema.py -v 2>&1 | tail -15
```

Expected: FAIL — 4 个 test 都 fail,大概率 `parsed` 仍然 None / TypeError 不接受 type[BaseModel]。

- [ ] **Step 3: 改 `llm_response.py:18` 扩 parsed type union**

读 `backend/app/services/llm_response.py`,定位 line 18 附近的 `parsed: dict[str, Any] | None = None`,改:

```python
parsed: "dict[str, Any] | BaseModel | None" = None
```

加文件顶部 import:

```python
from pydantic import BaseModel
```

(若 pydantic 已 import,加进现有 import 行)

- [ ] **Step 4: 改 `llm_service.py:72` chat 方法**

读 `backend/app/services/llm_service.py:72-110` 附近,改 chat 签名 + 实现:

签名(Step 4a):
```python
def chat(
    self,
    prompt: str,
    tier: Tier = "fast",
    schema: dict[str, Any] | type[BaseModel] | None = None,
    request_id: str | None = None,
    parent_span_id: str | None = None,
) -> LLMResponse:
```

import(文件顶部):
```python
from pydantic import BaseModel
```

实现(Step 4b),在 `if self._budget is not None: ...` 之后,model = self._tier_router.resolve(tier) 之前,加:

```python
# v0.8.2: support Pydantic class as schema (auto-convert + auto-parse)
pydantic_class: type[BaseModel] | None = None
client_schema: dict[str, Any] | None
if isinstance(schema, type) and issubclass(schema, BaseModel):
    pydantic_class = schema
    client_schema = schema.model_json_schema()
else:
    client_schema = schema  # dict | None
```

把 `raw = self._client.chat(prompt=prompt, model=model, schema=schema)` 改成:

```python
raw = self._client.chat(prompt=prompt, model=model, schema=client_schema)
```

把 `parsed=None,` 改成:

```python
parsed=pydantic_class.model_validate_json(raw.content) if pydantic_class else None,
```

- [ ] **Step 5: Run new tests + 跑全 unit/integration 防回归**

```bash
cd backend
uv run pytest tests/unit/test_llm_service_pydantic_schema.py -v 2>&1 | tail -10
uv run pytest tests/unit tests/integration -q 2>&1 | tail -5
```

Expected:
- new test 4 PASS
- 全 unit/integration 全 PASS(不能挂 v0.8.1 baseline)

- [ ] **Step 6: 跑 mypy strict**

```bash
cd backend
uv run mypy app/services/llm_service.py app/services/llm_response.py 2>&1 | tail -5
```

Expected: Success。

- [ ] **Step 7: Commit**

```bash
git add backend/app/services/llm_service.py backend/app/services/llm_response.py backend/tests/unit/test_llm_service_pydantic_schema.py
git commit -m "$(cat <<'EOF'
feat(llm-service): chat(schema=) accepts Pydantic class with auto-parse (v0.8.2)

Backward compatible: dict schema goes through legacy path (parsed=None),
Pydantic class schema is auto-converted via model_json_schema() and
response content is auto-parsed into the class instance.

Required by v0.8.2 Writer refactor (CreditInvestigationReport schema)
and useful for future structured agents (planner / critic). Memory
project_llm_service_contract contract extension; will be updated post-PR.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3 — Schema → markdown renderer(TDD)

**Files:**
- Create: `backend/app/agents/credit_report_renderer.py`
- Create: `backend/tests/unit/test_credit_report_renderer.py`

- [ ] **Step 1: 写 failing test 文件**

写 `backend/tests/unit/test_credit_report_renderer.py`:

```python
"""Unit test for credit_report_renderer."""

from __future__ import annotations

from datetime import datetime

from app.agents.credit_report_renderer import render_credit_report_markdown
from app.agents.credit_report_schema import (
    CompanyOverview,
    CreditInvestigationReport,
    CreditRecommendation,
    FinancialAnalysis,
    FinancialMetric,
    IndustryAnalysis,
    LegalQualification,
    RiskAssessment,
    RiskItem,
)


def _full_report() -> CreditInvestigationReport:
    return CreditInvestigationReport(
        company_name="贵州茅台",
        request_id="req-test-002",
        generated_at=datetime(2026, 5, 3, 12, 0, 0),
        company_overview=CompanyOverview(
            narrative="贵州茅台是国内白酒龙头。",
            unified_credit_code="91520000215123456X",
            registered_capital="12.56 亿元",
            main_business="白酒生产销售",
            controlling_shareholder="贵州省国资委",
            listing_status="A 股主板",
            evidence=["maotai_2024_h1::3", "maotai_2024_h1::5"],
        ),
        legal_qualification=LegalQualification(
            narrative="主体合规,无重大不良。",
            legal_status="合规",
            business_qualifications=["白酒生产许可"],
            adverse_records=[],
            evidence=["maotai_2024_h1::10"],
        ),
        financial_analysis=FinancialAnalysis(
            narrative="财务稳健,盈利能力突出。",
            key_metrics=[
                FinancialMetric(name="营业收入", value="819 亿元", period="2024 H1", yoy_change="+17%"),
                FinancialMetric(name="净利润", value="417 亿元", period="2024 H1", yoy_change="+15%"),
            ],
            solvency_analysis="偿债能力优异,现金充裕。",
            profitability_analysis="毛利率 91%,行业第一。",
            cash_flow_analysis="经营性现金流持续为正。",
            year_over_year_summary="同比双位数增长。",
            evidence=["maotai_2024_h1::20", "maotai_2024_h1::25"],
        ),
        industry_analysis=IndustryAnalysis(
            narrative="白酒行业景气度温和。",
            industry_name="白酒",
            industry_outlook="稳定增长",
            competitive_position="高端龙头",
            key_competitors=["五粮液", "泸州老窖"],
            policy_impact="消费税政策无重大变化。",
            evidence=["maotai_2024_h1::40"],
        ),
        risk_assessment=RiskAssessment(
            narrative="整体风险低。",
            operational_risks=[
                RiskItem(title="渠道库存", description="经销商压货。", severity="low", mitigations=["渠道优化"]),
            ],
            financial_risks=[],
            industry_risks=[],
            compliance_risks=[],
            overall_risk_level="low",
            evidence=["maotai_2024_h1::50"],
        ),
        credit_recommendation=CreditRecommendation(
            narrative="建议批准 50 亿元 3 年期流动资金贷款。",
            decision="approve",
            recommended_credit_limit="50 亿元",
            recommended_term="3 年",
            recommended_rate_range="LPR + 0bp ~ LPR + 30bp",
            guarantee_requirements=[],
            conditions=[],
            evidence=["maotai_2024_h1::60"],
        ),
    )


def test_renders_all_six_sections() -> None:
    md = render_credit_report_markdown(_full_report())
    assert "# 信贷调查报告 — 贵州茅台" in md
    assert "## § 1 基本信息" in md
    assert "## § 2 主体资格" in md
    assert "## § 3 财务分析" in md
    assert "## § 4 行业分析" in md
    assert "## § 5 风险评估" in md
    assert "## § 6 信贷建议" in md


def test_includes_metric_table() -> None:
    md = render_credit_report_markdown(_full_report())
    assert "营业收入" in md
    assert "819 亿元" in md


def test_includes_evidence_footer_per_section() -> None:
    md = render_credit_report_markdown(_full_report())
    assert "maotai_2024_h1::3" in md   # company_overview evidence
    assert "maotai_2024_h1::20" in md  # financial_analysis evidence
    assert "maotai_2024_h1::60" in md  # credit_recommendation evidence


def test_idempotent() -> None:
    r = _full_report()
    assert render_credit_report_markdown(r) == render_credit_report_markdown(r)


def test_decision_label_zh() -> None:
    md = render_credit_report_markdown(_full_report())
    assert "批准" in md  # approve → 批准
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd backend
uv run pytest tests/unit/test_credit_report_renderer.py -v 2>&1 | tail -10
```

Expected: ImportError(`credit_report_renderer` 不存在)。

- [ ] **Step 3: 写 renderer 文件**

写 `backend/app/agents/credit_report_renderer.py`:

```python
"""信贷调查报告 Pydantic schema → markdown 渲染器。

设计 ref: docs/superpowers/specs/2026-05-03-v0.8.2-credit-research-report-design.md § 2.4
"""

from __future__ import annotations

from app.agents.credit_report_schema import (
    CreditInvestigationReport,
    CreditRecommendation,
    FinancialAnalysis,
    FinancialMetric,
    RiskAssessment,
    RiskItem,
)

_DECISION_ZH: dict[str, str] = {
    "approve": "批准",
    "reject": "拒绝",
    "approve_with_conditions": "有条件批准",
}

_RISK_LEVEL_ZH: dict[str, str] = {
    "low": "低",
    "medium": "中",
    "high": "高",
    "very_high": "极高",
}

_SEVERITY_ZH: dict[str, str] = {"low": "低", "medium": "中", "high": "高"}


def _render_evidence(chunk_ids: list[str]) -> str:
    if not chunk_ids:
        return ""
    items = " · ".join(f"`{cid}`" for cid in chunk_ids)
    return f"\n> **引用**:{items}\n"


def _render_metric_table(metrics: list[FinancialMetric]) -> str:
    if not metrics:
        return "_暂无关键财务指标_\n"
    lines = ["| 指标 | 期间 | 数值 | 同比 |", "|---|---|---|---|"]
    for m in metrics:
        yoy = m.yoy_change if m.yoy_change else "—"
        lines.append(f"| {m.name} | {m.period} | {m.value} | {yoy} |")
    return "\n".join(lines) + "\n"


def _render_risk_items(items: list[RiskItem], category: str) -> str:
    if not items:
        return f"- _{category}:无_\n"
    out: list[str] = [f"### {category}"]
    for ri in items:
        sev = _SEVERITY_ZH.get(ri.severity, ri.severity)
        out.append(f"- **{ri.title}**(严重度:{sev}) — {ri.description}")
        if ri.mitigations:
            mits = " / ".join(ri.mitigations)
            out.append(f"  缓释措施:{mits}")
    return "\n".join(out) + "\n"


def render_credit_report_markdown(r: CreditInvestigationReport) -> str:  # noqa: PLR0915
    parts: list[str] = []

    # Header
    parts.append(f"# 信贷调查报告 — {r.company_name}\n")
    parts.append(f"_生成时间:{r.generated_at.isoformat()} · request_id:`{r.request_id}`_\n")

    # § 1 CompanyOverview
    co = r.company_overview
    parts.append("## § 1 基本信息\n")
    parts.append(f"{co.narrative}\n")
    if co.unified_credit_code:
        parts.append(f"- 统一社会信用代码:{co.unified_credit_code}")
    if co.registered_capital:
        parts.append(f"- 注册资本:{co.registered_capital}")
    parts.append(f"- 主营业务:{co.main_business}")
    if co.controlling_shareholder:
        parts.append(f"- 实际控制人:{co.controlling_shareholder}")
    if co.listing_status:
        parts.append(f"- 上市情况:{co.listing_status}")
    parts.append(_render_evidence(co.evidence))

    # § 2 LegalQualification
    lq = r.legal_qualification
    parts.append("## § 2 主体资格\n")
    parts.append(f"{lq.narrative}\n")
    parts.append(f"- 法律主体:{lq.legal_status}")
    if lq.business_qualifications:
        parts.append("- 经营资质:")
        for q in lq.business_qualifications:
            parts.append(f"  - {q}")
    if lq.adverse_records:
        parts.append("- 不良记录:")
        for a in lq.adverse_records:
            parts.append(f"  - {a}")
    else:
        parts.append("- 不良记录:无")
    parts.append(_render_evidence(lq.evidence))

    # § 3 FinancialAnalysis
    fa = r.financial_analysis
    parts.append("## § 3 财务分析\n")
    parts.append(f"{fa.narrative}\n")
    parts.append("### 关键财务指标\n")
    parts.append(_render_metric_table(fa.key_metrics))
    parts.append(f"### 偿债能力\n{fa.solvency_analysis}\n")
    parts.append(f"### 盈利能力\n{fa.profitability_analysis}\n")
    parts.append(f"### 现金流分析\n{fa.cash_flow_analysis}\n")
    if fa.year_over_year_summary:
        parts.append(f"### 同比变化\n{fa.year_over_year_summary}\n")
    parts.append(_render_evidence(fa.evidence))

    # § 4 IndustryAnalysis
    ia = r.industry_analysis
    parts.append("## § 4 行业分析\n")
    parts.append(f"{ia.narrative}\n")
    parts.append(f"- 所属行业:{ia.industry_name}")
    parts.append(f"- 景气度:{ia.industry_outlook}")
    parts.append(f"- 竞争地位:{ia.competitive_position}")
    if ia.key_competitors:
        comps = " / ".join(ia.key_competitors)
        parts.append(f"- 主要竞争对手:{comps}")
    parts.append(f"- 政策影响:{ia.policy_impact}")
    parts.append(_render_evidence(ia.evidence))

    # § 5 RiskAssessment
    ra = r.risk_assessment
    parts.append("## § 5 风险评估\n")
    parts.append(f"{ra.narrative}\n")
    parts.append(f"**整体风险等级:{_RISK_LEVEL_ZH.get(ra.overall_risk_level, ra.overall_risk_level)}**\n")
    parts.append(_render_risk_items(ra.operational_risks, "经营风险"))
    parts.append(_render_risk_items(ra.financial_risks, "财务风险"))
    parts.append(_render_risk_items(ra.industry_risks, "行业风险"))
    parts.append(_render_risk_items(ra.compliance_risks, "合规风险"))
    parts.append(_render_evidence(ra.evidence))

    # § 6 CreditRecommendation
    cr = r.credit_recommendation
    parts.append("## § 6 信贷建议\n")
    parts.append(f"{cr.narrative}\n")
    decision_zh = _DECISION_ZH.get(cr.decision, cr.decision)
    parts.append(f"**决策建议:{decision_zh}**\n")
    if cr.recommended_credit_limit:
        parts.append(f"- 建议额度:{cr.recommended_credit_limit}")
    if cr.recommended_term:
        parts.append(f"- 建议期限:{cr.recommended_term}")
    if cr.recommended_rate_range:
        parts.append(f"- 建议利率区间:{cr.recommended_rate_range}")
    if cr.guarantee_requirements:
        parts.append("- 担保要求:")
        for g in cr.guarantee_requirements:
            parts.append(f"  - {g}")
    if cr.conditions:
        parts.append("- 附加条件:")
        for c in cr.conditions:
            parts.append(f"  - {c}")
    parts.append(_render_evidence(cr.evidence))

    return "\n".join(parts)
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
cd backend
uv run pytest tests/unit/test_credit_report_renderer.py -v 2>&1 | tail -10
```

Expected: 5 PASS。

- [ ] **Step 5: 跑 mypy**

```bash
cd backend
uv run mypy app/agents/credit_report_renderer.py 2>&1 | tail -3
```

Expected: Success。

- [ ] **Step 6: Commit**

```bash
git add backend/app/agents/credit_report_renderer.py backend/tests/unit/test_credit_report_renderer.py
git commit -m "$(cat <<'EOF'
feat(agents): credit_report markdown renderer (v0.8.2)

Schema → markdown renderer for CreditInvestigationReport. Renders 6
sections with anchor headings, financial metrics as table, risk items
grouped by category with severity, and evidence chunk_ids per section
as a markdown blockquote footer (`> **引用**:...`).

Idempotent (same schema → same markdown). Source of truth is the
schema; renderer is presentation-only.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4 — ResearchState + critic_subgraph 加 `credit_report` field

**Files:**
- Modify: `backend/app/agents/schemas.py`(ResearchState 加字段 + import)
- Modify: `backend/app/orchestration/critic_subgraph.py:32`(透传字段)

- [ ] **Step 1: Read schemas.py:152 ResearchState 现状**

```bash
cd backend
sed -n '145,180p' app/agents/schemas.py
```

观察 ResearchState 现有字段 + 决定 credit_report 加哪个位置(建议放 report_markdown / chart_specs 附近)。

- [ ] **Step 2: 改 schemas.py:加 import + field**

读 `backend/app/agents/schemas.py`,在文件顶部 `from pydantic import ...` 区下面 加(如已 import 跳):

```python
from app.agents.credit_report_schema import CreditInvestigationReport
```

在 `ResearchState` 类内现有 `report_markdown` 和 `chart_specs` 字段附近加:

```python
credit_report: CreditInvestigationReport | None = None
```

- [ ] **Step 3: Read critic_subgraph.py:30-50 现状**

```bash
sed -n '25,60p' backend/app/orchestration/critic_subgraph.py
```

观察 critic subgraph state 定义,找到 report_markdown 那条字段。

- [ ] **Step 4: 改 critic_subgraph.py 加透传字段**

读 `backend/app/orchestration/critic_subgraph.py`,在文件顶部加 import:

```python
from app.agents.credit_report_schema import CreditInvestigationReport
```

在 line 32 `report_markdown: str | None = None` 附近加:

```python
credit_report: CreditInvestigationReport | None = None
```

在 line 57 附近 (subgraph build 部分,把 ResearchState → CriticSubState 拷贝时)加传递:

```python
credit_report=s.credit_report,
```

(具体行号取决于现有代码,grep `report_markdown=s.report_markdown` 找到位置后在同一 dict 里加 `credit_report=s.credit_report`)

- [ ] **Step 5: 写 backward-compat smoke test(确保 default None 不挂)**

```bash
cd backend
uv run python - <<'PYEOF'
from app.agents.schemas import ResearchState
s = ResearchState(request_id="test", user_message="hi", thread_id="t1")
assert s.credit_report is None
print("ResearchState.credit_report defaults None ✓")
from app.orchestration.critic_subgraph import CriticSubState  # 假设 class 名,grep verify
PYEOF
```

(若 CriticSubState 类名不同,改成实际名;grep `class.*Critic.*State` 找)

Expected: `ResearchState.credit_report defaults None ✓` 输出无 exception。

- [ ] **Step 6: 跑 mypy + 全 unit/integration 防回归**

```bash
cd backend
uv run mypy app/agents/schemas.py app/orchestration/critic_subgraph.py 2>&1 | tail -3
uv run pytest tests/unit tests/integration -q 2>&1 | tail -5
```

Expected: mypy Success;全 unit + integration PASS(default None 是 backward-compat,旧 cassette 和老测试不挂)。

- [ ] **Step 7: Commit**

```bash
git add backend/app/agents/schemas.py backend/app/orchestration/critic_subgraph.py
git commit -m "$(cat <<'EOF'
feat(state): add ResearchState.credit_report field (v0.8.2)

CreditInvestigationReport | None = None default — backward compat for
existing cassettes (v0.5/v0.6/v0.7/v0.8.1 cassettes recorded without
this field still validate). Critic subgraph state mirrors the field
for read-only pass-through (critic doesn't write report).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5 — Writer agent 改造 + prompt 重写(TDD,L1 mock)

**Files:**
- Modify: `backend/app/agents/writer.py`(完整重写)
- Create: `backend/tests/integration/test_writer_credit_report.py`

- [ ] **Step 1: 写 failing L1 integration test**

写 `backend/tests/integration/test_writer_credit_report.py`:

```python
"""L1 integration: Writer agent emits CreditInvestigationReport (v0.8.2)."""

from __future__ import annotations

import json
from datetime import datetime
from typing import Any

import pytest

from app.agents.credit_report_schema import CreditInvestigationReport
from app.agents.schemas import Insight, ResearchState
from app.agents.writer import Writer
from app.services.llm_response import LLMResponse


class _StubLlm:
    """Returns a fixed schema-conformant CreditInvestigationReport JSON."""

    def __init__(self, report_json: str) -> None:
        self.report_json = report_json
        self.last_schema: Any = None

    def chat(  # noqa: PLR0913
        self,
        prompt: str,
        tier: str = "balanced",
        schema: Any = None,
        request_id: str | None = None,
        parent_span_id: str | None = None,
    ) -> LLMResponse:
        self.last_schema = schema
        # auto-parse path (simulate v0.8.2 LLMService extension)
        if isinstance(schema, type) and issubclass(schema, CreditInvestigationReport):
            parsed = CreditInvestigationReport.model_validate_json(self.report_json)
        else:
            parsed = None
        return LLMResponse(
            content=self.report_json,
            parsed=parsed,
            model="deepseek-v4-flash",
            tier="balanced",
            prompt_tokens=10,
            completion_tokens=200,
            total_tokens=210,
            cost_cny=0.001,
            latency_ms=100,
            request_id=request_id or "req-test",
        )


@pytest.fixture
def stub_report_json() -> str:
    return json.dumps(
        {
            "company_name": "测试公司",
            "request_id": "will-be-overwritten",
            "generated_at": datetime(2026, 5, 3).isoformat(),
            "company_overview": {
                "narrative": "测试综述。", "main_business": "测试业务。",
                "evidence": ["c1::0"],
            },
            "legal_qualification": {
                "narrative": "合规。", "legal_status": "正常",
                "business_qualifications": [], "adverse_records": [],
                "evidence": ["c1::1"],
            },
            "financial_analysis": {
                "narrative": "财务稳健。",
                "key_metrics": [{"name": "营收", "value": "100", "period": "2024", "yoy_change": None}],
                "solvency_analysis": "强。", "profitability_analysis": "稳定。",
                "cash_flow_analysis": "健康。",
                "year_over_year_summary": None,
                "evidence": ["c1::2"],
            },
            "industry_analysis": {
                "narrative": "景气。", "industry_name": "X", "industry_outlook": "稳",
                "competitive_position": "前列", "key_competitors": [], "policy_impact": "无",
                "evidence": ["c1::3"],
            },
            "risk_assessment": {
                "narrative": "可控。", "operational_risks": [], "financial_risks": [],
                "industry_risks": [], "compliance_risks": [],
                "overall_risk_level": "low",
                "evidence": ["c1::4"],
            },
            "credit_recommendation": {
                "narrative": "建议批准。", "decision": "approve",
                "recommended_credit_limit": None, "recommended_term": None,
                "recommended_rate_range": None,
                "guarantee_requirements": [], "conditions": [],
                "evidence": ["c1::5"],
            },
        }
    )


def _state_with_insights() -> ResearchState:
    return ResearchState(
        request_id="req-test-write-001",
        user_message="评估贵州茅台 50 亿元 3 年期信贷",
        thread_id="t-1",
        insights=[
            Insight(subtask_id="s1", finding="茅台营收 819 亿元", confidence=0.9),
        ],
    )


def test_writer_emits_credit_report(stub_report_json: str) -> None:
    state = _state_with_insights()
    writer = Writer(llm=_StubLlm(stub_report_json))
    sr = writer.step(state)

    assert "credit_report" in sr.state_update
    report = sr.state_update["credit_report"]
    assert isinstance(report, CreditInvestigationReport)
    assert report.request_id == "req-test-write-001"   # writer overwrites
    assert report.credit_recommendation.decision == "approve"


def test_writer_passes_schema_to_llm(stub_report_json: str) -> None:
    """Writer must call LLMService.chat with schema=CreditInvestigationReport."""
    state = _state_with_insights()
    stub = _StubLlm(stub_report_json)
    writer = Writer(llm=stub)
    writer.step(state)
    assert stub.last_schema is CreditInvestigationReport


def test_writer_renders_markdown(stub_report_json: str) -> None:
    state = _state_with_insights()
    writer = Writer(llm=_StubLlm(stub_report_json))
    sr = writer.step(state)
    md = sr.state_update["report_markdown"]
    assert "# 信贷调查报告 — 测试公司" in md
    assert "## § 1 基本信息" in md
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd backend
uv run pytest tests/integration/test_writer_credit_report.py -v 2>&1 | tail -10
```

Expected: FAIL — Writer 尚未改造,old writer 输出 report_markdown(free text)+ chart_specs。

- [ ] **Step 3: 整体重写 `backend/app/agents/writer.py`**

完整覆盖文件内容(67 行 → 新版本):

```python
"""Writer — emits CreditInvestigationReport schema-conformant JSON (v0.8.2).

设计 ref: docs/superpowers/specs/2026-05-03-v0.8.2-credit-research-report-design.md § 3
"""

from __future__ import annotations

from datetime import datetime

from app.agents.base import Agent
from app.agents.credit_report_renderer import render_credit_report_markdown
from app.agents.credit_report_schema import CreditInvestigationReport
from app.agents.schemas import ResearchState, StepResult
from app.services.llm_response import Tier

_SYSTEM_PROMPT = """你是银行公司金融部 / 信贷研究分析师。

任务:基于已收集的 Insights + 用户提供的企业名 + 信贷场景,产出一份**信贷调查报告**(JSON 格式),
报告将作为信贷审批决策的依据。

报告结构(全部必填):
1. § 基本信息(CompanyOverview):企业基本信息,主营业务一句话
2. § 主体资格(LegalQualification):法律主体合规情况、经营资质、不良记录
3. § 财务分析(FinancialAnalysis):3-4 个关键财务指标 + 偿债 / 盈利 / 现金流三段分析
4. § 行业分析(IndustryAnalysis):所属行业、景气度、竞争地位、政策影响
5. § 风险评估(RiskAssessment):经营 / 财务 / 行业 / 合规四类风险 + 整体等级
6. § 信贷建议(CreditRecommendation):决策建议(批准 / 拒绝 / 有条件批准) + 额度 / 期限 / 利率 / 担保

**强制要求**:
- 每个 section 必须填 evidence 字段(chunk_id 列表,至少 1 个)
- 所有 narrative 用规范中文金融术语(资产负债率 / 经营性现金流 / 不良贷款率 等)
- 风险等级和决策建议要保守:有重大风险信号时建议 reject 或 approve_with_conditions
- 数据来自 Insights / KB / Web 搜索,无依据的结论不要编造,在 narrative 里诚实声明 limitation

输出符合 CreditInvestigationReport schema 的 JSON,**不要**输出 markdown / 解释 / 多余文字。
"""


def build_credit_report_prompt(state: ResearchState) -> str:
    insights_str = "\n".join(
        f"- [{i.subtask_id}] {i.finding}(confidence={i.confidence})" for i in state.insights
    )
    return (
        _SYSTEM_PROMPT
        + f"\n\n# Insights\n{insights_str}\n"
        + f"\n# 用户原始需求 / 信贷场景\n{state.user_message}\n"
        + "\n请按 schema 输出 JSON。"
    )


class Writer(Agent):
    name = "Writer"
    model_tier: Tier = "balanced"

    def step(self, state: ResearchState) -> StepResult:  # type: ignore[override]
        prompt = build_credit_report_prompt(state)
        r = self._llm.chat(
            prompt=prompt,
            tier=self.model_tier,
            schema=CreditInvestigationReport,
            request_id=state.request_id,
        )
        if not isinstance(r.parsed, CreditInvestigationReport):
            raise RuntimeError(
                f"Writer expected CreditInvestigationReport from LLMService.chat(parsed=), "
                f"got {type(r.parsed).__name__}"
            )
        report = r.parsed
        # writer 覆盖 request_id 和 generated_at(LLM 输出值不可信)
        report = report.model_copy(update={
            "request_id": state.request_id,
            "generated_at": datetime.now(),
        })

        markdown = render_credit_report_markdown(report)

        return StepResult(
            state_update={
                "credit_report": report,
                "report_markdown": markdown,   # 兼容老 SSE / 前端
                "chart_specs": [],             # v0.8.2 不做 chart;v0.8.5 加回
            },
            span_metadata={"agent": "Writer", "model": r.model, "cost_cny": r.cost_cny},
        )
```

- [ ] **Step 4: Run new tests + 跑全 unit/integration 防回归**

```bash
cd backend
uv run pytest tests/integration/test_writer_credit_report.py -v 2>&1 | tail -10
uv run pytest tests/unit tests/integration -q 2>&1 | tail -5
```

Expected:
- new test 3 PASS
- 全 unit + integration PASS(老 writer 测试若有 — grep `test_writer` 看是否需要更新)

如果 `tests/integration/test_writer*.py` 老测试 fail(老 writer 输出形态变了),case-by-case 评估:
- 老 test 测的是"writer 产 markdown"行为 → 需要更新为新行为
- 老 test 测的是 critic / planner 跟 writer 的 contract → 通常通过 `report_markdown` / `chart_specs` 兼容字段不挂

- [ ] **Step 5: mypy strict spot check**

```bash
cd backend
uv run mypy app/agents/writer.py 2>&1 | tail -3
```

Expected: Success。

- [ ] **Step 6: Commit**

```bash
git add backend/app/agents/writer.py backend/tests/integration/test_writer_credit_report.py
git commit -m "$(cat <<'EOF'
refactor(writer): emit CreditInvestigationReport schema (v0.8.2)

Writer rewritten to:
- prompt analyst as 银行公司金融部 / 信贷研究分析师
- call LLMService.chat(schema=CreditInvestigationReport) for structured output
- overwrite request_id + generated_at on the parsed report (LLM values not trusted)
- render markdown via credit_report_renderer (schema is source of truth)
- emit state_update.credit_report (new) + report_markdown (legacy compat) + chart_specs=[] (v0.8.5)

Charts deferred to v0.8.5 per Spec B § 10.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6 — research_nodes SSE 适配 verify

**Files:**
- Modify(potentially): `backend/app/orchestration/research_nodes.py`

- [ ] **Step 1: Read research_nodes.py 现状**

```bash
cd backend
rg -n "writer\.step\|report_markdown\|chart_specs\|sse" app/orchestration/research_nodes.py 2>&1 | head -20
sed -n '1,50p' app/orchestration/research_nodes.py
```

观察:
- writer node 怎么把 state_update 应用到 state
- SSE 事件触发(若有)是怎么发 report_markdown / chart_specs

- [ ] **Step 2: Verify state_update 透传 credit_report**

写一个 quick spike(不留代码,只 verify):

```bash
cd backend
uv run python - <<'PYEOF'
from app.agents.schemas import ResearchState
from app.agents.credit_report_schema import CreditInvestigationReport
from datetime import datetime
# Simulate state_update with credit_report
s = ResearchState(request_id="x", user_message="y", thread_id="z")
upd = {
    "credit_report": None,  # smoke
    "report_markdown": "# test",
    "chart_specs": [],
}
new_s = s.model_copy(update=upd)
assert new_s.report_markdown == "# test"
print("State update with credit_report = compat ✓")
PYEOF
```

Expected: ✓ 输出无 exception。

- [ ] **Step 3: 决定是否需要改 research_nodes.py**

- 如果 research_nodes.py 只是把 writer.step 的 state_update 直接 merge 到 state(LangGraph reducer 自动)→ **不需要改**
- 如果 research_nodes.py 显式 read `state.report_markdown` 推 SSE 事件 → 不动(report_markdown 已被 renderer 填充,流向不变)
- 如果 research_nodes.py 显式 read `state.chart_specs` 推 SSE chart 事件 → chart_specs=[] 时事件不发(自然降级)

**预期**:不需要改 research_nodes.py。**Step 4 verify 之**。

- [ ] **Step 4: 跑 v0.5 research_agent cassette 防回归**

```bash
cd backend
uv run pytest tests/e2e/test_research_agent_cassette.py -v 2>&1 | tail -10
```

Expected: 全 PASS(v0.5 cassette 应该在 ResearchState.credit_report=None 默认下 replay 通过)。

如果 fail → 检查具体 fail 原因:
- schema validation fail → ResearchState 加字段方式有问题(回 Task 4 fix)
- SSE event 多了 / 少了 → research_nodes 真有逻辑依赖,需要 fix(再加一步)

- [ ] **Step 5: Commit(若有改动)**

```bash
# 如果 Step 3 决定不改 research_nodes.py,跳过 Step 5(纯 verify task)
# 如果改了:
git add backend/app/orchestration/research_nodes.py
git commit -m "$(cat <<'EOF'
chore(orchestration): research_nodes verify compat with credit_report (v0.8.2)

[describe whatever you actually changed, e.g. "explicit credit_report
SSE event after writer node" or "no change needed"]

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7 — B-1 茅台端到端 e2e cassette 录制

**Files:**
- Create: `backend/tests/e2e/test_b1_maotai_credit_report_cassette.py`
- Create: `backend/tests/fixtures/cassettes/test_b1_maotai_credit_report_cassette/*.yaml`(录制)

- [ ] **Step 1: 写 test 文件**

写 `backend/tests/e2e/test_b1_maotai_credit_report_cassette.py`:

```python
"""L2 e2e — B-1 信贷尽调 茅台端到端 cassette (v0.8.2)."""

from __future__ import annotations

from typing import Any

import pytest

from app.agents.credit_report_schema import CreditInvestigationReport
from app.router.research import build_research_pipeline_from_env

pytestmark = pytest.mark.vcr


@pytest.fixture
async def b1_pipeline(
    milvus_test_container: dict[str, Any],
    monkeypatch: pytest.MonkeyPatch,
):
    monkeypatch.setenv("KB_MODE", "real")
    monkeypatch.setenv("BOCHA_MODE", "real")
    monkeypatch.setenv("MILVUS_HOST", str(milvus_test_container["host"]))
    monkeypatch.setenv("MILVUS_PORT", str(milvus_test_container["port"]))
    return build_research_pipeline_from_env()


@pytest.mark.asyncio
async def test_b1_maotai_credit_report_e2e_cassette(b1_pipeline: Any) -> None:
    """B-1 端到端:输入茅台 + 信贷场景,产出符合 CreditInvestigationReport schema 的报告。"""
    user_message = (
        "评估贵州茅台(600519.SH)50 亿元 3 年期流动资金贷款。"
        "请输出完整信贷调查报告。"
    )
    final_state = await b1_pipeline.run(user_message=user_message, thread_id="b1-maotai-test-1")

    # 1. State 含 credit_report
    assert final_state.credit_report is not None
    report = final_state.credit_report
    assert isinstance(report, CreditInvestigationReport)

    # 2. company_name 锚定茅台
    assert "茅台" in report.company_name

    # 3. 6 sections 都非 None(schema 强制必填,这一行实际是 redundant 但 sanity)
    assert report.company_overview is not None
    assert report.legal_qualification is not None
    assert report.financial_analysis is not None
    assert report.industry_analysis is not None
    assert report.risk_assessment is not None
    assert report.credit_recommendation is not None

    # 4. 每个 section evidence 非空(schema min_length=1 强制)
    assert len(report.company_overview.evidence) >= 1
    assert len(report.financial_analysis.evidence) >= 1
    assert len(report.credit_recommendation.evidence) >= 1

    # 5. decision 是合法值
    assert report.credit_recommendation.decision in {
        "approve", "reject", "approve_with_conditions"
    }

    # 6. markdown 已渲染且足够长
    assert final_state.report_markdown is not None
    assert len(final_state.report_markdown) > 1500
    assert "# 信贷调查报告 — " in final_state.report_markdown
```

(注:如果 `build_research_pipeline_from_env` 名字 / 调用方式不匹配实际,grep `app/router/research.py` 找正确入口名 + 调整 — 实施时 fix。)

- [ ] **Step 2: 录 cassette**

前置:
- Milvus 容器 + 6000+ chunks(Task 0 Step 3 已 verify)
- post-pay DASHSCOPE 后付费 endpoint(Task 0 Step 2 已 verify)

```bash
cd backend
mkdir -p tests/fixtures/cassettes/test_b1_maotai_credit_report_cassette/
unset all_proxy https_proxy http_proxy
set -a && source .env && set +a
VCR_RECORD_MODE=rewrite uv run pytest tests/e2e/test_b1_maotai_credit_report_cassette.py -v 2>&1 | tail -30
```

Expected: PASS(可能 dogfood prompt 调整后再次)。撞到 max_tokens truncation → writer step 内 max_tokens 升 4096(memory `feedback_max_tokens_per_model_calibration`,inline policy)。

如果 Pydantic ValidationError(LLM 输出不合 schema)→ 调 prompt(在 Task 8 dogfood 阶段处理),先 record 一次得到一份基线 cassette。

- [ ] **Step 3: Verify cassette 内容 + sanitize**

```bash
ls backend/tests/fixtures/cassettes/test_b1_maotai_credit_report_cassette/
grep -m3 'uri:' backend/tests/fixtures/cassettes/test_b1_maotai_credit_report_cassette/*.yaml
grep -aoE '"model":"[^"]+"' backend/tests/fixtures/cassettes/test_b1_maotai_credit_report_cassette/*.yaml | sort -u
grep -iE "Authorization: |sk-[a-zA-Z0-9]{20,}" backend/tests/fixtures/cassettes/test_b1_maotai_credit_report_cassette/*.yaml
```

Expected:
- uri host = `dashscope.aliyuncs.com`(LLM)+ Bocha host(web search)+ Milvus 本地(可能不进 cassette)
- model 主要是 `deepseek-v4-flash`(可能含 `qwen-plus` 如 search tier 被 hit)
- 无残留 sk-* token,只有 `Authorization: [filtered]` / `Authorization: REDACTED`

- [ ] **Step 4: Replay verify(模拟无 key)**

```bash
DASHSCOPE_API_KEY="" VCR_RECORD_MODE=none uv run pytest tests/e2e/test_b1_maotai_credit_report_cassette.py -v 2>&1 | tail -10
```

Expected: PASS(纯 replay)。

- [ ] **Step 5: Commit**

```bash
git add backend/tests/e2e/test_b1_maotai_credit_report_cassette.py backend/tests/fixtures/cassettes/test_b1_maotai_credit_report_cassette/
git commit -m "$(cat <<'EOF'
test(v0.8.2): L2 e2e — B-1 maotai credit report cassette

End-to-end recording of B-1 信贷尽调 流程: input 贵州茅台 + 50 亿元 3
年期信贷场景 → planner → data_collector(KB + Bocha) → analyst →
critic → writer (CreditInvestigationReport schema) → renderer.

KB_MODE=real (Milvus + 13 PDF KB ingest) + BOCHA_MODE=real for full
fidelity. Asserts schema-conformant report with non-empty evidence per
section, valid decision enum, markdown > 1500 chars.

VCR_RECORD_MODE=rewrite per v0.8.1 spec § 6 R2 (avoid new_episodes
append-pollution).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 8 — Dogfood prompt 调试 + 全 ci verify

**Files:** May modify `backend/app/agents/writer.py`(prompt 迭代)

- [ ] **Step 1: 完整 ci pipeline**

```bash
cd backend
unset all_proxy https_proxy http_proxy
uv run poe ci 2>&1 | tail -20
```

Expected:
- ruff format / ruff check pass
- mypy strict pass(全 backend)
- pytest unit + integration full PASS
- e2e cassette tests:`VCR_RECORD_MODE` 默认 none → 纯 replay,Task 7 录的 cassette 应 PASS

记下任何 FAIL,case-by-case 修。

- [ ] **Step 2: Smoke dogfood — 跑 1 次完整 B-1 看输出**

```bash
cd backend
unset all_proxy https_proxy http_proxy
set -a && source .env && set +a
KB_MODE=real BOCHA_MODE=real uv run python - <<'PYEOF'
import asyncio
from app.router.research import build_research_pipeline_from_env

async def main():
    pipeline = build_research_pipeline_from_env()
    result = await pipeline.run(
        user_message="评估贵州茅台 50 亿元 3 年期流动资金贷款,请输出信贷调查报告",
        thread_id="dogfood-1",
    )
    if result.credit_report:
        print(result.report_markdown[:2000])
        print("...")
        print(f"[total markdown len: {len(result.report_markdown)}]")
    else:
        print("NO REPORT")
PYEOF
```

(具体调用名 verify 实施时调整,取自 Task 7 Step 1 同样入口)

- [ ] **Step 3: 评估输出质量并迭代 prompt(0-N 轮)**

人工评估 markdown:
- 6 sections 都填了吗?narrative 有没有空话 / 套话?
- evidence 引用质量(chunk 真支持结论吗)?
- 风险等级判断保守度合不合理?
- 中文金融术语规范(没有英文夹杂 / 翻译腔)?
- 决策建议有没有具体数字依据?

评估不通过 → 改 `build_credit_report_prompt` 里 `_SYSTEM_PROMPT`,例如:
- 风险评估太乐观 → 加"对应收账款 / 现金流 / 政策依赖度等保持职业怀疑"
- 数据依据不足时编造 → 加"数据缺失时在 narrative 里诚实声明 'KB 中未检索到 ...,建议补充材料'"
- 中文术语不规范 → 加"使用规范财务术语:资产负债率(不要用 D/A ratio)、经营性现金流(不要用 OCF)等"

每轮迭代 → 重跑 Step 2 → 评估 → 决定再迭代或收敛。

预期 0-5 轮收敛(prompt 草稿 Task 5 已经覆盖大部分约束,极端情况下需要更多迭代)。

- [ ] **Step 4: 若 prompt 改了,重录 cassette**

```bash
cd backend
rm -rf tests/fixtures/cassettes/test_b1_maotai_credit_report_cassette/
mkdir -p tests/fixtures/cassettes/test_b1_maotai_credit_report_cassette/
unset all_proxy https_proxy http_proxy
set -a && source .env && set +a
VCR_RECORD_MODE=rewrite uv run pytest tests/e2e/test_b1_maotai_credit_report_cassette.py -v
```

Expected: PASS。然后跑 Task 7 Step 3 verify cassette 内容 + sanitize。

- [ ] **Step 5: 最终全 ci sanity**

```bash
cd backend
unset all_proxy https_proxy http_proxy
uv run poe ci 2>&1 | tail -10
```

Expected: 全 PASS。

- [ ] **Step 6: Commit prompt 迭代(若有)**

```bash
# 如 Step 3 没改 prompt,跳过 Step 6;如改了:
git add backend/app/agents/writer.py backend/tests/fixtures/cassettes/test_b1_maotai_credit_report_cassette/
git commit -m "$(cat <<'EOF'
chore(writer): tune credit report prompt after B-1 maotai dogfood

Iterations from N rounds of dogfood:
- [list specific prompt adjustments]

Cassette re-recorded with refined prompt.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 9 — PR open

**Files:** None(git 操作)

- [ ] **Step 1: 检查 commit history 整洁**

```bash
git log origin/main..HEAD --oneline
```

Expected: ~9 个 commit(2 个 spec + 1 schema + 1 LLMService + 1 renderer + 1 state + 1 writer + 1 cassette + 0-1 prompt 迭代)。按时间线讲故事。

- [ ] **Step 2: Push branch**

```bash
git push -u origin feat/v0.8.2-credit-research-report
```

- [ ] **Step 3: 开 PR**

```bash
gh pr create --title "feat(v0.8.2): credit investigation report schema + Writer refactor + B-1 maotai e2e" --body "$(cat <<'EOF'
## Summary

- Introduces `CreditInvestigationReport` Pydantic schema with 6 sections (CompanyOverview / LegalQualification / FinancialAnalysis / IndustryAnalysis / RiskAssessment / CreditRecommendation) and per-section `evidence: list[chunk_id]` for D1 strong citation.
- Extends `LLMService.chat(schema=)` to accept Pydantic class with auto JSON-schema conversion + auto Pydantic validation (backward compatible with dict path).
- Rewrites Writer agent to emit schema-conformant JSON; markdown derived via new `credit_report_renderer` module.
- B-1 端到端 cassette on 贵州茅台 with KB_MODE=real (13 PDF Milvus KB) + BOCHA_MODE=real (web search).
- Adds `ResearchState.credit_report` field (default None — backward compat for existing cassettes).

## Brainstorming context

Spec: `docs/superpowers/specs/2026-05-03-v0.8.2-credit-research-report-design.md`
Roadmap: `docs/superpowers/specs/2026-05-03-product-positioning-v1-roadmap.md` § 9 v0.8.2-β
Plan: `docs/superpowers/plans/2026-05-03-v0.8.2-credit-research-report.md`

This is the first sub-version of the v1.0 product positioning roadmap (also written in this PR). v0.8.3 will add memory subsystem + B-3 客户跟踪简报; v0.8.4 will add BGE local embed + agent-level eval; v0.8.5 will add B-7 audit Q&A + citation UI.

## Test plan

- [x] `uv run poe ci` clean (ruff + mypy + unit + integration + e2e replay)
- [x] L0 unit: `test_credit_report_schema.py` (8) + `test_credit_report_renderer.py` (5) + `test_llm_service_pydantic_schema.py` (4)
- [x] L1 integration: `test_writer_credit_report.py` (3) — Writer mock LLM
- [x] L2 e2e: `test_b1_maotai_credit_report_cassette.py` (1) — 茅台端到端 cassette
- [x] manual smoke: B-1 茅台 dogfood → markdown 看着像样,prompt 迭代收敛

## Out of scope (deferred)

- Real tushare wiring (v0.8.3, with B-3)
- B-3 client tracking brief (v0.8.3)
- B-7 audit Q&A (v0.8.5)
- D1 citation click-through UI (v0.8.5; evidence schema field is here)
- D2 BGE / local LLM (v0.8.4)
- D6 agent-level eval (v0.8.4)
- Memory subsystem (v0.8.3)
- Charts (v0.8.5; `chart_specs=[]` placeholder)

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 4: 提示 user 后续动作**

PR 开完后,告诉 user:
1. PR URL
2. v0.8.2-β ship 后,简历"Agent 投研助手"标题下可补充第一条 functions 描述(Spec B § 12 提到):
   > 产出符合银行信贷调查报告模板的 6-section 结构化研究报告,每章节带 chunk-level evidence 追溯
3. memory `project_llm_service_contract` 需要更新(契约扩展记录),在 v0.8.2 ship 后 + 用户授权下做

---

## Self-Review

(writing-plans skill 执行后填写;不在 plan 文档里运行,只在 plan 完成时 author 自查)

**Spec coverage:**
- Spec § 0 背景 → Plan Task 0 verify env / branch
- Spec § 1 范围 8 块 → Plan Task 1-8 共 9 task
  - 块 1 schema → Task 1
  - 块 2 LLMService 扩展 → Task 2
  - 块 3 Writer → Task 5
  - 块 4 renderer → Task 3
  - 块 5 ResearchState + critic_subgraph → Task 4
  - 块 6 SSE 适配 → Task 6
  - 块 7 dogfood → Task 8
  - 块 8 测试 → 嵌在 Task 1/2/3/5/7 各 TDD 阶段
- Spec § 2 schema 6 sections → Task 1 完整覆盖
- Spec § 3 Writer 改造 → Task 5 完整覆盖
- Spec § 4 demo 公司茅台 → Task 7 + Task 8
- Spec § 5 文件改动清单 → Plan 各 Task Files 段对应
- Spec § 6 测试策略(L0/L1/L2)→ Task 1/2/3 (L0) / Task 5 (L1) / Task 7 (L2)
- Spec § 7 验收 gate → Task 8 Step 1 跑 ci
- Spec § 8 R1-R7 → 各 Task Step 提到对应 mitigation
- Spec § 9 cost → Task 7/8 实测,记 PR description
- Spec § 10 Out of scope → 不入 plan(显式 by design),记 PR description
- Spec § 11 memory cross-ref → 实施过程中保留 inline reference,memory 更新留 PR ship 后

**Placeholder scan:**
- 无 TBD / TODO
- Task 6 Step 3 "决定是否需要改" + Step 5 "若有改动" 是 spec § 5(SSE 路由"若需要")的实施期决策 inline 化,**不是 placeholder**(给了具体决策依据 + 命令)
- Task 8 Step 3 "迭代 prompt(0-N 轮)" 是 spec § 4.3 / R2 inline policy,prompt 调优**不在 plan 阶段冻结**(spec 显式声明),**不是 placeholder**

**Type consistency:**
- `CreditInvestigationReport` 类名一致 across Task 1/2/4/5/7
- `report.evidence` field name 一致(每 section 都用 `evidence: list[str]`)
- `state_update["credit_report"]` key 一致 across Task 5/6/7
- `ResearchState.credit_report` field name + type 一致
- `LLMService.chat(schema=...)` 签名一致(Task 2 定 + Task 5 用)

**已知 plan-spec gap:** 无。

**已知 plan 实施风险:**
- Task 7 写测试时 `build_research_pipeline_from_env` 入口名是猜的,实施时需 grep `app/router/research.py` verify 实际导出名
- Task 4 critic_subgraph.py 行号 32/57 是 spec 引用的 anchor,实施时需 verify 当前实际行号
- Task 6 是 verify task,grep 后可能发现需要改 SSE 事件触发,plan 留了灵活空间
