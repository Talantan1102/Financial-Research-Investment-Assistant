# v0.8.3 Implementation Plan — tushare 真接入 + B-3 持仓预警

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 LLM 生成 mock 的 tushare 替换为真接入(8 接口),并新建分层架构的 B-3 持仓预警引擎(signal layer + agent layer escalation),配套前端 3 页 + email 通知 + dogfood 5 客户。

**Architecture:** Tushare 走 Protocol + Real/Mock 双实现 + sqlite cache + rate limit(沿用 v0.6 BOCHA pattern,第 8 次应用)。Monitoring engine = 新模块 `app/services/monitoring/`,内部 SignalDetector 跑 5 个 SignalRule,红色 alert 触发 EscalationCoordinator 调用现有 ResearchGraph 出 deep_dive,经 EmailNotifier 发邮件 + 站内通知。前端 3 页(列表/详情/配置)用 Antd,**component-level 设计强制 invoke `taste` skill**。

**Tech Stack:** Python 3.11+ / FastAPI / Pydantic 2 / LangGraph 1.x / APScheduler / SQLAlchemy + sqlite / pytest + pytest-recording / smtplib + email.mime / Antd 5 + React 18 + TS / uv + ruff + mypy strict

**Spec anchor:** `docs/superpowers/specs/2026-05-04-v0.8.3-tushare-and-portfolio-monitoring-design.md`

**Branch:** `feat/v0.8.3-tushare-and-portfolio-monitoring` (从 main `7353d39` 起)

**关键约定**:
- 全部命令前缀 `uv run`(不用 conda)
- `cwd = repo root`(不是 `backend/`),`pytest` / `mypy` 都从 repo root 跑
- 测试时 `unset all_proxy https_proxy http_proxy`(memory `feedback_test_env_modeling`)
- 所有 cassette 走 `backend/tests/fixtures/cassettes/<module-stem>/`
- 所有新模块(`app/services/monitoring/*` / `app/services/tushare_*`)必须 mypy strict 通过
- legacy `app/service/mock_tushare_service.py` 仍在 mypy ignore;改造为 protocol-conforming 时不引入新 mypy strict 文件
- Commit message 走 conventional commits(`feat:` / `test:` / `fix:` / `chore:` / `docs:`);**fix 类必须带 `原因 layer: <impl|plan|spec>`**(memory `feedback_fix_commit_layer_marker`)

---

## File Structure(本 plan 涉及)

### 创建

```
backend/app/services/tushare_client.py            # HTTP client, ~120 LOC
backend/app/services/tushare_cache.py             # sqlite cache, ~150 LOC
backend/app/services/tushare_service.py           # Protocol + RealTushareService, ~250 LOC
backend/app/services/tushare_factory.py           # build_tushare_service, ~50 LOC
backend/app/services/monitoring/__init__.py
backend/app/services/monitoring/scheduler.py      # MonitoringScheduler, ~120 LOC
backend/app/services/monitoring/signal_detector.py # 编排 + 汇总, ~80 LOC
backend/app/services/monitoring/signal_rules/__init__.py
backend/app/services/monitoring/signal_rules/base.py             # ABC + types, ~60 LOC
backend/app/services/monitoring/signal_rules/defaults.py         # 默认阈值, ~40 LOC
backend/app/services/monitoring/signal_rules/financial_ratio.py  # ~80 LOC
backend/app/services/monitoring/signal_rules/cash_flow.py        # ~70 LOC
backend/app/services/monitoring/signal_rules/shareholder_count.py # ~60 LOC
backend/app/services/monitoring/signal_rules/announcement.py     # ~80 LOC(LLM)
backend/app/services/monitoring/signal_rules/price_anomaly.py    # ~70 LOC
backend/app/services/monitoring/escalation.py     # 三闸 + ResearchGraph 调用, ~150 LOC
backend/app/services/monitoring/monitoring_service.py # 门面, ~150 LOC
backend/app/services/monitoring/notifications/__init__.py
backend/app/services/monitoring/notifications/email_notifier.py  # SMTP, ~100 LOC
backend/app/services/monitoring/notifications/mock_email_notifier.py # ~50 LOC
backend/app/services/monitoring/notifications/factory.py # ~30 LOC
backend/app/agents/portfolio_warning_schema.py    # Pydantic schemas, ~150 LOC
backend/app/agents/portfolio_warning_renderer.py  # md renderer, ~80 LOC
backend/app/router/monitoring_router.py           # REST API, ~250 LOC
backend/app/scripts/init_monitoring_tables.py     # SQL migration, ~80 LOC
frontend/src/pages/monitoring/index.tsx           # 列表页, ~250 LOC(taste 后微调)
frontend/src/pages/monitoring/alert-detail.tsx    # 详情页, ~250 LOC
frontend/src/pages/monitoring/config.tsx          # 配置页, ~250 LOC
frontend/src/api/monitoring.ts                    # API client, ~100 LOC
frontend/src/types/monitoring.ts                  # TS types, ~80 LOC
backend/tests/unit/test_tushare_client.py
backend/tests/unit/test_tushare_cache.py
backend/tests/unit/test_tushare_service.py
backend/tests/unit/test_tushare_factory.py
backend/tests/unit/test_monitoring_signal_rules.py
backend/tests/unit/test_monitoring_signal_detector.py
backend/tests/unit/test_monitoring_escalation.py
backend/tests/unit/test_monitoring_email_notifier.py
backend/tests/unit/test_portfolio_warning_schema.py
backend/tests/unit/test_portfolio_warning_renderer.py
backend/tests/unit/test_monitoring_service.py
backend/tests/unit/test_monitoring_router.py
backend/tests/integration/test_monitoring_e2e_mock.py
backend/tests/e2e/test_tushare_real_cassette.py
backend/tests/e2e/test_monitoring_e2e_cassette.py
backend/tests/eval/test_b3_dogfood_golden.py
backend/tests/fixtures/cassettes/test_tushare_real_cassette/  # 8×5 episodes
backend/tests/fixtures/cassettes/test_monitoring_e2e_cassette/
```

### 修改

```
backend/app/agents/schemas.py                       # ResearchState 加 mode / alert_signals / deep_dive_section
backend/app/agents/writer.py                        # 加 alert_writer mode
backend/app/orchestration/research_nodes.py         # writer_node 适配 alert mode
backend/app/orchestration/research_graph.py         # 暴露 build_research_graph 给 escalation 复用
backend/app/tools/get_financials.py                 # __init__ 改接 TushareService Protocol
backend/app/tools/get_stock_quote.py                # __init__ 改接 TushareService Protocol
backend/app/orchestration/agent_graph.py            # DI wiring 改 build_tushare_service()
backend/app/service/mock_tushare_service.py         # 加 5 个新接口的 mock 方法 + 实现 Protocol
backend/app/service/scheduler_service.py            # 暴露 get_apscheduler_instance() 让 MonitoringScheduler 复用单例
backend/app/core/database.py                        # 启动时 init_monitoring_tables
backend/app/app_main.py                             # 注册 monitoring_router + 启动时 init monitoring scheduler
backend/.env.example                                # 加 TUSHARE_TOKEN / TUSHARE_MODE / EMAIL_MODE / SMTP_* / MONITORING_* env
frontend/src/router/index.tsx                       # 加 /monitoring 路由
frontend/src/layout/<sidebar component>             # 加 "持仓预警" 入口
```

---

## 总体顺序 + 依赖

| Task | 依赖 | 估工(天) |
|---|---|---|
| 1. TushareClient | — | 0.5 |
| 2. TushareCache | — | 0.5 |
| 3. TushareService Protocol + RealTushareService + factory | 1, 2 | 1 |
| 4. MockTushareService 扩 + 旧 tools 切换 | 3 | 0.5 |
| 5. Tushare cassettes | 1, 3 | 0.5 |
| 6. DB migration + init script | — | 0.5 |
| 7. SignalRule ABC + types + defaults | — | 0.5 |
| 8. 5 个 SignalRule 实现 | 3, 7 | 1.5 |
| 9. SignalDetector | 7, 8 | 0.5 |
| 10. PortfolioWarningReport schema + renderer | — | 1 |
| 11. EmailNotifier Protocol + Real + Mock + factory | — | 0.5 |
| 12. ResearchState 扩 + Writer alert mode | 10 | 1 |
| 13. EscalationCoordinator | 10, 12 | 1 |
| 14. MonitoringService + MonitoringScheduler | 6, 9, 11, 13 | 1 |
| 15. monitoring_router(REST API) | 14 | 1 |
| 16. Frontend 监控列表页(taste) | 15 | 1 |
| 17. Frontend alert 详情页(taste) | 15 | 1 |
| 18. Frontend 配置页(taste) | 15 | 1 |
| 19. e2e cassette + dogfood golden + 简历更新 | 14, 15, 18 | 1.5 |
| **合计** | | **14 天** |

(spec § 14 估 10.5-15.5 天,本 plan 中位数 14 天,接受超期理由见 spec § 14.2)

---

## Task 1: TushareClient

**Files:**
- Create: `backend/app/services/tushare_client.py`
- Create: `backend/tests/unit/test_tushare_client.py`

**Spec ref**: § 3.2

**Background**:
- Tushare Pro 协议:POST 到 `http://api.tushare.pro`,body 是 `{"api_name": "...", "token": "...", "params": {...}, "fields": "..."}`,response 是 `{"request_id": "...", "code": 0, "data": {"fields": [...], "items": [[...], ...]}}`
- 失败时 `code != 0`,`msg` 字段说明
- 用 `httpx.AsyncClient` 走 async
- **base_url 必须默认等于 cassette 录制时的 host**(memory `feedback_cassette_host_in_match_on`)

- [ ] **Step 1.1**: 写失败的 unit test

写入 `backend/tests/unit/test_tushare_client.py`:

```python
"""Unit tests for TushareClient — pure transport, no business logic."""
from __future__ import annotations

from unittest.mock import AsyncMock, MagicMock, patch

import pytest

from app.services.tushare_client import TushareClient, TushareError


@pytest.fixture
def fake_response_ok() -> dict:
    return {
        "request_id": "abc",
        "code": 0,
        "msg": None,
        "data": {
            "fields": ["ts_code", "trade_date", "close"],
            "items": [["600519.SH", "20240501", 1850.0], ["600519.SH", "20240502", 1860.0]],
        },
    }


@pytest.fixture
def fake_response_err() -> dict:
    return {"request_id": "x", "code": 40001, "msg": "token invalid", "data": None}


@pytest.mark.asyncio
async def test_call_returns_dataframe_on_ok(fake_response_ok: dict) -> None:
    fake_resp = MagicMock()
    fake_resp.status_code = 200
    fake_resp.json = MagicMock(return_value=fake_response_ok)
    fake_resp.raise_for_status = MagicMock()

    fake_post = AsyncMock(return_value=fake_resp)

    with patch("httpx.AsyncClient.post", fake_post):
        client = TushareClient(token="fake-token")
        df = await client.call("daily", {"ts_code": "600519.SH"})

    assert list(df.columns) == ["ts_code", "trade_date", "close"]
    assert len(df) == 2
    assert df.iloc[0]["close"] == 1850.0


@pytest.mark.asyncio
async def test_call_raises_tushare_error_on_nonzero_code(fake_response_err: dict) -> None:
    fake_resp = MagicMock()
    fake_resp.status_code = 200
    fake_resp.json = MagicMock(return_value=fake_response_err)
    fake_resp.raise_for_status = MagicMock()

    fake_post = AsyncMock(return_value=fake_resp)

    with patch("httpx.AsyncClient.post", fake_post):
        client = TushareClient(token="fake-token")
        with pytest.raises(TushareError, match="token invalid"):
            await client.call("daily", {"ts_code": "600519.SH"})


def test_default_base_url_matches_cassette_host() -> None:
    """Cassette host fixture default — feedback_cassette_host_in_match_on memory."""
    client = TushareClient(token="x")
    assert client.base_url == "http://api.tushare.pro"
```

- [ ] **Step 1.2**: 跑测试确认失败

```bash
unset all_proxy https_proxy http_proxy
uv run pytest backend/tests/unit/test_tushare_client.py -v
```
Expected: FAIL with `ModuleNotFoundError: No module named 'app.services.tushare_client'`

- [ ] **Step 1.3**: 实现 `TushareClient`

写入 `backend/app/services/tushare_client.py`:

```python
"""TushareClient — pure async HTTP transport for tushare Pro API.

No business logic, no cache, no rate limit. Returns pd.DataFrame.

Default base_url matches cassette recording host so VCR can replay
without env override (memory feedback_cassette_host_in_match_on).
"""
from __future__ import annotations

from typing import Any

import httpx
import pandas as pd


class TushareError(RuntimeError):
    """Raised when tushare API returns code != 0."""


class TushareClient:
    def __init__(
        self,
        token: str,
        base_url: str = "http://api.tushare.pro",
        timeout_s: float = 30.0,
    ) -> None:
        if not token:
            raise ValueError("token must not be empty")
        self._token = token
        self.base_url = base_url
        self._timeout_s = timeout_s

    async def call(
        self,
        api_name: str,
        params: dict[str, Any],
        fields: str | None = None,
    ) -> pd.DataFrame:
        body: dict[str, Any] = {
            "api_name": api_name,
            "token": self._token,
            "params": params,
        }
        if fields is not None:
            body["fields"] = fields

        async with httpx.AsyncClient(timeout=self._timeout_s) as session:
            resp = await session.post(self.base_url, json=body)
            resp.raise_for_status()
            payload = resp.json()

        code = payload.get("code")
        if code != 0:
            raise TushareError(
                f"tushare api {api_name} failed: code={code} msg={payload.get('msg')!r}"
            )

        data = payload.get("data") or {}
        cols = data.get("fields") or []
        rows = data.get("items") or []
        return pd.DataFrame(rows, columns=cols)
```

- [ ] **Step 1.4**: 跑测试确认通过

```bash
uv run pytest backend/tests/unit/test_tushare_client.py -v
```
Expected: 3 passed

- [ ] **Step 1.5**: mypy strict + ruff check

```bash
uv run mypy backend
uv run ruff check backend/app/services/tushare_client.py backend/tests/unit/test_tushare_client.py
```
Expected: 0 errors

- [ ] **Step 1.6**: Commit

```bash
git add backend/app/services/tushare_client.py backend/tests/unit/test_tushare_client.py
git commit -m "feat(tushare): TushareClient pure async HTTP transport for tushare Pro"
```

---

## Task 2: TushareCache

**Files:**
- Create: `backend/app/services/tushare_cache.py`
- Create: `backend/tests/unit/test_tushare_cache.py`

**Spec ref**: § 3.3

**Background**:
- 4 类 TTL:financial(income/balance/cashflow/fina_indicator/stk_holdernumber)→ 永久(按 end_date);anns/disclosure → 24h;daily → 当日 16:00 后冻结;default → 1h
- sqlite 表 `tushare_cache`,列 = (api_name, params_hash, response_blob, fetched_at, expires_at)
- 序列化 DataFrame 用 pyarrow / pickle;**用 pickle 简化**(纯本地缓存,不跨进程兼容)

- [ ] **Step 2.1**: 写失败的 unit test

写入 `backend/tests/unit/test_tushare_cache.py`:

```python
"""Unit tests for TushareCache — sqlite-based cache with TTL."""
from __future__ import annotations

import asyncio
from datetime import datetime, timedelta, timezone
from pathlib import Path

import pandas as pd
import pytest

from app.services.tushare_cache import TushareCache, classify_ttl


@pytest.fixture
def cache(tmp_path: Path) -> TushareCache:
    return TushareCache(db_path=tmp_path / "cache.sqlite")


@pytest.mark.asyncio
async def test_set_then_get_hits(cache: TushareCache) -> None:
    df = pd.DataFrame({"a": [1, 2], "b": ["x", "y"]})
    await cache.set("daily", {"ts_code": "600519.SH"}, df, ttl_s=60)
    got = await cache.get("daily", {"ts_code": "600519.SH"})
    assert got is not None
    pd.testing.assert_frame_equal(got, df)


@pytest.mark.asyncio
async def test_get_returns_none_on_miss(cache: TushareCache) -> None:
    got = await cache.get("daily", {"ts_code": "000001.SZ"})
    assert got is None


@pytest.mark.asyncio
async def test_get_returns_none_on_expired(cache: TushareCache) -> None:
    df = pd.DataFrame({"a": [1]})
    await cache.set("anns", {"ts_code": "x"}, df, ttl_s=0.01)
    await asyncio.sleep(0.05)
    got = await cache.get("anns", {"ts_code": "x"})
    assert got is None


def test_classify_ttl_financial() -> None:
    assert classify_ttl("income") == 365 * 24 * 3600  # 永久(用 1 年代表)
    assert classify_ttl("balance_sheet") == 365 * 24 * 3600
    assert classify_ttl("cashflow") == 365 * 24 * 3600
    assert classify_ttl("fina_indicator") == 365 * 24 * 3600
    assert classify_ttl("stk_holdernumber") == 365 * 24 * 3600


def test_classify_ttl_announcements() -> None:
    assert classify_ttl("anns") == 24 * 3600
    assert classify_ttl("disclosure_date") == 24 * 3600


def test_classify_ttl_daily() -> None:
    assert classify_ttl("daily") == 3600  # 1h(简化版,spec 写"当日 16:00",这里用 1h cache 起步)


def test_classify_ttl_default() -> None:
    assert classify_ttl("unknown_api") == 3600
```

- [ ] **Step 2.2**: 跑测试确认失败

```bash
uv run pytest backend/tests/unit/test_tushare_cache.py -v
```
Expected: FAIL `ModuleNotFoundError: No module named 'app.services.tushare_cache'`

- [ ] **Step 2.3**: 实现 `TushareCache`

写入 `backend/app/services/tushare_cache.py`:

```python
"""TushareCache — sqlite-backed cache for tushare API responses.

Per-api TTL classification (financial永久 / anns 24h / daily 1h / default 1h).
Uses pickle to round-trip pd.DataFrame.
"""
from __future__ import annotations

import asyncio
import hashlib
import json
import pickle
import sqlite3
import time
from pathlib import Path

import pandas as pd

# TTL classification per spec § 3.3
_TTL_FINANCIAL_S = 365 * 24 * 3600  # 财务接口"永久" — 用 1 年代表
_TTL_ANNS_S = 24 * 3600
_TTL_DAILY_S = 3600
_TTL_DEFAULT_S = 3600

_FINANCIAL_APIS = frozenset(
    {"income", "balance_sheet", "cashflow", "fina_indicator", "stk_holdernumber"}
)
_ANNS_APIS = frozenset({"anns", "disclosure_date"})


def classify_ttl(api_name: str) -> int:
    """Return cache TTL in seconds for given api."""
    if api_name in _FINANCIAL_APIS:
        return _TTL_FINANCIAL_S
    if api_name in _ANNS_APIS:
        return _TTL_ANNS_S
    if api_name == "daily":
        return _TTL_DAILY_S
    return _TTL_DEFAULT_S


def _hash_params(params: dict) -> str:
    raw = json.dumps(params, sort_keys=True, ensure_ascii=True)
    return hashlib.sha256(raw.encode("utf-8")).hexdigest()


class TushareCache:
    def __init__(self, db_path: Path) -> None:
        self._db_path = db_path
        self._lock = asyncio.Lock()
        self._init()

    def _init(self) -> None:
        self._db_path.parent.mkdir(parents=True, exist_ok=True)
        with sqlite3.connect(self._db_path) as conn:
            conn.execute(
                """
                CREATE TABLE IF NOT EXISTS tushare_cache (
                    api_name TEXT NOT NULL,
                    params_hash TEXT NOT NULL,
                    response_blob BLOB NOT NULL,
                    fetched_at REAL NOT NULL,
                    expires_at REAL NOT NULL,
                    PRIMARY KEY (api_name, params_hash)
                )
                """
            )

    async def get(self, api_name: str, params: dict) -> pd.DataFrame | None:
        async with self._lock:
            now = time.time()
            params_hash = _hash_params(params)
            with sqlite3.connect(self._db_path) as conn:
                cur = conn.execute(
                    "SELECT response_blob, expires_at FROM tushare_cache "
                    "WHERE api_name = ? AND params_hash = ?",
                    (api_name, params_hash),
                )
                row = cur.fetchone()
            if row is None:
                return None
            blob, expires_at = row
            if expires_at < now:
                return None
            df = pickle.loads(blob)
            assert isinstance(df, pd.DataFrame)
            return df

    async def set(
        self, api_name: str, params: dict, df: pd.DataFrame, ttl_s: float
    ) -> None:
        async with self._lock:
            now = time.time()
            params_hash = _hash_params(params)
            blob = pickle.dumps(df)
            with sqlite3.connect(self._db_path) as conn:
                conn.execute(
                    "INSERT OR REPLACE INTO tushare_cache "
                    "(api_name, params_hash, response_blob, fetched_at, expires_at) "
                    "VALUES (?, ?, ?, ?, ?)",
                    (api_name, params_hash, blob, now, now + ttl_s),
                )
```

- [ ] **Step 2.4**: 跑测试 + mypy

```bash
uv run pytest backend/tests/unit/test_tushare_cache.py -v
uv run mypy backend
```
Expected: 7 passed, 0 mypy errors

- [ ] **Step 2.5**: Commit

```bash
git add backend/app/services/tushare_cache.py backend/tests/unit/test_tushare_cache.py
git commit -m "feat(tushare): sqlite cache with per-api TTL classification"
```

---

## Task 3: TushareService Protocol + RealTushareService + factory

**Files:**
- Create: `backend/app/services/tushare_service.py`
- Create: `backend/app/services/tushare_factory.py`
- Create: `backend/tests/unit/test_tushare_service.py`
- Create: `backend/tests/unit/test_tushare_factory.py`

**Spec ref**: § 3.2

**Background**:
- 8 个接口包成 8 个 method,每个 method 内部包了:cache check → rate limiter acquire → client.call → cache set → return DataFrame
- Rate limiter:`RateLimiter(max_calls=400, window_s=60)`(tushare Pro 高积分 ≤ 500/min,留 100 buffer)
- Factory 读 env `TUSHARE_MODE`(default `mock`),`real` 时需 `TUSHARE_TOKEN`

- [ ] **Step 3.1**: 写失败的 unit test for service

写入 `backend/tests/unit/test_tushare_service.py`:

```python
"""Unit tests for RealTushareService — uses fake client + cache + rate limiter."""
from __future__ import annotations

from pathlib import Path
from typing import Any
from unittest.mock import AsyncMock

import pandas as pd
import pytest

from app.services.rate_limiter import RateLimiter
from app.services.tushare_cache import TushareCache
from app.services.tushare_service import RealTushareService, TushareService


class FakeTushareClient:
    def __init__(self) -> None:
        self.calls: list[tuple[str, dict]] = []

    async def call(self, api_name: str, params: dict[str, Any], fields: str | None = None) -> pd.DataFrame:
        self.calls.append((api_name, params))
        return pd.DataFrame({"ts_code": ["600519.SH"], "x": [1.0]})


@pytest.fixture
def cache(tmp_path: Path) -> TushareCache:
    return TushareCache(db_path=tmp_path / "cache.sqlite")


@pytest.fixture
def service(cache: TushareCache) -> tuple[RealTushareService, FakeTushareClient]:
    fake = FakeTushareClient()
    svc = RealTushareService(
        client=fake,  # type: ignore[arg-type]
        cache=cache,
        rate_limiter=RateLimiter(max_calls=10, window_s=60.0),
    )
    return svc, fake


@pytest.mark.asyncio
async def test_get_daily_calls_client_first_time(service: tuple[RealTushareService, FakeTushareClient]) -> None:
    svc, fake = service
    df = await svc.get_daily(ts_code="600519.SH", start="20240501", end="20240502")
    assert len(df) == 1
    assert len(fake.calls) == 1
    assert fake.calls[0][0] == "daily"


@pytest.mark.asyncio
async def test_get_daily_uses_cache_second_time(service: tuple[RealTushareService, FakeTushareClient]) -> None:
    svc, fake = service
    await svc.get_daily(ts_code="600519.SH", start="20240501", end="20240502")
    await svc.get_daily(ts_code="600519.SH", start="20240501", end="20240502")
    assert len(fake.calls) == 1  # 第二次不应再 call


@pytest.mark.parametrize(
    "method,api_name",
    [
        ("get_income", "income"),
        ("get_fina_indicator", "fina_indicator"),
        ("get_balance_sheet", "balance_sheet"),
        ("get_cashflow", "cashflow"),
        ("get_stk_holdernumber", "stk_holdernumber"),
        ("get_anns", "anns"),
    ],
)
@pytest.mark.asyncio
async def test_8_methods_dispatch_to_correct_api(
    service: tuple[RealTushareService, FakeTushareClient],
    method: str,
    api_name: str,
) -> None:
    svc, fake = service
    fn = getattr(svc, method)
    if api_name == "anns":
        await fn(ts_code="600519.SH", start="20240501", end="20240601")
    else:
        await fn(ts_code="600519.SH")
    assert fake.calls[-1][0] == api_name


@pytest.mark.asyncio
async def test_get_disclosure_date_dispatches(service: tuple[RealTushareService, FakeTushareClient]) -> None:
    svc, fake = service
    await svc.get_disclosure_date(ts_code=None, start="20240501", end="20240601")
    assert fake.calls[-1][0] == "disclosure_date"


def test_protocol_runtime_check() -> None:
    """RealTushareService satisfies TushareService Protocol structurally."""
    # No assertion needed — mypy + isinstance via @runtime_checkable
    assert hasattr(RealTushareService, "get_daily")
    assert hasattr(RealTushareService, "get_income")
    assert hasattr(RealTushareService, "get_fina_indicator")
    assert hasattr(RealTushareService, "get_balance_sheet")
    assert hasattr(RealTushareService, "get_cashflow")
    assert hasattr(RealTushareService, "get_stk_holdernumber")
    assert hasattr(RealTushareService, "get_disclosure_date")
    assert hasattr(RealTushareService, "get_anns")
    # Verify protocol exists
    assert TushareService is not None
```

- [ ] **Step 3.2**: 写失败的 unit test for factory

写入 `backend/tests/unit/test_tushare_factory.py`:

```python
"""Unit tests for tushare_factory — env-driven mock/real switch."""
from __future__ import annotations

import pytest

from app.services.tushare_factory import build_tushare_service


def test_default_mode_is_mock(monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.delenv("TUSHARE_MODE", raising=False)
    monkeypatch.delenv("TUSHARE_TOKEN", raising=False)
    svc = build_tushare_service()
    # Mock svc 类型是 LegacyMockTushareAdapter,具体名后 Task 4 引入,这里只验返回非 None
    assert svc is not None


def test_real_mode_requires_token(monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.setenv("TUSHARE_MODE", "real")
    monkeypatch.delenv("TUSHARE_TOKEN", raising=False)
    with pytest.raises(KeyError, match="TUSHARE_TOKEN"):
        build_tushare_service()


def test_real_mode_with_token_returns_real(monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.setenv("TUSHARE_MODE", "real")
    monkeypatch.setenv("TUSHARE_TOKEN", "fake-token")
    svc = build_tushare_service()
    from app.services.tushare_service import RealTushareService
    assert isinstance(svc, RealTushareService)


def test_invalid_mode_raises(monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.setenv("TUSHARE_MODE", "weird")
    with pytest.raises(ValueError, match="TUSHARE_MODE"):
        build_tushare_service()
```

- [ ] **Step 3.3**: 跑测试确认失败

```bash
uv run pytest backend/tests/unit/test_tushare_service.py backend/tests/unit/test_tushare_factory.py -v
```
Expected: FAIL `ModuleNotFoundError`

- [ ] **Step 3.4**: 实现 `TushareService` Protocol + `RealTushareService`

写入 `backend/app/services/tushare_service.py`:

```python
"""TushareService Protocol + RealTushareService.

8 接口对应 8 个 method(per spec § 3.1).
内部:cache → rate limit → client.call → cache set → return DataFrame.
"""
from __future__ import annotations

from typing import Any, Protocol, runtime_checkable

import pandas as pd

from app.services.rate_limiter import RateLimiter
from app.services.tushare_cache import TushareCache, classify_ttl
from app.services.tushare_client import TushareClient


@runtime_checkable
class TushareService(Protocol):
    async def get_daily(self, *, ts_code: str, start: str, end: str) -> pd.DataFrame: ...
    async def get_income(self, *, ts_code: str, end_date: str | None = None) -> pd.DataFrame: ...
    async def get_fina_indicator(self, *, ts_code: str, end_date: str | None = None) -> pd.DataFrame: ...
    async def get_balance_sheet(self, *, ts_code: str, end_date: str | None = None) -> pd.DataFrame: ...
    async def get_cashflow(self, *, ts_code: str, end_date: str | None = None) -> pd.DataFrame: ...
    async def get_stk_holdernumber(self, *, ts_code: str, end_date: str | None = None) -> pd.DataFrame: ...
    async def get_disclosure_date(self, *, ts_code: str | None, start: str, end: str) -> pd.DataFrame: ...
    async def get_anns(self, *, ts_code: str, start: str, end: str) -> pd.DataFrame: ...


class RealTushareService:
    def __init__(
        self,
        *,
        client: TushareClient,
        cache: TushareCache,
        rate_limiter: RateLimiter,
    ) -> None:
        self._client = client
        self._cache = cache
        self._rl = rate_limiter

    async def _call_cached(self, api_name: str, params: dict[str, Any]) -> pd.DataFrame:
        cached = await self._cache.get(api_name, params)
        if cached is not None:
            return cached
        await self._rl.acquire()
        df = await self._client.call(api_name, params)
        await self._cache.set(api_name, params, df, ttl_s=classify_ttl(api_name))
        return df

    async def get_daily(self, *, ts_code: str, start: str, end: str) -> pd.DataFrame:
        return await self._call_cached("daily", {"ts_code": ts_code, "start_date": start, "end_date": end})

    async def get_income(self, *, ts_code: str, end_date: str | None = None) -> pd.DataFrame:
        params: dict[str, Any] = {"ts_code": ts_code}
        if end_date:
            params["end_date"] = end_date
        return await self._call_cached("income", params)

    async def get_fina_indicator(self, *, ts_code: str, end_date: str | None = None) -> pd.DataFrame:
        params: dict[str, Any] = {"ts_code": ts_code}
        if end_date:
            params["end_date"] = end_date
        return await self._call_cached("fina_indicator", params)

    async def get_balance_sheet(self, *, ts_code: str, end_date: str | None = None) -> pd.DataFrame:
        params: dict[str, Any] = {"ts_code": ts_code}
        if end_date:
            params["end_date"] = end_date
        return await self._call_cached("balance_sheet", params)

    async def get_cashflow(self, *, ts_code: str, end_date: str | None = None) -> pd.DataFrame:
        params: dict[str, Any] = {"ts_code": ts_code}
        if end_date:
            params["end_date"] = end_date
        return await self._call_cached("cashflow", params)

    async def get_stk_holdernumber(self, *, ts_code: str, end_date: str | None = None) -> pd.DataFrame:
        params: dict[str, Any] = {"ts_code": ts_code}
        if end_date:
            params["end_date"] = end_date
        return await self._call_cached("stk_holdernumber", params)

    async def get_disclosure_date(
        self, *, ts_code: str | None, start: str, end: str
    ) -> pd.DataFrame:
        params: dict[str, Any] = {"start_date": start, "end_date": end}
        if ts_code:
            params["ts_code"] = ts_code
        return await self._call_cached("disclosure_date", params)

    async def get_anns(self, *, ts_code: str, start: str, end: str) -> pd.DataFrame:
        return await self._call_cached(
            "anns", {"ts_code": ts_code, "start_date": start, "end_date": end}
        )
```

- [ ] **Step 3.5**: 实现 factory

写入 `backend/app/services/tushare_factory.py`:

```python
"""tushare factory — env-driven mock/real switch.

Sibling to bocha_factory / kb_factory(8th application of Protocol pattern).
"""
from __future__ import annotations

import os
from pathlib import Path
from typing import TYPE_CHECKING

from app.services.rate_limiter import RateLimiter
from app.services.tushare_cache import TushareCache
from app.services.tushare_client import TushareClient
from app.services.tushare_service import RealTushareService, TushareService

if TYPE_CHECKING:
    pass

_CACHE_PATH = Path(__file__).resolve().parents[2] / "data" / "tushare_cache.sqlite"


def build_tushare_service() -> TushareService:
    """Pick TushareService impl per env:
    - TUSHARE_MODE unset or "mock" → MockTushareAdapter (Task 4 will install)
    - TUSHARE_MODE = "real" → RealTushareService(httpx, tushare Pro)
    """
    mode = os.environ.get("TUSHARE_MODE", "mock").lower()
    if mode == "mock":
        # Task 4 installs LegacyMockTushareAdapter; for now import lazily
        from app.services.tushare_mock_adapter import LegacyMockTushareAdapter
        return LegacyMockTushareAdapter()
    if mode == "real":
        if "TUSHARE_TOKEN" not in os.environ:
            raise KeyError("TUSHARE_TOKEN required when TUSHARE_MODE=real")
        client = TushareClient(token=os.environ["TUSHARE_TOKEN"])
        cache = TushareCache(db_path=_CACHE_PATH)
        return RealTushareService(
            client=client,
            cache=cache,
            rate_limiter=RateLimiter(max_calls=400, window_s=60.0),
        )
    raise ValueError(f"TUSHARE_MODE must be 'mock' or 'real', got {mode!r}")
```

- [ ] **Step 3.6**: 跑 unit test(`test_default_mode_is_mock` 会因为 adapter 未实现而失败 — 这是预期的,Task 4 修)

```bash
uv run pytest backend/tests/unit/test_tushare_service.py -v
uv run pytest backend/tests/unit/test_tushare_factory.py::test_real_mode_with_token_returns_real backend/tests/unit/test_tushare_factory.py::test_real_mode_requires_token backend/tests/unit/test_tushare_factory.py::test_invalid_mode_raises -v
```
Expected: test_tushare_service.py 9 passed; 3 selected factory tests passed

`test_default_mode_is_mock` skip 到 Task 4。注释里加 reminder:

在 `test_tushare_factory.py::test_default_mode_is_mock` 上方加 `@pytest.mark.skip(reason="depends on Task 4 LegacyMockTushareAdapter")`。

- [ ] **Step 3.7**: mypy

```bash
uv run mypy backend
```
Expected: 0 errors

- [ ] **Step 3.8**: Commit

```bash
git add backend/app/services/tushare_service.py backend/app/services/tushare_factory.py backend/tests/unit/test_tushare_service.py backend/tests/unit/test_tushare_factory.py
git commit -m "feat(tushare): TushareService Protocol + RealTushareService + env factory"
```

---

## Task 4: MockTushareService 扩 5 个新接口 + 旧 tools 切换 Protocol

**Files:**
- Modify: `backend/app/service/mock_tushare_service.py`(加 5 个新方法)
- Create: `backend/app/services/tushare_mock_adapter.py`(adapter 实现 TushareService Protocol)
- Modify: `backend/app/tools/get_financials.py`(__init__ 改接 Protocol)
- Modify: `backend/app/tools/get_stock_quote.py`(__init__ 改接 Protocol)
- Modify: `backend/app/orchestration/agent_graph.py`(DI 改 build_tushare_service())
- Update test: `backend/tests/unit/test_tushare_factory.py`(unskip)

**Spec ref**: § 3.5

**Background**:
- legacy mock 在 `app/service/mock_tushare_service.py`(单数 `service`,mypy ignored)
- 已有 `generate_daily_data` / `generate_income` / `generate_fina_indicator` 3 个方法
- 需要加 5 个 mock 方法:`generate_balance_sheet` / `generate_cashflow` / `generate_stk_holdernumber` / `generate_disclosure_date` / `generate_anns`
- LegacyMockTushareAdapter 在 `app/services/`(复数,mypy strict)包一层适配 Protocol
- 现有 tools `__init__(self, mock_tushare: MockTushareService)` 改成 `__init__(self, tushare: TushareService)`,内部调用方法改 `self._tushare.get_daily(...)` 等

- [ ] **Step 4.1**: grep verify 现有 tools 用法 + 调用方

```bash
grep -rn "mock_tushare\|MockTushareService" backend/app/ backend/tests/ 2>/dev/null
grep -rn "from app.tools.get_financials\|from app.tools.get_stock_quote" backend/ 2>/dev/null
grep -rn "GetFinancialsTool\|GetStockQuoteTool" backend/ 2>/dev/null
```

把输出贴到本 task self-review 报告(memory `feedback_unguarded_imports_after_delete` + `feedback_legacy_module_init_grep`),识别全部调用方。

- [ ] **Step 4.2**: 加 5 个 mock 方法到 `mock_tushare_service.py`

在文件末尾,`MockTushareService` 类内部追加(沿用现有 `generate_daily_data` 风格,LLM 提示生成假数据):

```python
async def generate_balance_sheet(self, ts_code: str, end_date: str | None = None) -> pd.DataFrame:
    """Mock 资产负债表(简化:返回 4 季 4 行)。"""
    rows = [
        {
            "ts_code": ts_code,
            "end_date": ed,
            "total_assets": 1.5e10 + i * 1e8,
            "total_liab": 6e9 + i * 5e7,
            "debt_to_assets": 0.4 + i * 0.02,
        }
        for i, ed in enumerate(["20240331", "20240630", "20240930", "20241231"])
    ]
    return pd.DataFrame(rows)

async def generate_cashflow(self, ts_code: str, end_date: str | None = None) -> pd.DataFrame:
    """Mock 现金流量表(4 季)。"""
    rows = [
        {
            "ts_code": ts_code,
            "end_date": ed,
            "n_cashflow_act": 5e8 - i * 2e7,  # 经营性现金流(故意递减,触发 yellow)
            "n_cashflow_inv_act": -2e8,
            "n_cash_flows_fnc_act": 1e8,
        }
        for i, ed in enumerate(["20240331", "20240630", "20240930", "20241231"])
    ]
    return pd.DataFrame(rows)

async def generate_stk_holdernumber(self, ts_code: str, end_date: str | None = None) -> pd.DataFrame:
    """Mock 股东户数。"""
    rows = [
        {
            "ts_code": ts_code,
            "end_date": ed,
            "holder_num": 100000 - i * 5000,
        }
        for i, ed in enumerate(["20240331", "20240630", "20240930", "20241231"])
    ]
    return pd.DataFrame(rows)

async def generate_disclosure_date(
    self, ts_code: str | None = None, start: str = "", end: str = ""
) -> pd.DataFrame:
    """Mock 披露日历(空 → 不触发事件)。"""
    return pd.DataFrame(columns=["ts_code", "ann_date", "end_date", "type"])

async def generate_anns(self, ts_code: str, start: str = "", end: str = "") -> pd.DataFrame:
    """Mock 公告(简化:返回 1 条普通公告)。"""
    return pd.DataFrame(
        [
            {
                "ts_code": ts_code,
                "ann_date": "20240501",
                "title": "关于 2023 年度股东大会决议公告",
                "content": "审议通过年度报告。",
                "url": "https://example.com/ann/1",
            }
        ]
    )
```

- [ ] **Step 4.3**: 实现 LegacyMockTushareAdapter

写入 `backend/app/services/tushare_mock_adapter.py`:

```python
"""Adapter wrapping legacy MockTushareService into TushareService Protocol shape.

Lazy import to avoid app/service/__init__.py legacy import chain at module load.
"""
from __future__ import annotations

from importlib.util import module_from_spec, spec_from_file_location
from pathlib import Path

import pandas as pd


class LegacyMockTushareAdapter:
    def __init__(self) -> None:
        path = Path(__file__).resolve().parents[1] / "service" / "mock_tushare_service.py"
        spec = spec_from_file_location("_mock_tushare_module", str(path))
        if spec is None or spec.loader is None:
            raise RuntimeError(f"cannot load {path}")
        module = module_from_spec(spec)
        spec.loader.exec_module(module)
        # MockTushareService 当前 __init__ 需要 LLMService;legacy 实现里有 default-arg 可走
        # 若 __init__ 必填,需先 grep verify(memory feedback_legacy_module_init_grep)
        self._inner = module.MockTushareService()  # 详见 Step 4.1 grep 结果

    async def get_daily(self, *, ts_code: str, start: str, end: str) -> pd.DataFrame:
        return await self._inner.generate_daily_data(ts_code=ts_code, start_date=start, end_date=end)

    async def get_income(self, *, ts_code: str, end_date: str | None = None) -> pd.DataFrame:
        return await self._inner.generate_income(ts_code=ts_code)

    async def get_fina_indicator(self, *, ts_code: str, end_date: str | None = None) -> pd.DataFrame:
        return await self._inner.generate_fina_indicator(ts_code=ts_code)

    async def get_balance_sheet(self, *, ts_code: str, end_date: str | None = None) -> pd.DataFrame:
        return await self._inner.generate_balance_sheet(ts_code=ts_code, end_date=end_date)

    async def get_cashflow(self, *, ts_code: str, end_date: str | None = None) -> pd.DataFrame:
        return await self._inner.generate_cashflow(ts_code=ts_code, end_date=end_date)

    async def get_stk_holdernumber(self, *, ts_code: str, end_date: str | None = None) -> pd.DataFrame:
        return await self._inner.generate_stk_holdernumber(ts_code=ts_code, end_date=end_date)

    async def get_disclosure_date(
        self, *, ts_code: str | None, start: str, end: str
    ) -> pd.DataFrame:
        return await self._inner.generate_disclosure_date(ts_code=ts_code, start=start, end=end)

    async def get_anns(self, *, ts_code: str, start: str, end: str) -> pd.DataFrame:
        return await self._inner.generate_anns(ts_code=ts_code, start=start, end=end)
```

如果 Step 4.1 grep 显示 `MockTushareService.__init__` 必填 LLMService,则 `self._inner = module.MockTushareService()` 改为先 `from app.services.openai_client import build_llm_service_from_env` 再传入。

- [ ] **Step 4.4**: 改造 `get_financials.py`

`backend/app/tools/get_financials.py:11-13` 处 `if TYPE_CHECKING: from app.service.mock_tushare_service import MockTushareService` 改为:

```python
if TYPE_CHECKING:
    from app.services.tushare_service import TushareService
```

`__init__` (line 38-39):
```python
def __init__(self, tushare: TushareService) -> None:
    self._tushare = tushare
```

`run` 方法(line 45-46)改:
```python
income_df = await self._tushare.get_income(ts_code=validated.ts_code)
fina_df = await self._tushare.get_fina_indicator(ts_code=validated.ts_code)
```

异常 message(line 48):
```python
raise ToolError(f"TushareService call failed: {exc}") from exc
```

- [ ] **Step 4.5**: 改造 `get_stock_quote.py`

类似改 `__init__` + `run` 方法,`self._mock_tushare` → `self._tushare`,call 改 `get_daily`。具体看 Step 4.1 grep 输出后微调。

- [ ] **Step 4.6**: 改造 DI wiring `agent_graph.py`

```bash
grep -n "mock_tushare\|MockTushareService\|build.*tushare" backend/app/orchestration/agent_graph.py
```

把 `MockTushareService(...)` 替换为 `build_tushare_service()`,import 改 `from app.services.tushare_factory import build_tushare_service`。

- [ ] **Step 4.7**: 更新 `test_tushare_factory.py::test_default_mode_is_mock` 去掉 skip + 跑

```bash
uv run pytest backend/tests/unit/test_tushare_factory.py -v
```
Expected: 4 passed

- [ ] **Step 4.8**: grep no leftover `MockTushareService` import as constructor arg

```bash
grep -rn "MockTushareService" backend/app/ 2>/dev/null
```
Expected: 仅出现在 `app/service/mock_tushare_service.py`(class def) + `app/services/tushare_mock_adapter.py`(lazy load)。如果有其他 import 直接构造,删 / 改成 build_tushare_service()。

- [ ] **Step 4.9**: 全套测 + mypy

```bash
unset all_proxy https_proxy http_proxy
uv run pytest backend/tests/unit -v
uv run mypy backend
```
Expected: 全 pass

- [ ] **Step 4.10**: Commit

```bash
git add backend/app/service/mock_tushare_service.py \
        backend/app/services/tushare_mock_adapter.py \
        backend/app/tools/get_financials.py \
        backend/app/tools/get_stock_quote.py \
        backend/app/orchestration/agent_graph.py \
        backend/tests/unit/test_tushare_factory.py
git commit -m "feat(tushare): mock adapter + tools switch to Protocol DI"
```

---

## Task 5: Tushare cassettes(8 接口 × 5 ts_code)

**Files:**
- Create: `backend/tests/e2e/test_tushare_real_cassette.py`
- Create: `backend/tests/fixtures/cassettes/test_tushare_real_cassette/*.yaml`(实跑录制)

**Spec ref**: § 3.4

**Background**:
- 用 5 个 ts_code(从 `backend/.env` `MONITORING_DOGFOOD_CUSTOMERS` 读;此 task 临时 hardcode 5 个常见 sample)
- VCR `match_on=['method', 'host', 'path', 'query']`(**不 match body**,因 token 在 body)
- `before_record_request` hook redact token
- `record_mode` env-driven:`new_episodes` 录制时,`none` CI replay 时

- [ ] **Step 5.1**: 写 e2e cassette test

写入 `backend/tests/e2e/test_tushare_real_cassette.py`:

```python
"""E2E cassette test — real tushare API replays for 8 endpoints × 5 ts_codes.

Recording: VCR_RECORD_MODE=new_episodes TUSHARE_MODE=real TUSHARE_TOKEN=...
Replay (CI): default (record_mode=none).
"""
from __future__ import annotations

import os
from pathlib import Path
from typing import Any

import pytest

from app.services.rate_limiter import RateLimiter
from app.services.tushare_cache import TushareCache
from app.services.tushare_client import TushareClient
from app.services.tushare_service import RealTushareService

# Sample ts_codes(临时 — Task 19 dogfood 会用 .env)
SAMPLE_TS_CODES = [
    "600519.SH",  # 茅台
    "601398.SH",  # 工行
    "000001.SZ",  # 平安
    "000063.SZ",  # 中兴
    "002594.SZ",  # 比亚迪
]


@pytest.fixture(scope="module")
def vcr_config() -> dict[str, Any]:
    return {
        "filter_headers": ["authorization"],
        "match_on": ["method", "scheme", "host", "port", "path", "query"],
        "record_mode": os.environ.get("VCR_RECORD_MODE", "none"),
        # Redact token in request body
        "before_record_request": _redact_token,
    }


def _redact_token(request: Any) -> Any:
    """Redact token in tushare request body before recording."""
    if request.body:
        import json
        try:
            body = json.loads(request.body) if isinstance(request.body, (str, bytes)) else None
        except (json.JSONDecodeError, TypeError):
            body = None
        if isinstance(body, dict) and "token" in body:
            body["token"] = "<TUSHARE_TOKEN>"
            request.body = json.dumps(body).encode("utf-8")
    return request


@pytest.fixture
def service(tmp_path: Path) -> RealTushareService:
    token = os.environ.get("TUSHARE_TOKEN", "<TUSHARE_TOKEN>")
    return RealTushareService(
        client=TushareClient(token=token),
        cache=TushareCache(db_path=tmp_path / "c.sqlite"),
        rate_limiter=RateLimiter(max_calls=400, window_s=60.0),
    )


@pytest.mark.vcr
@pytest.mark.parametrize("ts_code", SAMPLE_TS_CODES)
@pytest.mark.asyncio
async def test_get_daily_real(service: RealTushareService, ts_code: str) -> None:
    df = await service.get_daily(ts_code=ts_code, start="20240301", end="20240501")
    assert not df.empty


@pytest.mark.vcr
@pytest.mark.parametrize("ts_code", SAMPLE_TS_CODES)
@pytest.mark.asyncio
async def test_get_income_real(service: RealTushareService, ts_code: str) -> None:
    df = await service.get_income(ts_code=ts_code)
    assert not df.empty


@pytest.mark.vcr
@pytest.mark.parametrize("ts_code", SAMPLE_TS_CODES)
@pytest.mark.asyncio
async def test_get_fina_indicator_real(service: RealTushareService, ts_code: str) -> None:
    df = await service.get_fina_indicator(ts_code=ts_code)
    assert not df.empty


@pytest.mark.vcr
@pytest.mark.parametrize("ts_code", SAMPLE_TS_CODES)
@pytest.mark.asyncio
async def test_get_balance_sheet_real(service: RealTushareService, ts_code: str) -> None:
    df = await service.get_balance_sheet(ts_code=ts_code)
    assert not df.empty


@pytest.mark.vcr
@pytest.mark.parametrize("ts_code", SAMPLE_TS_CODES)
@pytest.mark.asyncio
async def test_get_cashflow_real(service: RealTushareService, ts_code: str) -> None:
    df = await service.get_cashflow(ts_code=ts_code)
    assert not df.empty


@pytest.mark.vcr
@pytest.mark.parametrize("ts_code", SAMPLE_TS_CODES)
@pytest.mark.asyncio
async def test_get_stk_holdernumber_real(service: RealTushareService, ts_code: str) -> None:
    df = await service.get_stk_holdernumber(ts_code=ts_code)
    # 部分股票可能无股东户数披露,允许 empty 但不应抛错
    _ = df


@pytest.mark.vcr
@pytest.mark.asyncio
async def test_get_disclosure_date_real(service: RealTushareService) -> None:
    df = await service.get_disclosure_date(ts_code=None, start="20240301", end="20240501")
    _ = df


@pytest.mark.vcr
@pytest.mark.parametrize("ts_code", SAMPLE_TS_CODES)
@pytest.mark.asyncio
async def test_get_anns_real(service: RealTushareService, ts_code: str) -> None:
    df = await service.get_anns(ts_code=ts_code, start="20240301", end="20240501")
    _ = df
```

- [ ] **Step 5.2**: 录制 cassette

确保 `.env` 有 `TUSHARE_TOKEN=<真 token>`。

```bash
unset all_proxy https_proxy http_proxy
TUSHARE_MODE=real VCR_RECORD_MODE=new_episodes uv run pytest backend/tests/e2e/test_tushare_real_cassette.py -v
```
Expected: ~36 个 cassette 文件生成在 `backend/tests/fixtures/cassettes/test_tushare_real_cassette/`

- [ ] **Step 5.3**: 验证 cassette 不含 token

```bash
grep -r "TUSHARE_TOKEN\|<真 token 前 8 字符>" backend/tests/fixtures/cassettes/test_tushare_real_cassette/ 2>/dev/null
```
Expected: 仅出现 `<TUSHARE_TOKEN>` 占位,无真 token 前缀。如果检出真 token,**立刻删 cassette + 改 redact 逻辑 + 重录**。

- [ ] **Step 5.4**: replay 测(unset env)

```bash
unset TUSHARE_TOKEN
uv run pytest backend/tests/e2e/test_tushare_real_cassette.py -v
```
Expected: 全 pass(走 cassette,不打真网络)

- [ ] **Step 5.5**: Commit

```bash
git add backend/tests/e2e/test_tushare_real_cassette.py backend/tests/fixtures/cassettes/test_tushare_real_cassette/
git commit -m "test(tushare): cassettes for 8 endpoints × 5 sample ts_codes"
```

---

## Task 6: DB schema migration(5 张新表)

**Files:**
- Create: `backend/app/scripts/init_monitoring_tables.py`
- Modify: `backend/app/core/database.py`(启动时调用 init)
- Create: `backend/tests/unit/test_init_monitoring_tables.py`

**Spec ref**: § 9

**Background**:
- 5 张新表(`monitoring_customers` / `monitoring_runs` / `monitoring_signals` / `monitoring_alerts` / `notifications`)
- 不引入 alembic(spec § 9.2 决议),手写 SQL + 启动时 `CREATE TABLE IF NOT EXISTS`
- `app/core/database.py` 已有 `SessionLocal` / `Base` / `engine`(legacy 模块)— spike grep 看现有 init pattern,follow 同样模式

- [ ] **Step 6.1**: grep verify `database.py` 当前 init pattern

```bash
grep -n "create_all\|init_db\|Base.metadata\|CREATE TABLE" backend/app/core/database.py
```

记录现有模式,新 init 走相同模式。

- [ ] **Step 6.2**: 写失败的 unit test

写入 `backend/tests/unit/test_init_monitoring_tables.py`:

```python
"""Unit tests for init_monitoring_tables — verifies 5 tables created with correct schema."""
from __future__ import annotations

import sqlite3
from pathlib import Path

import pytest

from app.scripts.init_monitoring_tables import init_monitoring_tables


@pytest.fixture
def db_path(tmp_path: Path) -> Path:
    return tmp_path / "test.sqlite"


def test_init_creates_5_tables(db_path: Path) -> None:
    init_monitoring_tables(db_path)
    with sqlite3.connect(db_path) as conn:
        rows = conn.execute(
            "SELECT name FROM sqlite_master WHERE type='table' "
            "AND name LIKE 'monitoring_%' OR name = 'notifications'"
        ).fetchall()
    names = {r[0] for r in rows}
    assert names == {
        "monitoring_customers",
        "monitoring_runs",
        "monitoring_signals",
        "monitoring_alerts",
        "notifications",
    }


def test_init_idempotent(db_path: Path) -> None:
    init_monitoring_tables(db_path)
    init_monitoring_tables(db_path)  # 重复跑不应抛错


@pytest.mark.parametrize(
    "table,required_cols",
    [
        ("monitoring_customers", {"id", "ts_code", "name", "industry", "enabled", "thresholds_override"}),
        ("monitoring_runs", {"id", "customer_id", "trigger_type", "started_at", "status"}),
        ("monitoring_signals", {"id", "run_id", "rule_name", "level", "detected_value", "threshold"}),
        ("monitoring_alerts", {"id", "run_id", "customer_id", "alert_level", "report_json", "escalated"}),
        ("notifications", {"id", "alert_id", "channel", "recipient", "send_status"}),
    ],
)
def test_table_required_columns(db_path: Path, table: str, required_cols: set[str]) -> None:
    init_monitoring_tables(db_path)
    with sqlite3.connect(db_path) as conn:
        rows = conn.execute(f"PRAGMA table_info({table})").fetchall()
    cols = {r[1] for r in rows}
    assert required_cols.issubset(cols), f"{table} missing: {required_cols - cols}"
```

- [ ] **Step 6.3**: 跑测试确认失败

```bash
uv run pytest backend/tests/unit/test_init_monitoring_tables.py -v
```
Expected: FAIL `ModuleNotFoundError`

- [ ] **Step 6.4**: 实现 `init_monitoring_tables`

写入 `backend/app/scripts/init_monitoring_tables.py`:

```python
"""Create monitoring + notifications tables. Idempotent (CREATE TABLE IF NOT EXISTS)."""
from __future__ import annotations

import sqlite3
from pathlib import Path

_SCHEMA_SQL = """
CREATE TABLE IF NOT EXISTS monitoring_customers (
    id TEXT PRIMARY KEY,
    ts_code TEXT NOT NULL UNIQUE,
    name TEXT NOT NULL,
    industry TEXT NOT NULL,
    enabled INTEGER NOT NULL DEFAULT 1,
    thresholds_override TEXT,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS monitoring_runs (
    id TEXT PRIMARY KEY,
    customer_id TEXT NOT NULL REFERENCES monitoring_customers(id),
    trigger_type TEXT NOT NULL,
    started_at TIMESTAMP NOT NULL,
    finished_at TIMESTAMP,
    status TEXT NOT NULL,
    error_message TEXT,
    cost_cny REAL DEFAULT 0
);

CREATE TABLE IF NOT EXISTS monitoring_signals (
    id TEXT PRIMARY KEY,
    run_id TEXT NOT NULL REFERENCES monitoring_runs(id),
    rule_name TEXT NOT NULL,
    level TEXT NOT NULL,
    detected_value TEXT,
    threshold TEXT,
    explanation TEXT,
    raw_data_ref TEXT
);

CREATE TABLE IF NOT EXISTS monitoring_alerts (
    id TEXT PRIMARY KEY,
    run_id TEXT NOT NULL REFERENCES monitoring_runs(id),
    customer_id TEXT NOT NULL REFERENCES monitoring_customers(id),
    alert_level TEXT NOT NULL,
    report_json TEXT NOT NULL,
    report_markdown TEXT,
    escalated INTEGER DEFAULT 0,
    escalation_status TEXT,
    deep_dive_text TEXT,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS notifications (
    id TEXT PRIMARY KEY,
    alert_id TEXT NOT NULL REFERENCES monitoring_alerts(id),
    channel TEXT NOT NULL,
    recipient TEXT,
    sent_at TIMESTAMP,
    send_status TEXT NOT NULL,
    error_message TEXT,
    read_at TIMESTAMP
);
"""


def init_monitoring_tables(db_path: Path) -> None:
    db_path.parent.mkdir(parents=True, exist_ok=True)
    with sqlite3.connect(db_path) as conn:
        conn.executescript(_SCHEMA_SQL)
```

- [ ] **Step 6.5**: 在 `app/core/database.py` 启动时调用 init

grep `app/core/database.py` 找现有 init 函数(可能叫 `init_db` 或 `Base.metadata.create_all(...)`),在合适位置加:

```python
from pathlib import Path
from app.scripts.init_monitoring_tables import init_monitoring_tables

# 在现有 init_db / startup 函数末尾追加
_MONITORING_DB_PATH = Path(__file__).resolve().parents[1] / "data" / "monitoring.sqlite"
init_monitoring_tables(_MONITORING_DB_PATH)
```

如果 `database.py` 没有显式 init 函数(用 SQLAlchemy `Base.metadata.create_all` 隐式),则在 `app/app_main.py` lifespan/startup 加调用。

- [ ] **Step 6.6**: 跑测试

```bash
uv run pytest backend/tests/unit/test_init_monitoring_tables.py -v
uv run mypy backend
```
Expected: 7 passed, 0 mypy errors

- [ ] **Step 6.7**: Commit

```bash
git add backend/app/scripts/init_monitoring_tables.py \
        backend/app/core/database.py \
        backend/tests/unit/test_init_monitoring_tables.py
git commit -m "feat(monitoring): create 5 monitoring + notifications tables on startup"
```

---

## Task 7: SignalRule ABC + types + defaults

**Files:**
- Create: `backend/app/services/monitoring/__init__.py`(空)
- Create: `backend/app/services/monitoring/signal_rules/__init__.py`(导出 5 个 rule + ABC)
- Create: `backend/app/services/monitoring/signal_rules/base.py`
- Create: `backend/app/services/monitoring/signal_rules/defaults.py`
- Create: `backend/tests/unit/test_signal_rule_base.py`

**Spec ref**: § 5.1, § 5.3

- [ ] **Step 7.1**: 写失败的 unit test

写入 `backend/tests/unit/test_signal_rule_base.py`:

```python
"""Unit tests for SignalRule ABC + types."""
from __future__ import annotations

import pytest

from app.services.monitoring.signal_rules.base import (
    SignalLevel,
    SignalResult,
    SignalRule,
)
from app.services.monitoring.signal_rules.defaults import DEFAULT_THRESHOLDS


def test_signal_level_enum() -> None:
    assert SignalLevel.GREEN.value == "green"
    assert SignalLevel.YELLOW.value == "yellow"
    assert SignalLevel.RED.value == "red"


def test_signal_result_serializable() -> None:
    r = SignalResult(
        rule_name="financial_ratio",
        level=SignalLevel.YELLOW,
        detected_value=0.85,
        threshold=0.80,
        explanation="资产负债率超阈值",
        raw_data_ref={"ts_code": "x"},
    )
    dumped = r.model_dump()
    assert dumped["rule_name"] == "financial_ratio"
    assert dumped["level"] == "yellow"


def test_signal_rule_is_abstract() -> None:
    with pytest.raises(TypeError):
        SignalRule()  # type: ignore[abstract]


def test_default_thresholds_has_5_rules() -> None:
    assert set(DEFAULT_THRESHOLDS.keys()) == {
        "financial_ratio",
        "cash_flow",
        "shareholder_count",
        "announcement",
        "price_anomaly",
    }
```

- [ ] **Step 7.2**: 实现 `base.py`

写入 `backend/app/services/monitoring/signal_rules/base.py`:

```python
"""SignalRule ABC + Level enum + Result schema."""
from __future__ import annotations

from abc import ABC, abstractmethod
from enum import Enum
from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

if TYPE_CHECKING:
    from app.services.bocha_factory import BochaService
    from app.services.llm_service import LLMService
    from app.services.tushare_service import TushareService


class SignalLevel(str, Enum):
    GREEN = "green"
    YELLOW = "yellow"
    RED = "red"


class SignalResult(BaseModel):
    model_config = ConfigDict(extra="forbid")

    rule_name: str
    level: SignalLevel
    detected_value: float | str | None = None
    threshold: float | str | None = None
    explanation: str
    raw_data_ref: dict[str, Any] | None = None


class MonitoringCustomer(BaseModel):
    """Subset of monitoring_customers row used by signal rules."""

    model_config = ConfigDict(extra="forbid")

    id: str
    ts_code: str
    name: str
    industry: str
    thresholds_override: dict[str, float] | None = None


class SignalRule(ABC):
    name: str = ""
    description: str = ""

    @abstractmethod
    async def evaluate(
        self,
        customer: MonitoringCustomer,
        tushare: "TushareService",
        bocha: "BochaService",
        llm: "LLMService",
        thresholds: dict[str, float],
    ) -> SignalResult: ...
```

- [ ] **Step 7.3**: 实现 `defaults.py`

写入 `backend/app/services/monitoring/signal_rules/defaults.py`:

```python
"""Default thresholds per signal rule(spec § 5.2)."""
from __future__ import annotations

DEFAULT_THRESHOLDS: dict[str, dict[str, float]] = {
    "financial_ratio": {
        "yellow_debt_ratio_qoq_pp": 5.0,        # 资产负债率单季环比 +5 pp
        "yellow_net_margin_negative": 0.0,
        "red_debt_ratio_abs": 80.0,             # 资产负债率 80%
        "red_consecutive_loss_quarters": 2,
    },
    "cash_flow": {
        "yellow_op_cf_qoq_drop_pct": 30.0,
        "red_consecutive_negative_quarters": 2,
    },
    "shareholder_count": {
        "yellow_min_drop_pct": 10.0,
        "yellow_max_drop_pct": 20.0,
        "red_drop_pct": 20.0,
    },
    "announcement": {
        "yellow_lower": 0.5,
        "yellow_upper": 0.8,
        "red_threshold": 0.8,
    },
    "price_anomaly": {
        "yellow_single_day_drop_pct": 5.0,
        "yellow_60d_drop_pct": 20.0,
        "red_single_day_drop_pct": 10.0,
        "red_consecutive_5d_drop_pct": 25.0,
    },
}
```

- [ ] **Step 7.4**: `__init__.py`

写入 `backend/app/services/monitoring/__init__.py`:(空)

写入 `backend/app/services/monitoring/signal_rules/__init__.py`:

```python
"""SignalRule package — ABC + 5 concrete rules + defaults."""
from app.services.monitoring.signal_rules.base import (
    MonitoringCustomer,
    SignalLevel,
    SignalResult,
    SignalRule,
)
from app.services.monitoring.signal_rules.defaults import DEFAULT_THRESHOLDS

__all__ = [
    "DEFAULT_THRESHOLDS",
    "MonitoringCustomer",
    "SignalLevel",
    "SignalResult",
    "SignalRule",
]
```

- [ ] **Step 7.5**: 跑测试 + mypy

```bash
uv run pytest backend/tests/unit/test_signal_rule_base.py -v
uv run mypy backend
```
Expected: 4 passed, 0 mypy errors

- [ ] **Step 7.6**: Commit

```bash
git add backend/app/services/monitoring/__init__.py \
        backend/app/services/monitoring/signal_rules/ \
        backend/tests/unit/test_signal_rule_base.py
git commit -m "feat(monitoring): SignalRule ABC + Level + Result + default thresholds"
```

---

## Task 8: 5 个 SignalRule 实现

**Files:**
- Create: `backend/app/services/monitoring/signal_rules/financial_ratio.py`
- Create: `backend/app/services/monitoring/signal_rules/cash_flow.py`
- Create: `backend/app/services/monitoring/signal_rules/shareholder_count.py`
- Create: `backend/app/services/monitoring/signal_rules/announcement.py`
- Create: `backend/app/services/monitoring/signal_rules/price_anomaly.py`
- Create: `backend/tests/unit/test_signal_rules_financial_ratio.py`
- Create: `backend/tests/unit/test_signal_rules_cash_flow.py`
- Create: `backend/tests/unit/test_signal_rules_shareholder_count.py`
- Create: `backend/tests/unit/test_signal_rules_announcement.py`
- Create: `backend/tests/unit/test_signal_rules_price_anomaly.py`

**Spec ref**: § 5.2

5 rule 实现 5 个 file。每 file 一个 sub-task。

### 8a. financial_ratio rule

- [ ] **Step 8a.1**: 写失败的 unit test

写入 `backend/tests/unit/test_signal_rules_financial_ratio.py`:

```python
"""Unit tests for FinancialRatioRule."""
from __future__ import annotations

from typing import Any
from unittest.mock import AsyncMock, MagicMock

import pandas as pd
import pytest

from app.services.monitoring.signal_rules.base import (
    MonitoringCustomer,
    SignalLevel,
)
from app.services.monitoring.signal_rules.defaults import DEFAULT_THRESHOLDS
from app.services.monitoring.signal_rules.financial_ratio import FinancialRatioRule


@pytest.fixture
def customer() -> MonitoringCustomer:
    return MonitoringCustomer(id="x", ts_code="600519.SH", name="茅台", industry="消费")


def _fake_tushare(balance_df: pd.DataFrame, fina_df: pd.DataFrame) -> MagicMock:
    fake = MagicMock()
    fake.get_balance_sheet = AsyncMock(return_value=balance_df)
    fake.get_fina_indicator = AsyncMock(return_value=fina_df)
    return fake


@pytest.mark.asyncio
async def test_green_when_stable(customer: MonitoringCustomer) -> None:
    balance = pd.DataFrame(
        [
            {"end_date": "20240331", "debt_to_assets": 0.40},
            {"end_date": "20240630", "debt_to_assets": 0.41},
            {"end_date": "20240930", "debt_to_assets": 0.42},
            {"end_date": "20241231", "debt_to_assets": 0.43},
        ]
    )
    fina = pd.DataFrame(
        [
            {"end_date": "20240331", "netprofit_margin": 0.30},
            {"end_date": "20240630", "netprofit_margin": 0.31},
            {"end_date": "20240930", "netprofit_margin": 0.32},
            {"end_date": "20241231", "netprofit_margin": 0.33},
        ]
    )
    rule = FinancialRatioRule()
    result = await rule.evaluate(
        customer,
        _fake_tushare(balance, fina),
        MagicMock(),
        MagicMock(),
        DEFAULT_THRESHOLDS["financial_ratio"],
    )
    assert result.level == SignalLevel.GREEN


@pytest.mark.asyncio
async def test_yellow_on_debt_ratio_jump(customer: MonitoringCustomer) -> None:
    balance = pd.DataFrame(
        [
            {"end_date": "20240331", "debt_to_assets": 0.40},
            {"end_date": "20240630", "debt_to_assets": 0.42},
            {"end_date": "20240930", "debt_to_assets": 0.44},
            {"end_date": "20241231", "debt_to_assets": 0.50},  # +6 pp QoQ
        ]
    )
    fina = pd.DataFrame(
        [{"end_date": "20241231", "netprofit_margin": 0.20}]
    )
    rule = FinancialRatioRule()
    result = await rule.evaluate(
        customer,
        _fake_tushare(balance, fina),
        MagicMock(),
        MagicMock(),
        DEFAULT_THRESHOLDS["financial_ratio"],
    )
    assert result.level == SignalLevel.YELLOW


@pytest.mark.asyncio
async def test_red_on_high_debt_ratio_abs(customer: MonitoringCustomer) -> None:
    balance = pd.DataFrame(
        [
            {"end_date": "20240331", "debt_to_assets": 0.78},
            {"end_date": "20240630", "debt_to_assets": 0.79},
            {"end_date": "20240930", "debt_to_assets": 0.80},
            {"end_date": "20241231", "debt_to_assets": 0.85},  # > 80%
        ]
    )
    fina = pd.DataFrame(
        [{"end_date": "20241231", "netprofit_margin": 0.15}]
    )
    rule = FinancialRatioRule()
    result = await rule.evaluate(
        customer,
        _fake_tushare(balance, fina),
        MagicMock(),
        MagicMock(),
        DEFAULT_THRESHOLDS["financial_ratio"],
    )
    assert result.level == SignalLevel.RED


@pytest.mark.asyncio
async def test_red_on_consecutive_loss(customer: MonitoringCustomer) -> None:
    balance = pd.DataFrame(
        [{"end_date": "20241231", "debt_to_assets": 0.50}]
    )
    fina = pd.DataFrame(
        [
            {"end_date": "20240930", "netprofit_margin": -0.05},
            {"end_date": "20241231", "netprofit_margin": -0.10},  # 连续 2 季亏损
        ]
    )
    rule = FinancialRatioRule()
    result = await rule.evaluate(
        customer,
        _fake_tushare(balance, fina),
        MagicMock(),
        MagicMock(),
        DEFAULT_THRESHOLDS["financial_ratio"],
    )
    assert result.level == SignalLevel.RED
```

- [ ] **Step 8a.2**: 跑测试确认失败

```bash
uv run pytest backend/tests/unit/test_signal_rules_financial_ratio.py -v
```
Expected: FAIL `ModuleNotFoundError`

- [ ] **Step 8a.3**: 实现 rule

写入 `backend/app/services/monitoring/signal_rules/financial_ratio.py`:

```python
"""FinancialRatioRule — detects 资产负债率 jump or 连续亏损."""
from __future__ import annotations

from typing import TYPE_CHECKING

from app.services.monitoring.signal_rules.base import (
    MonitoringCustomer,
    SignalLevel,
    SignalResult,
    SignalRule,
)

if TYPE_CHECKING:
    from app.services.bocha_factory import BochaService
    from app.services.llm_service import LLMService
    from app.services.tushare_service import TushareService


class FinancialRatioRule(SignalRule):
    name = "financial_ratio"
    description = "Detects 资产负债率 jump (QoQ +5pp) or 连续 2 季亏损"

    async def evaluate(
        self,
        customer: MonitoringCustomer,
        tushare: "TushareService",
        bocha: "BochaService",
        llm: "LLMService",
        thresholds: dict[str, float],
    ) -> SignalResult:
        balance = await tushare.get_balance_sheet(ts_code=customer.ts_code)
        fina = await tushare.get_fina_indicator(ts_code=customer.ts_code)

        balance = balance.sort_values("end_date").tail(4)
        fina = fina.sort_values("end_date").tail(4)

        # Red: debt ratio > 80%
        if not balance.empty:
            latest_debt_pct = float(balance["debt_to_assets"].iloc[-1]) * 100.0
            if latest_debt_pct >= thresholds["red_debt_ratio_abs"]:
                return SignalResult(
                    rule_name=self.name,
                    level=SignalLevel.RED,
                    detected_value=latest_debt_pct,
                    threshold=thresholds["red_debt_ratio_abs"],
                    explanation=f"资产负债率 {latest_debt_pct:.1f}% 超 {thresholds['red_debt_ratio_abs']}%",
                    raw_data_ref={"ts_code": customer.ts_code, "end_date": str(balance["end_date"].iloc[-1])},
                )

        # Red: 连续 2 季净利率为负
        if len(fina) >= 2:
            last_two_margins = fina["netprofit_margin"].tail(2).tolist()
            if all(m < 0 for m in last_two_margins):
                return SignalResult(
                    rule_name=self.name,
                    level=SignalLevel.RED,
                    detected_value=str(last_two_margins),
                    threshold="0",
                    explanation=f"连续 2 季净利率为负: {last_two_margins}",
                    raw_data_ref={"ts_code": customer.ts_code},
                )

        # Yellow: debt ratio QoQ jump >= +5pp
        if len(balance) >= 2:
            qoq_delta_pp = (
                float(balance["debt_to_assets"].iloc[-1]) - float(balance["debt_to_assets"].iloc[-2])
            ) * 100.0
            if qoq_delta_pp >= thresholds["yellow_debt_ratio_qoq_pp"]:
                return SignalResult(
                    rule_name=self.name,
                    level=SignalLevel.YELLOW,
                    detected_value=qoq_delta_pp,
                    threshold=thresholds["yellow_debt_ratio_qoq_pp"],
                    explanation=f"资产负债率单季环比 +{qoq_delta_pp:.1f}pp 超阈值",
                    raw_data_ref={"ts_code": customer.ts_code},
                )

        # Yellow: 净利率为负
        if not fina.empty:
            latest_margin = float(fina["netprofit_margin"].iloc[-1])
            if latest_margin < thresholds["yellow_net_margin_negative"]:
                return SignalResult(
                    rule_name=self.name,
                    level=SignalLevel.YELLOW,
                    detected_value=latest_margin,
                    threshold=0.0,
                    explanation=f"最新净利率 {latest_margin:.2%} 为负",
                    raw_data_ref={"ts_code": customer.ts_code},
                )

        return SignalResult(
            rule_name=self.name,
            level=SignalLevel.GREEN,
            detected_value=None,
            threshold=None,
            explanation="财务比率正常",
            raw_data_ref=None,
        )
```

- [ ] **Step 8a.4**: 跑测试 + mypy + commit

```bash
uv run pytest backend/tests/unit/test_signal_rules_financial_ratio.py -v
uv run mypy backend
git add backend/app/services/monitoring/signal_rules/financial_ratio.py \
        backend/tests/unit/test_signal_rules_financial_ratio.py
git commit -m "feat(monitoring): financial_ratio signal rule (debt ratio + loss detection)"
```

### 8b. cash_flow rule

- [ ] **Step 8b.1**: 写测试 + 实现

写入 `backend/tests/unit/test_signal_rules_cash_flow.py`(参考 8a 结构):

```python
"""Unit tests for CashFlowRule."""
from __future__ import annotations

from unittest.mock import AsyncMock, MagicMock

import pandas as pd
import pytest

from app.services.monitoring.signal_rules.base import MonitoringCustomer, SignalLevel
from app.services.monitoring.signal_rules.cash_flow import CashFlowRule
from app.services.monitoring.signal_rules.defaults import DEFAULT_THRESHOLDS


@pytest.fixture
def customer() -> MonitoringCustomer:
    return MonitoringCustomer(id="x", ts_code="x.SH", name="x", industry="x")


def _ts(df: pd.DataFrame) -> MagicMock:
    fake = MagicMock()
    fake.get_cashflow = AsyncMock(return_value=df)
    return fake


@pytest.mark.asyncio
async def test_green_when_stable(customer: MonitoringCustomer) -> None:
    df = pd.DataFrame([{"end_date": ed, "n_cashflow_act": v} for ed, v in [
        ("20240331", 5e8), ("20240630", 5.1e8), ("20240930", 5.2e8), ("20241231", 5.3e8),
    ]])
    rule = CashFlowRule()
    result = await rule.evaluate(customer, _ts(df), MagicMock(), MagicMock(), DEFAULT_THRESHOLDS["cash_flow"])
    assert result.level == SignalLevel.GREEN


@pytest.mark.asyncio
async def test_yellow_on_qoq_drop(customer: MonitoringCustomer) -> None:
    df = pd.DataFrame([{"end_date": ed, "n_cashflow_act": v} for ed, v in [
        ("20240930", 5e8), ("20241231", 3e8),  # -40% QoQ
    ]])
    rule = CashFlowRule()
    result = await rule.evaluate(customer, _ts(df), MagicMock(), MagicMock(), DEFAULT_THRESHOLDS["cash_flow"])
    assert result.level == SignalLevel.YELLOW


@pytest.mark.asyncio
async def test_red_on_consecutive_negative(customer: MonitoringCustomer) -> None:
    df = pd.DataFrame([{"end_date": ed, "n_cashflow_act": v} for ed, v in [
        ("20240930", -1e8), ("20241231", -2e8),
    ]])
    rule = CashFlowRule()
    result = await rule.evaluate(customer, _ts(df), MagicMock(), MagicMock(), DEFAULT_THRESHOLDS["cash_flow"])
    assert result.level == SignalLevel.RED
```

写入 `backend/app/services/monitoring/signal_rules/cash_flow.py`:

```python
"""CashFlowRule — detects 经营性现金流恶化 or 连续负值."""
from __future__ import annotations

from typing import TYPE_CHECKING

from app.services.monitoring.signal_rules.base import (
    MonitoringCustomer,
    SignalLevel,
    SignalResult,
    SignalRule,
)

if TYPE_CHECKING:
    from app.services.bocha_factory import BochaService
    from app.services.llm_service import LLMService
    from app.services.tushare_service import TushareService


class CashFlowRule(SignalRule):
    name = "cash_flow"
    description = "Detects 经营性现金流单季环比下滑 / 连续 2 季负值"

    async def evaluate(
        self,
        customer: MonitoringCustomer,
        tushare: "TushareService",
        bocha: "BochaService",
        llm: "LLMService",
        thresholds: dict[str, float],
    ) -> SignalResult:
        df = await tushare.get_cashflow(ts_code=customer.ts_code)
        df = df.sort_values("end_date").tail(4)

        if len(df) >= 2:
            last_two = df["n_cashflow_act"].tail(2).tolist()
            if all(v < 0 for v in last_two):
                return SignalResult(
                    rule_name=self.name,
                    level=SignalLevel.RED,
                    detected_value=str(last_two),
                    threshold="0",
                    explanation=f"经营性现金流连续 2 季为负: {last_two}",
                    raw_data_ref={"ts_code": customer.ts_code},
                )

            prev = float(df["n_cashflow_act"].iloc[-2])
            curr = float(df["n_cashflow_act"].iloc[-1])
            if prev > 0:
                drop_pct = (prev - curr) / prev * 100.0
                if drop_pct >= thresholds["yellow_op_cf_qoq_drop_pct"]:
                    return SignalResult(
                        rule_name=self.name,
                        level=SignalLevel.YELLOW,
                        detected_value=drop_pct,
                        threshold=thresholds["yellow_op_cf_qoq_drop_pct"],
                        explanation=f"经营性现金流单季环比 -{drop_pct:.1f}% 超阈值",
                        raw_data_ref={"ts_code": customer.ts_code},
                    )

        return SignalResult(
            rule_name=self.name,
            level=SignalLevel.GREEN,
            explanation="现金流正常",
        )
```

- [ ] **Step 8b.2**: 跑 + commit

```bash
uv run pytest backend/tests/unit/test_signal_rules_cash_flow.py -v
uv run mypy backend
git add backend/app/services/monitoring/signal_rules/cash_flow.py backend/tests/unit/test_signal_rules_cash_flow.py
git commit -m "feat(monitoring): cash_flow signal rule (QoQ drop + consecutive negative)"
```

### 8c. shareholder_count rule

- [ ] **Step 8c.1**: 写测试 + 实现

测试 `backend/tests/unit/test_signal_rules_shareholder_count.py`(类比 8b):

```python
"""Unit tests for ShareholderCountRule."""
from __future__ import annotations

from unittest.mock import AsyncMock, MagicMock

import pandas as pd
import pytest

from app.services.monitoring.signal_rules.base import MonitoringCustomer, SignalLevel
from app.services.monitoring.signal_rules.defaults import DEFAULT_THRESHOLDS
from app.services.monitoring.signal_rules.shareholder_count import ShareholderCountRule


@pytest.fixture
def customer() -> MonitoringCustomer:
    return MonitoringCustomer(id="x", ts_code="x.SH", name="x", industry="x")


def _ts(df: pd.DataFrame) -> MagicMock:
    fake = MagicMock()
    fake.get_stk_holdernumber = AsyncMock(return_value=df)
    return fake


@pytest.mark.asyncio
async def test_green_stable(customer: MonitoringCustomer) -> None:
    df = pd.DataFrame([{"end_date": ed, "holder_num": v} for ed, v in [
        ("20240930", 100000), ("20241231", 99000),
    ]])
    r = await ShareholderCountRule().evaluate(customer, _ts(df), MagicMock(), MagicMock(), DEFAULT_THRESHOLDS["shareholder_count"])
    assert r.level == SignalLevel.GREEN


@pytest.mark.asyncio
async def test_yellow_15pct_drop(customer: MonitoringCustomer) -> None:
    df = pd.DataFrame([{"end_date": ed, "holder_num": v} for ed, v in [
        ("20240930", 100000), ("20241231", 85000),
    ]])
    r = await ShareholderCountRule().evaluate(customer, _ts(df), MagicMock(), MagicMock(), DEFAULT_THRESHOLDS["shareholder_count"])
    assert r.level == SignalLevel.YELLOW


@pytest.mark.asyncio
async def test_red_25pct_drop(customer: MonitoringCustomer) -> None:
    df = pd.DataFrame([{"end_date": ed, "holder_num": v} for ed, v in [
        ("20240930", 100000), ("20241231", 75000),
    ]])
    r = await ShareholderCountRule().evaluate(customer, _ts(df), MagicMock(), MagicMock(), DEFAULT_THRESHOLDS["shareholder_count"])
    assert r.level == SignalLevel.RED


@pytest.mark.asyncio
async def test_green_when_data_insufficient(customer: MonitoringCustomer) -> None:
    df = pd.DataFrame(columns=["end_date", "holder_num"])
    r = await ShareholderCountRule().evaluate(customer, _ts(df), MagicMock(), MagicMock(), DEFAULT_THRESHOLDS["shareholder_count"])
    assert r.level == SignalLevel.GREEN
```

实现 `backend/app/services/monitoring/signal_rules/shareholder_count.py`:

```python
"""ShareholderCountRule — detects 股东户数骤减(筹码集中预警)."""
from __future__ import annotations

from typing import TYPE_CHECKING

from app.services.monitoring.signal_rules.base import (
    MonitoringCustomer,
    SignalLevel,
    SignalResult,
    SignalRule,
)

if TYPE_CHECKING:
    from app.services.bocha_factory import BochaService
    from app.services.llm_service import LLMService
    from app.services.tushare_service import TushareService


class ShareholderCountRule(SignalRule):
    name = "shareholder_count"
    description = "Detects 股东户数骤减 (yellow: -10~-20% / red: < -20%)"

    async def evaluate(
        self,
        customer: MonitoringCustomer,
        tushare: "TushareService",
        bocha: "BochaService",
        llm: "LLMService",
        thresholds: dict[str, float],
    ) -> SignalResult:
        df = await tushare.get_stk_holdernumber(ts_code=customer.ts_code)
        df = df.sort_values("end_date").tail(2)
        if len(df) < 2:
            return SignalResult(
                rule_name=self.name, level=SignalLevel.GREEN, explanation="数据不足"
            )

        prev = float(df["holder_num"].iloc[-2])
        curr = float(df["holder_num"].iloc[-1])
        if prev <= 0:
            return SignalResult(
                rule_name=self.name, level=SignalLevel.GREEN, explanation="数据异常"
            )

        drop_pct = (prev - curr) / prev * 100.0

        if drop_pct >= thresholds["red_drop_pct"]:
            return SignalResult(
                rule_name=self.name,
                level=SignalLevel.RED,
                detected_value=drop_pct,
                threshold=thresholds["red_drop_pct"],
                explanation=f"股东户数环比 -{drop_pct:.1f}% 超 RED 阈值",
                raw_data_ref={"ts_code": customer.ts_code},
            )

        if drop_pct >= thresholds["yellow_min_drop_pct"]:
            return SignalResult(
                rule_name=self.name,
                level=SignalLevel.YELLOW,
                detected_value=drop_pct,
                threshold=thresholds["yellow_min_drop_pct"],
                explanation=f"股东户数环比 -{drop_pct:.1f}% 超 YELLOW 阈值",
                raw_data_ref={"ts_code": customer.ts_code},
            )

        return SignalResult(rule_name=self.name, level=SignalLevel.GREEN, explanation="股东户数正常")
```

- [ ] **Step 8c.2**: 跑 + commit

```bash
uv run pytest backend/tests/unit/test_signal_rules_shareholder_count.py -v
uv run mypy backend
git add backend/app/services/monitoring/signal_rules/shareholder_count.py backend/tests/unit/test_signal_rules_shareholder_count.py
git commit -m "feat(monitoring): shareholder_count signal rule"
```

### 8d. announcement rule(LLM 分类)

- [ ] **Step 8d.1**: 写测试 + 实现

测试 `backend/tests/unit/test_signal_rules_announcement.py`:

```python
"""Unit tests for AnnouncementRule(LLM 分类)."""
from __future__ import annotations

from unittest.mock import AsyncMock, MagicMock

import pandas as pd
import pytest

from app.services.monitoring.signal_rules.announcement import AnnouncementRule
from app.services.monitoring.signal_rules.base import MonitoringCustomer, SignalLevel
from app.services.monitoring.signal_rules.defaults import DEFAULT_THRESHOLDS


@pytest.fixture
def customer() -> MonitoringCustomer:
    return MonitoringCustomer(id="x", ts_code="x.SH", name="x", industry="x")


def _llm(score: float) -> MagicMock:
    fake = MagicMock()
    # AnnouncementRule 用 llm.chat(...) 返回 LLMResponse with parsed.score
    parsed = MagicMock()
    parsed.score = score
    parsed.summary = "mock"
    response = MagicMock()
    response.parsed = parsed
    response.cost_cny = 0.05
    fake.chat = AsyncMock(return_value=response)
    return fake


def _ts(anns_df: pd.DataFrame) -> MagicMock:
    fake = MagicMock()
    fake.get_anns = AsyncMock(return_value=anns_df)
    return fake


@pytest.mark.asyncio
async def test_green_no_announcement(customer: MonitoringCustomer) -> None:
    df = pd.DataFrame(columns=["title", "content"])
    r = await AnnouncementRule().evaluate(customer, _ts(df), MagicMock(), _llm(0.0), DEFAULT_THRESHOLDS["announcement"])
    assert r.level == SignalLevel.GREEN


@pytest.mark.asyncio
async def test_yellow_on_mid_score(customer: MonitoringCustomer) -> None:
    df = pd.DataFrame([{"title": "诉讼公告", "content": "x"}])
    r = await AnnouncementRule().evaluate(customer, _ts(df), MagicMock(), _llm(0.6), DEFAULT_THRESHOLDS["announcement"])
    assert r.level == SignalLevel.YELLOW


@pytest.mark.asyncio
async def test_red_on_high_score(customer: MonitoringCustomer) -> None:
    df = pd.DataFrame([{"title": "立案调查", "content": "x"}])
    r = await AnnouncementRule().evaluate(customer, _ts(df), MagicMock(), _llm(0.9), DEFAULT_THRESHOLDS["announcement"])
    assert r.level == SignalLevel.RED
```

实现 `backend/app/services/monitoring/signal_rules/announcement.py`:

```python
"""AnnouncementRule — uses LLM to classify recent announcements as 重大/可疑/普通."""
from __future__ import annotations

from datetime import datetime, timedelta
from typing import TYPE_CHECKING

from pydantic import BaseModel, Field

from app.services.monitoring.signal_rules.base import (
    MonitoringCustomer,
    SignalLevel,
    SignalResult,
    SignalRule,
)

if TYPE_CHECKING:
    from app.services.bocha_factory import BochaService
    from app.services.llm_service import LLMService
    from app.services.tushare_service import TushareService


class AnnouncementClassification(BaseModel):
    score: float = Field(ge=0.0, le=1.0, description="负面程度 0-1")
    summary: str


_PROMPT = """以下是 A 股上市公司{name}({ts_code})最近 7 天的公告标题和摘要,请判断这些公告整体上对公司是否构成负面信号。

公告列表:
{anns_text}

输出 JSON: {{"score": float 0~1, "summary": "一句话原因"}}
0.0 = 完全无影响(常规公告);0.5 = 有一定关注价值;0.8+ = 重大负面(立案/处罚/退市/造假/重大诉讼/巨额减持等)
"""


class AnnouncementRule(SignalRule):
    name = "announcement"
    description = "LLM 分类最近 7 天公告负面程度"

    async def evaluate(
        self,
        customer: MonitoringCustomer,
        tushare: "TushareService",
        bocha: "BochaService",
        llm: "LLMService",
        thresholds: dict[str, float],
    ) -> SignalResult:
        end = datetime.now().strftime("%Y%m%d")
        start = (datetime.now() - timedelta(days=7)).strftime("%Y%m%d")
        df = await tushare.get_anns(ts_code=customer.ts_code, start=start, end=end)
        if df.empty:
            return SignalResult(
                rule_name=self.name, level=SignalLevel.GREEN, explanation="近 7 天无公告"
            )

        anns_text = "\n".join(
            f"- {row.get('title', '')}: {(row.get('content', '') or '')[:200]}"
            for _, row in df.iterrows()
        )
        prompt = _PROMPT.format(name=customer.name, ts_code=customer.ts_code, anns_text=anns_text)

        response = await llm.chat(prompt=prompt, tier="cheap", schema=AnnouncementClassification)
        parsed = response.parsed
        if parsed is None:
            return SignalResult(
                rule_name=self.name, level=SignalLevel.GREEN, explanation="LLM 解析失败"
            )

        score = float(parsed.score)
        if score >= thresholds["red_threshold"]:
            level = SignalLevel.RED
        elif score >= thresholds["yellow_lower"]:
            level = SignalLevel.YELLOW
        else:
            level = SignalLevel.GREEN

        return SignalResult(
            rule_name=self.name,
            level=level,
            detected_value=score,
            threshold=thresholds["red_threshold"],
            explanation=f"LLM 评分 {score:.2f}: {parsed.summary}",
            raw_data_ref={"ts_code": customer.ts_code, "anns_count": len(df)},
        )
```

- [ ] **Step 8d.2**: 跑 + commit

```bash
uv run pytest backend/tests/unit/test_signal_rules_announcement.py -v
uv run mypy backend
git add backend/app/services/monitoring/signal_rules/announcement.py backend/tests/unit/test_signal_rules_announcement.py
git commit -m "feat(monitoring): announcement signal rule (LLM-classified, cheap tier)"
```

### 8e. price_anomaly rule

- [ ] **Step 8e.1**: 写测试 + 实现

测试 `backend/tests/unit/test_signal_rules_price_anomaly.py`:

```python
"""Unit tests for PriceAnomalyRule."""
from __future__ import annotations

from unittest.mock import AsyncMock, MagicMock

import pandas as pd
import pytest

from app.services.monitoring.signal_rules.base import MonitoringCustomer, SignalLevel
from app.services.monitoring.signal_rules.defaults import DEFAULT_THRESHOLDS
from app.services.monitoring.signal_rules.price_anomaly import PriceAnomalyRule


@pytest.fixture
def customer() -> MonitoringCustomer:
    return MonitoringCustomer(id="x", ts_code="x.SH", name="x", industry="x")


def _ts(df: pd.DataFrame) -> MagicMock:
    fake = MagicMock()
    fake.get_daily = AsyncMock(return_value=df)
    return fake


def _build_daily(prices: list[float]) -> pd.DataFrame:
    return pd.DataFrame(
        [
            {"trade_date": f"2024050{i+1}", "close": p, "pct_chg": 0.0}
            for i, p in enumerate(prices)
        ]
    )


@pytest.mark.asyncio
async def test_green_stable(customer: MonitoringCustomer) -> None:
    df = _build_daily([100.0] * 60)
    r = await PriceAnomalyRule().evaluate(customer, _ts(df), MagicMock(), MagicMock(), DEFAULT_THRESHOLDS["price_anomaly"])
    assert r.level == SignalLevel.GREEN


@pytest.mark.asyncio
async def test_yellow_single_day_drop(customer: MonitoringCustomer) -> None:
    df = _build_daily([100.0] * 59 + [93.0])  # -7%
    r = await PriceAnomalyRule().evaluate(customer, _ts(df), MagicMock(), MagicMock(), DEFAULT_THRESHOLDS["price_anomaly"])
    assert r.level == SignalLevel.YELLOW


@pytest.mark.asyncio
async def test_red_single_day_drop(customer: MonitoringCustomer) -> None:
    df = _build_daily([100.0] * 59 + [88.0])  # -12%
    r = await PriceAnomalyRule().evaluate(customer, _ts(df), MagicMock(), MagicMock(), DEFAULT_THRESHOLDS["price_anomaly"])
    assert r.level == SignalLevel.RED


@pytest.mark.asyncio
async def test_yellow_60d_cumulative(customer: MonitoringCustomer) -> None:
    df = _build_daily([100.0] + [85.0] * 59)  # 60d -15%(yellow)
    r = await PriceAnomalyRule().evaluate(customer, _ts(df), MagicMock(), MagicMock(), DEFAULT_THRESHOLDS["price_anomaly"])
    assert r.level == SignalLevel.YELLOW
```

实现 `backend/app/services/monitoring/signal_rules/price_anomaly.py`:

```python
"""PriceAnomalyRule — detects 单日 / 60 日累计跌幅 anomaly."""
from __future__ import annotations

from datetime import datetime, timedelta
from typing import TYPE_CHECKING

from app.services.monitoring.signal_rules.base import (
    MonitoringCustomer,
    SignalLevel,
    SignalResult,
    SignalRule,
)

if TYPE_CHECKING:
    from app.services.bocha_factory import BochaService
    from app.services.llm_service import LLMService
    from app.services.tushare_service import TushareService


class PriceAnomalyRule(SignalRule):
    name = "price_anomaly"
    description = "Detects 单日跌幅 / 60 日累计跌幅"

    async def evaluate(
        self,
        customer: MonitoringCustomer,
        tushare: "TushareService",
        bocha: "BochaService",
        llm: "LLMService",
        thresholds: dict[str, float],
    ) -> SignalResult:
        end = datetime.now().strftime("%Y%m%d")
        start = (datetime.now() - timedelta(days=90)).strftime("%Y%m%d")
        df = await tushare.get_daily(ts_code=customer.ts_code, start=start, end=end)
        df = df.sort_values("trade_date").tail(60)
        if len(df) < 2:
            return SignalResult(rule_name=self.name, level=SignalLevel.GREEN, explanation="数据不足")

        prev_close = float(df["close"].iloc[-2])
        curr_close = float(df["close"].iloc[-1])
        single_drop_pct = (prev_close - curr_close) / prev_close * 100.0

        if single_drop_pct >= thresholds["red_single_day_drop_pct"]:
            return SignalResult(
                rule_name=self.name,
                level=SignalLevel.RED,
                detected_value=single_drop_pct,
                threshold=thresholds["red_single_day_drop_pct"],
                explanation=f"单日跌幅 -{single_drop_pct:.1f}% 超 RED 阈值",
                raw_data_ref={"ts_code": customer.ts_code},
            )

        if single_drop_pct >= thresholds["yellow_single_day_drop_pct"]:
            return SignalResult(
                rule_name=self.name,
                level=SignalLevel.YELLOW,
                detected_value=single_drop_pct,
                threshold=thresholds["yellow_single_day_drop_pct"],
                explanation=f"单日跌幅 -{single_drop_pct:.1f}% 超 YELLOW 阈值",
                raw_data_ref={"ts_code": customer.ts_code},
            )

        if len(df) >= 60:
            first = float(df["close"].iloc[0])
            cum_drop_pct = (first - curr_close) / first * 100.0
            if cum_drop_pct >= thresholds["yellow_60d_drop_pct"]:
                return SignalResult(
                    rule_name=self.name,
                    level=SignalLevel.YELLOW,
                    detected_value=cum_drop_pct,
                    threshold=thresholds["yellow_60d_drop_pct"],
                    explanation=f"60 日累计跌幅 -{cum_drop_pct:.1f}% 超阈值",
                    raw_data_ref={"ts_code": customer.ts_code},
                )

        return SignalResult(rule_name=self.name, level=SignalLevel.GREEN, explanation="价格平稳")
```

- [ ] **Step 8e.2**: 跑 + commit

```bash
uv run pytest backend/tests/unit/test_signal_rules_price_anomaly.py -v
uv run mypy backend
git add backend/app/services/monitoring/signal_rules/price_anomaly.py backend/tests/unit/test_signal_rules_price_anomaly.py
git commit -m "feat(monitoring): price_anomaly signal rule (single-day + 60d cumulative)"
```

---

## Task 9: SignalDetector(编排 5 rule + 汇总)

**Files:**
- Create: `backend/app/services/monitoring/signal_detector.py`
- Create: `backend/tests/unit/test_monitoring_signal_detector.py`

**Spec ref**: § 5.2 汇总规则

- [ ] **Step 9.1**: 写失败的 unit test

写入 `backend/tests/unit/test_monitoring_signal_detector.py`:

```python
"""Unit tests for SignalDetector."""
from __future__ import annotations

from unittest.mock import AsyncMock, MagicMock

import pytest

from app.services.monitoring.signal_detector import SignalDetector
from app.services.monitoring.signal_rules.base import (
    MonitoringCustomer,
    SignalLevel,
    SignalResult,
    SignalRule,
)


class FakeRule(SignalRule):
    def __init__(self, name: str, level: SignalLevel) -> None:
        self.name = name  # type: ignore[misc]
        self._level = level

    async def evaluate(self, customer, tushare, bocha, llm, thresholds):  # type: ignore[no-untyped-def]
        return SignalResult(rule_name=self.name, level=self._level, explanation="fake")


@pytest.fixture
def customer() -> MonitoringCustomer:
    return MonitoringCustomer(id="x", ts_code="x.SH", name="x", industry="x")


@pytest.mark.asyncio
async def test_all_green_returns_green(customer: MonitoringCustomer) -> None:
    detector = SignalDetector(rules=[FakeRule("a", SignalLevel.GREEN), FakeRule("b", SignalLevel.GREEN)])
    overall, results = await detector.detect(customer, MagicMock(), MagicMock(), MagicMock(), {})
    assert overall == SignalLevel.GREEN
    assert len(results) == 2


@pytest.mark.asyncio
async def test_one_yellow_returns_yellow(customer: MonitoringCustomer) -> None:
    detector = SignalDetector(rules=[FakeRule("a", SignalLevel.GREEN), FakeRule("b", SignalLevel.YELLOW)])
    overall, _ = await detector.detect(customer, MagicMock(), MagicMock(), MagicMock(), {})
    assert overall == SignalLevel.YELLOW


@pytest.mark.asyncio
async def test_one_red_returns_red(customer: MonitoringCustomer) -> None:
    detector = SignalDetector(rules=[FakeRule("a", SignalLevel.YELLOW), FakeRule("b", SignalLevel.RED)])
    overall, _ = await detector.detect(customer, MagicMock(), MagicMock(), MagicMock(), {})
    assert overall == SignalLevel.RED


@pytest.mark.asyncio
async def test_rule_exception_caught_as_green(customer: MonitoringCustomer) -> None:
    """规则抛错不应阻塞其他 rule;该 rule 算 green + log."""
    class BoomRule(SignalRule):
        name = "boom"
        async def evaluate(self, customer, tushare, bocha, llm, thresholds):  # type: ignore[no-untyped-def]
            raise RuntimeError("kaboom")

    detector = SignalDetector(rules=[BoomRule(), FakeRule("ok", SignalLevel.GREEN)])
    overall, results = await detector.detect(customer, MagicMock(), MagicMock(), MagicMock(), {})
    assert overall == SignalLevel.GREEN
    assert len(results) == 2
    assert any(r.rule_name == "boom" and "error" in r.explanation.lower() for r in results)
```

- [ ] **Step 9.2**: 跑测试确认失败

```bash
uv run pytest backend/tests/unit/test_monitoring_signal_detector.py -v
```
Expected: FAIL `ModuleNotFoundError`

- [ ] **Step 9.3**: 实现 SignalDetector

写入 `backend/app/services/monitoring/signal_detector.py`:

```python
"""SignalDetector — runs all SignalRules concurrently, aggregates max level."""
from __future__ import annotations

import asyncio
import logging
from typing import TYPE_CHECKING

from app.services.monitoring.signal_rules.base import (
    MonitoringCustomer,
    SignalLevel,
    SignalResult,
    SignalRule,
)

if TYPE_CHECKING:
    from app.services.bocha_factory import BochaService
    from app.services.llm_service import LLMService
    from app.services.tushare_service import TushareService

_logger = logging.getLogger(__name__)


_LEVEL_ORDER = {SignalLevel.GREEN: 0, SignalLevel.YELLOW: 1, SignalLevel.RED: 2}


class SignalDetector:
    def __init__(self, rules: list[SignalRule]) -> None:
        if not rules:
            raise ValueError("rules must not be empty")
        self._rules = rules

    async def detect(
        self,
        customer: MonitoringCustomer,
        tushare: "TushareService",
        bocha: "BochaService",
        llm: "LLMService",
        thresholds_per_rule: dict[str, dict[str, float]],
    ) -> tuple[SignalLevel, list[SignalResult]]:
        async def _safe(rule: SignalRule) -> SignalResult:
            try:
                rule_thresh = thresholds_per_rule.get(rule.name, {})
                return await rule.evaluate(customer, tushare, bocha, llm, rule_thresh)
            except Exception as exc:
                _logger.warning("signal rule %s failed: %s", rule.name, exc)
                return SignalResult(
                    rule_name=rule.name,
                    level=SignalLevel.GREEN,
                    explanation=f"error: {exc}",
                )

        results = await asyncio.gather(*(_safe(r) for r in self._rules))
        overall = max(results, key=lambda r: _LEVEL_ORDER[r.level]).level
        return overall, list(results)
```

- [ ] **Step 9.4**: 跑测试 + mypy + commit

```bash
uv run pytest backend/tests/unit/test_monitoring_signal_detector.py -v
uv run mypy backend
git add backend/app/services/monitoring/signal_detector.py backend/tests/unit/test_monitoring_signal_detector.py
git commit -m "feat(monitoring): SignalDetector concurrent rule eval + max-level aggregation"
```

---

## Task 10: PortfolioWarningReport schema + renderer

**Files:**
- Create: `backend/app/agents/portfolio_warning_schema.py`
- Create: `backend/app/agents/portfolio_warning_renderer.py`
- Create: `backend/tests/unit/test_portfolio_warning_schema.py`
- Create: `backend/tests/unit/test_portfolio_warning_renderer.py`

**Spec ref**: § 8

- [ ] **Step 10.1**: 写失败的 schema test

写入 `backend/tests/unit/test_portfolio_warning_schema.py`:

```python
"""Unit tests for PortfolioWarningReport schema."""
from __future__ import annotations

from datetime import datetime

import pytest
from pydantic import ValidationError

from app.agents.portfolio_warning_schema import (
    DeepDiveSection,
    PortfolioWarningReport,
    ReferenceItem,
    RiskDiagnosis,
)
from app.services.monitoring.signal_rules.base import SignalLevel, SignalResult


def _green_report() -> PortfolioWarningReport:
    return PortfolioWarningReport(
        customer_id="c1",
        customer_name="x",
        ts_code="x.SH",
        industry="x",
        run_id="r1",
        alert_id="a1",
        generated_at=datetime(2026, 5, 4, 16, 30),
        alert_level=SignalLevel.GREEN,
        summary="一切正常",
        triggered_signals=[],
        risk_diagnosis=None,
        deep_dive=None,
        recommendations=[],
        data_sources=["tushare"],
        data_limitations=[],
        references=[],
    )


def test_green_report_validates() -> None:
    report = _green_report()
    assert report.alert_level == SignalLevel.GREEN


def test_yellow_report_with_signals() -> None:
    report = _green_report().model_copy(update={
        "alert_level": SignalLevel.YELLOW,
        "summary": "有 1 项风险",
        "triggered_signals": [
            SignalResult(rule_name="cash_flow", level=SignalLevel.YELLOW, explanation="x"),
        ],
        "risk_diagnosis": RiskDiagnosis(narrative="风险叙述", severity="medium"),
        "recommendations": ["关注现金流"],
    })
    assert report.alert_level == SignalLevel.YELLOW
    assert len(report.triggered_signals) == 1


def test_red_with_deep_dive() -> None:
    report = _green_report().model_copy(update={
        "alert_level": SignalLevel.RED,
        "summary": "高危",
        "deep_dive": DeepDiveSection(content="深度调查内容...", evidence_chunk_ids=["c1", "c2"]),
        "recommendations": ["紧急回访"],
    })
    assert report.deep_dive is not None
    assert len(report.deep_dive.evidence_chunk_ids) == 2


def test_extra_field_rejected() -> None:
    with pytest.raises(ValidationError):
        PortfolioWarningReport.model_validate({"customer_id": "x", "_extra": "bad"})


def test_reference_item_basic() -> None:
    ref = ReferenceItem(source="tushare:income", url=None, snippet="...")
    assert ref.source == "tushare:income"
```

- [ ] **Step 10.2**: 实现 schema

写入 `backend/app/agents/portfolio_warning_schema.py`:

```python
"""PortfolioWarningReport — B-3 持仓预警 deliverable schema.

Spec ref: docs/superpowers/specs/2026-05-04-v0.8.3-tushare-and-portfolio-monitoring-design.md § 8
"""
from __future__ import annotations

from datetime import datetime
from typing import Literal

from pydantic import BaseModel, ConfigDict, Field

from app.services.monitoring.signal_rules.base import SignalLevel, SignalResult


class ReferenceItem(BaseModel):
    model_config = ConfigDict(extra="forbid")
    source: str
    url: str | None = None
    snippet: str = ""


class RiskDiagnosis(BaseModel):
    model_config = ConfigDict(extra="forbid")
    narrative: str
    severity: Literal["low", "medium", "high"]


class DeepDiveSection(BaseModel):
    model_config = ConfigDict(extra="forbid")
    content: str
    evidence_chunk_ids: list[str] = Field(default_factory=list)


class PortfolioWarningReport(BaseModel):
    model_config = ConfigDict(extra="forbid")

    customer_id: str
    customer_name: str
    ts_code: str
    industry: str
    run_id: str
    alert_id: str
    generated_at: datetime
    alert_level: SignalLevel

    summary: str

    triggered_signals: list[SignalResult] = Field(default_factory=list)
    risk_diagnosis: RiskDiagnosis | None = None
    deep_dive: DeepDiveSection | None = None
    recommendations: list[str] = Field(default_factory=list)

    data_sources: list[str] = Field(default_factory=list)
    data_limitations: list[str] = Field(default_factory=list)
    references: list[ReferenceItem] = Field(default_factory=list)
```

- [ ] **Step 10.3**: 写失败的 renderer test

写入 `backend/tests/unit/test_portfolio_warning_renderer.py`:

```python
"""Unit tests for portfolio_warning_renderer."""
from __future__ import annotations

from datetime import datetime

from app.agents.portfolio_warning_renderer import render_portfolio_warning_markdown
from app.agents.portfolio_warning_schema import (
    DeepDiveSection,
    PortfolioWarningReport,
    RiskDiagnosis,
)
from app.services.monitoring.signal_rules.base import SignalLevel, SignalResult


def _build_report(level: SignalLevel, with_deep_dive: bool = False) -> PortfolioWarningReport:
    return PortfolioWarningReport(
        customer_id="c1",
        customer_name="测试公司",
        ts_code="600519.SH",
        industry="消费",
        run_id="r1",
        alert_id="a1",
        generated_at=datetime(2026, 5, 4, 16, 30),
        alert_level=level,
        summary="测试综述",
        triggered_signals=[
            SignalResult(rule_name="cash_flow", level=SignalLevel.YELLOW, detected_value=35.0, threshold=30.0, explanation="现金流恶化"),
        ] if level != SignalLevel.GREEN else [],
        risk_diagnosis=RiskDiagnosis(narrative="风险...", severity="medium") if level != SignalLevel.GREEN else None,
        deep_dive=DeepDiveSection(content="深度调查...") if with_deep_dive else None,
        recommendations=["关注现金流"] if level != SignalLevel.GREEN else [],
        data_sources=["tushare"],
        data_limitations=[],
        references=[],
    )


def test_green_renders_minimum() -> None:
    md = render_portfolio_warning_markdown(_build_report(SignalLevel.GREEN))
    assert "测试公司" in md
    assert "GREEN" in md.upper() or "正常" in md or "绿色" in md


def test_yellow_renders_signals() -> None:
    md = render_portfolio_warning_markdown(_build_report(SignalLevel.YELLOW))
    assert "cash_flow" in md
    assert "现金流恶化" in md


def test_red_with_deep_dive_renders_section() -> None:
    md = render_portfolio_warning_markdown(_build_report(SignalLevel.RED, with_deep_dive=True))
    assert "深度调查" in md


def test_yellow_no_deep_dive_no_section() -> None:
    md = render_portfolio_warning_markdown(_build_report(SignalLevel.YELLOW))
    assert "深度调查" not in md
```

- [ ] **Step 10.4**: 实现 renderer

写入 `backend/app/agents/portfolio_warning_renderer.py`:

```python
"""Render PortfolioWarningReport → markdown(类比 credit_report_renderer)."""
from __future__ import annotations

from app.agents.portfolio_warning_schema import PortfolioWarningReport
from app.services.monitoring.signal_rules.base import SignalLevel

_LEVEL_LABEL = {
    SignalLevel.GREEN: "🟢 绿色 (正常)",
    SignalLevel.YELLOW: "🟡 黄色 (警示)",
    SignalLevel.RED: "🔴 红色 (高危)",
}


def render_portfolio_warning_markdown(report: PortfolioWarningReport) -> str:
    lines: list[str] = []
    lines.append(f"# {report.customer_name} — 持仓预警报告")
    lines.append("")
    lines.append(f"- **客户**: {report.customer_name} ({report.ts_code})")
    lines.append(f"- **行业**: {report.industry}")
    lines.append(f"- **生成时间**: {report.generated_at.isoformat()}")
    lines.append(f"- **alert 等级**: {_LEVEL_LABEL[report.alert_level]}")
    lines.append("")
    lines.append("## 综述")
    lines.append("")
    lines.append(report.summary)
    lines.append("")

    if report.triggered_signals:
        lines.append("## 触发信号")
        lines.append("")
        lines.append("| 信号 | 等级 | 检测值 | 阈值 | 说明 |")
        lines.append("|---|---|---|---|---|")
        for s in report.triggered_signals:
            lines.append(
                f"| {s.rule_name} | {s.level.value} | {s.detected_value} | {s.threshold} | {s.explanation} |"
            )
        lines.append("")

    if report.risk_diagnosis:
        lines.append("## 风险诊断")
        lines.append("")
        lines.append(f"**严重度**: {report.risk_diagnosis.severity}")
        lines.append("")
        lines.append(report.risk_diagnosis.narrative)
        lines.append("")

    if report.deep_dive:
        lines.append("## 深度调查")
        lines.append("")
        lines.append(report.deep_dive.content)
        lines.append("")
        if report.deep_dive.evidence_chunk_ids:
            lines.append(f"_引用 chunk: {', '.join(report.deep_dive.evidence_chunk_ids)}_")
            lines.append("")

    if report.recommendations:
        lines.append("## 行动建议")
        lines.append("")
        for r in report.recommendations:
            lines.append(f"- {r}")
        lines.append("")

    if report.data_sources or report.data_limitations:
        lines.append("## 数据声明")
        lines.append("")
        if report.data_sources:
            lines.append(f"- 数据来源: {', '.join(report.data_sources)}")
        if report.data_limitations:
            for lim in report.data_limitations:
                lines.append(f"- 数据局限: {lim}")
        lines.append("")

    if report.references:
        lines.append("## 引用")
        lines.append("")
        for ref in report.references:
            url_part = f" [link]({ref.url})" if ref.url else ""
            lines.append(f"- **{ref.source}**{url_part}: {ref.snippet}")
        lines.append("")

    return "\n".join(lines)
```

- [ ] **Step 10.5**: 跑测试 + mypy + commit

```bash
uv run pytest backend/tests/unit/test_portfolio_warning_schema.py backend/tests/unit/test_portfolio_warning_renderer.py -v
uv run mypy backend
git add backend/app/agents/portfolio_warning_schema.py \
        backend/app/agents/portfolio_warning_renderer.py \
        backend/tests/unit/test_portfolio_warning_schema.py \
        backend/tests/unit/test_portfolio_warning_renderer.py
git commit -m "feat(b3): PortfolioWarningReport schema + markdown renderer"
```

---

## Task 11: EmailNotifier Protocol + Real + Mock + factory

**Files:**
- Create: `backend/app/services/monitoring/notifications/__init__.py`
- Create: `backend/app/services/monitoring/notifications/email_notifier.py`
- Create: `backend/app/services/monitoring/notifications/mock_email_notifier.py`
- Create: `backend/app/services/monitoring/notifications/factory.py`
- Create: `backend/tests/unit/test_monitoring_email_notifier.py`

**Spec ref**: § 7

- [ ] **Step 11.1**: 写失败的 unit test

写入 `backend/tests/unit/test_monitoring_email_notifier.py`:

```python
"""Unit tests for EmailNotifier (real + mock + factory)."""
from __future__ import annotations

from pathlib import Path
from unittest.mock import MagicMock, patch

import pytest

from app.services.monitoring.notifications.email_notifier import (
    EmailNotifier,
    EmailSendResult,
    RealEmailNotifier,
)
from app.services.monitoring.notifications.factory import build_email_notifier
from app.services.monitoring.notifications.mock_email_notifier import (
    MockEmailNotifier,
)


@pytest.mark.asyncio
async def test_mock_records_to_sqlite(tmp_path: Path) -> None:
    notifier = MockEmailNotifier(db_path=tmp_path / "emails.sqlite")
    result = await notifier.send(
        to_addrs=["a@b.com"],
        subject="测试",
        body_text="hi",
    )
    assert result.success is True
    rows = notifier.list_recorded()
    assert len(rows) == 1
    assert rows[0]["subject"] == "测试"


@pytest.mark.asyncio
async def test_real_notifier_uses_smtp() -> None:
    fake_smtp = MagicMock()
    fake_smtp.__enter__ = MagicMock(return_value=fake_smtp)
    fake_smtp.__exit__ = MagicMock(return_value=False)
    fake_smtp.send_message = MagicMock()

    with patch("smtplib.SMTP_SSL", return_value=fake_smtp):
        n = RealEmailNotifier(
            host="smtp.test.com",
            port=465,
            user="u@t.com",
            password="x",
            from_addr="u@t.com",
        )
        result = await n.send(to_addrs=["a@b.com"], subject="s", body_text="hi")

    assert result.success
    fake_smtp.login.assert_called_once_with("u@t.com", "x")
    fake_smtp.send_message.assert_called_once()


def test_factory_default_mock(monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.delenv("EMAIL_MODE", raising=False)
    n = build_email_notifier()
    assert isinstance(n, MockEmailNotifier)


def test_factory_real_requires_smtp_envs(monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.setenv("EMAIL_MODE", "real")
    monkeypatch.delenv("SMTP_HOST", raising=False)
    with pytest.raises(KeyError):
        build_email_notifier()


def test_factory_real_with_envs(monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.setenv("EMAIL_MODE", "real")
    monkeypatch.setenv("SMTP_HOST", "smtp.test.com")
    monkeypatch.setenv("SMTP_PORT", "465")
    monkeypatch.setenv("SMTP_USER", "u@t.com")
    monkeypatch.setenv("SMTP_PASSWORD", "x")
    monkeypatch.setenv("SMTP_FROM", "u@t.com")
    n = build_email_notifier()
    assert isinstance(n, RealEmailNotifier)


def test_protocol_runtime_check() -> None:
    assert hasattr(MockEmailNotifier, "send")
    assert hasattr(RealEmailNotifier, "send")
    assert EmailNotifier is not None
```

- [ ] **Step 11.2**: 跑测试确认失败

```bash
uv run pytest backend/tests/unit/test_monitoring_email_notifier.py -v
```
Expected: FAIL `ModuleNotFoundError`

- [ ] **Step 11.3**: 实现 notifier files

写入 `backend/app/services/monitoring/notifications/__init__.py`(空)

写入 `backend/app/services/monitoring/notifications/email_notifier.py`:

```python
"""EmailNotifier Protocol + RealEmailNotifier(SMTP)."""
from __future__ import annotations

import asyncio
import smtplib
from email.message import EmailMessage
from typing import Protocol, runtime_checkable

from pydantic import BaseModel


class EmailSendResult(BaseModel):
    success: bool
    error: str | None = None


@runtime_checkable
class EmailNotifier(Protocol):
    async def send(
        self,
        to_addrs: list[str],
        subject: str,
        body_text: str,
        body_html: str | None = None,
    ) -> EmailSendResult: ...


class RealEmailNotifier:
    def __init__(
        self,
        host: str,
        port: int,
        user: str,
        password: str,
        from_addr: str,
    ) -> None:
        self._host = host
        self._port = port
        self._user = user
        self._password = password
        self._from = from_addr

    async def send(
        self,
        to_addrs: list[str],
        subject: str,
        body_text: str,
        body_html: str | None = None,
    ) -> EmailSendResult:
        msg = EmailMessage()
        msg["Subject"] = subject
        msg["From"] = self._from
        msg["To"] = ", ".join(to_addrs)
        msg.set_content(body_text)
        if body_html:
            msg.add_alternative(body_html, subtype="html")

        def _send() -> None:
            with smtplib.SMTP_SSL(self._host, self._port) as smtp:
                smtp.login(self._user, self._password)
                smtp.send_message(msg)

        try:
            await asyncio.get_event_loop().run_in_executor(None, _send)
            return EmailSendResult(success=True)
        except Exception as exc:
            return EmailSendResult(success=False, error=str(exc))
```

写入 `backend/app/services/monitoring/notifications/mock_email_notifier.py`:

```python
"""MockEmailNotifier — records to sqlite + log instead of SMTP."""
from __future__ import annotations

import logging
import sqlite3
import time
from pathlib import Path

from app.services.monitoring.notifications.email_notifier import EmailSendResult

_logger = logging.getLogger(__name__)


class MockEmailNotifier:
    def __init__(self, db_path: Path) -> None:
        self._db_path = db_path
        self._init()

    def _init(self) -> None:
        self._db_path.parent.mkdir(parents=True, exist_ok=True)
        with sqlite3.connect(self._db_path) as conn:
            conn.execute(
                """
                CREATE TABLE IF NOT EXISTS emails_mock (
                    id INTEGER PRIMARY KEY AUTOINCREMENT,
                    sent_at REAL NOT NULL,
                    to_addrs TEXT NOT NULL,
                    subject TEXT NOT NULL,
                    body_text TEXT NOT NULL,
                    body_html TEXT
                )
                """
            )

    async def send(
        self,
        to_addrs: list[str],
        subject: str,
        body_text: str,
        body_html: str | None = None,
    ) -> EmailSendResult:
        with sqlite3.connect(self._db_path) as conn:
            conn.execute(
                "INSERT INTO emails_mock (sent_at, to_addrs, subject, body_text, body_html) "
                "VALUES (?, ?, ?, ?, ?)",
                (time.time(), ",".join(to_addrs), subject, body_text, body_html),
            )
        _logger.info("[mock email] to=%s subject=%r", to_addrs, subject)
        return EmailSendResult(success=True)

    def list_recorded(self) -> list[dict[str, str]]:
        with sqlite3.connect(self._db_path) as conn:
            cur = conn.execute(
                "SELECT to_addrs, subject, body_text FROM emails_mock ORDER BY id"
            )
            return [
                {"to_addrs": row[0], "subject": row[1], "body_text": row[2]}
                for row in cur.fetchall()
            ]
```

写入 `backend/app/services/monitoring/notifications/factory.py`:

```python
"""build_email_notifier — env-driven mock/real switch."""
from __future__ import annotations

import os
from pathlib import Path

from app.services.monitoring.notifications.email_notifier import (
    EmailNotifier,
    RealEmailNotifier,
)
from app.services.monitoring.notifications.mock_email_notifier import (
    MockEmailNotifier,
)

_MOCK_DB_PATH = Path(__file__).resolve().parents[3] / "data" / "emails_mock.sqlite"


def build_email_notifier() -> EmailNotifier:
    mode = os.environ.get("EMAIL_MODE", "mock").lower()
    if mode == "mock":
        return MockEmailNotifier(db_path=_MOCK_DB_PATH)
    if mode == "real":
        for k in ("SMTP_HOST", "SMTP_PORT", "SMTP_USER", "SMTP_PASSWORD", "SMTP_FROM"):
            if k not in os.environ:
                raise KeyError(f"{k} required when EMAIL_MODE=real")
        return RealEmailNotifier(
            host=os.environ["SMTP_HOST"],
            port=int(os.environ["SMTP_PORT"]),
            user=os.environ["SMTP_USER"],
            password=os.environ["SMTP_PASSWORD"],
            from_addr=os.environ["SMTP_FROM"],
        )
    raise ValueError(f"EMAIL_MODE must be 'mock' or 'real', got {mode!r}")
```

- [ ] **Step 11.4**: 跑测试 + mypy + commit

```bash
uv run pytest backend/tests/unit/test_monitoring_email_notifier.py -v
uv run mypy backend
git add backend/app/services/monitoring/notifications/ backend/tests/unit/test_monitoring_email_notifier.py
git commit -m "feat(monitoring): EmailNotifier Protocol + Real(SMTP) + Mock + factory"
```

---

## Task 12: ResearchState 扩 + Writer alert_writer mode

**Files:**
- Modify: `backend/app/agents/schemas.py`(ResearchState 加 mode / alert_signals / portfolio_warning_report / deep_dive_section)
- Modify: `backend/app/agents/writer.py`(加 alert_writer mode 分支)
- Modify: `backend/app/orchestration/research_nodes.py`(writer_node 适配)
- Create: `backend/tests/unit/test_writer_alert_mode.py`

**Spec ref**: § 6.3, § 8.2

- [ ] **Step 12.1**: grep verify ResearchState 当前用法

```bash
grep -rn "ResearchState\|state\.report_markdown\|state\.credit_report" backend/app/ 2>/dev/null | head -30
```

记录所有 ResearchState 字段 reader,确认加新字段(`mode` / `alert_signals` / `portfolio_warning_report` / `deep_dive_section`)不影响现有读取。

- [ ] **Step 12.2**: 修改 ResearchState

`backend/app/agents/schemas.py:154-172` `ResearchState` 类内追加 4 个字段:

```python
# v0.8.3 — alert mode for portfolio monitoring (B-3)
mode: Literal["full_research", "alert_deep_dive"] = "full_research"
alert_signals: list["SignalResult"] | None = None
portfolio_warning_report: "PortfolioWarningReport | None" = None
deep_dive_section: str | None = None
```

文件顶部 import:

```python
from typing import TYPE_CHECKING, Literal

if TYPE_CHECKING:
    from app.agents.portfolio_warning_schema import PortfolioWarningReport
    from app.services.monitoring.signal_rules.base import SignalResult
```

- [ ] **Step 12.3**: 写失败的 writer alert mode test

写入 `backend/tests/unit/test_writer_alert_mode.py`:

```python
"""Unit tests for Writer in alert_writer mode."""
from __future__ import annotations

from datetime import datetime
from unittest.mock import AsyncMock, MagicMock

import pytest

from app.agents.portfolio_warning_schema import (
    PortfolioWarningReport,
    RiskDiagnosis,
)
from app.agents.schemas import ResearchState
from app.agents.writer import Writer
from app.services.monitoring.signal_rules.base import SignalLevel, SignalResult


def _llm_returning(report: PortfolioWarningReport) -> MagicMock:
    fake = MagicMock()
    response = MagicMock()
    response.parsed = report
    response.cost_cny = 0.5
    fake.chat = AsyncMock(return_value=response)
    return fake


@pytest.mark.asyncio
async def test_writer_alert_mode_produces_portfolio_warning_report() -> None:
    expected = PortfolioWarningReport(
        customer_id="c1", customer_name="x", ts_code="x.SH",
        industry="x", run_id="r", alert_id="a",
        generated_at=datetime(2026, 5, 4),
        alert_level=SignalLevel.YELLOW,
        summary="risk!",
        triggered_signals=[],
        risk_diagnosis=RiskDiagnosis(narrative="...", severity="medium"),
        recommendations=["watch"],
        data_sources=["tushare"],
    )

    writer = Writer(llm=_llm_returning(expected))
    state = ResearchState(
        user_id="u",
        session_id="s",
        user_message="alert",
        request_id="r",
        mode="alert_deep_dive",
        alert_signals=[
            SignalResult(rule_name="cash_flow", level=SignalLevel.YELLOW, explanation="..."),
        ],
    )

    new_state = await writer.run(state)
    assert new_state.portfolio_warning_report is not None
    assert new_state.portfolio_warning_report.alert_level == SignalLevel.YELLOW
```

- [ ] **Step 12.4**: 修改 Writer 加 alert mode 分支

打开 `backend/app/agents/writer.py`,在 `run` / `__call__` 入口处加分支:

```python
async def run(self, state: ResearchState) -> ResearchState:
    if state.mode == "alert_deep_dive":
        return await self._run_alert_writer(state)
    return await self._run_full_research_writer(state)

async def _run_alert_writer(self, state: ResearchState) -> ResearchState:
    """Output PortfolioWarningReport (alert mode)."""
    from app.agents.portfolio_warning_schema import PortfolioWarningReport

    prompt = self._build_alert_prompt(state)
    response = await self._llm.chat(
        prompt=prompt,
        tier="cheap",  # alert deep_dive 短任务,走 cheap
        schema=PortfolioWarningReport,
    )
    if response.parsed is None:
        # Defensive fallback — Writer 走 schema 模式未解析时直接抛
        raise RuntimeError("alert writer LLM returned no parsed PortfolioWarningReport")
    return state.model_copy(update={"portfolio_warning_report": response.parsed})

def _build_alert_prompt(self, state: ResearchState) -> str:
    signals = state.alert_signals or []
    signals_text = "\n".join(
        f"- {s.rule_name}({s.level.value}): {s.explanation}" for s in signals
    )
    return f"""为已触发以下信号的客户生成 PortfolioWarningReport JSON:

触发信号:
{signals_text}

要求:summary 100-200 字;triggered_signals 复制输入信号;risk_diagnosis.narrative 200-400 字;recommendations 至少 2 条.
"""
```

(`_run_full_research_writer` = 现有 writer 逻辑,把现有方法 rename。)

- [ ] **Step 12.5**: 修改 writer_node 适配

`backend/app/orchestration/research_nodes.py` 的 `writer_node`(grep 找)改成调用 `writer.run(state)` 返回 state(已 update),不需要其他逻辑改。

- [ ] **Step 12.6**: 跑测试 + mypy

```bash
uv run pytest backend/tests/unit/test_writer_alert_mode.py backend/tests/unit/test_writer.py -v
uv run mypy backend
```
Expected: alert mode 测试通过,旧 test_writer.py 全 pass(向后兼容)

- [ ] **Step 12.7**: Commit

```bash
git add backend/app/agents/schemas.py backend/app/agents/writer.py \
        backend/app/orchestration/research_nodes.py \
        backend/tests/unit/test_writer_alert_mode.py
git commit -m "feat(b3): ResearchState alert mode + Writer alert_writer branch"
```

---

## Task 13: EscalationCoordinator(三道闸 + ResearchGraph 调用)

**Files:**
- Create: `backend/app/services/monitoring/escalation.py`
- Create: `backend/tests/unit/test_monitoring_escalation.py`

**Spec ref**: § 6

- [ ] **Step 13.1**: 写失败的 unit test

写入 `backend/tests/unit/test_monitoring_escalation.py`:

```python
"""Unit tests for EscalationCoordinator(三道闸)."""
from __future__ import annotations

import asyncio
from datetime import date
from pathlib import Path
from unittest.mock import AsyncMock, MagicMock

import pytest

from app.services.cost_budget import CostBudget
from app.services.monitoring.escalation import (
    EscalationCoordinator,
    EscalationResult,
    EscalationStatus,
)
from app.services.monitoring.signal_rules.base import (
    MonitoringCustomer,
    SignalLevel,
    SignalResult,
)


@pytest.fixture
def customer() -> MonitoringCustomer:
    return MonitoringCustomer(id="c1", ts_code="x.SH", name="x", industry="x")


@pytest.fixture
def signals() -> list[SignalResult]:
    return [SignalResult(rule_name="cash_flow", level=SignalLevel.RED, explanation="x")]


def _fake_graph(deep_dive: str, cost: float = 0.5) -> MagicMock:
    fake = MagicMock()
    state = MagicMock()
    state.deep_dive_section = deep_dive
    state.cost_cny = cost
    fake.ainvoke = AsyncMock(return_value={"deep_dive_section": deep_dive, "cost_cny": cost})
    return fake


@pytest.mark.asyncio
async def test_success_path(customer, signals, tmp_path: Path) -> None:
    coord = EscalationCoordinator(
        graph=_fake_graph("deep dive content"),
        daily_budget=CostBudget(limit_cny=10.0),
        per_call_cap_cny=1.0,
        concurrent_limit=1,
        recent_dedup_check=lambda cid, d: False,
    )
    result = await coord.escalate(customer, signals, run_id="r1")
    assert result.status == EscalationStatus.SUCCESS
    assert result.deep_dive == "deep dive content"


@pytest.mark.asyncio
async def test_per_call_cap_exceeded(customer, signals) -> None:
    """单次 cost > cap → 标 budget_per_call_exceeded."""
    coord = EscalationCoordinator(
        graph=_fake_graph("x", cost=2.0),  # 超 1.0 cap
        daily_budget=CostBudget(limit_cny=10.0),
        per_call_cap_cny=1.0,
        concurrent_limit=1,
        recent_dedup_check=lambda cid, d: False,
    )
    result = await coord.escalate(customer, signals, run_id="r1")
    assert result.status == EscalationStatus.BUDGET_PER_CALL_EXCEEDED


@pytest.mark.asyncio
async def test_daily_dedup(customer, signals) -> None:
    coord = EscalationCoordinator(
        graph=_fake_graph("x"),
        daily_budget=CostBudget(limit_cny=10.0),
        per_call_cap_cny=1.0,
        concurrent_limit=1,
        recent_dedup_check=lambda cid, d: True,  # 已 dedup
    )
    result = await coord.escalate(customer, signals, run_id="r1")
    assert result.status == EscalationStatus.DAILY_DEDUP


@pytest.mark.asyncio
async def test_daily_budget_exceeded(customer, signals) -> None:
    budget = CostBudget(limit_cny=0.1)  # 极小预算
    budget.track(0.5)  # 已 spent 超 limit
    coord = EscalationCoordinator(
        graph=_fake_graph("x"),
        daily_budget=budget,
        per_call_cap_cny=1.0,
        concurrent_limit=1,
        recent_dedup_check=lambda cid, d: False,
    )
    result = await coord.escalate(customer, signals, run_id="r1")
    assert result.status == EscalationStatus.DAILY_BUDGET_EXCEEDED


@pytest.mark.asyncio
async def test_concurrent_serialization(customer, signals) -> None:
    """concurrent_limit=1 时,2 次并发应顺序执行."""
    fake_graph = MagicMock()
    call_log: list[float] = []

    async def slow_invoke(state):
        import time
        call_log.append(time.monotonic())
        await asyncio.sleep(0.05)
        return {"deep_dive_section": "x", "cost_cny": 0.1}

    fake_graph.ainvoke = slow_invoke

    coord = EscalationCoordinator(
        graph=fake_graph,
        daily_budget=CostBudget(limit_cny=10.0),
        per_call_cap_cny=1.0,
        concurrent_limit=1,
        recent_dedup_check=lambda cid, d: False,
    )

    await asyncio.gather(
        coord.escalate(customer, signals, run_id="r1"),
        coord.escalate(customer, signals, run_id="r2"),
    )
    # 第二次调用应等到第一次完成后才开始(间隔 ≥ 0.04)
    assert len(call_log) == 2
    assert call_log[1] - call_log[0] >= 0.04
```

- [ ] **Step 13.2**: 跑测试确认失败

```bash
uv run pytest backend/tests/unit/test_monitoring_escalation.py -v
```
Expected: FAIL `ModuleNotFoundError`

- [ ] **Step 13.3**: 实现 EscalationCoordinator

写入 `backend/app/services/monitoring/escalation.py`:

```python
"""EscalationCoordinator — 三道闸 + ResearchGraph 调用 for red alerts."""
from __future__ import annotations

import asyncio
from datetime import date
from enum import Enum
from typing import Any, Callable

from pydantic import BaseModel

from app.services.cost_budget import CostBudget
from app.services.monitoring.signal_rules.base import (
    MonitoringCustomer,
    SignalResult,
)


class EscalationStatus(str, Enum):
    SUCCESS = "success"
    FAILED = "failed"
    BUDGET_PER_CALL_EXCEEDED = "budget_per_call_exceeded"
    DAILY_BUDGET_EXCEEDED = "daily_budget_exceeded"
    DAILY_DEDUP = "daily_dedup"


class EscalationResult(BaseModel):
    status: EscalationStatus
    deep_dive: str | None = None
    cost_cny: float = 0.0
    error: str | None = None


class EscalationCoordinator:
    def __init__(
        self,
        graph: Any,  # compiled LangGraph
        daily_budget: CostBudget,
        per_call_cap_cny: float = 1.0,
        concurrent_limit: int = 1,
        recent_dedup_check: Callable[[str, date], bool] = lambda cid, d: False,
    ) -> None:
        self._graph = graph
        self._daily_budget = daily_budget
        self._per_call_cap = per_call_cap_cny
        self._semaphore = asyncio.Semaphore(concurrent_limit)
        self._recent_dedup = recent_dedup_check

    async def escalate(
        self,
        customer: MonitoringCustomer,
        signals: list[SignalResult],
        run_id: str,
    ) -> EscalationResult:
        # Gate 1: daily dedup
        if self._recent_dedup(customer.id, date.today()):
            return EscalationResult(status=EscalationStatus.DAILY_DEDUP)

        # Gate 2: daily budget
        try:
            self._daily_budget.assert_under_limit()
        except Exception:
            return EscalationResult(status=EscalationStatus.DAILY_BUDGET_EXCEEDED)

        # Gate 3: concurrent limit
        async with self._semaphore:
            try:
                state = {
                    "user_id": "monitoring",
                    "session_id": customer.id,
                    "user_message": f"alert_deep_dive for {customer.name}",
                    "request_id": run_id,
                    "mode": "alert_deep_dive",
                    "alert_signals": signals,
                }
                result = await self._graph.ainvoke(state)
                cost = float(result.get("cost_cny", 0.0))
                deep_dive = result.get("deep_dive_section")

                # Gate 4: per-call cap (post-hoc, after pipeline ran)
                if cost > self._per_call_cap:
                    self._daily_budget.track(cost)
                    return EscalationResult(
                        status=EscalationStatus.BUDGET_PER_CALL_EXCEEDED,
                        cost_cny=cost,
                    )

                self._daily_budget.track(cost)
                return EscalationResult(
                    status=EscalationStatus.SUCCESS,
                    deep_dive=deep_dive,
                    cost_cny=cost,
                )
            except Exception as exc:
                return EscalationResult(status=EscalationStatus.FAILED, error=str(exc))
```

- [ ] **Step 13.4**: 跑测试 + mypy + commit

```bash
uv run pytest backend/tests/unit/test_monitoring_escalation.py -v
uv run mypy backend
git add backend/app/services/monitoring/escalation.py backend/tests/unit/test_monitoring_escalation.py
git commit -m "feat(monitoring): EscalationCoordinator with 4 hard gates (dedup/daily/per-call/concurrent)"
```

---

## Task 14: MonitoringService + MonitoringScheduler

**Files:**
- Create: `backend/app/services/monitoring/monitoring_service.py`
- Create: `backend/app/services/monitoring/scheduler.py`
- Create: `backend/tests/unit/test_monitoring_service.py`
- Create: `backend/tests/integration/test_monitoring_e2e_mock.py`

**Spec ref**: § 4.2, § 4.3

**Background**:
- `MonitoringService.run_scan(customer_ids)` 是统一入口(cron / event / manual 都调它)
- 内部:for each customer concurrent (max 5) → SignalDetector.detect → 写 monitoring_runs/signals → 黄/红 写 monitoring_alerts → call writer (alert mode) → 红色调 EscalationCoordinator → 拼 deep_dive → 写 notifications + 调 EmailNotifier
- `MonitoringScheduler` 内部用 legacy `get_scheduler_service()` 单例 + add_job

- [ ] **Step 14.1**: grep verify legacy scheduler 单例 API

```bash
grep -n "get_scheduler_service\|_scheduler\|add_job" backend/app/service/scheduler_service.py
```

确认 `get_scheduler_service()` 返回的对象 `._scheduler` 属性是 `AsyncIOScheduler`(memory `feedback_legacy_module_init_grep`)。

- [ ] **Step 14.2**: 写 unit test for MonitoringService

写入 `backend/tests/unit/test_monitoring_service.py`:

```python
"""Unit tests for MonitoringService — uses fake detector + repository."""
from __future__ import annotations

from datetime import datetime
from pathlib import Path
from unittest.mock import AsyncMock, MagicMock

import pytest

from app.scripts.init_monitoring_tables import init_monitoring_tables
from app.services.monitoring.escalation import EscalationResult, EscalationStatus
from app.services.monitoring.monitoring_service import MonitoringService
from app.services.monitoring.signal_rules.base import (
    MonitoringCustomer,
    SignalLevel,
    SignalResult,
)


@pytest.fixture
def db_path(tmp_path: Path) -> Path:
    p = tmp_path / "monitoring.sqlite"
    init_monitoring_tables(p)
    return p


@pytest.fixture
def customer(db_path: Path) -> MonitoringCustomer:
    import sqlite3
    cust = MonitoringCustomer(id="c1", ts_code="x.SH", name="测试公司", industry="消费")
    with sqlite3.connect(db_path) as conn:
        conn.execute(
            "INSERT INTO monitoring_customers (id, ts_code, name, industry) VALUES (?, ?, ?, ?)",
            (cust.id, cust.ts_code, cust.name, cust.industry),
        )
    return cust


@pytest.fixture
def service(db_path: Path) -> MonitoringService:
    detector = MagicMock()
    detector.detect = AsyncMock(
        return_value=(SignalLevel.GREEN, [SignalResult(rule_name="x", level=SignalLevel.GREEN, explanation="ok")])
    )
    writer = MagicMock()
    escalator = MagicMock()
    escalator.escalate = AsyncMock(return_value=EscalationResult(status=EscalationStatus.SUCCESS, deep_dive="dd"))
    notifier = MagicMock()
    notifier.send = AsyncMock()
    return MonitoringService(
        db_path=db_path,
        detector=detector,
        writer=writer,
        escalator=escalator,
        email_notifier=notifier,
        tushare=MagicMock(),
        bocha=MagicMock(),
        llm=MagicMock(),
        thresholds_per_rule={},
        recipient_emails=["a@b.com"],
    )


@pytest.mark.asyncio
async def test_green_run_no_alert(service: MonitoringService, customer: MonitoringCustomer, db_path: Path) -> None:
    import sqlite3
    await service.run_scan(["c1"], trigger_type="manual")
    with sqlite3.connect(db_path) as conn:
        runs = conn.execute("SELECT status FROM monitoring_runs").fetchall()
        alerts = conn.execute("SELECT id FROM monitoring_alerts").fetchall()
    assert len(runs) == 1
    assert runs[0][0] == "success"
    assert len(alerts) == 0  # green 不写 alert


@pytest.mark.asyncio
async def test_red_triggers_escalation(service: MonitoringService, customer: MonitoringCustomer, db_path: Path) -> None:
    service._detector.detect = AsyncMock(
        return_value=(SignalLevel.RED, [SignalResult(rule_name="cf", level=SignalLevel.RED, explanation="x")])
    )
    # mock writer.run to return state with portfolio_warning_report set
    from app.agents.portfolio_warning_schema import PortfolioWarningReport
    fake_state = MagicMock()
    fake_state.portfolio_warning_report = PortfolioWarningReport(
        customer_id="c1", customer_name="测试公司", ts_code="x.SH",
        industry="消费", run_id="r", alert_id="a",
        generated_at=datetime(2026, 5, 4),
        alert_level=SignalLevel.RED,
        summary="risk!",
        triggered_signals=[],
        recommendations=[],
    )
    service._writer.run = AsyncMock(return_value=fake_state)

    await service.run_scan(["c1"], trigger_type="manual")
    service._escalator.escalate.assert_called_once()
    service._email_notifier.send.assert_called_once()
```

- [ ] **Step 14.3**: 实现 MonitoringService

写入 `backend/app/services/monitoring/monitoring_service.py`:

```python
"""MonitoringService — 门面 for cron / event / manual scan trigger.

Per spec § 4.2 data flow.
"""
from __future__ import annotations

import asyncio
import json
import sqlite3
import uuid
from datetime import datetime
from pathlib import Path
from typing import TYPE_CHECKING, Any, Literal

from app.agents.portfolio_warning_renderer import render_portfolio_warning_markdown
from app.agents.schemas import ResearchState
from app.services.monitoring.escalation import (
    EscalationCoordinator,
    EscalationResult,
    EscalationStatus,
)
from app.services.monitoring.signal_detector import SignalDetector
from app.services.monitoring.signal_rules.base import (
    MonitoringCustomer,
    SignalLevel,
)

if TYPE_CHECKING:
    from app.agents.writer import Writer
    from app.services.bocha_factory import BochaService
    from app.services.llm_service import LLMService
    from app.services.monitoring.notifications.email_notifier import EmailNotifier
    from app.services.tushare_service import TushareService

TriggerType = Literal["cron", "disclosure_event", "manual"]


class MonitoringService:
    def __init__(
        self,
        *,
        db_path: Path,
        detector: SignalDetector,
        writer: "Writer",
        escalator: EscalationCoordinator,
        email_notifier: "EmailNotifier",
        tushare: "TushareService",
        bocha: "BochaService",
        llm: "LLMService",
        thresholds_per_rule: dict[str, dict[str, float]],
        recipient_emails: list[str],
        max_concurrent_customers: int = 5,
    ) -> None:
        self._db_path = db_path
        self._detector = detector
        self._writer = writer
        self._escalator = escalator
        self._email_notifier = email_notifier
        self._tushare = tushare
        self._bocha = bocha
        self._llm = llm
        self._thresholds = thresholds_per_rule
        self._recipients = recipient_emails
        self._sem = asyncio.Semaphore(max_concurrent_customers)

    def _load_customer(self, cid: str) -> MonitoringCustomer | None:
        with sqlite3.connect(self._db_path) as conn:
            row = conn.execute(
                "SELECT id, ts_code, name, industry, thresholds_override "
                "FROM monitoring_customers WHERE id = ? AND enabled = 1",
                (cid,),
            ).fetchone()
        if row is None:
            return None
        thresholds = json.loads(row[4]) if row[4] else None
        return MonitoringCustomer(
            id=row[0], ts_code=row[1], name=row[2], industry=row[3],
            thresholds_override=thresholds,
        )

    async def run_scan(
        self,
        customer_ids: list[str],
        *,
        trigger_type: TriggerType = "manual",
    ) -> list[str]:
        """Returns list of run_ids."""
        async def _scan_one(cid: str) -> str:
            async with self._sem:
                return await self._scan_customer(cid, trigger_type)

        return await asyncio.gather(*(_scan_one(c) for c in customer_ids))

    async def _scan_customer(self, cid: str, trigger_type: TriggerType) -> str:
        run_id = str(uuid.uuid4())
        started_at = datetime.now()
        customer = self._load_customer(cid)
        if customer is None:
            self._record_run(run_id, cid, trigger_type, started_at, datetime.now(), "failed", "customer not found")
            return run_id

        try:
            overall, results = await self._detector.detect(
                customer, self._tushare, self._bocha, self._llm, self._thresholds
            )

            self._record_run(run_id, cid, trigger_type, started_at, datetime.now(), "success", None)
            for sig in results:
                self._record_signal(run_id, sig)

            if overall == SignalLevel.GREEN:
                return run_id

            # Yellow / Red → write alert + writer + maybe escalate + notify
            alert_id = str(uuid.uuid4())
            state = ResearchState(
                user_id="monitoring",
                session_id=cid,
                user_message=f"alert for {customer.name}",
                request_id=run_id,
                mode="alert_deep_dive",
                alert_signals=results,
            )
            new_state = await self._writer.run(state)
            report = new_state.portfolio_warning_report
            if report is None:
                # writer didn't produce — record failed alert
                return run_id
            report = report.model_copy(update={"alert_id": alert_id, "alert_level": overall})

            escalation: EscalationResult | None = None
            if overall == SignalLevel.RED:
                escalation = await self._escalator.escalate(customer, results, run_id)
                if escalation.status == EscalationStatus.SUCCESS and escalation.deep_dive:
                    from app.agents.portfolio_warning_schema import DeepDiveSection
                    report = report.model_copy(update={
                        "deep_dive": DeepDiveSection(content=escalation.deep_dive)
                    })

            markdown = render_portfolio_warning_markdown(report)
            self._record_alert(alert_id, run_id, cid, overall, report, markdown, escalation)

            await self._send_notifications(alert_id, overall, customer, report)

            return run_id
        except Exception as exc:
            self._record_run(run_id, cid, trigger_type, started_at, datetime.now(), "failed", str(exc))
            return run_id

    def _record_run(
        self, run_id: str, cid: str, trigger_type: str,
        started: datetime, finished: datetime, status: str, err: str | None,
    ) -> None:
        with sqlite3.connect(self._db_path) as conn:
            conn.execute(
                "INSERT INTO monitoring_runs "
                "(id, customer_id, trigger_type, started_at, finished_at, status, error_message) "
                "VALUES (?, ?, ?, ?, ?, ?, ?)",
                (run_id, cid, trigger_type, started.isoformat(), finished.isoformat(), status, err),
            )

    def _record_signal(self, run_id: str, sig: Any) -> None:
        with sqlite3.connect(self._db_path) as conn:
            conn.execute(
                "INSERT INTO monitoring_signals "
                "(id, run_id, rule_name, level, detected_value, threshold, explanation, raw_data_ref) "
                "VALUES (?, ?, ?, ?, ?, ?, ?, ?)",
                (
                    str(uuid.uuid4()), run_id, sig.rule_name, sig.level.value,
                    str(sig.detected_value) if sig.detected_value is not None else None,
                    str(sig.threshold) if sig.threshold is not None else None,
                    sig.explanation,
                    json.dumps(sig.raw_data_ref) if sig.raw_data_ref else None,
                ),
            )

    def _record_alert(
        self, alert_id: str, run_id: str, cid: str, level: SignalLevel,
        report: Any, markdown: str, escalation: EscalationResult | None,
    ) -> None:
        esc_status = escalation.status.value if escalation else None
        deep_dive = escalation.deep_dive if escalation else None
        with sqlite3.connect(self._db_path) as conn:
            conn.execute(
                "INSERT INTO monitoring_alerts "
                "(id, run_id, customer_id, alert_level, report_json, report_markdown, "
                " escalated, escalation_status, deep_dive_text) "
                "VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)",
                (
                    alert_id, run_id, cid, level.value,
                    report.model_dump_json(), markdown,
                    1 if escalation else 0, esc_status, deep_dive,
                ),
            )

    async def _send_notifications(
        self, alert_id: str, level: SignalLevel,
        customer: MonitoringCustomer, report: Any,
    ) -> None:
        # 站内
        with sqlite3.connect(self._db_path) as conn:
            conn.execute(
                "INSERT INTO notifications (id, alert_id, channel, send_status) "
                "VALUES (?, ?, ?, ?)",
                (str(uuid.uuid4()), alert_id, "in_app", "sent"),
            )

        if level != SignalLevel.RED:
            return  # spec § 7.4 — yellow 默认不发邮件

        subject = f"[B-3 持仓预警] {customer.name} - 红色 等级 alert"
        body_text = f"客户: {customer.name}({customer.ts_code})\n等级: 红色\n\n{report.summary}"
        body_html = f"<p>客户: <b>{customer.name}</b> ({customer.ts_code})</p><p>{report.summary}</p>"
        result = await self._email_notifier.send(
            to_addrs=self._recipients,
            subject=subject, body_text=body_text, body_html=body_html,
        )
        with sqlite3.connect(self._db_path) as conn:
            for to in self._recipients:
                conn.execute(
                    "INSERT INTO notifications (id, alert_id, channel, recipient, send_status, error_message) "
                    "VALUES (?, ?, ?, ?, ?, ?)",
                    (
                        str(uuid.uuid4()), alert_id, "email", to,
                        "sent" if result.success else "failed",
                        result.error,
                    ),
                )
```

- [ ] **Step 14.4**: 实现 MonitoringScheduler

写入 `backend/app/services/monitoring/scheduler.py`:

```python
"""MonitoringScheduler — adds cron + disclosure jobs to legacy APScheduler singleton."""
from __future__ import annotations

import logging
from datetime import datetime

from apscheduler.triggers.cron import CronTrigger

from app.service.scheduler_service import get_scheduler_service
from app.services.monitoring.monitoring_service import MonitoringService

_logger = logging.getLogger(__name__)


class MonitoringScheduler:
    def __init__(
        self,
        monitoring_service: MonitoringService,
        load_active_customer_ids: callable,  # type: ignore[type-arg]
        load_disclosure_customer_ids: callable,  # type: ignore[type-arg]
    ) -> None:
        self._service = monitoring_service
        self._load_active = load_active_customer_ids
        self._load_disclosure = load_disclosure_customer_ids

    def install(self) -> None:
        scheduler = get_scheduler_service()._scheduler
        assert scheduler is not None

        scheduler.add_job(
            self._daily_scan,
            CronTrigger(hour=16, minute=30, timezone="Asia/Shanghai"),
            id="monitoring_daily_scan",
            name="持仓预警日扫描",
            replace_existing=True,
        )
        scheduler.add_job(
            self._disclosure_scan,
            CronTrigger(hour=17, minute=0, timezone="Asia/Shanghai"),
            id="monitoring_disclosure_check",
            name="持仓预警披露日触发",
            replace_existing=True,
        )
        _logger.info("monitoring scheduler installed: daily_scan @16:30, disclosure_check @17:00")

    async def _daily_scan(self) -> None:
        cids = self._load_active()
        _logger.info("daily monitoring scan starting for %d customers", len(cids))
        await self._service.run_scan(cids, trigger_type="cron")

    async def _disclosure_scan(self) -> None:
        cids = self._load_disclosure(datetime.now().date())
        _logger.info("disclosure-event monitoring scan for %d customers", len(cids))
        await self._service.run_scan(cids, trigger_type="disclosure_event")
```

- [ ] **Step 14.5**: 写 integration mock e2e test

写入 `backend/tests/integration/test_monitoring_e2e_mock.py`:

```python
"""Integration test — full MonitoringService.run_scan with all-mock dependencies."""
from __future__ import annotations

from pathlib import Path

import pytest

from app.scripts.init_monitoring_tables import init_monitoring_tables


@pytest.fixture(autouse=True)
def setup_env(monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.setenv("TUSHARE_MODE", "mock")
    monkeypatch.setenv("EMAIL_MODE", "mock")
    monkeypatch.setenv("LLM_MODE", "mock")


@pytest.mark.asyncio
async def test_full_scan_with_mock_pipeline(tmp_path: Path) -> None:
    """Smoke: 1 customer → mock detector returns yellow → writer mock → email mock recorded."""
    db_path = tmp_path / "monitoring.sqlite"
    init_monitoring_tables(db_path)

    import sqlite3
    with sqlite3.connect(db_path) as conn:
        conn.execute(
            "INSERT INTO monitoring_customers (id, ts_code, name, industry) "
            "VALUES ('c1', '600519.SH', '茅台', '消费')"
        )

    # 详细 wiring 留 Task 19 dogfood 时跑
    # 这里只验证 MonitoringService 可被 import 且 init 不抛
    from app.services.monitoring.monitoring_service import MonitoringService
    assert MonitoringService is not None
```

- [ ] **Step 14.6**: 跑测试 + mypy + commit

```bash
uv run pytest backend/tests/unit/test_monitoring_service.py backend/tests/integration/test_monitoring_e2e_mock.py -v
uv run mypy backend
git add backend/app/services/monitoring/monitoring_service.py \
        backend/app/services/monitoring/scheduler.py \
        backend/tests/unit/test_monitoring_service.py \
        backend/tests/integration/test_monitoring_e2e_mock.py
git commit -m "feat(monitoring): MonitoringService facade + APScheduler integration"
```

---

## Task 15: monitoring_router(REST API)

**Files:**
- Create: `backend/app/router/monitoring_router.py`
- Modify: `backend/app/app_main.py`(注册 router + 启动时安装 scheduler)
- Modify: `backend/.env.example`(加新 env)
- Create: `backend/tests/unit/test_monitoring_router.py`

**Spec ref**: § 1 块 8

- [ ] **Step 15.1**: 写失败的 unit test

写入 `backend/tests/unit/test_monitoring_router.py`:

```python
"""Unit tests for monitoring_router endpoints."""
from __future__ import annotations

import pytest
from fastapi.testclient import TestClient


@pytest.fixture
def client() -> TestClient:
    from app.app_main import app
    return TestClient(app)


def test_list_customers_empty(client: TestClient) -> None:
    resp = client.get("/api/monitoring/customers")
    assert resp.status_code == 200
    assert resp.json() == {"customers": []}


def test_create_customer(client: TestClient) -> None:
    resp = client.post(
        "/api/monitoring/customers",
        json={"ts_code": "600519.SH", "name": "茅台", "industry": "消费"},
    )
    assert resp.status_code == 201
    body = resp.json()
    assert body["ts_code"] == "600519.SH"
    assert "id" in body


def test_list_runs_for_customer(client: TestClient) -> None:
    resp = client.get("/api/monitoring/runs?customer_id=c1")
    assert resp.status_code == 200


def test_trigger_immediate_scan(client: TestClient) -> None:
    resp = client.post("/api/monitoring/runs", json={"customer_ids": ["c1"]})
    # 后台任务,立即返回 run_ids
    assert resp.status_code in (200, 202)


def test_get_alert_detail_404_when_missing(client: TestClient) -> None:
    resp = client.get("/api/monitoring/alerts/nonexistent")
    assert resp.status_code == 404


def test_get_config(client: TestClient) -> None:
    resp = client.get("/api/monitoring/config")
    assert resp.status_code == 200
    assert "scan_time" in resp.json() or "thresholds" in resp.json()
```

- [ ] **Step 15.2**: 实现 router

写入 `backend/app/router/monitoring_router.py`:

```python
"""REST API for B-3 monitoring (customers / runs / alerts / config)."""
from __future__ import annotations

import json
import sqlite3
import uuid
from pathlib import Path
from typing import Any

from fastapi import APIRouter, BackgroundTasks, HTTPException
from pydantic import BaseModel, Field

router = APIRouter(prefix="/api/monitoring", tags=["monitoring"])

_DB_PATH = Path(__file__).resolve().parents[1] / "data" / "monitoring.sqlite"


class CustomerCreate(BaseModel):
    ts_code: str
    name: str
    industry: str
    enabled: bool = True
    thresholds_override: dict[str, float] | None = None


class CustomerOut(BaseModel):
    id: str
    ts_code: str
    name: str
    industry: str
    enabled: bool
    thresholds_override: dict[str, float] | None = None


class RunRequest(BaseModel):
    customer_ids: list[str]


@router.get("/customers")
def list_customers() -> dict[str, list[CustomerOut]]:
    with sqlite3.connect(_DB_PATH) as conn:
        rows = conn.execute(
            "SELECT id, ts_code, name, industry, enabled, thresholds_override "
            "FROM monitoring_customers"
        ).fetchall()
    customers = [
        CustomerOut(
            id=r[0], ts_code=r[1], name=r[2], industry=r[3],
            enabled=bool(r[4]),
            thresholds_override=json.loads(r[5]) if r[5] else None,
        )
        for r in rows
    ]
    return {"customers": customers}


@router.post("/customers", status_code=201)
def create_customer(payload: CustomerCreate) -> CustomerOut:
    cid = str(uuid.uuid4())
    with sqlite3.connect(_DB_PATH) as conn:
        try:
            conn.execute(
                "INSERT INTO monitoring_customers "
                "(id, ts_code, name, industry, enabled, thresholds_override) "
                "VALUES (?, ?, ?, ?, ?, ?)",
                (
                    cid, payload.ts_code, payload.name, payload.industry,
                    1 if payload.enabled else 0,
                    json.dumps(payload.thresholds_override) if payload.thresholds_override else None,
                ),
            )
        except sqlite3.IntegrityError:
            raise HTTPException(status_code=409, detail="ts_code already exists")
    return CustomerOut(id=cid, **payload.model_dump())


@router.delete("/customers/{cid}", status_code=204)
def delete_customer(cid: str) -> None:
    with sqlite3.connect(_DB_PATH) as conn:
        cur = conn.execute("DELETE FROM monitoring_customers WHERE id = ?", (cid,))
    if cur.rowcount == 0:
        raise HTTPException(status_code=404)


@router.get("/runs")
def list_runs(customer_id: str | None = None) -> dict[str, list[dict[str, Any]]]:
    with sqlite3.connect(_DB_PATH) as conn:
        if customer_id:
            rows = conn.execute(
                "SELECT id, customer_id, trigger_type, started_at, finished_at, status "
                "FROM monitoring_runs WHERE customer_id = ? ORDER BY started_at DESC",
                (customer_id,),
            ).fetchall()
        else:
            rows = conn.execute(
                "SELECT id, customer_id, trigger_type, started_at, finished_at, status "
                "FROM monitoring_runs ORDER BY started_at DESC LIMIT 100"
            ).fetchall()
    return {"runs": [
        {"id": r[0], "customer_id": r[1], "trigger_type": r[2],
         "started_at": r[3], "finished_at": r[4], "status": r[5]}
        for r in rows
    ]}


@router.post("/runs", status_code=202)
async def trigger_scan(payload: RunRequest, bg: BackgroundTasks) -> dict[str, list[str]]:
    from app.app_main import get_monitoring_service
    svc = get_monitoring_service()

    async def _run() -> None:
        await svc.run_scan(payload.customer_ids, trigger_type="manual")

    bg.add_task(_run)
    return {"customer_ids": payload.customer_ids, "status": "queued"}


@router.get("/alerts/{aid}")
def get_alert(aid: str) -> dict[str, Any]:
    with sqlite3.connect(_DB_PATH) as conn:
        row = conn.execute(
            "SELECT id, run_id, customer_id, alert_level, report_json, report_markdown, "
            "       escalated, escalation_status, deep_dive_text, created_at "
            "FROM monitoring_alerts WHERE id = ?",
            (aid,),
        ).fetchone()
    if row is None:
        raise HTTPException(status_code=404)
    return {
        "id": row[0], "run_id": row[1], "customer_id": row[2], "alert_level": row[3],
        "report_json": json.loads(row[4]),
        "report_markdown": row[5],
        "escalated": bool(row[6]),
        "escalation_status": row[7],
        "deep_dive_text": row[8],
        "created_at": row[9],
    }


@router.get("/alerts")
def list_alerts(customer_id: str | None = None, limit: int = 50) -> dict[str, list[dict[str, Any]]]:
    with sqlite3.connect(_DB_PATH) as conn:
        if customer_id:
            rows = conn.execute(
                "SELECT id, run_id, customer_id, alert_level, created_at "
                "FROM monitoring_alerts WHERE customer_id = ? ORDER BY created_at DESC LIMIT ?",
                (customer_id, limit),
            ).fetchall()
        else:
            rows = conn.execute(
                "SELECT id, run_id, customer_id, alert_level, created_at "
                "FROM monitoring_alerts ORDER BY created_at DESC LIMIT ?",
                (limit,),
            ).fetchall()
    return {"alerts": [
        {"id": r[0], "run_id": r[1], "customer_id": r[2], "alert_level": r[3], "created_at": r[4]}
        for r in rows
    ]}


@router.get("/config")
def get_config() -> dict[str, Any]:
    """Returns current default thresholds + scheduler settings."""
    from app.services.monitoring.signal_rules.defaults import DEFAULT_THRESHOLDS
    return {
        "scan_time": "16:30",
        "daily_budget_cny": 10.0,
        "thresholds": DEFAULT_THRESHOLDS,
    }
```

- [ ] **Step 15.3**: 注册 router + scheduler init

修改 `backend/app/app_main.py`:

加 import + 注册:

```python
from app.router.monitoring_router import router as monitoring_router
app.include_router(monitoring_router)
```

加 lifespan/startup:

```python
from app.services.monitoring.monitoring_service import MonitoringService
from app.services.monitoring.scheduler import MonitoringScheduler

_monitoring_service_singleton: MonitoringService | None = None


def get_monitoring_service() -> MonitoringService:
    """Used by router for dependency injection."""
    global _monitoring_service_singleton
    if _monitoring_service_singleton is None:
        from app.services.monitoring.signal_detector import SignalDetector
        from app.services.monitoring.signal_rules import (
            DEFAULT_THRESHOLDS,
        )
        from app.services.monitoring.signal_rules.announcement import AnnouncementRule
        from app.services.monitoring.signal_rules.cash_flow import CashFlowRule
        from app.services.monitoring.signal_rules.financial_ratio import FinancialRatioRule
        from app.services.monitoring.signal_rules.price_anomaly import PriceAnomalyRule
        from app.services.monitoring.signal_rules.shareholder_count import ShareholderCountRule
        from app.services.monitoring.escalation import EscalationCoordinator
        from app.services.monitoring.notifications.factory import build_email_notifier
        from app.services.tushare_factory import build_tushare_service
        from app.services.bocha_factory import build_bocha_service_from_env
        from app.services.openai_client import build_llm_service_from_env
        from app.services.cost_budget import CostBudget
        from app.agents.writer import Writer
        # ... build ResearchGraph for escalation (see research_graph.py)
        # 此处 wiring 详细代码留实施时按 agent_graph.py 现有 pattern 写
        # MonitoringService(...) 构造完赋给 _monitoring_service_singleton
        ...
    assert _monitoring_service_singleton is not None
    return _monitoring_service_singleton


@app.on_event("startup")
async def _on_startup() -> None:
    svc = get_monitoring_service()

    def _load_active_ids() -> list[str]:
        import sqlite3
        from pathlib import Path
        db = Path(__file__).resolve().parent / "data" / "monitoring.sqlite"
        with sqlite3.connect(db) as conn:
            return [r[0] for r in conn.execute("SELECT id FROM monitoring_customers WHERE enabled = 1")]

    def _load_disclosure_ids(d) -> list[str]:
        # 简化:返回空,实施时调 tushare.get_disclosure_date
        return []

    sched = MonitoringScheduler(svc, _load_active_ids, _load_disclosure_ids)
    sched.install()
```

(实际 wiring 复杂,此 step 给 skeleton。实施时参照 `app/orchestration/agent_graph.py` 的现有 DI pattern。)

- [ ] **Step 15.4**: 更新 .env.example

在 `backend/.env.example` 末尾追加:

```bash
# v0.8.3 — tushare 真接入
TUSHARE_MODE=mock                       # mock | real
# TUSHARE_TOKEN=                        # required when TUSHARE_MODE=real

# v0.8.3 — Email notifier
EMAIL_MODE=mock                         # mock | real
# SMTP_HOST=smtp.qq.com
# SMTP_PORT=465
# SMTP_USER=
# SMTP_PASSWORD=                        # 授权码,不是邮箱密码
# SMTP_FROM=
MONITORING_ALERT_RECIPIENTS=            # 逗号分隔 email 列表

# v0.8.3 — Monitoring engine
MONITORING_DAILY_BUDGET_CNY=10
MONITORING_ESCALATION_PER_CALL_CAP_CNY=1
MONITORING_ESCALATION_MAX_CONCURRENT=1
MONITORING_DOGFOOD_CUSTOMERS=           # implementation 时填,如 "600519.SH,000001.SZ,..."
```

- [ ] **Step 15.5**: 跑测 + mypy + commit

```bash
unset all_proxy https_proxy http_proxy
uv run pytest backend/tests/unit/test_monitoring_router.py -v
uv run mypy backend
git add backend/app/router/monitoring_router.py backend/app/app_main.py backend/.env.example backend/tests/unit/test_monitoring_router.py
git commit -m "feat(monitoring): REST API + app_main wiring + .env.example"
```

---

## Task 16: Frontend 监控列表页(走 taste skill)

**Files:**
- Create: `frontend/src/types/monitoring.ts`
- Create: `frontend/src/api/monitoring.ts`
- Create: `frontend/src/pages/monitoring/index.tsx`
- Modify: `frontend/src/router/<index>.tsx`(加路由 — grep 找现有 router 文件)
- Modify: 侧边栏 component(加"持仓预警" tab — grep 找)

**Spec ref**: § 10.2 监控列表

**Acceptance criterion**: **Component-level design 必须 invoke `taste` skill**(memory `feedback_design_doc_format` + spec § 10.3)。Layout / spacing / interaction state / hardware acceleration / 字体层级 / metric-based rules 走 taste 输出。**Sub-agent dispatched to do this task must call `taste` skill before writing JSX.**

- [ ] **Step 16.1**: 调 taste skill 设计页面

```
Invoke taste skill with brief:
"设计 B-3 持仓预警 监控列表页(/monitoring),Antd 5 + React 18 + TS。
内容:① 顶部 4 个统计卡(总客户数/今日扫描数/24h 黄+红 alert 数)
② 工具条(搜索 + 行业 filter + 状态 filter + '立即扫描全部'按钮)
③ Antd Table(列:客户名 / ts_code / 行业 tag / 启用状态 / 最新扫描时间 / **当前最高 alert 等级状态色 dot** / 24h alert 数 / 操作)
④ Polling 5s 拉最新数据
设计语言对齐 Perplexity Finance / 同花顺(已在 v0.8.3-pre 设定 design tokens),克制米色 + 赭红色强调。
Output: layout spec + Antd component tree + spacing/typography metrics + interaction states + hardware acceleration props."
```

记录 taste output(file 或 inline)。

- [ ] **Step 16.2**: 写 TS types

写入 `frontend/src/types/monitoring.ts`:

```typescript
export type AlertLevel = "green" | "yellow" | "red";

export interface MonitoringCustomer {
  id: string;
  ts_code: string;
  name: string;
  industry: string;
  enabled: boolean;
  thresholds_override?: Record<string, number> | null;
}

export interface MonitoringRun {
  id: string;
  customer_id: string;
  trigger_type: "cron" | "disclosure_event" | "manual";
  started_at: string;
  finished_at: string | null;
  status: "running" | "success" | "failed";
}

export interface MonitoringAlert {
  id: string;
  run_id: string;
  customer_id: string;
  alert_level: AlertLevel;
  created_at: string;
}

export interface MonitoringConfig {
  scan_time: string;
  daily_budget_cny: number;
  thresholds: Record<string, Record<string, number>>;
}
```

- [ ] **Step 16.3**: 写 API client

写入 `frontend/src/api/monitoring.ts`:

```typescript
import {
  MonitoringAlert,
  MonitoringConfig,
  MonitoringCustomer,
  MonitoringRun,
} from "@/types/monitoring";

const BASE = "/api/monitoring";

export async function listCustomers(): Promise<MonitoringCustomer[]> {
  const res = await fetch(`${BASE}/customers`);
  const data = await res.json();
  return data.customers;
}

export async function createCustomer(payload: {
  ts_code: string;
  name: string;
  industry: string;
}): Promise<MonitoringCustomer> {
  const res = await fetch(`${BASE}/customers`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(payload),
  });
  return await res.json();
}

export async function deleteCustomer(id: string): Promise<void> {
  await fetch(`${BASE}/customers/${id}`, { method: "DELETE" });
}

export async function listAlerts(customerId?: string): Promise<MonitoringAlert[]> {
  const url = customerId ? `${BASE}/alerts?customer_id=${customerId}` : `${BASE}/alerts`;
  const res = await fetch(url);
  const data = await res.json();
  return data.alerts;
}

export async function getAlert(aid: string): Promise<{
  id: string;
  alert_level: string;
  report_markdown: string;
  deep_dive_text: string | null;
  escalation_status: string | null;
  report_json: Record<string, unknown>;
}> {
  const res = await fetch(`${BASE}/alerts/${aid}`);
  if (!res.ok) throw new Error(`alert ${aid} not found`);
  return await res.json();
}

export async function listRuns(customerId?: string): Promise<MonitoringRun[]> {
  const url = customerId ? `${BASE}/runs?customer_id=${customerId}` : `${BASE}/runs`;
  const res = await fetch(url);
  const data = await res.json();
  return data.runs;
}

export async function triggerScan(customerIds: string[]): Promise<void> {
  await fetch(`${BASE}/runs`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ customer_ids: customerIds }),
  });
}

export async function getConfig(): Promise<MonitoringConfig> {
  const res = await fetch(`${BASE}/config`);
  return await res.json();
}
```

- [ ] **Step 16.4**: 写监控列表页 — 按 taste 输出实现

写入 `frontend/src/pages/monitoring/index.tsx`(基于 taste skill output 的 layout):

```typescript
// 实现按 Step 16.1 taste output 微调; 此处给骨架
import { useEffect, useState, useMemo } from "react";
import { Card, Col, Row, Table, Button, Input, Select, Tag, Space, Badge } from "antd";
import { Link } from "react-router-dom";
import {
  listCustomers,
  listAlerts,
  listRuns,
  triggerScan,
} from "@/api/monitoring";
import type { MonitoringCustomer, MonitoringAlert, MonitoringRun } from "@/types/monitoring";

const POLL_MS = 5000;

const LEVEL_BADGE: Record<string, "default" | "warning" | "error" | "success"> = {
  green: "success",
  yellow: "warning",
  red: "error",
};

export default function MonitoringIndex() {
  const [customers, setCustomers] = useState<MonitoringCustomer[]>([]);
  const [alerts, setAlerts] = useState<MonitoringAlert[]>([]);
  const [runs, setRuns] = useState<MonitoringRun[]>([]);
  const [search, setSearch] = useState("");
  const [industryFilter, setIndustryFilter] = useState<string | undefined>();

  async function refresh() {
    const [c, a, r] = await Promise.all([listCustomers(), listAlerts(), listRuns()]);
    setCustomers(c);
    setAlerts(a);
    setRuns(r);
  }

  useEffect(() => {
    refresh();
    const t = setInterval(refresh, POLL_MS);
    return () => clearInterval(t);
  }, []);

  const stats = useMemo(() => {
    const since24h = Date.now() - 24 * 3600 * 1000;
    const recent = alerts.filter((a) => new Date(a.created_at).getTime() > since24h);
    const todayRuns = runs.filter((r) => new Date(r.started_at).toDateString() === new Date().toDateString());
    return {
      total: customers.length,
      todayScans: todayRuns.length,
      yellow24h: recent.filter((a) => a.alert_level === "yellow").length,
      red24h: recent.filter((a) => a.alert_level === "red").length,
    };
  }, [customers, alerts, runs]);

  const customerLevelMap = useMemo(() => {
    const m: Record<string, string> = {};
    for (const a of alerts) {
      const cur = m[a.customer_id];
      if (!cur || (cur === "green" && a.alert_level !== "green") || (cur === "yellow" && a.alert_level === "red")) {
        m[a.customer_id] = a.alert_level;
      }
    }
    return m;
  }, [alerts]);

  const filtered = customers
    .filter((c) => !search || c.name.includes(search) || c.ts_code.includes(search))
    .filter((c) => !industryFilter || c.industry === industryFilter);

  const columns = [
    {
      title: "客户",
      dataIndex: "name",
      render: (_: string, c: MonitoringCustomer) => (
        <Link to={`/monitoring/${c.id}`}>{c.name}</Link>
      ),
    },
    { title: "ts_code", dataIndex: "ts_code" },
    { title: "行业", dataIndex: "industry", render: (i: string) => <Tag>{i}</Tag> },
    {
      title: "状态",
      dataIndex: "id",
      render: (cid: string) => (
        <Badge status={LEVEL_BADGE[customerLevelMap[cid] ?? "green"]} text={customerLevelMap[cid] ?? "green"} />
      ),
    },
    {
      title: "24h alert",
      dataIndex: "id",
      render: (cid: string) => alerts.filter((a) => a.customer_id === cid).length,
    },
    {
      title: "操作",
      dataIndex: "id",
      render: (cid: string) => (
        <Space>
          <Button size="small" onClick={() => triggerScan([cid])}>立即扫描</Button>
        </Space>
      ),
    },
  ];

  return (
    <div style={{ padding: 24 }}>
      <Row gutter={16}>
        <Col span={6}><Card><div>总客户数</div><h2>{stats.total}</h2></Card></Col>
        <Col span={6}><Card><div>今日扫描数</div><h2>{stats.todayScans}</h2></Card></Col>
        <Col span={6}><Card><div>黄色 alert (24h)</div><h2 style={{ color: "#faad14" }}>{stats.yellow24h}</h2></Card></Col>
        <Col span={6}><Card><div>红色 alert (24h)</div><h2 style={{ color: "#ff4d4f" }}>{stats.red24h}</h2></Card></Col>
      </Row>
      <Space style={{ margin: "16px 0" }}>
        <Input.Search placeholder="搜索客户名/ts_code" onChange={(e) => setSearch(e.target.value)} style={{ width: 240 }} />
        <Select placeholder="行业" allowClear style={{ width: 140 }} onChange={setIndustryFilter}
          options={Array.from(new Set(customers.map((c) => c.industry))).map((i) => ({ value: i, label: i }))} />
        <Button type="primary" onClick={() => triggerScan(filtered.map((c) => c.id))}>立即扫描全部</Button>
      </Space>
      <Table rowKey="id" columns={columns} dataSource={filtered} pagination={{ pageSize: 20 }} />
    </div>
  );
}
```

最终样式 / spacing / typography 按 Step 16.1 taste 输出微调,**不偏离 design tokens**。

- [ ] **Step 16.5**: 加路由 + 侧边栏

`grep -n "Routes\|<Route" frontend/src/router/` 找到现有 router file,加:

```typescript
import MonitoringIndex from "@/pages/monitoring";
// ...
<Route path="/monitoring" element={<MonitoringIndex />} />
```

侧边栏 component(grep `frontend/src/layout/` 找 sidebar):

```typescript
{ key: "monitoring", icon: <BellOutlined />, label: "持仓预警", path: "/monitoring" }
```

- [ ] **Step 16.6**: 浏览器手验

```bash
# 终端 1
cd backend && uv run uvicorn app.app_main:app --reload --port 8000 --app-dir backend
# 终端 2
cd frontend && npm run dev
```

打开 `http://localhost:5173/monitoring` 验证:
- 4 个统计卡渲染
- 工具条交互正常
- Table 渲染(空数据时显示 "无数据")
- "立即扫描全部" 按钮点击不报错

- [ ] **Step 16.7**: TypeScript check + 提交

```bash
cd frontend && npm run build  # 等价 tsc + vite build,catch types error
```

```bash
git add frontend/src/types/monitoring.ts frontend/src/api/monitoring.ts \
        frontend/src/pages/monitoring/index.tsx \
        frontend/src/router/ frontend/src/layout/
git commit -m "feat(b3-frontend): monitoring index page (Antd Table + polling 5s, taste-designed)"
```

---

## Task 17: Frontend alert 详情页(走 taste skill)

**Files:**
- Create: `frontend/src/pages/monitoring/alert-detail.tsx`
- Modify: `frontend/src/router/<index>.tsx`(加路由)

**Spec ref**: § 10.2 alert 详情

**Acceptance criterion**: **Component-level design 必须 invoke `taste` skill** for layout / banner / signal cards / collapse / markdown rendering hierarchy。

- [ ] **Step 17.1**: 调 taste skill 设计页面

```
Invoke taste skill with brief:
"设计 alert 详情页(/monitoring/:cid/alert/:aid),Antd 5。
内容:
① 顶部状态 banner(状态色 + 客户名 + alert 时间 + 等级 + escalation 状态)
② 信号触发列表(Card grid 或 Antd List;每 signal = rule 名 / detected_value / threshold / explanation)
③ 报告正文(markdown 渲染,复用 v0.8.2 markdown-it 或 react-markdown)
④ 红色 alert 才出现的"深度调查"折叠区(Antd Collapse,默认展开)
⑤ 引用源 + 操作按钮(再次扫描 / 标记已读 / 分享链接)
设计语言对齐 v0.8.3-pre 已设 token; 红色 banner 不要太刺眼。
Output: layout + spacing + typography + interactive states."
```

- [ ] **Step 17.2**: 实现页面

写入 `frontend/src/pages/monitoring/alert-detail.tsx`:

```typescript
// 按 Step 17.1 taste output 实现; 此处给骨架
import { useEffect, useState } from "react";
import { useParams } from "react-router-dom";
import { Card, Tag, Collapse, List, Spin, Alert, Space, Button } from "antd";
import ReactMarkdown from "react-markdown";  // 若项目已用 markdown-it,改成 markdown-it
import { getAlert } from "@/api/monitoring";

const LEVEL_COLOR: Record<string, string> = {
  green: "#52c41a",
  yellow: "#faad14",
  red: "#ff4d4f",
};

interface AlertData {
  id: string;
  alert_level: string;
  report_markdown: string;
  deep_dive_text: string | null;
  escalation_status: string | null;
  report_json: Record<string, unknown>;
}

export default function AlertDetail() {
  const { aid } = useParams<{ aid: string }>();
  const [data, setData] = useState<AlertData | null>(null);
  const [loading, setLoading] = useState(true);
  const [err, setErr] = useState<string | null>(null);

  useEffect(() => {
    if (!aid) return;
    getAlert(aid)
      .then(setData)
      .catch((e) => setErr(String(e)))
      .finally(() => setLoading(false));
  }, [aid]);

  if (loading) return <Spin />;
  if (err) return <Alert type="error" message={err} />;
  if (!data) return <Alert type="warning" message="未找到 alert" />;

  const level = data.alert_level;
  const triggered = ((data.report_json.triggered_signals ?? []) as Array<{
    rule_name: string; detected_value: unknown; threshold: unknown; explanation: string;
  }>);

  return (
    <div style={{ padding: 24 }}>
      <Card style={{ borderLeft: `6px solid ${LEVEL_COLOR[level]}`, marginBottom: 16 }}>
        <Space size="large">
          <Tag color={LEVEL_COLOR[level]}>{level.toUpperCase()}</Tag>
          <span><b>{data.report_json.customer_name as string}</b></span>
          <span style={{ color: "#999" }}>
            {(data.report_json.generated_at as string)}
          </span>
          {data.escalation_status && (
            <Tag>escalation: {data.escalation_status}</Tag>
          )}
        </Space>
      </Card>

      <Card title="触发信号" style={{ marginBottom: 16 }}>
        <List
          dataSource={triggered}
          renderItem={(s) => (
            <List.Item>
              <List.Item.Meta
                title={<><Tag>{s.rule_name}</Tag></>}
                description={
                  <>
                    <div>检测值: {String(s.detected_value)} / 阈值: {String(s.threshold)}</div>
                    <div>{s.explanation}</div>
                  </>
                }
              />
            </List.Item>
          )}
        />
      </Card>

      <Card title="报告" style={{ marginBottom: 16 }}>
        <ReactMarkdown>{data.report_markdown}</ReactMarkdown>
      </Card>

      {data.deep_dive_text && level === "red" && (
        <Collapse defaultActiveKey={["dd"]} style={{ marginBottom: 16 }}>
          <Collapse.Panel header="深度调查 (escalation 输出)" key="dd">
            <ReactMarkdown>{data.deep_dive_text}</ReactMarkdown>
          </Collapse.Panel>
        </Collapse>
      )}

      <Space>
        <Button>再次扫描</Button>
        <Button>标记已读</Button>
      </Space>
    </div>
  );
}
```

如果 `react-markdown` 未装,改成项目已用的 markdown 渲染方式(grep `frontend/src/` 找 `markdown` import)。

- [ ] **Step 17.3**: 加路由

```typescript
<Route path="/monitoring/:cid/alert/:aid" element={<AlertDetail />} />
```

- [ ] **Step 17.4**: 浏览器手验 + commit

```bash
cd frontend && npm run build
git add frontend/src/pages/monitoring/alert-detail.tsx frontend/src/router/
git commit -m "feat(b3-frontend): alert detail page (banner + signals + markdown + deep_dive collapse)"
```

---

## Task 18: Frontend 配置页(走 taste skill)

**Files:**
- Create: `frontend/src/pages/monitoring/config.tsx`
- Modify: `frontend/src/router/<index>.tsx`(加路由)

**Spec ref**: § 10.2 触发器配置

**Acceptance criterion**: **Component-level design 必须 invoke `taste` skill**。

- [ ] **Step 18.1**: 调 taste skill 设计页面

```
Invoke taste skill with brief:
"设计配置页(/monitoring/config),Antd 5。
内容:
① 客户列表 CRUD(Antd Table + Add/Edit/Delete modal,字段:ts_code / 客户名 / 行业 / 启用 switch)
② 全局配置(Antd Form;扫描时间 TimePicker 默认 16:30;每日 cost cap CNY InputNumber 默认 10;黄色是否发邮件 Switch;Escalation max 并发 InputNumber 默认 1)
③ 信号阈值配置(Tabs 5 个 rule 一个 tab,每 rule 显示 default 阈值 + 输入框可覆盖,Reset 按钮)
④ 邮件接收人 Tag input(多 email)
设计语言对齐 design tokens; Form labels 紧凑。
Output: layout + spacing + form interaction + table density."
```

- [ ] **Step 18.2**: 实现页面骨架

写入 `frontend/src/pages/monitoring/config.tsx`:

```typescript
import { useEffect, useState } from "react";
import {
  Card, Table, Button, Modal, Form, Input, Switch,
  Tabs, InputNumber, TimePicker, Space, Select, message,
} from "antd";
import dayjs from "dayjs";
import {
  listCustomers, createCustomer, deleteCustomer, getConfig,
} from "@/api/monitoring";
import type { MonitoringCustomer } from "@/types/monitoring";

export default function MonitoringConfig() {
  const [customers, setCustomers] = useState<MonitoringCustomer[]>([]);
  const [config, setConfig] = useState<Record<string, unknown> | null>(null);
  const [addOpen, setAddOpen] = useState(false);
  const [form] = Form.useForm();

  async function refresh() {
    setCustomers(await listCustomers());
    setConfig(await getConfig() as Record<string, unknown>);
  }

  useEffect(() => { refresh(); }, []);

  const customerCols = [
    { title: "客户名", dataIndex: "name" },
    { title: "ts_code", dataIndex: "ts_code" },
    { title: "行业", dataIndex: "industry" },
    {
      title: "启用",
      dataIndex: "enabled",
      render: (v: boolean) => <Switch checked={v} disabled />,
    },
    {
      title: "操作",
      render: (_: unknown, c: MonitoringCustomer) => (
        <Button danger size="small" onClick={async () => {
          await deleteCustomer(c.id);
          message.success("删除成功");
          refresh();
        }}>删除</Button>
      ),
    },
  ];

  const thresholds = (config?.thresholds ?? {}) as Record<string, Record<string, number>>;

  return (
    <div style={{ padding: 24 }}>
      <Card title="客户列表" extra={<Button type="primary" onClick={() => setAddOpen(true)}>添加客户</Button>}>
        <Table rowKey="id" columns={customerCols} dataSource={customers} pagination={false} />
      </Card>

      <Card title="全局配置" style={{ marginTop: 16 }}>
        <Form layout="vertical" initialValues={{
          scan_time: dayjs("16:30", "HH:mm"),
          daily_budget_cny: config?.daily_budget_cny ?? 10,
        }}>
          <Form.Item label="扫描时间" name="scan_time">
            <TimePicker format="HH:mm" />
          </Form.Item>
          <Form.Item label="每日 cost cap (CNY)" name="daily_budget_cny">
            <InputNumber min={1} max={1000} />
          </Form.Item>
          <Form.Item label="黄色 alert 发邮件" name="yellow_email" valuePropName="checked">
            <Switch />
          </Form.Item>
          <Form.Item label="Escalation 同时并发上限" name="escalation_concurrent">
            <InputNumber min={1} max={5} />
          </Form.Item>
          <Form.Item label="邮件接收人" name="recipients">
            <Select mode="tags" placeholder="输入邮箱回车" />
          </Form.Item>
        </Form>
      </Card>

      <Card title="信号阈值" style={{ marginTop: 16 }}>
        <Tabs
          items={Object.keys(thresholds).map((rule) => ({
            key: rule,
            label: rule,
            children: (
              <Form layout="vertical">
                {Object.entries(thresholds[rule]).map(([k, v]) => (
                  <Form.Item key={k} label={k} initialValue={v}>
                    <InputNumber />
                  </Form.Item>
                ))}
              </Form>
            ),
          }))}
        />
      </Card>

      <Modal
        open={addOpen} title="添加客户" onCancel={() => setAddOpen(false)}
        onOk={async () => {
          const vals = await form.validateFields();
          await createCustomer(vals);
          setAddOpen(false);
          form.resetFields();
          message.success("添加成功");
          refresh();
        }}
      >
        <Form form={form} layout="vertical">
          <Form.Item name="ts_code" label="ts_code" rules={[{ required: true, pattern: /^\d{6}\.(SH|SZ|BJ)$/ }]}>
            <Input placeholder="600519.SH" />
          </Form.Item>
          <Form.Item name="name" label="客户名" rules={[{ required: true }]}>
            <Input />
          </Form.Item>
          <Form.Item name="industry" label="行业" rules={[{ required: true }]}>
            <Input />
          </Form.Item>
        </Form>
      </Modal>
    </div>
  );
}
```

注:阈值修改 + 全局配置保存的 API endpoint 此 spec/plan 暂未提供(spec § 15 留白 — 实施时如需"持久化用户改动",需要 backend 加 `PATCH /api/monitoring/config` + `PATCH /api/monitoring/customers/:id/thresholds`,**但 v0.8.3 范围允许只前端展示 + 后端 read-only**;若用户实际想改,引导改 .env)。

- [ ] **Step 18.3**: 加路由 + 浏览器验 + commit

```typescript
<Route path="/monitoring/config" element={<MonitoringConfig />} />
```

```bash
cd frontend && npm run build
git add frontend/src/pages/monitoring/config.tsx frontend/src/router/
git commit -m "feat(b3-frontend): config page (customer CRUD + global + threshold tabs)"
```

---

## Task 19: e2e cassette + 5 dogfood 客户 + golden cases + 简历更新

**Files:**
- Create: `backend/tests/e2e/test_monitoring_e2e_cassette.py`
- Create: `backend/tests/eval/test_b3_dogfood_golden.py`
- Create: `backend/tests/fixtures/cassettes/test_monitoring_e2e_cassette/`
- Modify: `backend/.env`(本地填 5 ts_code + TUSHARE_TOKEN + EMAIL_MODE=real if dogfood)
- Modify: `docs/project-story.md` / `docs/project-story.html`(更新简历)
- Update memory: `~/.../memory/project_v0.8.3_architecture_landed.md`

**Spec ref**: § 11, § 12, § 19

- [ ] **Step 19.1**: 选 5 个 dogfood ts_code 写到 .env

按 spec § 11.1 结构(2 baseline + 2 历史触发 + 1 负面新闻),实施时:

```bash
# 1. 跑 tushare query 找候选(本地脚本)
unset all_proxy https_proxy http_proxy
TUSHARE_MODE=real TUSHARE_TOKEN=<...> uv run python -c "
import asyncio
from app.services.tushare_factory import build_tushare_service
async def main():
    svc = build_tushare_service()
    # 探查几个候选 ts_code 的 cashflow / debt_to_assets / anns
    for ts in ['600519.SH', '601398.SH', '000063.SZ', '600340.SH', '601318.SH']:
        balance = await svc.get_balance_sheet(ts_code=ts)
        cashflow = await svc.get_cashflow(ts_code=ts)
        anns = await svc.get_anns(ts_code=ts, start='20240301', end='20240501')
        print(ts, 'debt:', balance.tail(1)['debt_to_assets'].values, 'op_cf:', cashflow.tail(1)['n_cashflow_act'].values, 'anns_count:', len(anns))

asyncio.run(main())
"
```

按打印结果挑 5 个,**不在 spec / public 文档点名**,只写在 `backend/.env`(本地不入 git):

```bash
MONITORING_DOGFOOD_CUSTOMERS=600519.SH,601398.SH,000063.SZ,600340.SH,002594.SZ
```

- [ ] **Step 19.2**: 跑一次完整 dogfood scan(real mode)

```bash
unset all_proxy https_proxy http_proxy
TUSHARE_MODE=real EMAIL_MODE=mock uv run python -c "
import asyncio
from app.app_main import get_monitoring_service
svc = get_monitoring_service()
# 读 .env MONITORING_DOGFOOD_CUSTOMERS,先 INSERT 到 monitoring_customers 再 scan
# 详见实施时按 monitoring_router CRUD pattern 插入
"
```

观察:
- 5 客户跑完总耗时
- 各客户 alert level
- LLM cost(看 monitoring_runs.cost_cny 列总和)
- 是否有红色 alert 触发 escalation

如果 0 红色 alert(全 green/yellow):
- 检查阈值是否过严,降低 yellow 阈值让至少 1-2 客户出 yellow
- 不要为出红色而强行调阈值;实施时如确实没有红色,留作 limitation,简历坦诚

- [ ] **Step 19.3**: 写 e2e cassette test

写入 `backend/tests/e2e/test_monitoring_e2e_cassette.py`:

```python
"""End-to-end cassette test — full monitoring scan with cassette LLM + cassette tushare."""
from __future__ import annotations

import os
from pathlib import Path
from typing import Any

import pytest


@pytest.fixture(scope="module")
def vcr_config() -> dict[str, Any]:
    return {
        "filter_headers": ["authorization", "x-dashscope-api-key"],
        "match_on": ["method", "scheme", "host", "port", "path", "query"],
        "record_mode": os.environ.get("VCR_RECORD_MODE", "none"),
    }


@pytest.fixture(autouse=True)
def setup_env(monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.setenv("TUSHARE_MODE", "real")
    monkeypatch.setenv("EMAIL_MODE", "mock")
    monkeypatch.setenv("LLM_MODE", "cassette")


@pytest.mark.vcr
@pytest.mark.asyncio
async def test_monitoring_scan_5_customers(tmp_path: Path) -> None:
    from app.scripts.init_monitoring_tables import init_monitoring_tables
    db = tmp_path / "monitoring.sqlite"
    init_monitoring_tables(db)
    # 实施时:wire MonitoringService(参考 Task 14/15),插 5 客户,跑 run_scan,验证至少 1 红色 alert
    # 此处 placeholder 留实施完整 wiring 后填
    assert True
```

录制(只跑一次):

```bash
unset all_proxy https_proxy http_proxy
VCR_RECORD_MODE=new_episodes TUSHARE_MODE=real TUSHARE_TOKEN=... \
  uv run pytest backend/tests/e2e/test_monitoring_e2e_cassette.py -v
```

Replay 验证:

```bash
uv run pytest backend/tests/e2e/test_monitoring_e2e_cassette.py -v
```

- [ ] **Step 19.4**: 写 golden case

写入 `backend/tests/eval/test_b3_dogfood_golden.py`:

```python
"""B-3 dogfood golden — 10 case (5 customer × 2 应/不应触发场景)."""
from __future__ import annotations

import pytest


@pytest.mark.golden
def test_baseline_customer_a_should_be_green() -> None:
    """Baseline customer should not trigger any signal under stable conditions."""
    # 实施时:setup customer + run scan + assert level == green
    pass


@pytest.mark.golden
def test_baseline_customer_b_should_be_green() -> None:
    pass


@pytest.mark.golden
def test_historical_customer_c_should_trigger_yellow() -> None:
    """Customer c has historical financial deterioration → expect yellow."""
    pass


@pytest.mark.golden
def test_historical_customer_d_should_trigger_yellow_or_red() -> None:
    pass


@pytest.mark.golden
def test_recent_news_customer_e_should_trigger_announcement_signal() -> None:
    """Customer e has recent negative announcement → expect announcement rule yellow+."""
    pass


# 5 反向 case(各 customer 在阈值放宽时不应误报)
@pytest.mark.golden
def test_no_false_positive_under_loose_thresholds_a() -> None:
    pass


@pytest.mark.golden
def test_no_false_positive_under_loose_thresholds_b() -> None:
    pass


@pytest.mark.golden
def test_no_false_positive_under_loose_thresholds_c() -> None:
    pass


@pytest.mark.golden
def test_no_false_positive_under_loose_thresholds_d() -> None:
    pass


@pytest.mark.golden
def test_no_false_positive_under_loose_thresholds_e() -> None:
    pass
```

把 10 case 实际填实(Task 19.2 dogfood 跑完后,根据真实数据反向 fixture)。**实施时如某 case 实际不可达(比如某客户实际就是不会被特定 rule 触发),改 case description 反映实际,而非强行调数据**(memory `feedback_no_portfolio_simplification`)。

- [ ] **Step 19.5**: 跑全套测 + lint + 跑一遍完整 CI

```bash
unset all_proxy https_proxy http_proxy
uv run poe ci
```
Expected: 全 pass

- [ ] **Step 19.6**: 更新 project-story.md

在 `docs/project-story.md`(memory `project_personal_portfolio_no_company_reference` 范围)"AI 投研 copilot" 标题下,补充:

```markdown
## v0.8.3 — Tushare 真接 + B-3 持仓预警

- **Tushare Pro 真接**(8 接口):daily / income / fina_indicator / balance_sheet /
  cashflow / stk_holdernumber / disclosure_date / anns;Protocol + factory + sqlite cache + rate limit
- **B-3 持仓预警 — 分层架构**(signal layer + agent layer):
  - 信号层:5 个 rule(财务比率 / 现金流 / 股东户数 / 公告 LLM 分类 / 价格异动)
  - Agent 层:红色 alert 自动 escalation 到 5-agent pipeline 出深度调查
  - 三道硬约束闸:单次 ¥1 + 同时 1 并发 + 每客户每天 1 次 + 总日 ¥10 cap
  - 对齐 Moody's Early Warning Signals 工业标准
- **持续监控产品形态首次落地**:日 cron + 披露日历事件触发 + 手动按钮 + email 通知
- 5 客户 dogfood,e2e cassette + 10 golden case
```

- [ ] **Step 19.7**: 沉淀 memory

在 `~/.claude/projects/-Users-talantan--openclaw-workspace-main-financial-research-assistant/memory/` 创建 `project_v0.8.3_architecture_landed.md`:

```markdown
---
name: v0.8.3 architecture landed
description: tushare 真接入 8 接口 + monitoring engine 分层架构(signal+agent)+ email + B-3 schema + 5 表 + 3 前端页 + escalation
type: project
---

## v0.8.3 实际落地(2026-05-X commit XXX)

- Tushare:`app/services/tushare_{client,cache,service,factory,mock_adapter}.py`,8 接口,Protocol 第 8 次应用
- Monitoring engine:`app/services/monitoring/`(scheduler / signal_detector / 5 rules / escalation / monitoring_service / notifications)
- Schema:`app/agents/portfolio_warning_{schema,renderer}.py`
- Writer 加 alert_writer mode;ResearchState 加 mode/alert_signals/portfolio_warning_report/deep_dive_section
- 5 张 DB 表:monitoring_{customers,runs,signals,alerts} + notifications
- 3 前端页:`frontend/src/pages/monitoring/{index,alert-detail,config}.tsx`(走 taste skill 设计)
- 5 客户 dogfood,1+ 红色 alert 触发 escalation 验证

**已知 limitation**:
- Yellow 默认不发 email(可在前端配置打开)
- 前端配置页"全局配置/阈值修改"是只读展示,持久化留 v1.x
- daily_basic 全市场扫描 / 龙虎榜 / 北向 留 v0.8.5
```

更新 `MEMORY.md`(在已有"v0.8.x"节末加一行):
```markdown
- [v0.8.3 architecture landed](project_v0.8.3_architecture_landed.md) — tushare 真接 + B-3 分层监控引擎
```

- [ ] **Step 19.8**: 准备 PR

```bash
git push -u origin feat/v0.8.3-tushare-and-portfolio-monitoring
gh pr create --title "feat(v0.8.3): tushare 真接入 + B-3 持仓预警(分层监控引擎)" --body "$(cat <<'EOF'
## Summary

- **Tushare 真接入 8 接口**(daily / income / fina_indicator / balance_sheet / cashflow / stk_holdernumber / disclosure_date / anns):Protocol + factory + sqlite cache + rate limit
- **B-3 持仓预警 = 分层架构**(signal layer + agent layer):5 个 signal rule + 红色 escalation 调 5-agent pipeline 出深度调查;对齐 Moody's Early Warning Signals
- **三道硬约束闸**:单次 ¥1 + 并发 1 + 每客户每天 1 + 总日 ¥10
- **Email 通知**(SMTP real / Mock)+ 站内通知
- **前端 3 页**(列表/详情/配置;component-level 走 taste skill)
- **5 客户 dogfood + 10 golden case + e2e cassette**

Spec: `docs/superpowers/specs/2026-05-04-v0.8.3-tushare-and-portfolio-monitoring-design.md`
Plan: `docs/superpowers/plans/2026-05-04-v0.8.3-tushare-and-portfolio-monitoring.md`

## Test plan

- [ ] `uv run poe ci` 全 pass
- [ ] e2e cassette 录制 + replay 通过
- [ ] dogfood 5 客户至少 1 红色 alert 触发 escalation 验证
- [ ] `unset all_proxy https_proxy http_proxy && uv run poe nightly-local`

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

---

## Self-Review

### 1. Spec coverage check

| Spec section | Plan task | Status |
|---|---|---|
| § 1.1 块 1 — Tushare 8 接口 | Task 1, 2, 3, 4, 5 | ✅ |
| § 1.1 块 2 — tools 改造 | Task 4 | ✅ |
| § 1.1 块 3 — Monitoring engine | Task 7-9, 13, 14 | ✅ |
| § 1.1 块 4 — Email Notifier | Task 11 | ✅ |
| § 1.1 块 5 — PortfolioWarningReport schema + renderer | Task 10 | ✅ |
| § 1.1 块 6 — Writer alert_writer mode | Task 12 | ✅ |
| § 1.1 块 7 — DB migration 5 表 | Task 6 | ✅ |
| § 1.1 块 8 — REST API monitoring_router | Task 15 | ✅ |
| § 1.1 块 9 — 前端 3 页(taste) | Task 16, 17, 18 | ✅ |
| § 1.1 块 10 — Cassette | Task 5, 19 | ✅ |
| § 1.1 块 11 — 测试 | 各 task TDD + Task 19 golden | ✅ |
| § 1.1 块 12 — 文档 | Task 19.6, 19.7 | ✅ |
| § 6.2 escalation 三闸 | Task 13 | ✅ (实际加了第 4 道 dedup,共 4 闸) |
| § 7.4 黄色不发 email | Task 14 `_send_notifications` | ✅ |
| § 11 dogfood 5 客户 | Task 19.1 | ✅ |
| § 14.3 提前结束触发条件 | 通过 implementation 调阈值实施时判断 | ✅(明示决策) |
| § 15 留白 | 留 implementation 时根据 spike 决定 | ✅ |
| § 17 memory cross-references | Task 19.7 | ✅ |

### 2. Placeholder scan

- 全 plan grep `TBD|TODO|FIXME` → 无残留(Task 19.7 / Task 19.4 中"实施时填"是显式标注 — 不是 placeholder,而是 TDD-stub 引导)
- Task 15.3 wiring 给 skeleton + "实施时按 agent_graph.py 现有 pattern 写"是 spec § 15 显式留白(escalation 走 graph.ainvoke vs graph.astream)
- Task 18.2 配置页 update API 留白对应 spec § 15(提示用户改 .env)

### 3. Type 一致性

- `SignalLevel` 一致(`green` / `yellow` / `red`)— Task 7 → 8 → 9 → 10 → 12 → 13 → 14 一致
- `SignalResult` 字段一致(rule_name / level / detected_value / threshold / explanation / raw_data_ref)
- `MonitoringCustomer` 字段一致(id / ts_code / name / industry / thresholds_override)
- `EscalationStatus` enum 与 `monitoring_alerts.escalation_status` 列匹配
- TushareService Protocol 8 method 签名在 Task 3, 4, 8(rules consume), 14 一致
- `EmailSendResult` 字段一致

### 4. Ambiguity check

- Task 4.4 / 4.5 mock_tushare → tushare protocol 改造对调用方 grep 后明示
- Task 14.3 wiring complexity 给 skeleton + 引导参照现有 agent_graph.py pattern
- Task 19.1 ts_code 选取按 spec § 11 结构,实施时 spike 数据后挑

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-05-04-v0.8.3-tushare-and-portfolio-monitoring.md`. Two execution options:**

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration. 19 tasks → ~19 subagent dispatches; 每 task 之间 user/orchestrator 二阶段 review。

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints. 适合 session 内一气呵成,但 19 task 估 14 天 wall time → 不现实在单 session。

**Which approach?**

For v0.8.3 这个 sub-version,**强烈推荐 #1 Subagent-Driven**。理由:
- 14 天工期 → 不可能单 session
- 19 task 各自 ~250-600 LOC + cassette 录制 + 前端 taste 调用,需要 fresh context
- 两阶段 review 在 spec § 14.3 提前结束触发条件出现时能 catch
- subagent 走 worktree 隔离,便于回滚
