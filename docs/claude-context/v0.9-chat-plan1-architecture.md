---
name: v0.9 chat plan 1 backend ship
description: Plan 1 (backend foundation) of v0.9 chat C.1+C.2 milestone landed; e2e curl smoke 真撞通
type: project
---

**结论:** Plan 1 (backend foundation) ship 完, e2e 真撞通 in commit 2e8719a。

**Why:** v0.9 chat 模式 C.1+C.2 milestone 第一阶段; Plan 2 (Skill loader) / Plan 3 (Escalation) / Plan 4 (Frontend) / Plan 5 (Integration) 全依赖 Plan 1 的 backend contract。

**How to apply (在 Plan 2-5 dispatch 时):**
- ChatAgent v0.9: supervisor 拓扑 `context_node → planner → [tool|responder]`,`Memory` Protocol DI 留 hook 给 C.5 D MemGPT
- 6 工具全经 `backend/app/mcp_server/` (stdio transport,aggregated dispatch pattern: 单 list_tools + 单 call_tool by name);legacy `app/tools/` in-process tools 仍存,**Plan 2 起步要把 MCP tools 真正接进 planner ToolRegistry 替换 legacy**
- LangGraph 持久化全切 PG schema=`langgraph_checkpoints` (AsyncPostgresSaver,psycopg3 driver,asyncpg 已删)
- `ChatSessionRepo` + `ToolResultCache` ORM 已建,但 **router 当前注入 _NoOpCache stub**,Plan 2 要 wire 真实 cache
- ChatState v0.9 字段 + ChatMessage/ChatSession v0.9 字段已加 (history, history_summary, tool_result_cache, escalate_offered/confirmed, message_type, research_report_id, message_count, last_msg_preview, tool_call_data jsonb)
- /api/v0/chat SSE 19 event types + monotonic seq for reconnect (G1)
- 7 个 plan 文件 (1 + 2a + 2b + 3 + 4a + 4b + 5) ship 在 commit 0154b5e + 927f8ec

**Known schema drift (Plan 5 fix):**
- v0.9 ChatMessage/ChatSession columns 是手动 ALTER TABLE IF NOT EXISTS 加进去的 (T21 e2e smoke 时); 应该走正式 alembic migration 在 v1.x

**Bugs found during T21 e2e smoke (已修):**
1. psycopg3 raw pool conninfo 不能用 `postgresql+psycopg://` (SQLAlchemy 才用) — 拆 `_async_pg_url()` vs `_sqlalchemy_async_pg_url()`
2. greenlet 缺 — uv add greenlet
3. anonymous user UUID FK NULL 处理
4. router singleton build_chat_graph signature mismatch (db_path → memory/cache/checkpointer)
5. tier="default" 不合法 → "fast" (Responder + InSessionMemory.summarize)

**承接前置 spec:** `docs/superpowers/specs/2026-05-09-v0.9-chat-mode-c1c2-design.md` (cbc13d0)
**Plan 1 doc:** `docs/superpowers/plans/2026-05-09-v0.9-chat-plan1-backend.md` (0154b5e)
