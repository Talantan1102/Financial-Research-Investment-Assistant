---
name: v0.9.x #2.5 PG + CI ship 完
description: roadmap #2.5 已 ship,serve path + 用户隔离已被 CI e2e 覆盖
type: project
---
Roadmap § 6 #2.5(本地 PG + CI 跑)已 ship。PR #21,branch `feat/v0.9.x-pg-and-ci-setup`,共 5 commit 收敛 4 个层叠 bug。

**最终覆盖:**
- ✅ `backend/tests/e2e/test_pg_serve_path_e2e.py` 在 CI(`pr.yml` services.postgres + step env)中走通完整 app_main lifespan + 真 PG
- ✅ register / POST /reports / GET /reports / 双用户隔离 全验
- ⚠️ SSE 只验 status 200,**不验流内容**(LLM mock 在 research graph 某些 node 不 honor,deeper issue 推到独立 PR)

**5 commits 修的 4 个 bug**(后续接手时遇到类似情况可参考):

1. `eee7093` — ruff format(我手写代码两处函数签名 ruff 偏好单行)
2. `dd8f905` — `TestClient(app)` 需要 context manager 才触发 lifespan(Starlette 经典坑;不 wrap 就不跑 startup 钩子)
3. `7668b0f` — 双轨 schema bug:`User.id=UUID` vs `ChatSession.user_id=VARCHAR` → `Base.metadata.create_all` 在 chat_sessions FK 死整体 txn 回滚 → workaround 是手动 `User.__table__.create()` + `ResearchReport.__table__.create()` 跳过 chat_sessions 雷区。**真正修补留给 #3.5 DB 统一**。同 commit 修 CI services env `POSTGRES_DB=industry_assistant`(否则 00-create-test-db.sql 与默认 db 冲突 halt init)
4. `268e595` — LLM mock 在 research_planner_node 路径不 honor(直连 OpenAI 401),SSE 早退 → 护栏 SSE 只验 status 200,不验流内容

**How to apply 后续:**
- 后续 v0.9.x 前端 dogfood 在 Mac 上,真 PG 路径已 CI 守护,放心走
- 如果 dogfood 触到 LLM mock 不 honor 的 deeper bug,grep `research_planner_node` 看哪条路径直接构造了 OpenAI client 而不走 `build_llm_service_from_env`
- 别忘 #3.5 时一起修双轨 schema(`docker/init-db/01-init.sql` 手写 vs SQLAlchemy `Base.metadata`)

参考 spec:`docs/superpowers/specs/2026-05-07-v0.9.x-pg-and-ci-setup.md`
