---
name: v0.9 chat plan 3 escalation channel ship
description: chat→research escalation with LLM-fill packet + user confirm + report double-write landed
type: project
---

**结论:** Plan 3 (Escalation 通道) ship 完。chat→research 走 user-explicit-confirm 范式,LLM 抽信号 + 用户确认/修改 + 全程 trace。

**Why:**
- 决策 2 (§ 1.2 spec) — escalation + user explicit confirm 是工业 SOTA;不 silent handoff,user 看到 LLM 抽出来的信号并能改
- Update 2 (§ 2.2 spec) — packet 抽取 deferred 到 escalate 时刻 (不是每轮都跑),省 token
- 工业难题 E1-E14 真撞 — 见 spec § 6 (P0 + P1 + P2 全做)

**How to apply:**
- `EscalationPacket` (`backend/app/agents/escalation_protocol.py`) 是 wire format,4 main + sub-classes,SSE event payload / API request body / PG jsonb 共用一份
- `EscalationExtractor` (`backend/app/agents/escalation_extractor.py`) 是 prompt-engineering 核心 — A/B 测试 prompt 时改这一处
- `EscalationRecord` (PG) 三 jsonb (`packet_draft` / `packet_confirmed` / `user_edits`) — 抽错信号 + LLM↔user 差异沉淀,prompt 调优依据
- `ResearchState` 新加 4 chat-derived 字段 (`chat_extracted_entities` / `chat_extracted_preferences` / `chat_known_tool_results` / `chat_session_id`); planner/analyst/writer prompt 全部改造 honor
- 报告双写: `research_reports.source_chat_session_id` FK + `ChatMessage(message_type="research_report", research_report_id=...)` — chat 历史里直接显示报告链接
- 失败回退 (E4): research crash → escalate_error + record.status=failed; double-write crash 同样 status=failed
- 进度可见 (E2): `ResearchAgent.run_streaming()` 用 LangGraph `astream_events(version="v2")` forward node lifecycle 为 SSE
- 后续 Plan 4 (frontend) 实现 `<EscalationConfirmDialog>` UI 消费 `escalate_packet_draft` event
- 后续 Plan 5 (cassette) 跑端到端真 LLM 验证 E9 extraction quality
- T18 curl smoke skipped — autonomous pipeline 不能 hit 真 LLM,留作手动 verification 步骤
