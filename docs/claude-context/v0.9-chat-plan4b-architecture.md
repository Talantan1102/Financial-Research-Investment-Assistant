---
name: v0.9 chat plan 4b frontend chatpane ship
description: ChatPane sub-components + EscalationConfirmDialog + Reports list + F1-F10 polish landed
type: project
---

**结论:** Plan 4b (frontend chatpane + escalation UI + reports + F1-F10) ship 完。Plan 4a 的 layout/store/hook 基础上,把用户可见层全部填充。

**Why:**
- v0.9 chat 模式的用户可见层 — 用户从 ⚡ 触发深度研究 → 确认 packet → 看研报渲染 全流程在 frontend 落地
- F1-F10 工业 polish 跟 backend Plan 1-3 完整契合(F1/F2/F3/F4/F5/F7/F9/F10);F6 + F8 来自 Plan 4a
- Playwright e2e 在 autonomous 流程中跳过(需要浏览器+dev server),留给手动验证

**How to apply:**
- ChatPane = MessageList(虚拟化) + TextMessage(md+hljs+KaTeX+chart_specs) + ToolCallCard(tri-state) + ResearchReportCard + StreamingIndicator + InputArea(4-channel) + CostMeter(cross-mode)
- EscalationConfirmDialog 4 sub-form 对齐 EscalationPacket schema(Plan 3 protocol);InlineEditField 把 FieldEdit 写进 escalationStore.user_edits
- ReportsListPage 用 `/api/v0/reports` REST;ReportDetailModal markdown 渲染;source_chat_session_id 提供 chat 回链
- 测试: vitest L0/L1 全覆盖(~143 tests / 39 files);Playwright e2e 留作 follow-up
- 后续 Plan 5(L2 cassette + golden)在此基础上 e2e 验证
- 已知 hook 留口: F10 真实 upload(C.4)/ memory(C.5)/ Playwright e2e(手动)
