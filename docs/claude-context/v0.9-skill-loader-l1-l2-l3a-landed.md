---
name: v0.9 skill loader L1 L2 L3a landed
description: Plan 2a — Skill progressive-disclosure loader (L1 list + L2 SKILL.md + L3a yaml/json/md resources, no script execution)
type: project
---

**结论:** Plan 2a (Skill loader L1+L2+L3a) ship 完。L3b script execution + sandbox 留 Plan 2b。

**Why:** v0.9 chat 模式渐进式披露的前半段 — planner 通过 L1 选 skill,context_node 加载 L2 + L3a 进 ChatState.skill_context,planner 下一轮拿到 full context 出 tool_call/respond。L3b 单独 ship 是因为 sandbox + I/O 协议 + timeout + 安全 (S6/S7/S8/S9) 是独立工程难题。

**How to apply:**
- L1 = 7 skills 的 name + description ≤ 512 chars,启动加载塞 ChatPlanner system prompt
- L2 = 单 skill SKILL.md 全文,planner emit `{"action": "load_skill", "name": "X"}` 触发
- L3a = `resources/*.{yaml,json,md}`,SKILL.md 通过 markdown link `(resources/foo.yaml)` 引用,SkillLoader 在 load_skill 时 BFS 走深度 2 自动拉取
- 50kB cap per resource (S5),nested ref depth ≤ 2 (S4),path traversal blocked
- Demo: `risk_assessment/resources/risk_thresholds.yaml` (~2.6kB,5 维度阈值 + 权重 + 风险等级 cut)
- 工业难题撞实:S2 (description schema cap) / S3 (SKILL.md 30kB soft warn) / S4 (depth ≤ 2) / S5 (50kB hard cap) / S10 (skill 是文本,tool 是 MCP endpoint,边界清晰)
- S1 retrieval acc benchmark 推到 Plan 5 eval pipeline (Task 15 标 optional 跳过)
- Plan 2b 接力:S6 sandbox / S7 I/O 协议 / S8 timeout / S9 安全 + financial_analysis/scripts/calculate_dcf.py demo

**Plan 1 hooks consumed:** ChatPlanner v0.9 (P1.T8) extended with load_skill/load_resource Plan fields; context_node (P1.T7) extended with skill action handlers; chat_graph (P1.T11) routes to skill_load_node/resource_load_node loop back to planner; SSE event types (P1.T18) already include skill_load.
