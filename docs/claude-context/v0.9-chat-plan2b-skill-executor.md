---
name: v0.9 chat plan 2b SkillExecutor + L3b 沙箱 ship
description: SkillExecutor sandbox + L3b script execution + calculate_dcf demo landed
type: project
---

**结论:** Plan 2b (SkillExecutor + L3b) ship 完。配合 Plan 2a (SkillLoader L1/L2/L3a) 形成完整 Anthropic Skills 渐进式披露。

**Why:** L3b script 是 Anthropic Agent Skills 哲学核心 ("LLM 编排 + 真实计算外包")。Plan 2b 撞透 S6/S7/S8/S9 四大工业难题。

**How to apply:**
- `SkillExecutor.execute(ref, args, timeout_s)` 在 `backend/app/skills/skill_executor.py`
- 沙箱 (S6): subprocess + cwd 隔离 + env 白名单 + RLIMIT_AS/CPU
- I/O 协议 (S7): stdin JSON / stdout JSON / stderr 截断 2kB / exit code
- 超时 (S8): 30s 默认 / 5min max / SIGKILL + 进程组清理
- 安全 (S9): 静态 AST 扫描禁 21 个 banned API,subprocess launch 前 reject
- demo: `backend/claude_skills/financial_analysis/scripts/calculate_dcf.py`
- planner schema: `Plan.script_calls: list[SkillScriptCall]`
- tool_node: 分发 script_calls → SkillExecutor → ToolResult(tool_name="skill_script")
- SSE: `skill_execute_start/_end/_error` 3 event types
- 9 sandbox escape attempts L1 验证全 pass

**设计取舍:** 没用 docker/firejail/nsjail/seccomp,subprocess+RLIMIT+AST 已覆盖 P0;production 化 hook 留口。AST scan 是 defence-in-depth,不是唯一防线。
