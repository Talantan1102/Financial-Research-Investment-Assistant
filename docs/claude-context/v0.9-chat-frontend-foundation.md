---
name: v0.9 chat frontend foundation landed
description: Plan 4a ship — AppShell + 240px Sidebar + chat-first routes + 3 valtio stores + useChatSSE (F6/F8 撞透);Plan 4b 填 ChatPane / EscalationConfirmDialog
type: project
---

**结论:** Plan 4a (chat frontend foundation) ship 完。AppShell + 240px Sidebar + chat-first routes + 3 valtio stores + useChatSSE(F6/F8 撞透)。`<ChatPane>` 内容渲染留给 Plan 4b。

**Why:**
- Plan 4(整个 frontend) 单 plan 太大,opus 起草 stall;controller 决定拆 4a/4b 缩小每个 plan 的输出 size
- Plan 4a 锁定基础设施(layout / routes / stores / hook)+ 工业难题 F6 (SSE 重连 last_event_id) / F8 (多 chat 切换 SSE lifecycle);Plan 4b 锁定内容渲染(MessageList / TextMessage / ToolCallCard 等 7 components)+ Plan 3 EscalationConfirmDialog 集成

**How to apply:**
- 加新 chat 内容组件: 直接在 `frontend/src/pages/chat/session.tsx` 的 `<ChatPane>` slot 渲染,读 `useSnapshot(currentChatState)`,无需改 layout / store / hook
- 新 SSE event type: 加进 `frontend/src/types/chat.ts` 的 discriminated union,`currentChatStore.dispatchEvent` 加 case;Plan 1 已锁 19 个 event 的 schema
- 新 escalation 字段: 加进 `frontend/src/types/escalation.ts`,`escalationStore` 不动;`<EscalationConfirmDialog>` 在 Plan 4b 写
- F6 / F8 行为已被 vitest + msw e2e 守护,改 `useChatSSE.ts` 必须保证 4 个 SSE 测试不退化:basic consume / abort / F6 reconnect+backoff / F8 swap
- 每个 chat 的 streaming state 在 `currentChatStore` (单例);切 chat = `setSession` 重置;不要把 streaming state 提到 `chatSessionsStore`
