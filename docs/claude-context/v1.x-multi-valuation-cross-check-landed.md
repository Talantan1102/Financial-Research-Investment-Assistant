---
title: v1.x A5a 多模型估值 cross-check ship 完
date: 2026-05-16
---

# 结论

v1.x A5a 代码层 ship 完:`InvestmentDueDiligenceReport.ValuationAnalysis` schema 从单 lens
(PE 历史分位)升级到 4 模型 cross-check(PE / PB / EV-EBITDA / DCF)+ IndustryModelRouter
+ DCF 3 场景 + sensitivity 5×5 矩阵 + OutlierDiagnosisAgent + Critic 7 维(新增
valuation_consistency rule-based)+ Writer prompt 加 outlier_diagnosis 引用约束 + writer
retry edge 加 valuation_consistency trigger。

**Critic 第 7 维已 wire 进生产 scorer list + critic_subgraph fan-out + 1 integration test
守护**(初次 ship 漏了 wire, hotfix 补 — 同型 c5 injection_classifier 死代码教训)。

工程量:~1700 行新代码(20 commits)/ 完成。

# Why

行业实践:严肃尽调必备 multi-model cross-check 防数字 hallucination。延续 v0.8.5 / v1.x A4
"数字 Python / judgment LLM" 主线 — 4 helper 全 deterministic,LLM 只做局部 judgment
(router override / outlier diagnosis)。

# 一句话总结

数字 Python deterministic / LLM judgment 局部 override / Writer narrative 必须引用诊断 /
Critic 第 7 维守护。

# How to apply

- 改估值层、Writer prompt 估值章节、Critic dim 时回看本 PR 的 layer 划分
- 新行业上 router:在 `INDUSTRY_VALUATION_MAPPING` 加映射;边界 case 用 LLM override
- 加新 valuation 模型(如 P/S、NAV、ROE-based):新 helper + router mapping + 不动其它
- A5b(bull/bear debate)衔接:BullAdvocate 引用 `dcf_bull + outlier_diagnosis`;
  BearAdvocate 引用 `dcf_bear + pe_value + outlier_diagnosis`

# 已知 follow-up

1. ~~**`_build_valuation_inputs_from_state` 真 wire tushare**~~ — **✅ done (2026-05-16,
   commit `545b5dd`)**: Analyst 从 tool_results 真提取 ValuationInputs 18 字段。现实约束下
   做的工程取舍:
   - eps/bvps via `price / daily_basic.{pe,pb}` 反推(避免依赖未暴露 income.basic_eps)
   - shares via `(total_mv / price) × 10000`(万元 / 元 → 股)
   - industry_pe 用 `get_pe_history.median_pe` 作可比 fallback(narrative 阶段标注 limit;
     无 `compare_industry_valuation` tool — 真行业可比留 v1.x++ tool 扩展)
   - industry_classification heuristic substring match on user_message + target_entity
     (无 stock_basic.industry tool;30+ keyword 兜底命中)
   - EBITDA 信号缺(无 DA 字段)→ EV-EBITDA model 自然 skip via InsufficientData
   - forecast_growth / company_beta 从 state 透传(Task 14 字段;DataCollector 未 wire → None)
   - 新增 `valuation_helpers/industry_defaults.py`(17 行业 WACC/terminal_growth +
     substring normalize),`_index_tool_results` / `_safe_float` 模块级 helper
   - 25 新测试(18 unit + 7 integration);全 PASS / mypy clean / ruff clean / pytest 1844 PASS
2. **B-1 maotai e2e cassette 重录 + DCF outlier eval golden**:#1 ship 后可做,
   验证 e2e LLM judge 看到 multi-model cross-check 真生效
3. **真 industry 可比 tool**(`compare_industry_valuation` 或 `get_industry_peers`)+
   `stock_basic.industry` tool — 解锁 PE/PB/EV-EBITDA 真行业可比 + industry_classification
   精确分类(目前 fallback heuristic 在非主流命名时降级到 `_default`)
4. **DataCollector wire Task 14 字段**(`forecast_growth` / `price_history_for_beta`)—
   DCF base scenario 走 forecast 而非 historical fallback;company_beta 真算 60-day β
5. **银行 ROE / 地产 NAV / P/S 估值模型**:留 v1.x++(用 router 加新模型映射即可)

锚: docs/superpowers/specs/2026-05-16-v1.x-multi-valuation-cross-check-design.md
    docs/superpowers/plans/2026-05-16-v1.x-multi-valuation-cross-check-implementation.md
